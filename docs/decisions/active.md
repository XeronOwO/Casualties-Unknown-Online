# CUO Active Decision Register

This is the **normative decision register**: the decisions that still constrain
current architecture, protocol, API, and development practice.

Historical delivery records, feature-slice logs, and Phase A–E daily sub-steps are
not here:

[- Historical delivery archive: `tech-decisions-archive.md`](archive.md)
[- Phase A–E evolution detail: `evolution/status.md`](../architecture/evolution/status.md)
[- Full traceability index: `tech-decisions-index.md`](index.md)

## Binding current decisions

| # | Decision | Canonical doc |
|---|---|---|
| 1 | BepInEx 5 / net48 / HarmonyX / protobuf-net / Microsoft.Extensions stack. | `AGENTS.md`, `docs/en/contributing/build-and-test.md` |
| 2 | Pure star topology, host-authoritative, no guest-guest traffic; reliability follows “can the loss self-heal”. | `docs/architecture/protocol.md` |
| 3 | Session owns its state; narrow `HandlerContext` capability interfaces; no broad DI/global mutable state. | `AGENTS.md`, `docs/architecture/current.md` |
| 6 | Perfect-match Harmony patch contracts must be tested against copied game assemblies. | `AGENTS.md`, tools |
| 9 | Mod API binding contract lives in `docs/en/reference/mod-api.md`; opaque mod messages, fail-closed manifest. | `docs/en/reference/mod-api.md` |
| 19 | Mod permissions, host commands, dependency ordering, strict SemVer. | `docs/en/reference/mod-api.md` |
| 25 | BepInEx `ConfigFile` → `IOptionsMonitor`; logging levels; state-stream cadence. | `AGENTS.md`, `docs/en/reference/mod-api.md` |
| 37 | Mod state saves are host-persistent and versioned. | `docs/en/reference/mod-api.md` |
| 38 | Mod content registration is a read-only framework registry. | `docs/en/reference/mod-api.md` |
| 46 | `ReadGameState` is a read-only player-character projection. | `docs/en/reference/mod-api.md` |
| 49 | Mod entity spawn is permission-gated native prefab replication. | `docs/en/reference/mod-api.md` |
| 50 | `AccessNativeApi` is a curated read-only registry, never open reflection. | `docs/en/reference/mod-api.md` |
| 63 | NetMsg direction registry is fail-closed; unregistered/incorrect-direction ids are rejected. | `docs/architecture/protocol.md` |
| 65 | Partial-aware architecture gate + debt ledger. | `AGENTS.md`, `SourceShapeGateTests.Architecture_OneTopLevelTypePerFileAndAggregateLimits` |
| 73 | HandlerContext per-domain narrowing via capability interfaces. | `docs/architecture/current.md` |
| 75 | GameAdapter depends on narrow control interfaces, not concrete session services. | `AGENTS.md`, `docs/architecture/domains.md` |
| 82 | IP-direct TCP transport is a supported non-Steam mode. | `docs/evidence/selfchecks/protocol/ip-direct-selfcheck.md` |

## Active kernel/protocol decisions

| # | Decision | Canonical doc |
|---|---|---|
| 128 | Four-envelope protocol, checkpoint+journal join, checkpoint-only save, kernel wire mapping. | `docs/architecture/protocol.md` |
| 129 | High-frequency player/enemy streams ride `StateStreamEnvelope` over `KernelEnvelope`. | `docs/architecture/protocol.md` |
| 137 | Pre-release protocol numbering was reset; `ProtocolVersion.Current` is bumped on behavioral wire changes, and the value itself plus its per-number log live in the constant's own doc comment (`ProtocolVersion.cs`) and are deliberately not restated here — the post-reset sequence reached the current value after the runtime-entity creation token, the snapshot's animal acknowledgement key list, the enemy/trap-layout backfill creation keys, the block-state support-loss verdict, the layer-boundary item reset, the runtime-entity rejection, and the guest partial-damage absolute re-report `BlockDamageReport`, the enemy snapshot's bind-time spawn anchor `EnemyStateMsg.SpawnPosition`, the enemy attack announcement `EnemyAttackMsg`, the medical operation start's target-body verdict of a parked start (`MedicalOperationTargetCheckRequest`/`Answer`), the single-outcome settlement of a medical operation unit that resolves once (`MedicalOperationEndCommittedMsg.TerminalReason.AlreadyHandled`), the creation-before-operation invariant that replaced the host's fixed 500 ms pickup hold window with an immediate refusal carrying the refused creation's own reason (`KernelProtocolCommandHandler`), and the local-initiation world-time model where a guest's manual speed applies on its own client first and the host's accept-first answer settles it (`WorldTimeSync` / `WorldTimeLocalInitiation`, the host-side movement veto deleted), and the world/layer generation stamp on the layer-relative direct world reports (`BlockPlaced`, `BlockDamaged`, the block-damage snapshot/report payload), which refuses a stale previous-layer report with a precise log and accepts the same-generation lost-air-write break report (`Verdict.LostAirWrite`) so its drops survive, and the exposure of the same stamp to the remaining position-keyed families — the trap-layout snapshot (host → guest) and the runtime-entity creation report with its absolute table (`EntitySpawnedMsg`, `RuntimeEntitySnapshotMsg`) — where a stale snapshot or report is refused before any entity is materialized and a refused creation report is answered with `RuntimeEntityRejectReason.StaleGeneration` so its pending re-report ends, and the recipe-unlock backfill that gives the blueprint unlock an absolute SET in both directions — `RecipeUnlockSnapshot`: the host's live unlocked-indices set on the world-entry and 60 s repair groups and a guest's own set on the fallback cadence, merged by the host through the ordinary unlock path and applied silently by a receiver that performed no unlock). | `docs/architecture/protocol.md`, `docs/en/reference/mod-api.md` |
| 152 | Player durable skills are kernel-owned in `PlayerState`. | `docs/architecture/domains.md` |
| 153 | Player kernel identity is ensured when entity sync starts. | `docs/architecture/domains.md` |
| 154 | Cross-player take/heal/use/carry are `HostValidatedNoPrediction`; push is `PresentationOnly`. | `docs/architecture/domains.md` |
| 155 | Carry relation requires a live carrier; kernel invariant. | `docs/architecture/domains.md` |
| 156 | Player/item ownership consistency and death preservation are kernel invariants. | `docs/architecture/domains.md` |
| 157 | Generic Prediction Runtime is future work; current cross-player operations are not client-predicted. | `docs/architecture/domains.md`, `docs/backlog/README.md` |
| 158 | Kernel reset centralized in `KernelProtocolService`; no-legacy/command-authority/kernel-shape guards are active. | `docs/architecture/guards.md` |
| 159 | SUPERSEDED IN PART (2026-09-18 ruling, decision 184) — manual world-time acceleration is now LOCAL FIRST: the initiator's own client applies `Fast`/`SuperFast` at once, the host arbitrates accept-first (only an unrepresentable request is refused) and answers every request, and the initiator reconciles with the host's value through a correction ramp; a teammate being awake is no longer grounds for refusal, and the host-side movement veto is deleted (movement is the mover's own key action). The all-unconscious sleep gate stands exactly as decided. | `docs/backlog/review/world-time-local-initiation.md`, `docs/backlog/resolved/sleep-behavior-policy.md` |
| 160 | Sleep policy: normal and forced sleep remain allowed; shared-clock acceleration is host-authoritative and only applies when every in-world alive player is unconscious; no sleep-gating host rule or new wire field. | `docs/backlog/resolved/sleep-behavior-policy.md` |
| 161 | Accept-first arbitration is limited to reports the host can REPRESENT: a runtime creation the host cannot materialize (its content set lacks the prefab/template) is rejected — never recorded, never relayed — and the rejection is answered to the reporter and logged/surfaced. An accepted-but-unowned record has no owner whose death can retract it, so it would leak into every later snapshot and resurrect state a peer already destroyed. | `AGENTS.md`, `docs/backlog/review/runtime-entity-spawn-backfill.md` |
| 162 | Save identity is transport-scoped and the save holds every member present at the cut: Steam keys are `steam-<steamId64>`, IP-direct keys are `name-<sanitized display name>` (that mode has no account identity). A player absent from the package joins as a NEW player with a fresh character and starting supplies; the two key spaces never silently collide. | `docs/architecture/save-archive-format.md`, `docs/backlog/review/save-system-mid-run-and-layer-end.md` |
| 163 | Save restore is repair mode with minimal loss: salvage is per ENTRY, not per domain — an entry whose content no longer exists (e.g. a prefab removed by a mod update) is skipped with a warning while the remaining entries of that domain still apply. Only an unreadable manifest is a hard gate, and then the newest readable backup is used and the fallback is reported loudly. Restoration never silently regenerates the layer. | `docs/architecture/save-archive-format.md` §6 |
| 164 | The save is a multi-world repository, not a slot list: one folder archive per world (`worldId` immutable `w-<yyyyMMdd>-<4 hex>`, display name freely renameable and never the directory key) holding an unpacked `live/` snapshot plus N backup archives (`backups/<kind>-<yyyyMMdd-HHmmss>.cuoz`). The native single-slot surface cannot express a mid-run cut, so CUO owns the storage design, including a configurable interval autosave. | `docs/architecture/save-archive-format.md` §2/§7 |
| 165 | The CUO save system is fully independent of the native `save.sv`: CUO never writes it and never reads it, because the native format cannot express a mid-run cut and coexistence would create two sources of truth. The native Continue entry (`PreRunScriptLoadRunPatch` already intercepts `PreRunScript.LoadRun`) becomes the CUO continue path. | `docs/backlog/review/save-system-mid-run-and-layer-end.md` |
| 166 | The v1 save scope covers the kernel checkpoint (all domains) + run baseline + character data + world diff + transient policy. Explicitly out of v1: mod-state persistence, host bans, and migration of the old protobuf kernel saves. | `docs/backlog/review/save-system-mid-run-and-layer-end.md` (stage table S1–S4) |
| 167 | A mid-run cut is never taken from inside the callback that asked for it: the trigger ARMS it and the host pump takes it at ONE seam — the CUO pump's last step (`cutPhase` = `frame-end`), the point where no CUO command batch is mid-commit and no CUO frame flush is mid-send (the game's own scripts may run before or after that pump, so the guarantee is the one synchronous read, not Unity's frame boundary). The console's `/save` (open to anyone; the SAVE LAYER refuses a guest, which is what keeps solo play — where there is no session role — able to save) and the host's deliberate menu return both go through that seam, and the menu return is a full mid-run cut taken BEFORE the leave; when the cut cannot be written the leave still happens and the previous snapshot stays. Layer-end cuts keep their own seam (the kernel's layer-advance commit, `cutPhase` = `layer-boundary`) and cannot be armed at the frame-end seam. | `docs/architecture/save-archive-format.md` §4, `docs/backlog/review/save-mid-run-consistent-cut.md` |
| 168 | In-flight state is decided per class, never silently: `WorldTransientPolicy` declares one verdict per class — `capture` (carried as data), `resolve-before-save` (the cut is deferred, request still armed, bounded by `WorldCutDeferral.MaxFrames` = 8 frames, and named if it outlasts the deadline), or `drop-with-log` — plus whether CUO can COUNT it (an `Observed` row is named with its count, a `Standing` row — the game's craft coroutine, item velocity, the run clock, the earthquake timers — is named as a class every mid-run cut leaves behind). An undeclared class reported by an owner REFUSES the cut. A native world table that cannot be read refuses the cut instead of being read as empty, and a restore's live-world write reports its refused rows (or an engine throw) back to the caller that started the restore (`WorldRestoreAudit`), so a restore can never be reported as a success while the game's own bounded table dropped a row. | `docs/architecture/save-archive-format.md` §4/§6, `docs/backlog/review/save-mid-run-consistent-cut.md` |
| 169 | The run values the native `SaveSystem` used to load are split by OWNER and by SEAM, not packed into one blob: the two rarity multipliers are world-generation INPUTS, so they ride the kernel run baseline (`RunState` → `WireRunState` → `WorldStartParams`) and a cut STAMPS the cut instant's value on them — a side that generates a layer with the game's fresh `1f` builds a different layer than the run's authority, which is what a guest joining a run mid-way used to do. The run clock base is a `run.json` `native-run-fields` row, written at the slot the native `SaveSystem.TryLoadGame` used to occupy because `WorldGeneration.Start` derives the layer's time limit from it. The recipe unlock table is the SAME row but lands at the world-entry seam with the other native layer facts, because the game rebuilds `Recipes.recipes` in `WorldGeneration.Awake` and CUO's mod-content provider appends the custom recipes on a LATER Update frame — a save-slot write would refuse every custom recipe's row. A reader that cannot read the run fields REFUSES the cut rather than storing a clock that restarts at zero and a re-locked recipe table; a snapshot without the row is restored with the live clock and recipe state and NAMES the gap, and a refused recipe row reaches the same restore account. | `docs/architecture/save-archive-format.md` §3.4/§4/§6.1, `docs/backlog/review/save-native-run-field-parity.md` |
| 171 | The native CHARACTER fields the game's own save kept ride the character snapshot, not the world archive or the run baseline: the body's happiness history (`Body.lastHappiness` — the game's ten-value window its own updater shifts in place), the per-run calorie counter (`PlayerCamera.caloriesConsumed`) and the wound window's `cInfo` — carried as one optional sub-message (`CharacterNativeFieldsMsg` on `CharacterDataMsg`), so an old sender simply carries none. They are read off the live scene at the same instant as the rest of the snapshot — the host's own at the cut, a guest's at its 1 Hz report, so a reconnect restore has them too — and the read is ALL-OR-NOTHING across the body and the two scene statics: a read that met a body but no camera describes no coherent instant and captures nothing rather than zeros, because zero is a real value here (a new character's window IS 0 cm / 0 y / #0). The apply is the local restore path's SECOND pass, after the wipe and the items, which is where the native load wrote them (`SaveSystem.cs:438-441`); a death → revive cycle carries them through instead of rebuilding them, and any other clone of a snapshot that gets SAVED (the interaction services' `CloneCharacter`) carries them too, since dropping one there degrades the next cut, reconnect and continue. A value the native contract cannot hold — a history that is not the game's whole ten-value window, a details array that is not the four `SetCharDetails` takes — is refused BY NAME and keeps the live value, and every refusal reaches the restore report as damage, for the characters actually BOUND to a present peer (a file nobody claims restores nothing, so it is not described). Without this a continued character silently shows the game's defaults, because the game skips its own fresh roll when a run is continued (`PlayerCamera.cs:726-729`). The Runtime owns those accounting rules (`CharacterNativeFieldPolicy`); the adapter owns the live reads and writes through `ICharacterNativeSystem`. | `docs/architecture/save-archive-format.md` §3.4/§6.1, `docs/backlog/review/save-layer-end-save-and-restore.md` |
| 170 | A continue hands the LOCAL player's own character back to the client that can apply it: the Runtime binds every stored key a present peer claims into the character table the reconnect path sends from, and returns the key the LOCAL player claims with the continue outcome (`WorldContinueOutcome.LocalCharacter`), which the adapter queues on the SAME local restore path a next-level respawn uses (`CharacterDataSync.QueueLocalRestore`, the two-frame wipe/apply). Nothing else can apply it: the scene — and therefore the only body it can go on — loads after the click, and the character-table slot holding it is the one the live 1 Hz snapshot overwrites. The queue remembers WHICH RUN queued it (`LocalCharacterRestoreQueue`): a restore this client's own run queued is dropped when this client instead follows a start it did not restore, a run this client starts ON ITS OWN drops whatever waited (a peer's un-followed hand-over included), a peer's hand-over survives the follow it belongs to, and session end clears it — so a restore armed for an abandoned continue can never land on another run's body, and a reconnect is never swallowed by the follow that brought it in. A `layer-end` cut's character POSITIONS are dropped at that restore — the snapshot names the layer being ENTERED, which is regenerated, so a position captured in the layer being left would teleport the body into a layer that no longer exists; a mid-run cut names the layer its bodies stood in and keeps its position. | `docs/architecture/save-archive-format.md` §3.4/§6.1, `docs/backlog/review/save-layer-end-save-and-restore.md` |

| 172 | A restored cut's world-entity facts (the one-shot trap consumptions, the durable trap-state machine, the opened lockables, the building health) land by ROLE, not by cut kind: a GUEST's checkpoint restore applies them immediately, because the world it stands in IS the layer the checkpoint describes, while the HOST/SOLO restore runs at the Continue click — the only world alive then is the layer being REPLACED — so the projection HOLDS them (`IRestoredWorldEntitySource`) and the world-entry seam writes them through the SAME three appliers the guest path uses (`EntityEventSync`'s trap replay, `WorldBuildingEntitySync`'s opened-entity and health applies), each returning what the live world took, so an entity the regenerated layer does not have reaches the restore report instead of the log alone. A death applied there stays a REMOTE death, so the drops the saved world already rolled are not rolled again. A mid-run restore therefore owes THREE live-world halves — the world facts and the world-entity facts at that seam, then the item reconcile a frame later — and a layer-end restore owes ONE, counted from the writers that are ACTUALLY armed when the click returns rather than from the cut kind (`WorldRestoreApplier.LiveWorldHalves`), because a count that names a half nobody will report would leave the restore awaiting forever; a restore reports exactly once, and the next generation's replay call reports nothing. A `layer-end` cut's world-entity rows describe the layer being replaced, so they are dropped before the audit begins AND the archive does not carry them (the encoder drops `world-entities.json`'s per-entity rows for one, exactly as it drops the WORLD-ROOTED item rows with the layer-boundary reset's own subtree rule `ItemLocationChain.IsWorldRooted`), which leaves a produced layer-end archive with only boundary-crossing item records and no in-layer fact at all. | `docs/architecture/save-archive-format.md` §3.4/§6.1, `docs/backlog/review/save-mid-run-consistent-cut.md` (scope 8 + the S3.5 increment) |

| 173 | Whether a restored trap row REACHED the live world is one Runtime rule fed by an adapter-observed tri-state verdict, never a boolean the adapter decides: the shared action library (`TrapStateActions` / `CrystalStateActions`) answers `TrapActionOutcome` — `Applied`, `AlreadyInState` (the local copy already carried the state; a duplicate from the two-trigger race, and the state the row names IS in the world, so it is NOT a lost row), or `NotApplicable` (this copy cannot carry the fact at all — e.g. a restored `CrystalMimicTriggered` row landing on a crystal that carries no mimic effect: the effect list is rolled per crystal, the mimic is one of seventeen weighted effects, and about seven crystals in ten are destroyed before any effect is set) — and `TrapActionVerdict.ReachedTheLiveWorld(outcome)` turns that into the account's currency, which is the same boolean `EntityEventSync.OnTrapStateProjected` counts into `LiveWorldWriteOutcome`; the input is the verdict or NULL for "no entity of the expected kind at the position", so both observations every caller has flow through the rule. The reached set is written POSITIVELY (`Applied` / `AlreadyInState`), so a follow-up outcome fails CLOSED — refused and named, never silently counted as restored — and has to be classified deliberately. A bool conflated the duplicate with the divergence, so a divergence was counted as restored and never reached the player's report; the tri-state is compile-enforced at both call sites (the host executor's `ApplyState` and the replay's `ReplayState` take `Func<T, TrapActionOutcome>`), so a new action cannot quietly fall back to it. The rule lives in the Runtime — where the account lives, and where it is machine-checkable without a game — while the adapter keeps only what it OBSERVED. The destructive families (mine/turret/unstable-crystal explosions) own their consumption checks inline because they do not go through the action library, so a change to what "reached" means has to be applied there too. | `docs/architecture/save-archive-format.md` §6, `docs/backlog/review/save-mid-run-consistent-cut.md` (the shared action verdict) |


| 174 | The layer-boundary kernel reset is ONE family decided by ONE rule — a fact about the layer being LEFT must not survive into the layer being entered — and the family has exactly four members: the world-rooted item rows, the world-entity facts, the enemy LIVE rows and the fluid chunks. The PLAYER table is not a member: `PlayerState` carries the cross-layer terminal facts (alive/conscious, the carry relation, the limb latches, body state, skills), so resetting it would erase a player's injuries on every descent, and its reset command family is deleted rather than left dead. The enemy TOMBSTONES are kept (`EnemyStateTable.WithoutLiveEnemies`): "this enemy was killed" is a terminal fact the player earned, and dropping it would let a stale live row resurrect the id. The seam is the WORLD-ENTRY reset (`WorldEventSync.TryCaptureWorldBaseline` -> `WorldService.ResetWorldLayerTables` -> `LayerScopedTableReset`), which is where the whole layer-scoped family already resets and which the mid-run restore's pending-replay gate already skips. The layer-end cut's own kernel commit is NOT a seam a snapshot may depend on: the reset and that cut run inside ONE call stack (the generation boundary resets, then publishes the params that commit the advance that takes the cut), while the per-frame enemy projection mirrors the LIVE scene back into the kernel — so which of the two runs first is a frame-timing fact, and the archive carries the rule on its own side too (a layer-end cut writes no live enemy row and no fluid chunk, and keeps the enemy tombstones). The four reset COMMANDS are host-local by construction — `WireCommandKind` maps none of them, which is the ACTUAL gate rather than a formality: the received-command path carries no role check (`KernelProtocolCommandHandler` executes what the envelope names, and the kernel's `CommandContext` has no role), so a wire-mapped layer-scoped reset is a destructive trigger any guest could fire — while the RESET EVENTS keep their wire form, so a guest's replay kernel learns at the same boundary; the layer-end restore that names the layer being entered runs the same two kernel resets for the enemies and the fluids after `kernel.Restore`. | `docs/architecture/domains.md`, `docs/architecture/save-archive-format.md` 3.4, `docs/evidence/sync-coverage-matrix.md` rows F4/N2/P2, `docs/backlog/review/save-mid-run-consistent-cut.md` |

| 175 | What a trap/entity row's fact IS decides its verdict, and the TRIGGER PATCH is the evidence: a patch that reports the entity's own latch or value (`MedStationScript.didHeal`, `ScrapEaterScript.scrapAmount`, an `activated` flag) names a state the shared action must WRITE, while a patch that reports a TRANSITION the action itself performs — the bio terminal's collider enabled → disabled, i.e. the `Backgroundify` — names a state the local copy may be unable to write at all, and that is `NotApplicable`, decided BEFORE any write for that row (no beep, no neighbouring doors: a half-unlocked terminal is worse than an untouched one). `NotApplicable` means "the fact is NOT in the world"; it is NOT a promise that nothing was written — the heat toggle cycles as far as it can before refusing, and a crystal action mirrors its effect's latch before it decides. The verdict must never report the row as reached. Six over-claims of that family are closed in one cycle: `ApplyShower` and `ApplyHeat` refuse when `LifepodController.shower` / `.heater` is null — the two inspector members the controller's OWN lifecycle never touches (`Start` writes heatSprite, disinfectSprite and heatButton only, LifepodController.cs:8-13), which is exactly what makes their absence silent instead of already-broken, while the unguarded `ActivateShower` / `ToggleHeatState` dereferences them (LifepodController.cs:45-49; the heater writes at :25-26/33-34/39) and threw out of the replay's row loop — with the heat row's state checked FIRST, so a copy already AT that state stays `Applied` (the heater is only the way to REACH a state). `ApplyBioTerminal` refuses when `terminal.GetComponent<BuildingEntity>()` is null. The three CRYSTAL rows whose patches report an `activated` latch now MIRROR it, because a replay that skipped it left the peer's copy ARMED: `ApplyCrystalEMP` writes the latch its patch reports (`TrapCrystalPatch.EmpTryEMPPostfix`) — without it the crystal's own Update returns (CrystalEMP.cs:54-58), so the copy stayed permanently WHITE and this side's own player could touch it again and drain their own batteries — and `ApplyCrystalShy` writes the latch the game writes after the scan whatever it found (CrystalShy.cs:31), while `ApplyCrystalMetamorphic` mirrors BOTH marks its row needs — the effect's `activated` latch and the death (health = 0 as a REMOTE death): the crystal dies inside its own `Touched` (CrystalMetamorphic.cs:26), the kind is NOT in `TrapDamageProfiles` so no health row rides the trigger batch, and the old claim that its death "rides BuildingEntityDamaged" was false. The death ALONE is not enough, which is why the latch is written first: `health = 0` only makes the entity's own Update destroy it on its next run (BuildingEntity.cs:56) and `RemoteEntityDeath` suppresses only the BUILDING's drop roll, so during that frame the peer's own player could still touch the crystal, run the game's own `Touched` — whose only guard IS the latch (CrystalMetamorphic.cs:18-21) — and roll a second set of 1..4 drops; the `ApplyCrystalFragile` analogy fails exactly there, because that kind has no latch and rolls no drops of its own. Those three replays' sounds also now match the game's own calls (`true, false` = 2D + no pitch shift, Sound.cs:52; the other five crystal calls already matched), and the mimic's and the unstable's effect lookups were extracted into ONE `CrystalEffectAccess`, so the new sites add no third and fourth copy of the untyped list scan. The shy row's scan stays a faithful mirror of the game's own, including its ability to match the crystal's own collider, and the row's POSITION is the one the report patch reads AFTER the swap (`TrapCrystalPatch.ShyTouchedPostfix` → `ReportCrystal`), so a replay re-runs the scan from the partner's pre-swap position and its pairing follows the same unspecified `Physics2D.OverlapCircleAll` order the game carries (recorded, not changed: the mirror is faithful and only a dual-client pass can settle it). A throwing ROW is scoped to the HALF it belongs to: the world-entity write is contained in its own step (`RestoredWorldFactReplay`), so a row reaching an engine call the local copy cannot serve reports that half as NOT FULLY written — with the throw as its reason — and releases only that half's handover, exactly like a refused row of the same half, while the halves that had already landed keep their accounting AND their commit, because the replay's own per-half contract — a refused entity row is not evidence that the block diff did not land (decision 172) — cannot stop holding when the row throws instead of being refused. The rows of the throwing half BEHIND that row are still lost; containing that one level deeper — a per-row catch inside the adapter's row loops, which the adversarial review showed can keep BOTH the exception in the log and an exact refused-row count — is `docs/backlog/review/restored-entity-row-containment.md`, not this cycle. | `docs/architecture/save-archive-format.md` §6, `docs/backlog/review/trap-action-divergence-hardening.md`, `docs/backlog/review/restored-entity-row-containment.md` |

| 176 | The menu-return trigger is the LEAVE ACTION, not a scene-state edge: a deliberate "leave the world" is intercepted at the game's own action (`PlayerCamera.ToMainMenu`, whose body is one `SceneManager.LoadScene("PreGen")` and whose callers are the tutorial pause exit, the console's `saveandquit`, the layer-end panel and the death screen's MAIN MENU button — the last two are scene-wired UnityEvents, not source call sites), recorded by `RunMenuReturnCoordinator`, and performed by the frame-end seam AFTER the cut — the scene load is skipped once and replayed on the pump, so the world the cut reads is still alive. An in-world → menu transition edge is too late to arm anything: the scene load destroys the world it would have to read. Because the leave is the trigger, SOLO play gets a menu-return cut (it has no session teardown event to ride), and the save-authority rule becomes "only a GUEST lacks the world-archive authority" (decision 164) rather than "a host saves": `RunMenuReturnPolicy` answers `SaveAndMenu` for a host and for the role-less solo player, `MenuOnly` for a guest, and the seam's own cut request still refuses a guest. Suppressing the game's load is only allowed when the seam WILL perform the leave — the record and the seam therefore share ONE Runtime rule (`RunMenuReturnPolicy.WouldLeaveWorld` / `DecideFlush`, `MenuExitInterception`), the request carries its ORIGIN (a session teardown is dropped when a session took over; the player's own leave and a host-pull are never dropped while a world is leavable), and the request is cleared only after the leave actually happened — a silent drop would consume the player's action and strand them in the world. A TUTORIAL entry gets no world archive at all and releases the previous run's identity, so the new trigger can neither create an archive the Continue entry would open nor aim a `/save` at the previous run's snapshot. | `docs/architecture/save-archive-format.md` §4, `docs/backlog/review/save-solo-menu-exit-trigger.md` |

| 177 | Which present peer claims a stored `characters/<playerKey>.json` — and which players a CUT may write one for — is ONE rule with THREE answers (`PlayerKeyResolution.Claim` → `PlayerKeyClaim.Unclaimed` / `Claimed` / `Ambiguous`), because decision 162's two failure modes are not the same fact: nobody present claims it (benign — that player joins as a NEW character and the file stays for a later claim) versus MORE THAN ONE present peer claims it (a REFUSAL). IP-direct keys a character by display name and that mode deliberately allows duplicate names (resolved ticket "IP-direct duplicate names"), so the first-match resolution that shipped with S2 silently handed one player's character to whichever peer the member list yielded first — the exact outcome scope 1 forbids — and reported it as the benign case. The ambiguous claim is now refused for BOTH claimants and travels in the restore account (`WorldCharacterBindResult.ClaimRefusals` → `WorldRestoreApplier`'s damage list) instead of the log alone, and the key-space mismatch (a Steam world opened over IP-direct) is a third refusal named as a loss rather than an information-level line. The rule is counted, never short-circuited, so only two DIFFERENT peer ids turn a claim ambiguous (a roster listing one peer twice is not a conflict, pinned by `Claim_OnePeerListedTwice_IsNotAmbiguous`); in the Steam space the match IS the account id, so ambiguity there needs two distinct peers under one id, and in IP-direct it is ordinary. The CUT side obeys the same rule: a key that two present players WHO BOTH CARRY A SNAPSHOT map to is carried by NEITHER file and the cut report names it (`WorldCharacterCutSet.NotCarried`), because a file under a shared key is worse than no file — a later restore could claim it for the wrong player (before this, the duplicate path made `SaveArchiveWriter` refuse EVERY cut while two same-named players were present, with an internal-invariant message that named no cause). A same-named player who reported NO snapshot is not a sharer: the key then has one claimant and that character is written, because there is nothing of the other player's to protect. | `docs/architecture/save-archive-format.md` §6/§6.1, `docs/backlog/review/save-guest-restore-claim-and-legacy-store-retirement.md` |

| 178 | A character has ONE persistent copy: the world archive. `CasualtiesUnknownOnline.character-data.bin` (`Session/CharacterData/CharacterDataFileStore` + `CharacterDataFile`, archived decision 27) is DELETED rather than kept in sync, because the host's per-SteamID disk copy and the archive's `characters/<playerKey>.json` were two persistent copies of the same facts — what decisions 162/164 exist to remove — and the module is unreleased, so the user decided on 2026-09-10 that there is no migration burden. `CharacterDataStore` is therefore in-memory and session-scoped: it starts EMPTY, the live 1 Hz reports fill it, and a restore refills it by binding the archive's stored characters into it (`WorldRestoreApplier` → `WorldCharacterBinder.Apply`); the `characterDataFile` parameter is gone from the composition root and the plugin. Nothing reachable is lost: a host restart can only re-enter a run by continuing (which restores from the archive) or by starting a new run (which clears the table), and `SendSavedCharacter` already refuses while the host is not in a world, so the disk copy was unreachable before either click — its only distinct behaviour was resurrecting a character the package had ceased to contain. | `docs/architecture/save-archive-format.md` §2, `docs/backlog/review/save-guest-restore-claim-and-legacy-store-retirement.md` |

| 179 | The save system's player-visible surface is the COMMAND CONSOLE it already had, and every answered save event is exactly one notification plus an itemized history. A resolved Continue attempt raises a `WorldRestoreReport` whose disposition distinguishes the three things a player must be able to tell apart — `Refused` (nothing was applied; decision 165 forbids falling through to the native path), `Applied` (the checkpoint and the stored characters came back and the run starts), and `Abandoned` (the click applied the archive but no world generation will consume it, so every handover was released) — and the console prints its HEADLINE as the line the player is interrupted by while the account itself (the per-domain counts, each skipped content id, each refused claimant, each unbindable native field, and the backup a load fell back to) goes into the history. Detail lines are therefore not notifiable (`ConsoleLine.Notifiable`, filtered by the Runtime's `ConsoleNotificationPolicy`), because a damaged restore is ONE event: announced items would fill the closed console's newest-few window and push every other notice out of it. No new UI is built for this — the console is the existing surface the cut report already uses (`CommandConsoleService` renders both) — and the same rule governs the log: ONE line per cut and ONE per restore carrying the world id, the cut kind, the cut phase, the revision, the layer and the per-domain record counts in the same vocabulary on both sides (`WorldSnapshotCounts`), so a restore that took fewer rows than the cut wrote is a two-line comparison rather than a deploy-and-reproduce loop. Both sides count the rows the ARCHIVE holds, never the writer's input: a layer-end cut drops its in-layer rows by design, so the cut's counts come from the encoder's own output (`EncodedSnapshot`) while the restore's come from the rows it decoded out of that same archive. | `docs/architecture/save-archive-format.md` §6, `docs/backlog/review/save-restore-account-surface.md` |

| 180 | The run's `startingsupplies` are handed to a player the world has no character for, once per BODY — and the game's own grant is never duplicated. The native grant fires on the run's POSITION, not on its origin: `WorldGeneration.WorldPlacePlayer` guards it with `totalTraveled <= 0 && biomeOverride == None && debugStartDepth == 0` (`WorldGeneration.cs:1891`, the setting read at `:1899`, the items created at `:1904-1912`), and a CUO continue reaches that guard exactly as a native load does — the click lets the original `PreRunScript.LoadRun` run (`PreRunScriptLoadRunPatch`) and CUO blocks `SaveSystem.TryLoadGame`, the only writer of a non-zero `totalTraveled` on a load, so the live field is the generation baseline's. A run still on its starting layer therefore gets its supplies from the game whether it was started here or restored; every entry that is NOT that layer — a restored run past it, a mid-run join, a run started at a debug depth — gets them from CUO, with the same content ids and the same slot numbers reproduced (`StartingSupplyPolicy.PlanFor`) so either path leaves one inventory, over the game's own calls (`Utils.Create` + `Body.PickUpItem(item, slot, force: true)`) behind `IStartingSupplyBehaviour`, an Abstractions seam that is what makes the whole decision runnable in the test host. The condition is the game's own and NOTHING ELSE — three clauses, and a second independent pass killed two candidate additions to it: "unless the generation came from an archive" is wrong (the game's grant DOES fire there, and the clause handed a second set into slots it had just filled, every item refused silently by `PickUpItem` and left on the ground, which is why this stage carries no Runtime "restored generation" fact at all), and "only if the run was not loaded" is wrong in the same direction (`WorldGeneration.cs:1891` does not read `LoadedRun`, and a native load writes `totalTraveled` from the save, so a stored run frozen at its starting layer still gets the game's grant and an `!LoadedRun` clause would answer with a second set). `debugStartDepth` IS one of the three clauses and is read from the live field rather than assumed; it cannot currently fire on either side (game code never writes that field), which is recorded where it is read instead of being claimed as two-sided. "The world has no character for me" is answered by the LOCAL pending character restore (`LocalCharacterRestoreQueue`, one instance shared by the character domain and this decision), read at the GRANT moment: a restore that lands between the body's entry and the grant is exactly the event that must cancel it, and one that lands later wipes the grant by construction (its own first pass empties the slots). Once per BODY, tracked by reference identity (never `Equals` — Unity objects compare by "destroyed") and cleared at a run start and at a session end, so a layer descent (which keeps the body) does not re-supply and a death or a reconnect (which does not) is judged on its own entry. The account is one console line in the same words as the log (decision 179), for every judged body INCLUDING the ones that got nothing — the run's `none` is an answer, not an absence — with two exceptions: a body the world already has a character for (that restore is its own reported event) and a generation whose baseline is not published yet (not a verdict: the body stays unjudged for a later frame). | `docs/backlog/review/save-new-player-starting-supplies.md`, `docs/backlog/review/save-multiplayer-restore-and-backups.md` |

| 181 | A world folder has ONE writer, and that rule is enforced on the FOLDER rather than by process convention (`WorldLease`, `world.lease`): every write records the writing process (`<machine>:<pid>`) and the instant, and a write that finds a lease another process refreshed inside a 30-minute window is refused with the holder named (`SaveWriteResult.Failure.LeaseHeld`, surfaced through the cut report the console already renders). The repository is the only writer of a world (decision 164), but that held per process: two CUO instances pointed at one folder — the same machine twice, a copied folder, a folder over a share — would interleave §5's transaction, both staging into the one `.staging/` and both renaming `live/` aside, so the loser's snapshot is deleted by a commit it never saw and nothing in the archive says why. A lease nobody refreshed for 30 minutes is taken over with a warning (three default autosave intervals, so a live host refreshes it long before it could look stale; a crashed one cannot lock the world forever), a lease this process already holds is refreshed rather than refused (a menu return and a later Continue are the same writer by construction), an unreadable lease file counts as stale because it names no live writer, and a lease file that cannot be WRITTEN is not a refusal — the folder being unwritable is the write transaction's own failure to report with its own step name. The restore path takes the lease BEFORE it applies anything (`WorldRestoreApplier` → `WorldRepository.TryHoldWorld`), because a restore writes (it can promote a backup over the live snapshot) and a restore that landed in a world another instance is playing would be overwritten by that instance's next cut without either side seeing the other; a clean shutdown releases the lease it owns, and never one another process took over. The same scope's other failure modes are answers rather than exceptions: a read-only save directory, a drive that went away and a file where the root should be all leave the run playable with the cause logged (`WorldCatalog.TryEnsureDirectory` → `WorldCreateResult.Failed` / `SaveWriteResult.Failure.StageFailed`), because those paths run inside the run's own start and inside the frame pump. | `docs/architecture/save-archive-format.md` §2/§5, `docs/backlog/review/save-interval-autosave-and-backup-recovery.md` |

| 182 | A snapshot CUO refuses is never deleted by the next cut: the refusal is recovered from the world's own newest DECODABLE backup, and the snapshot it replaces is preserved as evidence (`WorldRestoreRecovery` + `WorldBackupPromotion`). Decision 163's salvage is per entry, and §6's fallback already ran while the MANIFEST was read — so a refusal that happens afterwards (a manifest whose kind contradicts its payload, a run baseline that cannot be read) had no retry at all, and the transaction of the NEXT cut renamed the refused `live/` to `.previous/` and deleted it: the evidence of what went wrong was destroyed by an autosave. The restore now walks the world's backups newest-first, skipping the one the load already used, decodes each with the same decoder that refused the live snapshot, and takes the first that decodes; that one is PROMOTED — the refused snapshot moves to `damaged-<stamp>/` untouched (nothing else reads or deletes that folder), the snapshot about to be replaced is archived into `backups/` with its provenance rewritten to the `pre-restore-backup` reason (so the world's history keeps a loadable copy where retention and the fallback can see it; its KIND is unchanged, because the kind says what the snapshot holds and it is the archive's name), and the backup becomes `live/`. Nothing moves until the replacement is unpacked and verified in a fresh `.staging/`, a promotion that cannot finish puts the preserved folder back and refuses, and when no backup decodes the continue is refused with the reason it already had while the folder is left exactly as found. The account is one line per half — what was preserved, what was promoted, what the pre-restore archive is called — and it rides the restore account decision 179 already defines, never the log alone. | `docs/architecture/save-archive-format.md` §6/§7, `docs/backlog/review/save-interval-autosave-and-backup-recovery.md` |

| 183 | The restored world-entity half's per-ROW containment is one Runtime rule, and the Game Adapter keeps only what it observed. Decision 175 scoped a THROW to the half that threw (the world-fact half keeps its accounting and its commit); the level below it — one throwing row costing every row BEHIND it in the same loop, plus every applier that had not run yet — lived in FIVE unguarded loops — the world-entity half's three appliers (`EntityEventSync.OnTrapStateProjected`, `WorldBuildingEntitySync.OnOpenedEntitiesProjected` / `OnBuildingHealthProjected`) and the two world-fact tables that iterate the restored rows into the world's block/damage API (`WorldBlockStateTable.Apply`, one row per restored cell and unbounded, and `GameBlockDamageTable.Apply`) — and the half could then only report "not fully written", because the throw had left no count. `ContainedRowLoop.Run` catches per row, counts a row as applied only when the applier said the live world took it, returns `LiveWorldWriteOutcome` so applied plus refused is always the row count, and logs the row's identity at error level together with the exception — the account names the ONE lost row and the log names WHICH copy the local world could not serve. It has two shapes: `Run` for an applier that reports whether the live world TOOK each row (applied plus refused is always the row count), and `RunContained` for an applier that keeps its own written/unchanged/refused accounting and gets back only the number of rows that threw, so no row is counted twice. Decision 196 adds a third shape, `RunLiveWorld`, for a loop whose unit is a LIVE world object rather than a cut row, and makes the error line's identity read defensively. The first five throwing rows are named and the rest are counted in ONE summary line, because a systematic failure is a demonstrated shape here (decision 175 exists because every copy of one entity threw) and a flood of stack traces must not replace an exact count. The three loops of the same restore that were NOT converted in this cycle — the keypad and geyser tables (they iterate the LIVE world and count matched rows, so the identity and the count would both have to change meaning) and the recipe table (validated-index field writes whose refusal record is index-keyed) — are recorded with that per-site reason in the ticket this cycle was split into, which landed in the next cycle (`review/restore-live-object-loops-containment.md`, decision 196). The rule lives in the Runtime because the account is the Runtime's (decision 173 moved the sibling reach rule there for the same reason) and because the test project loads `GameAdapter.dll` reflectively instead of compiling against it, so an adapter-internal helper could not be driven by the Runtime-seam test the ticket requires; the adapter keeps the per-row action and the row's identity, and its three call sites are read-only reviewed because their bodies are game-typed. | `docs/backlog/review/restored-entity-row-containment.md`, `docs/backlog/review/trap-action-divergence-hardening.md`, `docs/decisions/active.md` 173 |

| 184 | Gameplay experience outranks authority purity: a judgment about what happens to a player belongs to THAT player's client, on its own view and timeline. The host owns the world it simulates and the arbitration of conflicting claims; it never decides the outcome of another player's body, reach, or operation timing. Applied by the user's 2026-09-18 ruling to five families: enemy hit determination moves to the victim (`review/enemy-hit-determination-local.md`, superseding the host-ordered attack recovery `resolved/enemy-attack-delivery-recovery.md`), interaction gates (line of sight and operation preconditions) move to the two clients involved (`review/remote-interaction-local-gating.md`), medical reservations change from exclusive locks to per-unit claims so several operators can work one victim (`review/concurrent-medical-operations.md`), item creation registers ahead of every operation on the item with a refused-creation tombstone instead of a wait-and-guess window (`review/item-creation-registration-first.md`), and world-time acceleration is initiated locally with host arbitration and a broadcast fallback (`review/world-time-local-initiation.md`, superseding the manual half of `resolved/sleep-behavior-policy.md` while its all-unconscious sleep gate stands). LATENCY IS NEVER A PARAMETER OF A JUDGMENT: a fixed tolerance window that ignores the peer's measured RTT is a smell, and the worst case of it is a locally completed pickup rolled back because the window guessed 500 ms. The host keeps the other half — conflicting claims stay host-arbitrated and first-writer-wins, with immediate precise answers, and a rollback when arbitration refuses is the accepted cost because the race is rare while a host verdict on somebody else's screen is paid on every hit. | `docs/development/agent-reference.md` Sync model, `docs/backlog/README.md` |

| 185 | An enemy attack is an ANNOUNCEMENT and the client it lands on judges it: the host publishes `{EnemyId, Kind, AttackSeq}` (a per-enemy monotonic identity) to every in-world guest, and each guest decides against its OWN screen — a spider bite needs a real collider contact plus the game's facing gate (`SpiderHandler.CheckForLimbDamage`), a crystal lunge runs the game's own ray with the first body winning and the ground stopping it (`CrystalEnemy.Lunge`) — then applies the game's damage locally and reports the terminal state through the existing kernel chain. The host keeps the enemy's action (AI, aim, its own bite retreat/cooldown, and the native collision path for its own body) and names neither a victim nor a limb, so one attack reaching two players is two local judgments and a spider lunging at air is a legal outcome. A repeat, a reorder and a malformed identity fail closed; no latency value enters the decision. | `docs/backlog/review/enemy-hit-determination-local.md`, `docs/en/internals/enemy-sync.md` |

| 186 | The recipe-unlock set is an absolute fact read from the GAME's own table, never a mirror CUO keeps: `Recipes.recipes[idx].INT = 0` is what the game's own blueprint use writes (`Item.cs:4284`) and that table is a per-process static rebuilt in `WorldGeneration.Awake`, so the unlock's one-shot report/relay (sync-coverage audit I6) left a swallowed send or a member that joined later with no way back to the fact. The host sends its live table's unlocked indices on the world-entry group and the 60 s in-session repair (the groups every other absolute table already rides), a guest reports its OWN set on the same fallback cadence until the host's set carries it, and the host merges a guest's set through the ordinary unlock path — apply locally, relay exactly the indices it had not learned, source excluded — so there is still one apply path and the host stays the authority. The set is unlock-ONLY and MONOTONIC (nothing writes an unlock back; the blueprint is spent on the side that used it), which is why an empty or partial set re-locks nothing, why the message needs no generation stamp (its keys are recipe indices and the table is a RUN fact), and why a receiver applies a backfill SILENTLY: a late joiner performed none of those unlocks, and the game's own alert belongs to the learn, not to the catch-up. A set that cannot be compared (no live table on either side) is never applied blindly — the reporter's entry stays pending and returns on the next cycle, and the receiver names the gap. | `docs/backlog/review/recipe-unlock-fallback.md`, `docs/evidence/sync-coverage-matrix.md` row I6 |
| 187 | The carried-inventory registration is an ABSOLUTE and REPEATABLE report of the guest's CURRENT carried set, and the host applies it REGISTRATION-ONLY (sync-coverage audit I8): one `CarriedInventory` frame lost in the lazy-P2P swallow window left the host's arbitration transfer table empty for the guest's self-assigned ids, and the guest's later reports only healed the KERNEL accepted-first. The guest now re-captures its carried set at every report — never a frozen frame, so an item it destroyed, dropped or handed over is simply absent and a repeat can never resurrect it — and repeats it on a dense-then-steady cadence (12 reports at 5 s ≈ 60 s, the entry swallow window, then one a minute for the rest of the world's life), with the window re-opened on the two edges that make a registration meaningful: the generation-finished falling edge and the host's id-watermark grant, which is the join AND reconnect signal because a rejoined guest restores a world instead of generating one. The host inserts only what it does not already have (an existing transfer-table entry is the arbitrated record — a cross-player heal may have consumed part of its condition — and a repeat never rolls it back) and only what the KERNEL accepts as this guest's carried item (an id the kernel places with another guest, still in the world, or has already terminated is refused with a log), because the kernel is the carried-ownership authority and an absolute report must not hand one item to two guests. The landing adds no wire member and changes no message shape — a fact about the mechanism, never a design input: the handshake's protocol check IS the compatibility boundary (decision 188), so a mechanism that needed a new message would take one and bump the protocol number (decision 137) — while the 1 Hz character snapshot stays a read model instead of feeding arbitration (that option was rejected for exactly that reason). The capture states EVERY authoritative item of the body, bound or not — a snapshot id the host knows, an id an earlier report of this reporter stamped, a product the crafting path stamped — because the first capture stamps an id on everything it reports: a capture that skipped bound items would come back empty on every repeat and send nothing at all, which is precisely how the first version of this landing shipped inert (the source gate `SourceShapeGateTests.CarriedInventoryCapture_DoesNotSkipItemsThatAlreadyCarryAnInstanceId` now pins the rule and `ItemIdCoordinator` names the failure in a warning instead of spending the window silently). A repeat never pushes KERNEL state either: a registration states ids, while the guest's own state channels (use/slot/container reports) are the ones that carry state, so a repeat can roll back neither the arbitrated entry nor the kernel fact. | `docs/backlog/review/carried-inventory-registration-re-report.md`, `docs/evidence/sync-coverage-matrix.md` row I8 |
| 188 | **Compatibility is never a design input — at any time, not only before release.** The compatibility boundary is the protocol-version check at handshake: the host drops a `HandshakeMsg` whose `Protocol` differs (`src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs`, "Rejecting.") and the guest ends the session on a mismatched `HandshakeAckMsg.Protocol` (`src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeAckHandler.cs`, "Ending session."), so a peer built on another protocol simply cannot join. Nothing therefore has to stay wire- or save-compatible, no legacy field has to survive, and no mechanism has to be weakened to avoid a protocol bump: change the wire the mechanism needs and bump `ProtocolVersion.Current` in the same change (decision 137 is the numbering policy). "No wire change" and "host untouched" are worth recording as facts, never as merits or constraints in a design argument. The user's 2026-09-19 ruling generalises the earlier "unreleased, so breaking changes are allowed" framing: that one said WHEN a break is permitted, this one says WHY compatibility never constrains the design — it supersedes the pre-release scoping in `review/remote-medical-native-minigame-parity.md` §1, `review/remote-medical-stage-1-injection-session.md` and decision 187's first wording. | `AGENTS.md` Engineering Discipline, `src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs`, `src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeAckHandler.cs` |

| 189 | Fallback stretch caps are decided by a MEASURED worst case, and a world-entry resend is healed by the member's own repeat report rather than by a shorter blind timer (the sync-coverage audit's cadence findings; `review/sync-cadence-review.md`). The governor's caps were longer than the divergence they heal: the world-item keyframe (5 s base, priority 2) could stretch to 30 s under Critical pressure, the trader fallback (5 s base) to 30 s, and the entry group's first absolute resend was the host's hardcoded 60 s cycle — while the documented lazy-P2P swallow window is up to ~30 s, so a host mutation made just after a world entry could stay invisible for a minute. Measured against the production catalog and policy the worst cases are 5 000 / 8 333 / 10 000 / 10 000 ms under Optimal / Moderate / High / Critical for the item keyframe (cap 30 000 → 10 000, so the cap already binds at High), 5 000 / 8 333 / 14 286 / 15 000 ms for the trader fallback (cap 30 000 → 15 000), and 1 000 / 1 667 / 2 857 / 6 667 ms for the fluid full viewport, which is ACCEPTED at its measured 6 667 ms because its 10 s cap never binds. The cap is applied after the byte budget by design, so a tightened cap deliberately outranks byte preservation under pressure, and the same measurement records why that is cheap here: the fallback payloads belong to the item-table family the traffic baseline measures (its 600-item checkpoint is asserted under 24 KB). The entry group's heal is EVENT-driven instead of a shortened timer at the edge because the guest's readiness window (`SessionControlConvergence`, 5 s repeats until the entry-group marker AND the start-gate release are both in) already states the two facts the host needs — the entry answer did not complete this member, and its uplink is up NOW — so the host answers a repeat with the absolute in-session repair set plus the adapter-owned entry tables (`ISessionControl.EntryRepairRequested`: keypad codes, geyser liquid types) BEFORE the two control facts, at most once per `EntryRepairSchedule.RepairIntervalMs` (10 s, six passes at most inside the guest's 60 s window) — the repair is free for an entry whose window closed on BOTH control facts, while a window a still-armed start gate holds open is answered and bounded (the host cannot tell that window from a swallowed one); the 60 s cycles stay the durable fallback, and the entry fan-out itself is still never re-run. The carried-inventory registration's 5 s × 12 → 60 s rhythm (decision 187) is measured and accepted in the same review. Every number above is asserted by `SyncCadenceDecisionTests` against the production catalog and policy, so a later cap change faces the decision instead of drifting away from the matrix. | `docs/evidence/sync-cadence-measurements.md`, `docs/backlog/review/sync-cadence-review.md`, `docs/evidence/sync-coverage-matrix.md` cadence findings |
| 190 | The guest's pending-report fallback gets an ENTRY phase — 5 s steps for 60 s after the guest's own world entry, then the steady 60 s (`review/guest-report-fallback-first-resend.md`). ONE shared policy (`PendingReportFallback`) covers every unacknowledged guest report table: the world-block, partial-damage and break-drop channels (audit rows W1/W2), the runtime entity creations (E3) and the recipe-unlock set (I6). Its flat 60 s step was longer than the divergence it heals — the documented lazy-P2P swallow window is up to ~30 s after a world entry, so a report made inside it whose live send was dropped stayed invisible on the host until the 60 s mark. The phase is anchored on the guest's own InWorld report (`ISessionControl.LocalSceneReported`, the edge `SessionControlConvergence` already arms its readiness window on) and bounded by that anchor rather than by an answer: 12 x 5 s, the same entry budget the readiness window uses, deliberately longer than the swallow window plus one step so a set that first becomes outstanding at the END of the swallow window still gets a 5 s first re-send. The boundary is DECLARED and pinned by a test rather than implied — a set that first becomes outstanding in the last seconds of the phase is already ~30 s past the swallow window, its live send is delivered, and it gets the steady step exactly as before this phase existed. The cost is paid only while something is unacknowledged: at most 11 dense re-sends of an outstanding set per entry phase — the twelfth deadline lands exactly on the phase boundary, where the steady step governs — and zero for a set the host answers before a step elapses, so the steady one-a-minute cadence is unchanged. The anchor is retired when the clock reads backwards past it, so an `Environment.TickCount` wrap falls back to the steady step instead of stranding the dense step in the future. No wire member was needed — a fact about the mechanism, never a design input (decisions 137/188). |

| 191 | A member-facing absolute table is re-derived from its LIVE source at SEND time, and the send is the single owner of that freshness. The trap-layout table (`TrapLayoutRegistry`) is only as fresh as its last SCAN: the generation-finished edge records the layer's entities once, the layer-boundary reset CLEARS the table (`WorldService.ResetDamagedBlocks` -> `ResetWorldLayerTables` -> `ClearWorldFacts(includeKernelWorldEntities: true)` -> `EntityEventChannel.ResetTrapLayouts`), and since W6 the 60 s repair re-derives it from the live scene — so what a send carried described an earlier instant at best: an entity the world had since removed was still in it (a self-destructed turret, a broken crystal) and became a phantom on the member, because the member-side apply is an absolute align (materialize missing, destroy surplus) and a freshly generated guest world holds no such entity; a table the boundary reset had cleared sent nothing at all, leaving the entering member with its own diverged generated traps. `WorldEntryFanout` therefore calls the Runtime port `ILiveTrapLayoutSource.RefreshFromLiveScene` in BOTH group sends — the entry group (the InWorld edge and the reconnect-while-InWorld handshake, which are the same fan-out) and the in-session repair group (the periodic wave and the entry-window repeat answer) — so no caller can send the table without it, instead of the previous arrangement where the re-derive belonged to one of the two callers. The port is OPTIONAL on the same convention as `INativeWorldFacts`: a composition without one (a Runtime-only build) sends the table as last derived rather than being unable to construct the fan-out. The Game Adapter owns the scan (`LiveTrapLayoutSource`, registered by the plugin's composition root next to `NativeWorldFacts`) and its three rules: host-only, no scan while a layer is generating (the generation edge's own scan owns that moment), and at most ONE scan per frame — the repair wave calls it once per in-world member inside one frame and a scan is one `FindObjectsOfType` pass per sync-domain component type, so a WAVE costs one scan however many members it heals, while the entry edge, the reconnect handshake and each entry-window repeat add their own bounded-by-one-per-frame scan at moments W6 did not scan at all. An EMPTY scan is refused by the registry against a non-empty table (a scene in transition is not a destroyed layer), which keeps the fail-safe direction a stale entry, corrected by the next scan that sees entities, never a mass destroy on every peer; a non-empty but PARTIAL scan is accepted (the inherited W6 asymmetry: the alternative is an unmeasured shrink threshold), and the `host layout re-derived from the live scene: <N> entries` line is what lets the field compare counts. | `docs/backlog/review/trap-layout-entry-snapshot-staleness.md`, `docs/evidence/sync-coverage-matrix.md` row W6 |

| 192 | Partial block damage is accounted PER SENDER, and the host's row is a single accumulating number that is never re-derived from the ledger (review/partial-damage-delta-report-overlap, protocol 32). The W2 recovery merged a guest's ABSOLUTE report with a per-cell MAXIMUM into a row the live DELTA keeps adding to, so the halves disagreed in both directions: two senders whose reports both arrived converged to the higher value (under-count), and a delta landing after the report that covered it doubled that hit (over-count). The fix is the semantics both halves already assumed: a report states what THAT SENDER has applied to the cell (the guest's own cumulative contribution, `LocalBlockDamageContributions`), a live delta carries the same cumulative value (`BlockDamagedMsg.Contribution`), and the host resolves every arrival against one entry per (sender, cell) (`RemoteBlockDamageLedger`) — the difference is what gets applied, so a repeat, a stale frame and a duplicate report are the same no-op, two senders' entries add up, and a delayed delta resolves to zero. The increment is applied through the game's own `DamageBlock` in accumulated units (bonus-metal multiplier off), so WHEN a block breaks is the game's rule and is unchanged, and a report-caused break runs the same state relay and support-loss marking as a relayed one. Two alternatives were rejected with reasons: sequenced deltas alone (a sequence makes a duplicate droppable but leaves the report an absolute total, so the sum stays out of reach) and accepting the loss (the row can exceed the block's health, which changes when the block breaks — a user-visible divergence, not a presentation artifact). Consequences recorded rather than hidden: the guest's answer is the host's answer to ONE report (`BlockDamageSnapshotMsg.AnswersReport`, sent to the reporter only) because clearing a third party's outstanding entry with state convergence is what silently dropped the second sender's contribution; a block write on the cell forgets the cell on both sides (a fresh block must not inherit the old block's accounting) and a world/layer baseline clears both tables; the wire now names the block CELL instead of a world position (the key every consumer of the family already used); the ledger's own cap degrades to "every report is new" with one log per episode instead of losing damage; and the adapter's engine-side half (the applied-increment read, `DamageBlock`'s accumulation, the break handling) rests on code review plus the dual-client pass, exactly as W2 recorded. | `docs/backlog/review/partial-damage-delta-report-overlap.md`, `docs/evidence/sync-coverage-matrix.md` row W2, `src/CasualtiesUnknownOnline.Runtime/Session/World/RemoteBlockDamageLedger.cs`, `src/CasualtiesUnknownOnline.Runtime/Session/World/LocalBlockDamageContributions.cs` |
| 193 | The run clock base and the layer timer reach EVERY member, and they do NOT ride the kernel run baseline (review/save-run-clock-not-sent.md + review/save-layer-time-not-carried.md, protocol 33). Both are per-PROCESS game state (`SaveSystem.savedRunTime` is a static that is 0 on a guest's fresh launch; `WorldGeneration.world.layerTimeSpent` belongs to the generated world) and neither is a generation INPUT — nothing about a layer's shape is generated from a clock — so appending them to `WorldStartParams`/`RunState` would have re-purposed the generation baseline as a side channel AND created a second carrier for the clock the archive already owns. They travel as their own absolute message (`RunFacts`), sent with the world-entry group and the 60 s repair group right after the kernel checkpoint, and stamped with the kernel run baseline's generation — the same identity every layer-relative world report carries, so a value from another layer is refused. The write into the live world is guarded so no reachable path can move either value backwards: the clock is written at most once per world (a new layer re-arms the marker at the generation boundary and takes the new base) and the layer timer only when it advances, which makes the 60 s repair re-send idempotent by construction rather than by timing. The re-arm half of that rule (the `!_sawRunClock` arm, which lets a NEW layer take a base BELOW the previous layer's final total) is HOST-only, at the generation boundary: a guest never re-arms it and does not need to, because every clock it receives is the host's own increasing total. A message whose stamp names ANOTHER layer of a run the receiver already knows still contributes its CLOCK (run-scoped) while its LAYER TIMER and its layer LIMIT are dropped together (layer-scoped) — the clock is never held hostage by a layer mismatch; a stamp that cannot be compared at all (this side has no run baseline yet) applies all three, because refusing an absolute value from the only host on the connection would leave a joining member's radiation timer at zero for that whole window. On the archive side the layer timer is an OPTIONAL member of the same `native-run-fields` row (`layerTimeSpent`; absent, never null, when nothing was recorded), written by a mid-run cut only: the user's 2026-09-19 ruling is RESUME — a continued layer keeps the time it had already spent, so the radiation line's countdown does not restart, which is better than the native continue. The LIMIT is not carried because the restored run settings determine it. | `docs/evidence/sync-coverage-matrix.md` row R9, `docs/architecture/save-archive-format.md` §3.4/§4, `docs/backlog/review/save-run-clock-not-sent.md`, `docs/backlog/review/save-layer-time-not-carried.md` |

| 194 | A checkpoint chunk set is validated against the run identity the host ANNOUNCED, and the pending set is buffered by its own identity (review/checkpoint-run-epoch-validation.md, protocol 34). The guest's OWN kernel epoch is not that identity: it is a local counter that the session teardown bumps and a restore overwrites, so it can be ahead of the host's (a process that hosted before it joined) as well as behind it — comparing an arriving set against it would refuse the host's own first checkpoint of a connection. The identity therefore travels on the edge at which a run legitimately begins, the enter-the-world instruction (`WorldJoinMsg.RunEpoch`, stamped from `ItemKernelAuthority.CurrentRunEpoch` at every send site — `WorldService.SendWorldJoin`/`SendWorldJoinTo` and the handshake's direct join), and the guest refuses a chunk whose epoch is not it BEFORE anything is buffered; a set that arrives with no instruction before it (a reconnect's entry group precedes the join) defines the identity instead, and the identity is released with the session, so the next connection's host may serve any run. The pending buffer is keyed by `(RunEpoch, GlobalRevision, ChunkCount)` rather than by chunk index alone: keyed by index, a leftover chunk of a superseded set satisfies the count check for a slot the live set never fills, so the member never restores again — permanently, since every later set leaves the same residue; that is the reachable defect (measured red on the pre-fix tree by `KernelProtocolServiceTests.Guest_StaleChunkDoesNotBlockTheLiveCheckpointSet` and `Guest_SupersededSetOfTheSameRunDoesNotBlockTheLiveSet`), and a frame whose envelope-header stamp and payload stamp disagree is refused as malformed. The announcement is deliberately NOT written into the guest's kernel epoch: the restore stays its only writer, because a member that stamped the new run onto the state it still holds from the previous one could apply a batch it has no baseline for — which leaves the pre-existing residual (a guest command or report sent between the join and the first restore carries this side's own epoch and can be refused by the host) unchanged and recorded in the K4 row. The finding's original wording — a "late complete set from a previous run" — cannot be delivered inside a connection at all, because both transports in the tree are reliable AND ordered (Steam's reliable channel; `IpDirectTransport` is TCP), so this validation is defense against a foreign-epoch frame or peer rather than a repaired race. | `docs/evidence/sync-coverage-matrix.md` rows K4/R3, `docs/backlog/review/checkpoint-run-epoch-validation.md` |

| 195 | The restore's live-write account is keyed by WHICH half reports, and every release path accounts for its own half (review/restore-account-arm-release.md). A restore owes one contribution per armed live-world half (`WorldRestoreHalf`: the world facts together with the adapter's native handover, the restored world-entity facts, the restored world items), and a session ending mid-restore used to release every arm while only the item half reached the account — so at the count the applier actually returns (both arms armed ⇒ 3) the account stayed awaiting forever — measured red on the pre-fix tree for all three session-end cases (`WorldSaveContinueTests.SessionEnd_ReportsTheWorldFactHalfTheRestoreStillOwed` and `...EvenWhenOnlyTheAdapterHoldsIt`, and `WorldEntityProjectionTests.SessionEnd_ReportsTheWorldEntityHalfTheRestoreStillOwed`: `awaiting=True` with no report) — and because the expectation was a bare COUNT a contribution from another armed half fills the slot a missing half owes, which is what the identity removes (reproducible on the pre-fix tree at a count that omits an armed half: that same projection test's report then carried the item reason and no world-entity entry). Each half is therefore contributed from the ONE place its arm ends — the projection for the world-entity half (session end, supersession, layer-end cut, a throw at the seam), `ItemService` for the item half, and the save layer for the world-fact/native half, which is the only holder of both the tables' stamped attempt (`IWorldFactSource.AppliedRestoreSequence`) and the adapter's unstamped native handover — while a contribution for a half the open account does not owe, or for a half of another attempt, is logged and dropped rather than counted, and a second release of a half the seam already reported (the arm's owner releasing it afterwards, or the session teardown releasing it again) is dropped as the expected shape of that path rather than as a producer bug — so one arm released along several paths counts exactly once. The save layer's item dependency is narrowed from `IItemControl` to `IRestoredWorldItemSource` (the pending flag plus the cancel), which is what makes that port wiring pinnable by a suite instead of by reading. | `docs/architecture/save-archive-format.md` §6, `docs/backlog/review/restore-account-arm-release.md` |

| 196 | The restore's last three row loops are contained, and each reports the count it can honestly report (review/restore-live-object-loops-containment.md). Decision 183's rule gains a third shape for a loop whose unit is a LIVE WORLD OBJECT rather than one of the cut's rows — the keypad (`KeypadCodeTable.Apply`, reached by both the restore's `ApplyAbsolute` and the host broadcast's `ApplyWhereUnset`) and geyser (`GeyserStateTable.Apply`) tables iterate `Object.FindObjectsOfType` and count the RESTORED rows they MATCHED, so the live object's position is the only identity they have and "matched rows" stays the count they report. `ContainedRowLoop.RunLiveWorld` contains each live object on its own and returns NOTHING on purpose: the caller increments its count only after the matched row's write path COMPLETED, so an object that threw is already outside `applied` and the caller's `(rows - applied)` names it refused — a returned count added to that row-based refused total would count the same object twice. `RecipeUnlockTable.Apply` runs the cut's own rows through `RunContained` and refuses a THROWING row as its own class (`RecipeUnlockApplyResult.RefusedByThrow`) instead of as a missing index: the index EXISTS in the table and the write threw, so "this table has no recipe at this index" would name the wrong reason, and the row itself is named by the containment's error line, whose identity is its index — the only key a recipe row has. The restore's ITEM half had the same shape and is contained too: `GeneratedItemReconcile.Apply` runs its entry loop through `RunContained` with `bound` / `materialized` taken only after the entry's write path completed, so a throwing entry is counted nowhere and lands in the refusal the item half reports, and its leftover-destruction loop — which iterates the LIVE world — is contained per object, so a leftover the cut never described that survives is reported instead of diverging silently. The error line's identity is now read DEFENSIVELY (`ContainedRowLoop.Describe`): the row shapes name rows from message data, but a live object's identity is its position, read off the very object whose engine call just threw, so an identity that cannot be produced is reported as `<identity unavailable>` instead of throwing out of the catch block — which would have discarded the original exception and escaped the containment the rule exists to provide. With no throw every count is identical to the pre-change tree, and decision 175's half scoping and decision 183's row rule are unchanged. This closes the family the parent ticket's adversarial pass opened; no wire member changed (a fact, never a design input — decisions 137/188). | `docs/backlog/review/restore-live-object-loops-containment.md`, `src/CasualtiesUnknownOnline.Runtime/Session/World/ContainedRowLoop.cs` |

| 197 | A guest's pending command request is settled by the REQUESTER's own deadline, and the per-mod pending map is capped — the host deliberately sends nothing for a frame it drops (review/mod-command-request-timeout.md). Only the result frame and the session-end fail-all used to resolve a pending callback, so a `ModCommandRequest` that the host's token bucket dropped (or whose result was lost) left the caller hanging until the session ended, and nothing bounded the map that those entries accumulated in: measured red on the pre-fix tree with `ModCommandTests.PendingBurstOverTheCap_IsRefusedAtTheSender_AndTheMapDrainsOnTheDeadline` (35 accepted where the cap is 32) and the two deadline cases (`ModCommandTests.RequestWithNoResult_FailsOnTheDeadline_NotAtSessionEnd`, `ModCommandTests.RateLimitedRequest_FailsOnTheDeadline_WithAnObservableReason`: no callback at all after the clock passed the deadline). The deadline is `ModCommandPolicy.CommandRequestTimeoutMs` (10 s), swept once per frame by `ModCommandService.PumpPendingTimeouts`, which `ModLifecycle.Update` drives with ONE clock reading per frame; an expiring entry is removed BEFORE its callback runs, so a result arriving later can never settle a request twice, and the failure names the request (its real id and command name) — which the session-end `FailPending` now reports too, instead of `0` / an empty name. The host is deliberately NOT made to answer a frame it dropped: a per-drop NACK would let a flooding peer drive the host's OUTBOUND traffic past the token bucket that exists to bound one member's consumption, so a drop and a lost result reach the requester as the same observable timeout reason — the requester's own deadline is the single mechanism, and it is why the host's silent drops stay silent. The map is bounded by `ModCommandPolicy.MaxPendingRequests` (32 per mod): a call over it is refused at the sender (`false` + log, the existing sender-side refusal contract, no callback), which bounds memory without turning the map into a queue that moves head-of-line blocking forward. The host-local path is unchanged (synchronous, no pending entry, no deadline), and the accepted loss of `ModMessage` / `ModStatusTransport` — opaque payloads with no backfill, the next message being the recovery — is documented rather than silently assumed reliable (docs/en/reference/mod-api.md, “Mod messages”, and the matrix's N8 accepted-loss row). No wire member changed; the protocol stays 34, a fact and never a design input (decisions 137/188). |

| 198 | A player-chosen restore is armed by the Online UI's new Worlds page and executed on the runtime frame pump, and a snapshot a player deliberately stepped back from is NOT kept as `damaged-` evidence (docs/backlog/review/world-and-backup-management-surface.md). The write half of this surface already existed: every committed cut archives a `.cuoz` (§5/§7) and a world whose live snapshot cannot be opened already recovers by promoting the newest readable backup (§6) — what was missing was any way to CHOOSE among a world's archives, and any picker at all for §2's multi-world repository, whose Continue entry resolved "the last opened world, else the first one with a snapshot" (`WorldSaveService.ContinueWorldId`). `WorldLibraryService` (+ `IWorldLibrary`) therefore owns decisions only: which world the entry opens (a world with no snapshot is refused, because the entry skips it and the page would otherwise mark a world the Load button never opens), which archive a click means, whether it may run now (the save layer's existing host-only rule, plus refused while a world is loaded or being generated — the same `worldActive` fact `IWorldSaveControl.TryArmIntervalAutosave` takes — and refused while another process holds the world's writer lease), and what it reports. The click only ARMS: it happens inside an IMGUI draw callback, which runs more than once per frame, while a restore must happen exactly ONCE, at a frame boundary and only while no world is loaded — the same arm/execute split every cut already uses. The archive passes the reader's manifest/checksum gate BEFORE the swap, so a package that cannot be opened is refused while the working snapshot is still `live/` instead of being promoted over it and discovered at the next Continue. `WorldBackupPromotion.Promote` gained a trigger: `RefusedSnapshot` (the recovery) keeps what it replaced as `damaged-<stamp>/` evidence, while `PlayerChoice` relies on the pre-restore archive it just wrote into `backups/` and swaps the replaced folder through the writer's own `.previous/`, removed once the new snapshot is in place — a `damaged-` folder per restore would accumulate one full snapshot each time under a name that means "refused" and that no cut, prune or load ever deletes; when that pre-restore archive could NOT be written the folder IS preserved, because it is then the only copy. A successful restore also makes the restored world the Continue target (restoring is choosing) and runs the §7 retention pass. The page's rows are cached — listing worlds walks folders and an IMGUI draw runs more than once per frame — and `IWorldLibrary.Revision` is the single invalidation signal: bumped by the save layer's `CutReported` (every finished cut attempt, whatever the trigger, so a `/save` while the page is open refreshes it) and by every management action. The independent review's one major finding was that the first shape cached the rows behind a flag nothing ever cleared, which made the page show the world as it was when it was first opened; the revision replaced that flag rather than adding a page-entry hook beside it. Scope closed in the same change, with reasons rather than deferral: the ticket's native `save.sv` backup contradicts decision 165/§8 ("CUO never writes `save.sv` and never depends on it"); its old-protobuf migration was already excluded from v1 and its store retired in decision 178; no world deletion (the repository has no delete primitive and a destructive surface is not what a missing picker needs); no separate importer UI (a `.cuoz` copied into `backups/` is listed and put through the same gate). Limits: the page's rendering, its availability at the main menu and every frame-level behaviour are user-acceptance items — the machine evidence is the service's branches, not a rendered window; the protocol stays 34 (a fact, never a design input — decisions 137/188). | `docs/architecture/save-archive-format.md` §6/§7, `docs/backlog/review/world-and-backup-management-surface.md`, `src/CasualtiesUnknownOnline.Runtime/Session/Persistence/WorldLibraryService.cs` |

| 199 | Pinyin search extends the game's OWN search surfaces, is opt-in, and its crafting half lands on the native predicate's operand instead of re-implementing the list (`review/pinyin-search-mod.md`, stage 1). The native crafting box already filters by `Recipe.simpleName.Contains(recipeFilter, OrdinalIgnoreCase)`, and `simpleName`/`fullName` are the LOCALISED names, so no parallel CUO search UI is built (AGENTS.md 8): `PlayerCamera.RefreshRecipeList`'s prefix opens a one-refresh query scope, its postfix AND a Harmony finalizer close it (a throwing native body must never leak the scope into the next frame), and while that scope is open `Recipe.get_simpleName` answers with the real name when the shared predicate matches (native substring OR pinyin) and with an empty string when it does not — no non-empty query is a substring of `""`, so the native filter drops exactly the non-matching recipes while ordering, the category filter, the row objects, the scroll position and the selection index all stay native. The scope is skipped while the camera's private `recipeItemFilter` is set, because the native refresh ignores the text filter in that mode; if that field is ever gone the guard refuses to filter and reports it once, since guessing would silently drop rows from an item-filter view. Rejected: the standalone mod's blank-the-filter + prune-the-rows postfix, which duplicates the native row surgery, mutates the private row list, and — because it blanks the very field the native category condition tests — silently re-enables the category filter during a search. The reading table is vendored whole from PinIn (26k characters, the table behind JustUnknownCharacters) as an embedded resource and loaded lazily, with the fuzzy alternates (c/ch, s/sh, z/zh, ang/an, eng/en, ing/in) expanded at load; the matcher is a pure Runtime port (NFA/backtracking, polyphonic readings, tone-optional, case-insensitive). `Search.PinyinSearch` is a local BepInEx option whose DEFAULT follows the system language (Simplified Chinese → on, everything else off), read through `IOptionsMonitor<PinyinSearchOptions>` (refreshed by the config's change event, not by polling) by the Game Adapter's construction-time gate, so "off" leaves the native behavior untouched and a config edit applies to the next search. The pure half of that decision — switch, typed query, item-filter precedence — is `RecipeSearchDecision` in Runtime precisely so it is testable without the game, and the game-coupled patch/gate/config wiring is not covered by tests at all (the test project excludes the Game Adapter from compilation): that half's evidence is the patch-contract check plus the user's in-game acceptance. The console's resource completion plugs in as an extra ranking stage behind the existing `IResourceLocationCatalog` seam, not into this patch (landed: decision 200). The read path the hook depends on is made observable instead of assumed, with the scope of that signal stated: the scope counts the names each refresh consults and, after three consecutive refreshes with an active query that consulted none, warns ONCE (either the recipe list was empty or the native filter no longer reads `Recipe.simpleName`, which would leave pinyin search inactive). What the signal does NOT cover is recorded as a limit rather than claimed: a predicate that switched to a DIFFERENT name while the row-building loop keeps reading `Recipe.simpleName` keeps the counter fed, so that shape stays a documented gap — no patch contract can see it either, because the read lives in a compiler-generated lambda. Evidence: `PinyinDictionaryTests` / `PinyinMatcherTests` / `NameSearchMatcherTests` / `RecipeSearchDecisionTests` (the ticket's own examples plus counterexamples, fuzzy sounds in both directions, polyphonic readings, the reading-table census floor, and the decision matrix including the switch-off path). Not provable by automation: the in-game rendering and frame-level feel — the user's acceptance. | `docs/evidence/selfchecks/search/pinyin-search-selfcheck.md` |
| 200 | Pinyin search reaches the console through a pluggable extra match stage, not through catalog logic (stage 2 of the pinyin-search ticket). `IResourceLocationMatchStage` answers one question — does this entry match this normalized prefix — while `ResourceLocationCatalog` keeps its four built-in ranks (exact canonical id, id prefix, bare path prefix, display-name prefix) and the 20-candidate cap, ranking every registered stage after them at 4 + its registration index (the framework's own stages first, then mod-contributed stages in their own registration order — `pinyin-search-standalone-mod`, stage 1); a stage is consulted only for entries the built-in ranks did not match, which is equivalent to ranking the stages after and keeps a switched-off stage off the path of everything already matched. The pinyin stage matches `ResourceLocationEntry.DisplayName` only (never the id string) with the shared `PinyinMatcher.Contains`, so the console and the native crafting box agree on what "matches" means — including the matcher's literal half, which lets an ASCII display name match mid-string once the switch is on, a widening recorded rather than hidden. The switch is the same `Search.PinyinSearch` option the crafting patch reads: the stage reads `IOptionsMonitor<PinyinSearchOptions>` live per query, so one config edit covers both surfaces without a restart, and the off state performs no match and no reading-table load. The console projection stays untouched (`CommandConsoleService.SuggestResourceLocations` still emits `entry.Id.ToString()`), so an accepted suggestion is always the canonical id. The content-vocabulary registrations moved from `CuoBootstrap` into `ContentVocabularyComposition` unchanged, because the composition root sits on its 600-line cap, and both surfaces now report the reading table through `PinyinTableReport.ReportOnce`, so a missing table logs one line per process instead of one per surface. Rejected: teaching the catalog about pinyin (the ranking contract every other completion rule shares would then carry a switch-dependent matcher) and a second matcher implementation for the console. | `docs/evidence/selfchecks/search/pinyin-console-completion-selfcheck.md`, `src/CasualtiesUnknownOnline.Runtime/Session/Content/ResourceLocationCatalog.cs` |
| 201 | The game-update contract toolchain is a metadata-only developer tool that lives OUTSIDE the plugin's dependency graph, and its patch-target rows are pinned to `PatchInventory` by a gate rather than by shared code (`tools/CasualtiesUnknownOnline.ContractTool`, `docs/development/game-update-runbook.md`, backlog `review/game-update-contract-toolchain.md`). It reads a game assembly with Mono.Cecil — nothing is loaded, executed or resolved — so a snapshot can be taken of a build that has never been launched (an incoming update, a rollback copy) and the tool needs no BepInEx, no Unity modules and no game directory; it references no `src/` project and no `src/` project references it, which is what makes "never a runtime dependency of the plugin" structural rather than a promise. Its rows come from the adapter assembly's METADATA because sharing `PatchInventory.BuildContracts` would put the Game Adapter — the only project allowed to bind the copyrighted game assemblies — into the tool's dependency set; the divergence risk that creates is closed by a test instead of by code: `PatchContractRowParityTests` asserts every recovered row equals the adapter's own row and that the ONLY rows the lens cannot see are the hand-declared dynamic contracts (`PatchInventory` names those patch classes `"(dynamic)"`), which stay covered by the contract tests. A snapshot is compared, never listed: each contract row gets exactly one verdict (removed or renamed, signature changed, Harmony target ambiguous, unchanged but still needing a semantic look) and every other difference is tiered by its distance from the lens (contract-adjacent, outside), so an update produces hook work instead of a changelog; `diff` refuses two snapshots taken with different contract sets. The tool is deliberately STRICTER than the runtime guard in one place: a re-typed parameter on a name-resolved (unconstrained) target installs fine, because Harmony matches patch arguments by name, and the tool still reports it as a signature change — `PatchContractChecker` cannot see that shape move, so a report that inherited the guard's rules exactly would call the target structurally identical. Two limits are recorded rather than hidden: no method body is compared (the semantic half stays in `future/adapter-shell-verification-harness.md`) and enum member ORDER is not compared (values are). Snapshots and reports are gitignored artifacts under `artifacts/contract/` — they carry game-assembly content and are never committed. |
| 202 | The adapter's patch set is inventoried as a CAPABILITY catalog, so which gameplay system a hook belongs to is a declared, gate-enforced fact instead of a naming convention (backlog `review/adapter-capability-catalog.md`, stage 1; `docs/evidence/selfchecks/tooling/adapter-capability-catalog-selfcheck.md`). Fourteen capabilities — `session`, `world`, `items`, `traps`, `medical`, `character`, `enemies`, `crafting`, `trading`, `save`, `tutorial` Required, `mods`, `pinyin-search`, `diagnostics` Optional — are declared in ONE table (`src/CasualtiesUnknownOnline.GameAdapter/Capabilities/AdapterCapabilityCatalog.cs`) whose patch owners are compile-bound `typeof(...)` references (a container type brings its nested patch classes), so the assembly's 205 `[HarmonyPatch]` classes and the nine hand-declared dynamic rows are each claimed by EXACTLY one capability, enforced by a totality gate that enumerates the assembly itself rather than trusting the production expansion (an unclaimed class, a double claim, an owner contributing nothing, a duplicate or malformed id, a dynamic row claimed twice — `AdapterCapabilityCatalogTests`). The join identity is the patch class's FULL CLR name (205 contracts share only 201 simple names), which is why `PatchContract` now carries `PatchClassType` and the contract-tool parity gate compares it against the snapshot's own fact; the dynamic rows keep `PatchInventory`'s `"(dynamic)"` pseudo name. The Required/Optional split is the user's 2026-09-20 yardstick — a feature the VANILLA GAME has is Required and a broken contract refuses multiplayer as a whole, while CUO's own additions may degrade alone — which is why the mod content surface, pinyin search and the diagnostics hooks are their own groups, and why a class carrying BOTH a vanilla path and a KrokMP-style addition is classified by the vanilla path (`PlayerCameraDragUsePatch`: the remote-backpack take is a game feature, the cross-player use-by-drag seam is ours; splitting the class is stage 3's install-unit work). Stage 1 is deliberately BEHAVIOUR-PRESERVING: `Install()` keeps the all-or-nothing verdict, read through `AdapterCapabilityReport.RefusesInstall` over the SAME structured rows `PatchInventory.VerifyMissing` now returns (the violation texts and their order are unchanged, every attributed failure still blocks whatever its capability's class says, and the rule lives in ONE place instead of being branched per capability), while the dynamic installer's target misses stay log-only facts the report NAMES without blocking, and a declared game member that no longer resolves becomes a reported reason rather than an assumption. The report is a projection, not a second decision path: one line per capability (class, id, contract count, reasons) plus the `ProbeGame` types declared as the session capability's game types so probe and report cannot disagree, logged once from `Initialize` (Error level when the session is refused). `ProbeGame`'s four `typeof` reads stay constant-true and its verdict is unchanged; stage 2 is where a probe result starts meaning something, and `AdapterCapabilityReportTests.RefusesInstall_IsAllOrNothingForBlockingFailures` is the expectation that changes then. Superseded in part 2026-09-21: the `pinyin-search` capability was deleted with the satellite pinyin mod (decision 209), so the catalog declares 13 capabilities and the built Game Adapter carries 203 `[HarmonyPatch]` classes over 199 distinct simple names. | `docs/evidence/selfchecks/tooling/adapter-capability-catalog-selfcheck.md` |

| 203 | The Mod API's public surface is a REVIEWED BASELINE with declared stability levels, so a contract is a decision rather than a side effect of visibility (`docs/en/reference/modification-policy.md`, `docs/contracts/abstractions-api-baseline.txt`, backlog `mod-api-contract-governance`). `Abstractions` is the only assembly a mod may reference, so its surface is recorded — every public type with its base list, every public member with its signature, and each entry's level — and `ApiSurfaceGateTests` re-derives all of it from the project's own source: an addition or a changed line fails until the reviewed baseline is updated, a removal fails unless the line is deleted AND a `*REMOVED* <key> — <reason>` tombstone names why (a removal is deliberate, never a quiet deletion), malformed/duplicated/contradictory lines fail as such, the failing run writes its candidate to the gitignored `artifacts/api-surface/`, and a census floor (60 source files / 54 public types / 450 entries, against a measured 92/92/756) keeps a scan that finds nothing from passing. The extraction records what a caller can actually reach — declaration modifiers, because `static` versus instance is an API difference, primary constructors, and the C# 14 `extension` block with its receiver folded into each member — while an `[ApiStability]` argument the gate cannot resolve fails instead of silently inheriting the default level. The cycle also cleared a pre-existing red it found at HEAD: `RepositoryGateTests.NoAbsolutePaths_NoTrackedMachinePaths` read the capability report's log literal as a drive-letter path, and because that gate enumerated TRACKED files only, this cycle's own new gate source re-created the same shape where the local run could not see it — so the gate now enumerates `--cached --others --exclude-standard` (tracked plus untracked-and-not-gitignored) and a brand-new file is checked before the commit that adds it. A level is declared with `[ApiStability]`, and `Stable` is what the ABSENCE of the attribute means — a default that merely happened is the accident the baseline exists to prevent; the two game-build-sensitive surfaces are `Advanced` on evidence (`IModNativeApi`, whose operation registry follows the adapter's registration, and `IModNativeLocalPlayerState`, whose members cite the game's own `Body` fields). `Runtime` and `GameAdapter` stay implementations a mod may Harmony-patch but is never promised — patching is allowed and not treated as hostile, and it buys no compatibility promise — and the promotion funnel is patch → several mods need the same thing → `Experimental` → `Stable`, with the diagnostics an author can expect (mod-scoped logger, discovery/validation lines, lifecycle isolation, permission refusals, command results, the adapter's capability report) listed on the policy page. The protocol number lives in ONE place — `ProtocolVersion.Current` and its own doc comment — so `docs/en/reference/mod-api.md` (Versioning discipline) points at it instead of restating it and decision 137's row does the same, enforced by `ProtocolNumberGateTests` over the LIVE documents (`AGENTS.md`, the four mod-facing reference pages under `docs/en|zh/reference/`, this register, `docs/architecture/current.md`) while historical records keep the number they were written with. | `docs/en/reference/modification-policy.md`, `docs/contracts/abstractions-api-baseline.txt`, `AGENTS.md` Engineering Conventions #14, `tests/CasualtiesUnknownOnline.NormativeGates.Tests/ApiSurfaceGate.cs`, `ApiSurfaceGateTests.cs`, `ProtocolNumberGateTests.cs`, `docs/evidence/normative-gates.md` |
| 204 | Compatibility and extensibility for upper-layer mods are TIERED — upper-layer mods are meant to flourish, so the framework yields space instead of welding the ceiling and "not in the contract" never has to mean "not allowed" (owner ruling, 2026-09-20). Tier 0 is the `Abstractions` contract (`[ApiStability]` levels plus the gated baseline, decision 203); Tier 1 is CUO's own implementation, which a mod may patch by name with no promise (the “Patching CUO itself” section of `docs/en/reference/modification-policy.md`) and which the promotion funnel (“The promotion funnel” on the same page) turns into Tier 0 once real consumers exist; Tier 2 is the DECLARED NATIVE BINDING — a mod that needs the game's own code declares that binding in its manifest, stays a CUO mod, becomes visible to the host and parity-checkable, and carries the game-update churn itself; Tier 3 is the unmanaged BepInEx plugin, which CUO neither manages nor detects — Tier 2 exists because Tier 3 cannot be policed, not because Tier 3 is an enemy. The literal wording that stood in `docs/en/reference/mod-api.md` (The manifest) ("a mod never touches BepInEx, Steamworks, the game assemblies, or CUO.Runtime") was corrected there to what it always meant: only `Abstractions` may be REFERENCED, while binding CUO's implementation or the game's own code is a tiered choice. The declaration is visibility, never enforcement: CUO takes no anti-cheat stance and cannot detect an undeclared binding. Consequence recorded for the ported pinyin search: a game quality-of-life feature belongs to a standalone mod (`docs/backlog/review/pinyin-search-standalone-mod.md`), with CUO keeping only the console's published completion seam. | `docs/en/reference/modification-policy.md` (The tiers), `docs/en/reference/mod-api.md` (The manifest), `docs/backlog/review/mod-native-binding-declaration.md` |
| 205 | Where a system lives is decided by a rule, not by taste: four layers — framework core (the plug-in ships it), satellite mod (game-facing, works with CUO uninstalled), repository tool (development/verification only, outside the plug-in's dependency graph), reusable component (shared machinery waiting for its second consumer) — and a six-question test whose first two decide on their own: does its vocabulary name session/authority/world/save/mod state, and does the framework still work without it (administration, safety, its own results reaching the player) (`docs/en/contributing/repository-map-and-pitfalls.md`, “Where a new system belongs”). The command console is the worked example on the framework side: `CommandConsoleService` is both the input and the output surface — its 200-line buffer carries the save/restore/starting-supply accounts, and a host without it loses the administration and save verbs — so it is framework core, while its input/completion engine is the component candidate, and the promotion funnel's own rule says a component waits for a real second consumer instead of being extracted pre-emptively. Pinyin search is the worked example on the satellite side (`docs/backlog/review/pinyin-search-standalone-mod.md`). Recorded so the next "should this be its own mod?" question is answered by the test rather than re-derived. | `docs/en/contributing/repository-map-and-pitfalls.md` “Where a new system belongs”, `docs/development/agent-reference.md` Repository Layout |
| 206 | The declared native binding is a MANIFEST FACT, not a permission: `[CuoMod]` gains `NativeBinding` — a string naming the game's own code the mod patches, or nothing at all — discovery carries it into `ModManifest` and reports it in the `[Mods] discovered …` line, and nothing else changes (no rejection cause is added, no permission, no wire). It is deliberately not a ninth `ModPermission`: that enum is what CUO ENFORCES, and CUO enforces nothing here — an undeclared binding is undetectable, which is decision 204's no-anti-cheat stance — so a permission flag would read as a grant the framework hands out. A blank value (empty or whitespace-only) normalizes to "no declaration" instead of rejecting the mod, because the declaration must never become a new way to fail discovery, and the declared name is trimmed so it is stable. It buys visibility, never stability: what it names belongs to the game, a game update may break it with no CUO decision, and the mod's own documentation is what the claim is checked against. Stage 1 is wire-free on purpose — carrying the declaration onto `HandshakeMsg.Mods` with the host's allow/warn/parity policy is its own ticket. | `docs/en/reference/mod-api.md` (The manifest), `docs/en/reference/modification-policy.md` (The tiers), `docs/backlog/review/mod-native-binding-declaration.md` |
| 207 | The backlog is worked to the END before the single acceptance pass (owner instruction 2026-09-21: "do not stop for acceptance — finish every remaining todo, then come back"), which replaces the 2026-09-20 "starts after the unified acceptance pass" ordering that had parked the five architecture tickets behind it. The earlier ordering was not wrong when it was written; it became circular once the acceptance pass was defined as "after all high-priority items are complete", because those tickets were themselves part of the remaining work — the owner broke the loop, and the ticket gate line and the backlog index no longer carry the superseded prefix. The unified acceptance pass still happens exactly once, after the queue is empty; it is a user release action, so development continues against simulation/static evidence until then. | `docs/backlog/README.md`, `docs/backlog/review/application-layer-first-slice.md` |
| 208 | The declared native binding is carried onto the session handshake and judged by a HOST RULE, never by a contract: `ModInfoMsg` gains `NativeBinding` (decision 206's declared fact), the host compares it with its own declaration per mod id and only for a mod BOTH sides list — a mod one side alone lists has no counterpart, and the NetworkMode rows still decide who may lack what — and `HostRules` -> `NativeBindingParity` decides allow (silent), warn (default: the member is admitted and the mismatch is recorded in the host's log) or require (the member is refused before it is created, the log naming the mod and both declarations). Warn is the default because the declaration is new: a host that refused by default would lock out every session whose host updated first, and a declaration carries no rejection force of its own. A blank declaration is "none" on both sides through one shared normalization rule (`NativeBindingDeclaration`, used by discovery and by the handshake), a declaration against none is a difference, and an undeclared binding stays undetectable (decision 204): parity proves the declarations agree, never that the behaviour does. Wire change -> `ProtocolVersion.Current` bumped in the same change (decision 188). | `docs/en/reference/mod-api.md` (Handshake consistency), `docs/backlog/review/mod-native-binding-handshake-parity.md`, `src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs` |
| 209 | The pinyin search is a satellite mod that lives IN THIS repository while it settles (owner ruling 2026-09-21: a new plug-in project inside the CUO directory for now, its own project once it grows), and it is built as TWO projects split along the BINDING rather than the feature: `src/CasualtiesUnknownOnline.PinyinSearch/` (BepInEx shell, Harmony patch, Unity types; references the game assemblies and NO CUO assembly) and `src/CasualtiesUnknownOnline.PinyinSearch.Core/` (reading table, matcher, shared name predicate, crafting decision, switch, console stage, `[CuoMod]` entry point; references `Abstractions` and no game assembly). The split is what makes "works with CUO uninstalled" structural instead of a promise: exactly two Core types (`PinyinSearchMod`, `PinyinSearchStage`) name an `Abstractions` type, so with CUO absent the crafting path loads and runs and resolves nothing out of CUO — the two CUO-facing types are simply never touched (what type ENUMERATION of the Core assembly does need is the Abstractions assembly, which is why the build's transitive copy of it beside the mod is load-bearing rather than incidental); both facts are asserted against the shipped assemblies rather than promised (`PinyinSearchContractTests`). The mod takes the narrowest tier that expresses the feature (decision 204): Tier 2, declared as `NativeBinding = "PlayerCamera.RefreshRecipeList + Recipe.simpleName getter"`, with `NetworkMode.ClientOnly`, because the crafting search box and the console's resource-id completion are local UI and the handshake must keep admitting members that do not have the mod. Its ONE switch is the mod's own BepInEx config entry (`General`/`Enabled`, default on a Simplified Chinese desktop), created by the plug-in shell and read live by both surfaces; CUO's own option, Preferences row, localization keys, `Runtime/Search` core, Game Adapter patch classes and `pinyin-search` capability row are DELETED in the same cycle (pre-release policy: no compatibility layers, no second switch). Deviation recorded with its reason: the localization keys were deleted rather than moved, because a config-file switch leaves no in-game row to localize — the entry's description carries English and Simplified Chinese in one line instead of a localization system for one sentence. | `docs/backlog/review/pinyin-search-standalone-mod.md`, `docs/en/reference/mod-api.md` (Resource completion), `docs/en/contributing/repository-map-and-pitfalls.md` “Where a new system belongs” |
| 210 | The Application layer is REAL, not aspirational: `src/CasualtiesUnknownOnline.Application/` sits between the Runtime and the kernel and is the ONLY path up from `GameState` (Runtime -> Application -> GameState), so the Runtime declares no direct `GameState` reference and a session-level decision has one owner instead of one per entry point. The declared direction is data, not prose — `ProjectDirectionPolicy.AllowedReferences` holds the table, `ProjectDirectionGateTests` reads the graph from `CasualtiesUnknownOnline.slnx`, applies a census floor and refuses an upward reference, a project that skips the layer, and a Runtime that stops referencing the layer (its synthetic cases keep the checker honest; tests and tools are exempt CONSUMERS by declaration, and a classification census refuses a project that is neither declared nor listed). Its first slice is COMMAND ADMISSION: `KernelCommandGateway` is the seam for a member's MAPPED wire submission (the handler's two protocol heals — a container-sync report and an update for an unknown carried id, which materialize the reporter's own carried parent — and a guest's range request stay deliberately outside it and are named in the class doc) and applies, in a fixed order, the actor-to-transport-sender binding (already guaranteed by `ProtocolFrameValidator`, now local to the seam), the declared authority policy a member may not author (`HostOnly`, `PresentationOnly` — the enum's own meaning, not a new rule), and the item-destroy eligibility check moved verbatim out of `KernelProtocolCommandHandler`'s entry point. It decides ELIGIBILITY only — the 2026-09-18 ruling: never what happens to another player's body, reach or timing — the kernel keeps every domain verdict, and the seam occupies the POSITION the old check held (after the protocol heals and the creation-before-operation invariant) so an eligibility refusal cannot mask a domain one: an id this host never judged is not the seam's to answer, so the handler's creation-before-operation invariant runs above it and refuses that input with its own reason first (the seam's null branch is a defensive guard, not the production path). The verdict is three-valued (`Admitted` / `Refused` answered with the reason / `Ignored` without an answer) because the moved destroy rule was SILENT — answering it would start a guest rollback that never happened before — and every refusal is audited with one uniform line naming the command, the item it names (when it names one), the actor, the sender and the reason. Two facts are recorded as limits rather than fixed here: the wire mapper stamps every member command `OwnerPredictedHostValidated`, so the authority half is structurally satisfied and a member's host-only wire kind is not yet distinguished (carried into `future/strict-validation-anti-cheat.md`), and the stage that moves the nine kernel-replication types into this layer is still open because `KernelEnvelopeHandler` extends the Runtime packet-handler base and reads its frame length, so that move needs ports rather than being mechanical. | `docs/architecture/current.md` §4 and §7.1, `docs/development/agent-reference.md` Repository Layout, `docs/backlog/review/application-layer-first-slice.md` |

| 211 | The kernel replication surface lives in the Application layer, and it reaches the Runtime through SEVEN DECLARED PORTS rather than a widened interface (`review/application-layer-first-slice.md` stage 3). Eight types moved into `CasualtiesUnknownOnline.Application/Kernel/`: `IKernelProtocolControl`, `RefusedItemCreations`, `KernelDomainWireMapper`, `KernelStateStreamService`, `WireCheckpointAssembler`, `GuestCheckpointReceiver`, `KernelProtocolService` and `KernelProtocolCommandHandler`. The move was a DESIGN step, not a mechanical one: those types read the session, the transport, the kernel authority, the guest's pending-command table and the item wire mapper, so the layer declares `IKernelSessionFacts` (the role as two sides, the handshaken peer ids a broadcast needs, and the session-ended edge — never a presence record), `IKernelFrameSender` (protocol frames rather than the transport's own message vocabulary, and no unused argument: the send primitive keeps its three shapes because that is exactly what its consumers call), `IKernelCommandExecution`, `IKernelCheckpointSource` and `IKernelBatchApplication` (the kernel authority's four capabilities, beside the existing read-only `IKernelItemFacts`), `IKernelPendingCommands` and `IKernelWireCodec` (exactly the mapper surface the replication path uses) — each answered by the Runtime service that owns it (`SessionService`, `PacketSender`, `ItemKernelAuthority`, `GuestCommandReconciliation`, and a thin `KernelWireCodec` adapter over the static mapper). The four Runtime services gained the interface on their declaration: eleven of the port members are explicit forwarders added with this change and the other eleven bind to Runtime members that were already public at HEAD, so no new member is reachable by name; `WireCheckpointAssembler.Split`/`Assemble` take the codec as a parameter so the checkpoint helpers stay pure functions; and the registrations moved out of `CuoBootstrap` (600 to 586 lines) into `KernelReplicationComposition` unchanged and in the same order, so the `ICuoService` update order does not move. Three of the nine named types deliberately STAYED, each blocker named and the remaining move filed as its own ticket (`review/legacy-wire-dto-slice.md`): `KernelWireMapper` (its enemy-combat branches map the legacy protobuf messages the Game Adapter also references), `KernelBatchItemProjection` (its contract and code carry the legacy item DTOs) and `KernelEnvelopeHandler` (the transport side of the seam: packet-handler base, frame length, traffic accounting — the responsibility split the stage text invited). `KernelReplicationLayerBoundaryTests` pins both halves: the moved types' assembly, the Application assembly's reference set, and the three exceptions by name. Behaviour preservation is claimed only for what moved — the full suite is green and no behaviour changed; this is simulation and static evidence, with no real-machine claim. | `docs/backlog/review/application-layer-first-slice.md`, `docs/evidence/selfchecks/architecture/application-layer-first-slice-selfcheck.md`, `tests/CasualtiesUnknownOnline.Tests/Application/KernelReplicationLayerBoundaryTests.cs` |

| 212 | The Game Adapter boundary is TEN CAPABILITY PORTS and the aggregate declares nothing (`review/adapter-capability-ports.md`). `IGameAdapter` (125 lines, 21 members) is now a composition — `IGameIntegrationLifecycle`, `IAdapterCapabilityQuery`, `IWorldPresenceQuery`, `IStartGateState`, `ILocalHealItemQuery`, `ITraderRecruitRequest`, `INativeInputBlocker`, `IRemoteInventoryPresentation`, `IRemoteMedicalPresentation`, `IPlayerAnchorQuery` plus `IDisposable` — with no member of its own, so a consumer resolves the port whose capability it uses, a version adapter implements per capability and a test double implements only what it stands in for; every port resolves to the one adapter singleton, registered exactly once in the plugin's composition root, while the aggregate itself is deliberately NOT registered — its compile-time proof is the class declaration `GameAdapter : IGameAdapter`, and a registration nothing resolves would be the same dead surface this change removes elsewhere. The set was decided by a CALL-SITE census, not by the member list: only the members a call site in `src/` or `tests/` reaches became ports (fourteen), and the seven no call site reached were REMOVED from the boundary rather than moved into a port nobody would resolve — `ProbeGame`/`Install`/`Uninstall` (driven by `ICuoService.Initialize`/`Stop` on the same instance, kept as private members), `CaptureWorldParams`/`ApplyWorldParams` (the service owns both boundary hooks itself: its generation boundary calls `CaptureAtBoundary` and `EnsureGuestApplied` calls `Apply`) and `CloseRemoteBackpack`/`CloseRemoteMedical` (the views close themselves — `RemoteBackpackView.ClearIfStale`, `RemoteMedicalCoordinator`'s own staleness edges — which also made `RemoteBackpackCoordinator.Close` unreachable and deleted). `AdapterCapabilityPortShapeTests` is the freeze: the aggregate declares no member of its own, each port declares exactly its pinned census, no member name is shared by two ports, the composition is exactly those ports plus `IDisposable` and carries fourteen members, and every port is registered from the adapter singleton in `PluginDependencyRegistrar` (read as source, because the tests project does not reference the plugin project); a synthetic composition that declares a member pins the matcher's own contract, and both mutations — a member back on the aggregate, a port left unregistered — were run red then green on the real tree. The `InternalsVisibleTo` grant to the adapter was REVIEWED, not reduced: removing it and building makes the compiler name the 22 internal Runtime types the adapter reads (the patch-contract facts, the capability-report types, the mod status/building tables, and the item/world/trap/trade decision values it turns into game writes), and a port per family would have to publish those types or carry copies of them, so the grant stays as the narrower seam; the one read a port can carry — the plugin's capability report — is now carried by `IAdapterCapabilityQuery`. Behaviour preservation is the whole claim, and only that: the full suite and the gates are green and no call site changed meaning (no real-machine behaviour is claimed). | `docs/backlog/review/adapter-capability-ports.md`, `docs/evidence/selfchecks/architecture/adapter-capability-ports-selfcheck.md`, `tests/CasualtiesUnknownOnline.Tests/Patching/AdapterCapabilityPortShapeTests.cs`, `src/CasualtiesUnknownOnline.Runtime/AssemblyInfo.cs`, `docs/architecture/current.md` §4, `docs/architecture/guards.md` |
| 213 | The plugin is a HOST SHELL, and the plugin project references no game assembly (`review/plugin-host-shell.md`). `Plugin.cs` is 584 → 294 lines: BepInEx host configuration (paths, the F6 bind, `+connect_lobby` parsing), Unity lifecycle forwarding and the Steam callbacks. Three responsibilities left it — the Online UI is `OnlineUiHost` (259 lines: the overlay's composition plus the frame-time half, the console/quick-panel hotkeys, the ESC-close suppression, the native input modal and the IMGUI pass), the lobby policy is `LobbySwitchActions` (109 lines: `TryJoin`/`TryCreate`/`TryLeave`/`CanJoin`/`CanCreate` and the last-refusal reason, so the Steam callbacks and the UI buttons run ONE policy), and the adapter registration is `GameAdapterComposition` (180 lines, in the ADAPTER project: the `GameAdapter` singleton, its twelve ports, `WorldLibraryService`, `NativeWorldFacts`, `LiveTrapLayoutSource`, the `IMod*` replacements, the content providers, `PlayerInteractionVisibility`, `LocalCharacterCapture`, the Mapster mapper). `PluginDependencyRegistrar` (413 → 275 lines) keeps the BepInEx config → DI bridge and the plugin-side editors and calls the composition ONCE at the position the old block occupied, so the `ICuoService` pump order is unchanged. The seam grew by two ports under the rule that a port is a CONSUMER CAPABILITY, never an implementation detail: `IJoinFlowPresentation.PrepareForDirectJoin()` replaced the public `GameAdapter.SkipIntro` static write (the field is private, the patch reads `internal static IsIntroSkipped`) and `ICarryPresentationPump.PinCarriedPresentation()` replaced the public `LateUpdateCarryPresentation` (explicit-interface only). The plugin consumes four ports and names no adapter type; `AdapterCapabilityPortShapeTests` now pins twelve ports / sixteen members and reads its registration pin from `GameAdapterComposition`. The layout rule is now structural instead of documented: the plugin project dropped `Assembly-CSharp` (measured on the built DLL: `CasualtiesUnknownOnline.dll` references no game assembly, while the adapter DLL references it — the positive control) and `GameAssemblyReferenceGateTests` fails the build when a project outside the declared set binds the game's code; the Unity module references were re-measured one at a time (Subsystems/SharedInternals unused and dropped, the rest kept with their reason in the csproj). Behaviour preservation is the claim, with ONE declared difference: UI and lobby messages log under `OnlineUiHost`/`LobbySwitchActions` instead of `Plugin` — same text, same levels, same triggers; no real-machine behaviour is claimed. | `docs/backlog/review/plugin-host-shell.md`, `docs/evidence/selfchecks/architecture/plugin-host-shell-selfcheck.md`, `tests/CasualtiesUnknownOnline.NormativeGates.Tests/GameAssemblyReferenceGateTests.cs`, `tests/CasualtiesUnknownOnline.Tests/Patching/AdapterCapabilityPortShapeTests.cs`, `docs/architecture/current.md` §4, `docs/architecture/guards.md` |

| 214 | The Harmony patch bridge is split BY DOMAIN and its aggregate is FROZEN (`review/patch-bridge-domain-ports.md`, stage 1). `IPatchBridge` is the seam the patch classes read (87 of the 118 files in `GameAdapter/Patches/`), so a domain's patch surface now lives in its own port instead: the fluid domain's eight members (`OnFluidFixedUpdate`, `OnFluidDrinkReported`, `TryRenderCustomLiquids`, the three custom-liquid resolution reads, `TryDrinkCustomLiquid`, `ApplyLiquidTileBodyTouch`) moved into `IFluidPatchPort`, and the aggregate neither declares nor composes it — `GameAdapterBridge : IPatchBridge, IFluidPatchPort` serves it and the static seam resolves it by casting the one bound bridge (`PatchBridge.Fluid`), which is also why the port stays reachable without adding a member to the frozen aggregate. The aggregate is closed on purpose: a call written against `IPatchBridge` cannot reach a migrated member at all, so the migration is enforced by the COMPILER rather than by convention and a port cannot degrade into the aggregate under a new name. `PatchBridgePortShapeGateTests` (19 cases, Roslyn over source, fast suite) pins the aggregate's 96 declared members, its composition (the four earlier patch seams), each seam interface's census, the one implementation's composition, public surface and non-public members, the static seam's census, and that no port member is reachable through the aggregate and no name is shared by two seams; `PatchBridgePortContractTests` (reflective, Integration) asserts the same on the built adapter. New patch work registers a domain port; the item, character, world/run, interaction and mod-content domains stay on the aggregate and migrate one at a time (stage 2), and the install unit that owns a port arrives with the capability catalog's stage 3. | `docs/backlog/review/patch-bridge-domain-ports.md`, `src/CasualtiesUnknownOnline.GameAdapter/IFluidPatchPort.cs` |
| 215 | The kernel <-> wire vocabulary lives in the Application layer and only MATERIALIZATION stays in the Runtime (`review/legacy-wire-dto-slice.md`, which closes the move `review/application-layer-first-slice.md` stage 3 left open). Re-censusing the dependency closure showed the blocker recorded for `KernelWireMapper` was STALE: it calls only the pure enemy-combat overloads and the pure limb conversion, while the legacy protobuf messages are touched by `EnemyCombatKernelCodec` (message builders and the character-limb codec) and by `EnemyCombatWireMapper`'s three `*Msg` overloads, whose only caller is `EnemyCombatKernelSubmitter`. `KernelWireMapper`, `ItemSpawnWireMapper` and `KernelPlayerInteractionWireMapper` (zero Runtime references; the kernel mapper was its only external caller) now live in `Application/Kernel/`, and the shared pure vocabulary they needed became `KernelLimbWireMapper`, `KernelComponentWireMapper` and `KernelEnemyCombatWireMapper`, so the Runtime's two duplicated component conversions and its enemy-limb conversion call one spelling instead of three. The layer-local `IKernelWireCodec` port was DELETED rather than kept: its declared reason was the mapper's absence from the layer, and with the mapper inside the layer it was a pure forwarder — the one conversion that genuinely needs the Runtime (the legacy character-snapshot normalization behind `ToKernelItemData`) is declared as the single-member `IKernelItemDataNormalizer`. `KernelBatchItemProjection` stays in the Runtime BY DECISION: it is a materialization projection whose constructor and every output path build the legacy item vocabulary (`WorldItem`, `WorldItemTable`, `CharacterItemMsg`, `NetVector2`) and write the rebuildable world-item table, so moving it means redesigning its output contract into kernel facts plus a Runtime materializer — the contract evidence is recorded in the ticket. `KernelEnvelopeHandler` keeps its earlier reason (transport side of the seam). No wire shape changed and the protocol version is untouched. | `docs/architecture/current.md` (the Application layer and its declared ports), `docs/backlog/review/legacy-wire-dto-slice.md` |

## Reference rules

- Older decision numbers remain traceable through
[  `tech-decisions-index.md`](index.md).
- Delivery-cycle detail and per-feature evidence live in
[  `tech-decisions-archive.md`](archive.md) and
  [`[docs/selfchecks/`](../evidence/selfchecks/).
