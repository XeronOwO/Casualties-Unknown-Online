# Sync Coverage Matrix — event sync + periodic fallback

- Status: Evidence (produced by `docs/backlog/review/sync-event-and-periodic-fallback-coverage-audit.md`)
- Audit date: 2026-09-09
- Scope: **A** kernel domains, **B** direct `NetMsg` families, **C** GameAdapter/Runtime sync
  surfaces, **D** adaptive streams
- Verdict vocabulary: `OK` | `Event-only gap` | `Fallback-only gap` | `Transient-by-design`

## Method

1. **S1 — freeze the inventory.** The row list was derived from the kernel domain table
   (`docs/architecture/domains.md`), the direct `NetMsg` classification
   (`docs/evidence/selfchecks/architecture/phase-e-legacy-inventory-selfcheck.md`), every
   `NetMsg` value in `src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs`, every
   `WireCommandKind` / `WireEventKind` member, and every `AdaptiveStreamId` member. No row
   was dropped without a reason.
2. **S2 — evidence collection.** Eight independent collectors produced per-row evidence:
   trigger message + numeric id, direction, reliability, periodic fallback cadence and apply
   semantics, backfill/reconnect path, loss semantics, and source references. The
   reference + quoted-text pairs are stored in `docs/evidence/sync-coverage-evidence.json`
   and **every one was mechanically verified against the working tree** (the file exists and
   the quoted text is still in it). `SyncCoverageGateTests` re-runs that verification on every
   `dotnet test`, so rotted evidence fails the gate instead of silently rotting. A reference is
   a path plus the quoted text and carries no line number: a line number drifts with every edit
   above it and then has to be re-pointed by hand, while the quoted text is the claim. The
   matrix row does not repeat those pairs: it declares how many anchors it owns and the gate
   checks that declaration against the JSON, so the same evidence is not hand-maintained twice.
3. **S3 — adjudication.** Every row got exactly one verdict. The adjudication changed four
   collector verdicts (N6/N7/N8/R7 → `Transient-by-design` because the loss is accepted by
   design, and F4 → `OK` because a continuous field has no discrete trigger) and, after the
   independent adversarial review, three more (W2/W6/I5 → `Event-only gap` because their own
   loss text admits a non-convergent direction). The reasons are recorded in the row.
4. **S4 — gaps, guard, docs.** Every gap got an independent ticket (R3+R4 merged into one
   session-control ticket), the rot guard `SyncCoverageGateTests` landed, and the active docs
   that contradicted the matrix were corrected.

The audit itself changed no sync behavior. One protocol defect found during the audit
(stale-`RunEpoch` state-stream frames were forwarded, contradicting
`docs/architecture/glossary.md`) was fixed in the same cycle as a separate, tested change.
The independent adversarial review then found that the first version of that fix could drop a
legitimate stream after a foreign-epoch checkpoint restore; `ItemKernelAuthority.Restore` now
adopts the restored checkpoint's epoch, with a red → green regression test.

## Verdict summary

| Verdict | Count | Rows |
|---|---|---|
| `OK` | 50 | all rows not listed below |
| `Event-only gap` | 5 | I5, I6, I8, R3, R4 |
| `Fallback-only gap` | 0 | — |
| `Transient-by-design` | 9 | N6, N7, N8, N9, F3, I9, P5, E6, R7 |
| `Unverified` | 0 | — |

**The user's seed finding is confirmed and closed.** World blocks: host → guest has a 60 s
absolute re-send and the world-entry backfill, but the guest → host direction had no diff
table and no periodic re-report, so a swallowed guest mutation was invisible to the host
table and the host's absolute snapshot could not heal it (row W1, closed; the partial-damage
half is row W2, closed by `docs/backlog/review/guest-partial-block-damage-re-report.md`).

## Matrix

Each row carries the DECISION only: the surface, the direction and roles it covers, its event
trigger, its periodic fallback, its backfill/recovery path, what a loss costs, its verdict, and
the number of evidence anchors it owns. The quoted sources live in
`docs/evidence/sync-coverage-evidence.json`, keyed by the row id, and are never copied into the
row: one evidence text has one home. `SyncCoverageGateTests` checks that the declared anchor
count matches the entries the JSON holds for the row.

| ID | Feature / domain (owner) | Direction + roles | Event sync | Periodic fallback | Backfill / recovery | Loss semantics | Verdict | Anchors | Gap ticket |
|---|---|---|---|---|---|---|---|---|---|
| K1 | Kernel command + committed batch path (guest->host intent, host->guests confirmation) (Runtime/Session/Items/KernelProtocolService.cs, KernelProtocolCommandHandler.cs, Runtime/Session/Handlers/KernelEnvelopeHandler.cs, Protocol/Wire/CommandEnvelope.cs, CommittedBatchEnvelope.cs) | Both. Guest -> host CommandEnvelope (EnvelopeKind.Command = 1); host -> guests CommittedBatchEnvelope (EnvelopeKind.CommittedBatch = 2); both ride one transport frame NetMsg.KernelEnvelope (id 122), payload CommittedBatch = 100. | Dedicated envelopes, not a snapshot: CommandEnvelope (guest intent) and CommittedBatchEnvelope (host confirmation). Real trigger is the guest local item operation report: src/CasualtiesUnknownOnline.GameAdapter/Items/PickupSync.cs and src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemMessageFlowService.cs call IKernelProtocolControl.SendCommand. The host decodes/executes via KernelProtocolCommandHandler.Handle and the commit raises BatchCommitted, fanned out by KernelProtocolService.BroadcastCommittedBatch. Numeric ids. Reliability, and — reliable in both directions, no reliable:false on this path. Wire members: CommandRejected, KernelEnvelope. | Absolute fallback is the kernel checkpoint (row K3): the 60 s host cycle plus world-entry/reconnect and the gap-driven fresh checkpoint. A checkpoint is an absolute overwrite of the guest kernel, so an uncommitted guest prediction or a swallowed batch is reverted to host authority. No periodic re-send of individual commands exists, and none is needed: the channel is reliable and the send is gated on a live handshake. | World entry / late join / reconnect: WorldEntryFanout.Send -> SendCheckpoint. Mid-generation join. Missed batch: revision-gap detection buffers the future batch and issues RequestRange, and the host replays the journal range or falls back to a fresh checkpoint. | Reliable ordered channel in both directions. Loss is not accepted: a duplicate is idempotent by OperationId and a gap is repaired by range request or checkpoint. Convergence guarantee: the host kernel is the single authority and the guest replay kernel converges to the host checkpoint/committed batch, so a guest prediction that was never committed is corrected, not preserved. | OK | 13 | |
| K2 | Checkpoint backfill + journal range recovery (late join / reconnect / gap) (Runtime/Session/Items/KernelProtocolService.cs (SendCheckpoint, HandleRangeRequest, RequestRange, journal), Runtime/Session/World/WorldEntryFanout.cs) | Host -> guest (checkpoint chunks and journal batches); guest -> host (WireCommandKind.RangeRequest, payload 202). | Trigger is a detected revision gap on the guest: computes the expected revision, the gap is buffered, and RequestRange is issued. RequestRange sends a RangeRequest through the same CommandEnvelope path. The host answers in HandleRangeRequest : journal batches in range, or a fresh checkpoint when the journal is empty/out of window. Wire members: RangeRequest. | The same SendCheckpoint is the periodic fallback, driven every 60 s by row K3 (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs -> src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs). It is absolute: the host takes authority.CreateCheckpoint and the guest Restore replaces the whole kernel store. This path never calls AdaptiveStreamRateService, so the governor cannot stretch it. | World entry / late join / reconnect: inside the ordered snapshot group that ends; mid-generation handshake; gap: RequestRange -> HandleRangeRequest; the journal is bounded and appended on every host commit, and an out-of-window range falls back to a fresh checkpoint. | Reliable channel: checkpoint chunks and journal tail use PacketSender.Send defaults (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs). A lost chunk leaves the buffer incomplete and is healed by the next checkpoint (60 s cycle or a range-request fallback); the receiver restores only a complete set. Mixed chunks are rejected rather than silently applied and the buffer is cleared on failure. Worst-case convergence wait is the 60 s cycle. | OK | 13 | |
| K3 | Periodic kernel checkpoint fallback (60 s host cycle) (GameAdapter/World/WorldEventSync.cs (Update), KernelProtocolService.SendCheckpoint) | Host -> each in-world guest (one SendCheckpoint per member). | The periodic cycle has no dedicated event envelope; its event-level counterparts share the same SendCheckpoint mechanism: world entry / late join, mid-generation handshake and gap recovery. The cycle is pumped from the frame loop (src/CasualtiesUnknownOnline.GameAdapter/GameAdapter.cs). | Cadence is a fixed 60 s wall-clock test in the host pump, restricted to in-world members and sent per member. Sender is KernelProtocolService.SendCheckpoint, which builds an absolute checkpoint. Apply semantics are absolute overwrite on the guest. The cadence is a hardcoded float and never calls AdaptiveStreamRateService, so the adaptive rate governor cannot stretch or remove it. | Same call path is reused for world entry, mid-generation handshake and out-of-window range requests. The 60 s block also re-sends the world block-state/damage snapshots, the trap layout and the keypad codes in the same cycle. | Reliable send (uses PacketSender default true). A lost or swallowed chunk is re-sent by the next cycle (<= 60 s) and a partial set is never applied. Convergence is absolute: the guest kernel is overwritten from the host authoritative checkpoint, healing any swallowed batch or uncommitted prediction. | OK | 8 | |
| K4 | Session reset / run-epoch isolation (KernelProtocolService.ResetForSessionEnd, Runtime/Session/SessionService.cs, RunEpoch filters) | Local reset (no wire message); isolation is enforced on the wire by the RunEpoch field carried in every EnvelopeHeader in both directions. | Trigger is the local session teardown: SessionService.TeardownSession fires SessionEnded. KernelProtocolService subscribes at construction and ResetForSessionEnd bumps the authority epoch and clears journal/chunk/pending state plus the stream seq counters. Inbound commands are epoch-filtered and batches too, with a second kernel-level check. | The epoch rides every envelope header and every checkpoint, and the row K3 60 s absolute checkpoint re-establishes the host epoch on a guest because restore adopts the checkpoint epoch. A guest stuck on an old epoch is healed by the next checkpoint (60 s cycle, world entry or handshake). The adaptive governor is not involved. | World entry / reconnect checkpoint carries the current epoch; the checkpoint assembler refuses chunks that disagree on epoch or revision, so a mixed old/new checkpoint is discarded rather than applied. | The local teardown trigger cannot be lost over the network. Epoch filtering applies to commands (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs), batches (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs) and kernel state streams (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs). The checkpoint receive site still does not compare RunEpoch, and src/CasualtiesUnknownOnline.GameState/Kernel/GameStateStore.cs overwrites the local epoch with the checkpoint epoch, so a stale checkpoint completing after a reset could rewind the guest epoch; tracked in todo/checkpoint-run-epoch-validation.md. Residual window: before the first checkpoint restore the guest still uses its local epoch, so a command or report sent in that window can be rejected by the host; the same ticket owns the handshake-carried expected epoch. The epoch filter warns once per sender and logs Debug afterwards, so a persistent mismatch cannot flood the log. Compatibility note: KernelWireMapper maps a wire batch epoch of 0 to the current epoch, so the batch filter only rejects a non-zero mismatched epoch (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelWireMapper.cs). | OK | 15 | |
| K5 | Kernel state-stream transport (StateStreamEnvelope) (Runtime/Session/Items/KernelStateStreamService.cs, Protocol/Wire/WireStateStream.cs, Runtime/Session/EntitySync/PlayerStreamExchange.cs) | Both. Host -> guests (player/enemy/item streams) and guest -> host (player state report), all over NetMsg.KernelEnvelope with StateStreamEnvelope. | Dedicated envelope StateStreamEnvelope, EnvelopeKind.StateStream = 4; payload types StateStream = 301, ItemSnapshotStream = 302, WorldItemsSnapshotStream = 303, PlayerStateStream = 304, EnemyStateStream = 305. Host broadcast site and guest report site are the shared player-stream exchange. Reliability is explicitly unreliable in both directions, and the shared kernel stream path defaults to unreliable. | The stream is itself the periodic fallback: EntitySyncService.Update paces it with the adaptive interval (for the host; the guest branch at src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs). The governor can stretch the interval but cannot remove the stream. Apply semantics are latest-wins absolute overwrite, seq-gated: applying. | World entry / rejoin: EntitySyncService.StartMemberSync announces PlayerJoin and immediately pushes a full player state so a clone renders without waiting for a tick; the roster backfill (PlayerJoin per existing member) precedes it. Enemy world-entry backfill is the direct EnemySnapshot NetMsg (not this envelope). Session end resets the seq gate. | Loss is explicitly accepted by design: src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs documents the 20 Hz player state stream as the unreliable-by-design path (overwrite semantics plus the snapshot sequence make drops harmless; other loss-tolerant streams such as the enemy, item-snapshot, fluid-region and tutorial-claw streams also pass reliable:false). Convergence: the next tick overwrites and the seq gate discards duplicates/out-of-order streams, so a swallowed stream self-heals within one adaptive interval. Epoch filter: the receive site (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs) drops a stream whose header RunEpoch differs from the current run before the seq gate runs; within the same epoch the seq gate still discards duplicates and out-of-order frames. The direct NetMsg presentation streams (FluidRegion, TraderState, TutorialClawState) carry no RunEpoch and are outside this filter, so the glossary's old-epoch stream wording is scoped to kernel state streams. | OK | 15 | |
| W1 | World block state: placed / broken / air writes (BlockPlaced, BlockDamaged, WorldBlockState) (GameAdapter/World/WorldEventSync.cs (OnBlockSet, Update 60s, OnRemoteBlockPlaced), Runtime/Session/World/WorldStateMessageService.cs, Runtime/Session/World/BlockReportChannel.cs, Runtime/Session/World/WorldReportGeneration.cs, Runtime/Session/World/PendingBlockReportTable.cs, Runtime/Session/World/PendingReportFallback.cs, Handlers/BlockPlacedHandler.cs, BlockDamagedHandler.cs, BlockStateHandler.cs, GameAdapter/World/BlockBreakSync.cs, Runtime/Session/World/PendingBreakDropTable.cs, Runtime/Session/World/PendingBreakDrops.cs, Runtime/Session/World/GuestBreakDropReportBookkeeping.cs, Runtime/Session/World/GuestReportFallbacks.cs, Runtime/Session/World/PendingBlockDamageTable.cs, Runtime/Session/World/BlockBreakArbitration.cs, Runtime/Session/World/BreakVerdict.cs, GameAdapter/Items/DropProtectionGuard.cs) | Both. Host -> guest: BroadcastBlockPlaced for every host-side mutation plus the 60 s absolute WorldBlockState table; a guest report is relayed to EVERY member including the reporter (the echo is the reporter's acknowledgement) or answered with the host's current cell when first-writer-wins refuses it. Guest -> host: SendBlockPlacedReport records the cell in the pending block-report table and reports it; the host arbitrates (placement must land on air, an air write must land on something). A swallowed report is re-reported by the guest's 60 s fallback until the host answers for the cell. A swallowed guest BREAK report also carries the break's drops, and the guest registers its drops nowhere — so the breaker now records them (PendingBreakDropTable, before the live send) and re-reports the outstanding set on the same 60 s fallback; the host's accepted relay carries those drops and includes the reporter (its echo is the acknowledgement), and the duplicate guard is the drop's ITEM ID. Every report of this family now carries the world/layer generation stamp (the kernel run baseline `(RunEpoch, LayerIndex)`, `WorldGenerationMsg` on BlockPlaced / BlockDamaged / the block-damage snapshot+report payload) and the receiver refuses a STALE one before the domain — air writes and damage rows whole and without an answer (the reporter's own generation boundary clears its pending table), a break report still delivered so its drops are rolled back with ItemReject. First-writer-wins is unchanged, and a break report naming a cell the host still holds is now ACCEPTED when the stamp proves it belongs to THIS generation: that shape is the lost air write (the sender's air-write report went missing with the drops carrier), which `Verdict.LostAirWrite` registers and relays instead of destroying the drops. | Dedicated messages: BlockPlaced = 42 bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs), BlockDamaged = 40 bidirectional, WorldBlockState = 41 host -> guest. Real trigger: the SetBlock postfix reports only a write that actually landed, which WorldEventSync.OnBlockSet routes host-side to a broadcast and guest-side to a recorded report. The host's received report path applies SetBlock, records the difference and relays to everyone including the reporter, or answers a refused report with the host's current cell. The receiver skips the air-write bookkeeping when the cell is unchanged, so a reporter's own echo cannot suppress its local building-drop roll. All sends use PacketSender.Send / Broadcast defaults, i.e. reliable = true (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs). Wire members: BlockDamaged, BlockPlaced, WorldBlockState. | Host -> guest: 60 s absolute block-state table. Cadence: send per in-world member. Apply is an absolute overwrite of every listed cell; the table is the host's deviation set, rebuilt on each generation. Guest -> host: the pending block-report table is re-reported every 60 s, driven by WorldReportFallbackPump; the window is armed by the first outstanding entry, so the live report is never duplicated immediately. Both cadences are hardcoded thresholds (Time.unscaledTime / ITimeSource); world blocks have no AdaptiveStreamId, so the adaptive governor cannot stretch or remove them. CADENCE FINDING: the 60 s windows mean a mutation inside the ~30 s lazy-P2P swallow window can stay invisible for up to 60 s (todo/sync-cadence-review.md). Guest -> host drops: the same 60 s fallback re-reports every unacknowledged break's drops until the host's relay names them; the breaker's own local drops are protected from the item keyframe's reconcile while the report is outstanding (the 5-30 s keyframe is far inside the window). | World entry / late join / reconnect / per-layer regeneration: the ordered world-entry fanout sends the absolute table, fired on the InWorld edge and on reconnect-while-in-world; every layer regeneration re-fires that edge, so the table is re-sent per layer as well as on the 60 s cycle. The guest's pending reports are cleared at the world/layer APPLY boundary (the adapter's WorldParamsService, symmetric to the host's ResetDamagedBlocks), NOT at the world-entry completion marker: a reconnect-while-in-world keeps the guest's local mutations, so its unanswered reports must stay pending and be re-reported. The absolute table applies under RemoteApply, so a snapshot cell is never echoed back as a guest mutation. Host-side: a report is dropped while the host's world is (re)generating, and a remote damage report that breaks the block records the air transition and relays it, because that SetBlock(0) runs under RemoteApply and the local-report hook stays silent. Missed-range recovery is the absolute table plus the guest's re-report. | Reliable channel (PacketSender default reliable = true). Host -> guest converges: the next 60 s snapshot, the next world-entry fanout or the host's answer rewrites the cell. Guest -> host converges: the pending table re-reports a swallowed mutation every 60 s until the host answers, and the answer (relay echo or correction) clears the entry, so an accepted report is not re-sent. First-writer-wins is unchanged: a first-writer refusal is answered with the host's cell, never re-applied (a STALE-generation refusal is deliberately answerless: the reporter's own generation boundary clears its pending entry, and answering with this side's values would write them into the reporter's older world). Both tables are bounded (host MaxDamagedBlocks and guest PendingBlockReportTable.DefaultCap; overflow is logged once per episode), and SendBlockStateSnapshot returns early on an empty table. A refused break's drops are rolled back on the breaker (ItemReject) and forgotten from the pending set. | OK | 45 | review/guest-block-mutation-re-report.md (landed), review/guest-break-drops-recovery.md |
| W2 | Partial block damage before the break (BlockDamageSnapshot, BlockDamageReport) (Runtime/Session/World/BlockDamageSnapshotSender.cs, Runtime/Session/World/GuestReportRecovery.cs, Runtime/Session/World/PendingBlockDamageTable.cs, Runtime/Session/World/GuestBlockDamageReportBookkeeping.cs, Handlers/BlockDamageSnapshotHandler.cs, Handlers/BlockDamageReportHandler.cs, GameAdapter/World/BlockDamageCleaner.cs) | Both for the live relay (BlockDamaged = 40). Host -> guest: the authoritative record and the periodic fallback (BlockDamageSnapshot = 89, PacketHandler direction HostToGuest). Guest -> host: the ABSOLUTE re-report of the cells the host has not answered (BlockDamageReport = 136, PacketHandler direction GuestToHost). | Dedicated message: BlockDamaged = 40 bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs). Real trigger: the DamageBlock postfix. A surviving block is reported immediately with raw damage + MetalBonus; a break holds one frame and goes out as ONE message with its drops. The host keeps no copy of its own: the rows a member receives are read from the GAME's own list at send time, so the set can never drift from the host's gameplay table — the same table the game itself caps and evicts. Reliable by PacketSender default. Wire members: BlockDamageSnapshot (host -> guest) and BlockDamageReport (guest -> host, one message per outstanding set). | 60 s absolute BlockDamageSnapshot, host -> in-world members. Apply is an absolute set, never additive, guarded against air cells, out-of-range damage and the game's 128-entry blockDamages cap (src/CasualtiesUnknownOnline.GameAdapter/World/BlockBreakSync.cs). Cadence is the hardcoded 60 s threshold at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs; BlockDamageSnapshotSender has no AdaptiveStreamId and no AdaptiveStreamRateService, so the governor cannot stretch it. Guest -> host: the live delta relay plus the 60 s fallback that re-reports every unacknowledged cell ABSOLUTELY (BlockDamageReport, sent as ONE message for the whole outstanding set). The host merges per cell into the GAME's own list — never below what it already holds — and answers EVERY reported cell with its authoritative value through BlockDamageSnapshot; a zero answer means "no damage here", so a row the game's own cap or range refused converges instead of re-reporting forever. | World entry / late join / reconnect / per-layer regeneration: in the ordered group, fired by the InWorld edge (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs) and by reconnect-while-InWorld (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs); the 60 s cycle repeats it. The guest applies through OnBlockDamageSnapshot (src/CasualtiesUnknownOnline.GameAdapter/World/BlockBreakSync.cs) as an absolute set with a refreshed crack sprite. | Reliable channel. Host -> guest converges: a swallowed damage relay is healed by the next absolute snapshot (world entry or <= 60 s). Guest -> host converges: a swallowed damage report is re-reported ABSOLUTELY by the guest's 60 s fallback (BlockDamageReport), the host merges it into the GAME's own list through INativeWorldFacts.MergeBlockDamages (per cell, never below what this host already holds: two players' contributions and the host's own all count) and answers every reported cell with its authoritative value through the snapshot, so the mismatch heals without a reconnect — an entry is dropped when the host answers for its cell or the cell goes air, and a new world/layer baseline clears the table. Snapshot entries the live table refuses are skipped with a log only (the per-verdict messages, e.g. the cap case at src/CasualtiesUnknownOnline.GameAdapter/World/GameBlockDamageTable.cs), and the game's own list cap is 128 (Cap at src/CasualtiesUnknownOnline.GameAdapter/World/GameBlockDamageTable.cs, applied by src/CasualtiesUnknownOnline.GameAdapter/World/GameBlockDamageTable.cs), so a LIVE snapshot's entries beyond the cap stay unhealed — a refusal there reaches the log, never the restore report. The restored replay CAN refuse: the archive's rows still have to land on cells that survive in the regenerated world, and a row the game's list will not take is counted by the replay, after clearing the live list. That count now reaches the caller that started the restore: the replay reports it to the restore audit, which raises the report the Continue path logs and the command console prints (scope 6 CLOSED by S3.3, decision 168), and an engine throw during the write is reported the same way with both handovers released. CLOSED 2026-09-18 by review/guest-partial-block-damage-re-report.md: the guest keeps each reported cell's ABSOLUTE damage (PendingBlockDamageTable, cap 65 536, upsert on a later hit) and the 60 s fallback re-reports the outstanding set as ONE BlockDamageReport — the live relay stays a delta, because replaying it would double-apply. The host merges through INativeWorldFacts.MergeBlockDamages (the adapter's GameBlockDamageTable.DecideMerge/Merge: air, range and the game's 128-entry cap, plus the never-lower rule) and broadcasts its authoritative value for EVERY reported cell, the reporter included — that answer is what clears the pending entry, so a refused row converges too (a zero row clears the receiver's local crack). Recorded limitation (BOTH directions): the merge is a per-cell MAXIMUM against a live DELTA the receiver accumulates, so two senders whose contributions were never visible to each other and whose reports were both swallowed converge to the higher value rather than the sum (under-count), AND a delta that arrives AFTER the absolute re-report covering it has been merged raises the row a second time (over-count — KeepHost cannot see a duplicate that arrives as a delta); both are tracked in docs/backlog/todo/partial-damage-delta-report-overlap.md. The adapter's engine-side path is not exercisable in the test host: the merge's world reads and crack-sprite refresh, the live DamageBlock hook and the guest's absolute read (whose record-before-send order is likewise adapter-side and code-reviewed only), plus the zero-row clearing a snapshot answer can now trigger (BlockBreakSync.OnBlockDamageSnapshot → BlockDamageCleaner.ClearForAirWrite, destructive local state removal); all of it rests on the pure DecideMerge rules plus the unified acceptance pass. | OK | 33 | review/guest-partial-block-damage-re-report.md (landed) |
| W3 | EarthquakeStart (Runtime/Session/World/WorldStateMessageService.cs:217-231, Handlers/EarthquakeStartHandler.cs, GameAdapter world quake suppression) | Host -> guest only (PacketHandler direction HostToGuest; a guest never starts a quake because its timer is frozen). | Dedicated message: EarthquakeStart = 55 host -> guest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs). Real trigger: the WorldGeneration.Update postfix detects earthquakeTime crossing from <= 0 to > 0. Host send: -> (SessionService.Broadcast -> PacketSender.SendToAll, reliable = true by default). Receive/apply: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs writes earthquakeTime/earthquakeDelay on the guest. Wire members: EarthquakeStart. | none. The only send call site is src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs; BroadcastEarthquakeStart has no other caller and no timer/cadence exists for it. There is no absolute quake state to resend: the guest's own timer is frozen every frame, so a missed start cannot be reconstructed locally and is not re-sent. The quake's durable effect (its air writes) is healed by W1: each side's SetBlock(0) rides the air-write relay (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs) and the host's 60 s WorldBlockState table. | No dedicated backfill for the event itself: a late joiner never receives an in-flight quake, and the next host quake is the only re-align (the guest applies earthquakeTime/earthquakeDelay at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs). Durable terrain converges through W1's air-write relay and the 60 s WorldBlockState table, so a swallowed EarthquakeStart cannot permanently desync the world. | Reliable channel (PacketSender default). A swallowed EarthquakeStart is not re-sent: the guest misses that shake, but no independent divergence follows because the guest's timer is frozen (it never starts its own quake), and the blocks the quake broke still arrive through W1's relay + 60 s absolute table. Transient presentation loss with a durable-state heal owned by W1. ADJUDICATED (adversarial review): the event itself is one-shot with no fallback, but the guest's quake timer is frozen so no independent divergence follows; the durable air writes ride W1. Kept OK with that dependency stated explicitly. | OK | 9 | |
| W4 | KeypadCode (Runtime/Session/World/WorldStateMessageService.cs:111-126, Handlers/KeypadCodeHandler.cs, GameAdapter keypad sync) | Host -> guest only (PacketHandler direction HostToGuest; the host is the code authority). | Dedicated message: KeypadCode = 57 host -> guest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs). Real trigger 1, generation-time keypads: the first full send happens on the generation-completed baseline capture. Real trigger 2, runtime-created keypads: the creation message carries the code as creation-time data (applied on the receiving copy). Send: reliable by PacketSender default. Wire members: KeypadCode. | 60 s full-set absolute re-send, host -> handshaken members. Apply is idempotent/absolute: the receiver writes only when the local code is empty. Cadence is the hardcoded 60 s threshold at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs; KeypadCode has no AdaptiveStreamId and WorldEventSync has no AdaptiveStreamRateService, so the governor cannot stretch it. | World entry: the generation-complete first send (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs). Re-entry/reconnect while in world: the session scene-changed hook re-sends immediately (wired at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs and src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs to ISessionControl.RemoteSceneChanged, which HandshakeHandler fires for a reconnect-while-InWorld and SceneStateHandler for an InWorld edge). Per-layer generation re-sends because the first send is on the generation-complete edge. The 60 s cycle covers the lazy-session swallow window. | Reliable channel and idempotent apply: a lost code is healed by the next 60 s cycle, the next generation edge, or a re-entry/reconnect. Explicit exception in code: a guest that already generated its own code (local first use raced the broadcast) is left alone (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs), so that local code wins on that side instead of converging to the host's — an accepted local race, not a transport loss. | OK | 11 | |
| W5 | GeyserStateSnapshot (Runtime/Session/World/WorldStateMessageService.cs:94-109, Handlers/GeyserStateSnapshotHandler.cs, GameAdapter/World/GeyserStateSync.cs) | Host -> guest only (PacketHandler direction HostToGuest; the host's roll is the authority). | Dedicated message: GeyserStateSnapshot = 69 host -> guest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs). Real trigger 1, generation-time geysers: the first full send on the generation-completed edge (guarded by _sentOnce). Real trigger 2, runtime-created geysers: the creation message carries LiquidType as creation-time data. Send: reliable by PacketSender default. Wire members: GeyserStateSnapshot. | 60 s full-set absolute re-send. Apply is an idempotent same-value absolute set. Cadence is the hardcoded 60 s threshold; GeyserStateSnapshot has no AdaptiveStreamId and GeyserStateSync has no AdaptiveStreamRateService, so the governor cannot stretch it. | World entry: the generation-complete first send (src/CasualtiesUnknownOnline.GameAdapter/World/GeyserStateSync.cs). Re-entry/reconnect: the scene-changed hook re-sends immediately. Per-layer generation re-sends because _sentOnce resets when generating starts and the first send re-fires. A snapshot that lands during local generation is deliberately dropped and healed by the next cycle, i.e. a missed application is recoverable within 60 s. | Reliable channel; absolute idempotent apply. A lost value is healed by the next 60 s cycle, the next generation edge, or a re-entry/reconnect. Limitation: the snapshot only writes the liquid type onto an existing local GeyserScript (position-keyed, < 3 m), so it cannot materialize a geyser entity that is missing on the guest — entity existence is the trap-layout / entity-creation domain's responsibility. | OK | 10 | |
| W6 | TrapLayoutSnapshot (Runtime/Session/World/TrapLayoutRegistry.cs, Handlers/TrapLayoutSnapshotHandler.cs, Runtime/Session/World/EntityEventChannel.cs, GameAdapter/World/TrapLayoutScanner.cs, TrapLayoutApplication.cs) | Host -> guest only (PacketHandler direction HostToGuest; the host's scene is the entity-layout authority). | Dedicated message: TrapLayoutSnapshot = 79 host -> guest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs). The real trigger is the host's generation-finished falling-edge scan (one scan per layer), which records each entity. The wire send does NOT happen at that instant: the layout goes out on the world-entry fanout and, since the W6 landing, on the 60 s in-session repair (both reliable by PacketSender default). Apply: (materialize missing, destroy surplus). Wire members: TrapLayoutSnapshot. | 60 s in-session repair, closed as W6: the host's periodic cycle re-derives the layout from the LIVE scene (src/CasualtiesUnknownOnline.GameAdapter/World/TrapLayoutScanner.cs RefreshLayout -> IWorldControl.ReplaceTrapLayout -> Runtime/Session/World/TrapLayoutRegistry.cs Replace) and re-sends it to every in-world member through WorldEntryFanout.SendInSessionRepair (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs). The kernel checkpoint still does not carry the layout (no TrapLayout type exists anywhere in src/CasualtiesUnknownOnline.GameState). The pump stays the hardcoded 60 s threshold in src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs and no adaptive stream id applies. | World entry / late join: the ordered fanout sends the layout (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs). Reconnect-while-InWorld: the same fanout. Re-entry and per-layer regeneration: the InWorld edge fires the fanout, and inWorld is false while generating (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs), so each layer's generation re-fires the edge and re-sends the new layer's layout. A snapshot arriving during local generation is deferred and applied by the pump (src/CasualtiesUnknownOnline.GameAdapter/World/TrapLayoutApplication.cs). | Reliable channel. Recovery is now in-session as well as edge-driven: a swallowed snapshot is healed by the next InWorld edge AND, while a member stays continuously in the world, by the 60 s in-session repair (<= 60 s). The repair re-derives the table from the live scene before sending, so an entity the host's world has since removed is not resurrected; Replace refuses an empty scan against a non-empty table and RETURNS false, so the adapter logs the refusal (a scene in transition must not wipe every guest's layout; the refused state clears at the next generation edge, which is also the only escape when a layer genuinely emptied). Recorded residual: the world-entry fanout still sends the table as last derived, so an entry fanout landing between two repairs can carry an entity removed in that window — the next repair corrects it. Application is intentionally partial: an entry whose prefab name does not load is skipped with a trace (structural members depend on the structure's deterministic placement), so those entities are never materialized on the guest. | OK | 13 | review/trap-layout-snapshot-recovery.md |
| W7 | World-entry snapshot group + WorldSnapshotComplete marker (Runtime/Session/World/WorldEntryFanout.cs, WorldStateMessageService.SendWorldSnapshotComplete, Handlers/WorldSnapshotCompleteHandler.cs, Runtime/Session/World/WorldService.cs) | Host -> guest only (PacketHandler direction HostToGuest). | Dedicated message: WorldSnapshotComplete = 110 host -> guest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs). Real trigger: the end of the ordered world-entry fanout, sent last (reliable by PacketSender default). Receive surface. The group members are BlockState, BlockDamage, TrapLayout, RadiationLineState, the kernel checkpoint, ItemSnapshot, EnemySnapshot and RuntimeEntitySnapshot (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs). Wire members: WorldSnapshotComplete. | none for the marker and the group as a whole. The only send call site of SendWorldSnapshotComplete is src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs. The 60 s cycle re-sends a subset of the group through WorldEntryFanout.SendInSessionRepair (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs): block state, block damage, trap layout, the kernel checkpoint and the runtime-entity snapshot, plus the keypad codes; it does not re-send the radiation line, item snapshot, enemy snapshot or the marker. The subset cadence is the hardcoded threshold at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs (no adaptive stream id applies). | The whole ordered group (and the marker) re-fires on every InWorld edge: first entry / late join, reconnect-while-InWorld, and per-layer regeneration because inWorld is false while generating (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs). The kernel checkpoint inside the group provides revision-based recovery for the world-entity facts. | Reliable channel for every message in the group. A lost group message is healed by the next InWorld edge; while a member stays continuously in the world, the 60 s cycle heals block state / block damage / trap layout / kernel facts / runtime entities / keypad codes, so a lost radiation line, item snapshot or enemy snapshot waits for the next edge. Separately, the marker itself is currently inert in production: WorldSnapshotCompleteReceived has no production subscriber (only tests subscribe), so the receiver cannot in practice distinguish a full backfill from a partial one. ADJUDICATED (adversarial review): the marker is inert (no production subscriber) and is NOT recovery evidence; the group's individual snapshots have their own rows/fallbacks. Marker tracked in todo/session-control-convergence.md. | OK | 9 | |
| E1 | Building entity damage / open (BuildingEntityDamaged, BuildingEntityOpened) (Runtime/Session/World/WorldStateMessageService.cs:152-193, Handlers/BuildingEntityDamagedHandler.cs, Handlers/BuildingEntityOpenedHandler.cs, GameAdapter/World/WorldBuildingEntitySync.cs, Patches/BuildingEntityPatches.cs) | Both: guest -> host report (host applies to its own copy and relays), host -> guest broadcast relay (source excluded). | Dedicated messages NetMsg.BuildingEntityDamaged = 51 and NetMsg.BuildingEntityOpened = 52, both bidirectional. Real triggers: the Body.Attack postfix detects the native health write by before/after diff; the three open paths (Openable.OnUse, LockpingMinigame, KeypadMinigame) report the direct health = 0 write. Report send. Channel: host broadcast or guest report. Reliable by PacketSender default. Wire members: BuildingEntityDamaged, BuildingEntityOpened. | 60 s absolute resend: ->, sent per in-world member. The checkpoint carries the host's building-health facts, recorded on every applied damage (the host-only late-joiner snapshot fact source), and the opened-entity facts. Apply is absolute overwrite, and an opened entity becomes health 0 + RemoteEntityDeath. Cadence is a hardcoded Time.unscaledTime > 60f; there is no AdaptiveStreamId for world entities and WorldEventSync has no AdaptiveStreamRateService, so the adaptive rate governor cannot stretch or remove it. | World entry / late join / reconnect: inside the ordered snapshot group that ends with WorldSnapshotComplete; a mid-generation handshake also sends it. Guest restore raises WorldEntityKernelProjection.OnCheckpointRestored -> BuildingHealthProjected / OpenedEntitiesProjected -> WorldBuildingEntitySync.OnBuildingHealthProjected / OnOpenedEntitiesProjected, applied with the same semantics as the live relay. Missed-range recovery: -> HandleRangeRequest replays journal batches or sends a fresh checkpoint. | Reliable channel for the live event and for the kernel committed batches / checkpoint (PacketSender default reliable = true). A lost host -> guest relay converges: the next 60 s checkpoint or world entry restores the host's absolute health / opened fact. A lost guest -> host report is NOT re-reported and there is no guest diff table: if the host already holds a fact for that position, the next checkpoint overwrites the guest's local damage with the host's stale value (silent revert); if the host never recorded that position, the damage stays host-unknown and the divergence persists until a later host write. A destroyed entity cannot be revived: a positive health report after a recorded 0 is rejected (src/CasualtiesUnknownOnline.GameState/Domains/WorldEntities/WorldEntityDomainModule.cs). | OK | 12 | |
| E2 | Trap/mechanism events (EntityEvent) (Runtime/Session/World/EntityEventChannel.cs, Handlers/EntityEventHandler.cs, GameAdapter/World/TrapEntityScan.cs, TrapEffectApplier.cs, TrapVisualReplay.cs) | Both: guest -> host report, host -> guest broadcast relay (source excluded); the host applies the event to its own world before relaying. | Dedicated message NetMsg.EntityEvent = 66, bidirectional; EntityEventMsg carries the numeric EntityEventKind, position, Extra, drops and (for checkpoint replay) ElapsedSeconds. Real triggers are the Harmony trap patches observing the native transition, e.g. the mine pressed -> exploded edge plus the OnDestroy chain path, and the equivalent edge patches for bear trap, turret, geyser, crystals, lifepod, unlocks, etc. Report send: EntityEventSync.OnTrapTriggered -> _world.SendEntityEvent; channel guest report or host broadcast. Reliable by PacketSender default. Wire members: EntityEvent. | 60 s absolute kernel checkpoint. The WorldEntities facts (one-shot consumptions + durable trap-state machine) are recorded by the host on every trigger and projected back on the guest through TrapSnapshotProjected -> EntityEventSync.OnTrapStateProjected replay. Transient repeatable kinds (GeyserActivated, TurretFired) and Warning edges are deliberately excluded from the snapshot (one-shot consumptions, transient repeatable states and warning edges; the same predicate the arm gate asks), because the native cooldown re-arms the entity and a resend would replay a stale shot/eruption. Cadence is hardcoded; no adaptive governor applies. | World entry / late join / reconnect: the generated trap layout snapshot aligns the guest's entity layout (TrapLayoutApplication materializes missing entries / destroys surplus) and the kernel checkpoint restores the one-shot consumptions and durable trap states, replayed by EntityEventSync.OnTrapStateProjected (each row applied individually). Reconnect uses the same WorldEntryFanout group. Missed-range recovery: kernel range request replays journal batches or a fresh checkpoint. | Reliable channel (PacketSender default). A lost host -> guest relay converges for one-shot and durable kinds: the next checkpoint replays the consumption/state, and the per-entity guards drop duplicates. Transient repeatable kinds (GeyserActivated, TurretFired) are an explicitly accepted loss with a recorded reason (they re-arm natively; replaying them would be stale). A lost guest -> host report is not re-reported: the host's trap stays armed while the triggering guest's copy is already consumed, and the checkpoint only carries host-side facts, so no mechanism reconciles that direction (accepted star-topology proposal loss, same pattern as E1). | OK | 11 | |
| E3 | Runtime entity spawn (EntitySpawned + RuntimeEntitySnapshot) (Runtime/Session/World/RuntimeEntityChannel.cs, RuntimeEntityRegistry.cs, PendingEntityReportTable.cs, RuntimeEntityKey.cs, RuntimeEntityMatch.cs, Handlers/EntitySpawnedHandler.cs, Handlers/RuntimeEntitySnapshotHandler.cs, GameAdapter/World/EntitySpawnSync.cs, RuntimeEntityFactory.cs) | Both: the creating side reports and keeps its local copy; the host creates its own copy and relays (source included), and sends the absolute creation table on world entry and on the 60 s cycle (RuntimeEntitySnapshot) plus an animal key list for acknowledgement only. A creation the host cannot materialize is REJECTED instead — neither recorded nor relayed, and answered to its reporter (RuntimeEntityRejected) — because the host can never own it. | Dedicated message NetMsg.EntitySpawned = 68, bidirectional; the real trigger is the BuildingEntity.Start postfix, filtered by generation/RemoteApply/SpawnReplayMarker, and creation-time data (keypad code, geyser liquid type, crystal tint) rides the same message. Reliable by PacketSender default and idempotent on every repeat, because the record binds by its CREATION KEY — prefab id + creation cell + a creation-instance token stamped by the creating side — never by proximity. Snapshot message NetMsg.RuntimeEntitySnapshot = 134 is host → guest, direction-locked at the handler. Wire members: EntitySpawned, RuntimeEntitySnapshot, RuntimeEntityRejected. | The host records every accepted NON-ANIMAL creation (its own and a relayed guest report) in a bounded table and sends it absolutely in the world-entry group and on the 60 s host cycle as NetMsg.RuntimeEntitySnapshot. An ANIMAL creation is tracked by KEY ONLY and rides the snapshot's key list, never the entry list, because the enemy domain owns the animal copy. The guest records its report before sending and the 60 s fallback re-reports every unacknowledged entry until the host answers; the snapshot is an answer too — its entry list acknowledges non-animals, its key list acknowledges animals without materializing anything. | World entry / late join / reconnect: the ordered fan-out carries the creation table (Runtime/Session/World/WorldEntryFanout.cs); a layer/world boundary resets the host table with the other world tables and the guest pending table at the params-apply boundary (GameAdapter/World/WorldParamsService.cs). A dead entity leaves both tables through the single death funnel reading the STAMPED creation key, so a drifted copy drops its own record and a same-cell sibling keeps its own. Apply is additive and idempotent: missing entities materialize, existing ones bind by their exact creation key and nothing else — the markerless 1 m positional pass is DELETED, because it absorbed a runtime creation into an unrelated markerless same-prefab copy. Every real copy carries that identity: the enemy backfill stamps the key published with EnemySnapshot.RuntimeSpawns and the trap-layout materialization stamps the host-scanned key, so a re-report binds a copy even after it drifted. A report the host cannot materialize is REJECTED (decision 161: neither recorded nor relayed) and answered to its reporter's creation key with a reason code (NetMsg.RuntimeEntityRejected = 135, direction-locked), which ends the pending entry, the 60 s fallback and the local copy; the reporter's half drops the pending entry and destroys its copy by the same stamped key through the remote-death funnel, so the rejection rolls no second drop set and the removal is a no-op when the copy is gone. A materialization failure is contained per entry, and a deferred geyser report re-locates by its QUEUED key instead of a 3 m first hit. | Reliable event with an idempotent apply. The E3 gap — a swallowed report or relay left a permanently one-sided entity until a reconnect — is closed: guest to host by the pending-table fallback, host to guest by the absolute table in the world-entry group and the 60 s cycle, and the animal half by the snapshot's key list. Recorded residual (in the ticket): a destroyed entity whose death signal is itself lost can be re-materialized until the host's copy dies. A creation the host cannot materialize is rejected rather than relayed, so no member — a later joiner included — holds an entity the host cannot own. | OK | 60 | review/runtime-entity-spawn-backfill.md |
| E4 | DynamiteExplosion (Runtime/Session/World/EntityEventChannel.cs:111-133, Handlers/DynamiteExplosionHandler.cs, GameAdapter/World/DynamiteExplosionSync.cs) | Both: guest -> host report, host -> guest broadcast relay (source excluded); the host applies the native explosion to its own world, guests replay the body/visual segment. | Dedicated message NetMsg.DynamiteExplosion = 105, bidirectional. Real trigger: the postfix on the native detonation (on CustomItemBehaviour.DynamiteExplode, after the native explosion already ran). Report send; channel guest report or host broadcast. Reliable by PacketSender default; one-shot identity is the ItemInstanceId, with a seen-set duplicate guard. Wire members: DynamiteExplosion. | none for the detonation event itself — a one-shot explosion has no absolute state to re-send, and there is no explosion snapshot anywhere. The durable consequences are healed by sibling rows: terrain by the block-state 60 s resend, buildings by the world-entities kernel checkpoint in the same cycle, world items by the item domain's own snapshot fallback. Host apply is the native explosion inside RemoteApply; guest replay is visual + real-body only. | No explosion replay on world entry; a late joiner correctly never re-runs a past detonation. The aftermath is reconstructed from the same world-entry snapshot group (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs): block state/damage carry the crater, the kernel checkpoint carries destroyed building health, the item snapshot carries the dropped items. Reconnect uses the same group. Missed-range recovery is kernel-only (committed batches / checkpoint), not an explosion replay. | Reliable event plus ItemInstanceId dedup, so a retransmit cannot double-apply. A swallowed event is not replayed for the blast's visual + instantaneous local-body damage segment; the world-state consequences still arrive through their own reliable channels (the trigger side already ran the native explosion and reported terrain/buildings/items) and converge via the block/building/item fallbacks named above. The 5 s lit-fuse visual on remote clones is a documented accepted local-only presentation residual (docs/evidence/selfchecks/items/dynamite-explosion-selfcheck.md). ADJUDICATED (adversarial review): the detonation event itself is one-shot with no fallback; the durable consequences ride the block / building / item fallbacks named in this row. Kept OK with that dependency stated explicitly. | OK | 10 | |
| E5 | RadiationLineState (Runtime/Session/World/WorldStateMessageService.cs:240-262, Handlers/RadiationLineStateHandler.cs, GameAdapter/World/RadiationLineSync.cs) | Host -> guest only (host owns the line's active/timeGone world state; guests apply the absolute state and keep running their local per-frame presentation). | Dedicated message NetMsg.RadiationLineState = 106, HostToGuest. There is no separate one-shot activation message: the real transition is detected in the host pump and published immediately by Publish. The line's trigger itself is the vanilla layer timer or the host straggler policy (RadiationLineSync.TryActivateForStragglers). Reliable by PacketSender default. Wire members: RadiationLineState. | 5 Hz absolute resend while the line is active: applied by the due test. Apply is absolute overwrite (line.active = true) plus WriteTimeGone(msg.TimeGone) (or line.Deactivate for the inactive state). The interval is a hardcoded constant; RadiationLineSync has no AdaptiveStreamRateService dependency and no AdaptiveStreamId covers it, so the adaptive rate governor cannot stretch or remove the resend. | World entry / late join / reconnect: sends the stored absolute snapshot, which is updated on every publish and by the solo/menu path (src/CasualtiesUnknownOnline.GameAdapter/World/RadiationLineSync.cs) so a solo -> lobby conversion hands the current state to the first guest. A new generation resets _hasPublished, so the first post-generation frame re-publishes the now-inactive line. No missed-range mechanism is needed (single small absolute message, reliable). | Reliable channel. The line is one-way per layer (once active it stays active until the next layer regenerates it), so the only loss that matters is a lost activation — healed within 0.2 s by the active-state resend. A lost deactivation does not exist: a new layer resets the state and the first frame re-publishes it (src/CasualtiesUnknownOnline.GameAdapter/World/RadiationLineSync.cs). Convergence is guaranteed by absolute overwrite of active + timeGone. | OK | 11 | |
| E6 | WorldBloodSpawn (transient decal) (Runtime/Session/World/EntityEventChannel.cs:161-177, Handlers/WorldBloodSpawnHandler.cs, GameAdapter/World/WorldBloodSync.cs, WorldBloodReplay.cs, Runtime/Protocol/Messages/WorldBloodSpawnMsg.cs) | Both: guest -> host report, host -> guest broadcast relay (source excluded); each receiver replays the decal on its own world. | Dedicated message NetMsg.WorldBloodSpawn = 121, bidirectional. Real trigger: the postfix observing the native BleedParticle dying-particle decal spawn, only for the local player's own body (remote clones are suppressed). Report send; channel guest report / host relay (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs; host relay). Reliable (default). Wire members: WorldBloodSpawn. | none, explicitly by design. The channel doc states it, and the message doc repeats it. The decal self-destructs after 120 s, so there is no durable state to reconcile. | none. The world-entry fan-out (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs) carries no blood-decal snapshot, and a late joiner or reconnect sees no previously spawned decals — accepted because the decal is presentation-only and expires in 120 s. No missed-range mechanism exists or is needed. | Reliable event; loss is explicitly accepted as presentation-only degradation (src/CasualtiesUnknownOnline.GameAdapter/World/WorldBloodSync.cs and the 120 s transient lifetime: a lost message is presentation-only degradation). No body or world state is touched, so no convergence mechanism is required. | Transient-by-design | 11 | |
| E7 | WorldEntities kernel facts (trap phase/consumption, opened entities, building health) (src/CasualtiesUnknownOnline.GameState/Domains/WorldEntities/*, Runtime/Session/World projection files, docs/architecture/domains.md) | Host -> guest. The facts are committed by host-only kernel commands; guests report the underlying game event through the direct channels (E1/E2) and receive the facts as committed batches / checkpoints. | The facts ride NetMsg.KernelEnvelope = 122 as EnvelopeKind.CommittedBatch. The trigger is the host command commit, e.g. the atomic trap batch and the building-health / opened-entity commands (ItemKernelAuthority.TryRecordBuildingEntityHealth / TryRecordOpenedEntity). KernelProtocolService subscribes to the commit event and broadcasts the batch through a reliable send (with SendToGuests default reliable = true). WorldEntities facts do NOT ride StateStreamEnvelope; HandleStateStream only surfaces item moves/item states/player-enemy streams. Wire members: BuildingEntityHealthUpdated, OpenedEntity, RecordBuildingEntityHealth, RecordOpenedEntity, RecordTrapConsumed, RecordTrapState, TrapConsumed, TrapStateChanged, WorldEntitiesReset. ResetWorldEntitiesCommand has NO wire identity (decision 174): the received-command path carries no role check (KernelProtocolCommandHandler executes what the envelope names and the kernel's CommandContext has no role), so a wire-mapped layer-scoped reset would be a destructive trigger any guest could fire; the encoder-side events keep their wire form because the guest's replay kernel must learn that the host's table restarted. This closes a live hole: the mapping was removed in this cycle after an adversarial pass showed a guest could reach it. | 60 s absolute kernel checkpoint to every in-world member. Apply is an absolute checkpoint restore: replaces the kernel state and raises CheckpointRestored -> WorldEntityKernelProjection.OnCheckpointRestored -> the flat fact lists applied by EntityEventSync / WorldBuildingEntitySync. Additionally the projection is rebuilt from QueryWorldEntities when a projection apply throws. The cadence is hardcoded; there is no AdaptiveStreamId for world entities, so the adaptive governor cannot stretch it. | World entry / late join: (same ordered group that ends with WorldSnapshotComplete). Mid-generation handshake. Reconnect uses the same WorldEntryFanout group. Missed-range recovery: KernelProtocolService.HandleCommittedBatch detects a revision gap and requests the range; HandleRangeRequest replays the journal batches or sends a fresh checkpoint. Session end / new layer clears the facts (WorldStateMessageService.ResetSessionState / ResetDamagedBlocks). | Committed batches and checkpoint chunks are reliable (PacketSender default reliable = true), and apply is idempotent by operation id (ItemKernelAuthority.Apply) and by position-keyed fact replacement (WorldEntityState.WithConsumption/WithBuildingHealth/WithTrapState replace by position). A swallowed batch is recovered by the revision-gap range request or by the next absolute checkpoint (<= 60 s). Domain invariants reject illegal transitions: a destroyed building cannot be revived (src/CasualtiesUnknownOnline.GameState/Domains/WorldEntities/WorldEntityDomainModule.cs) and illegal trap-phase transitions are rejected. Convergence is the absolute WorldEntities state from the host. | OK | 9 | |
| F1 | Fluid region stream (10 Hz diff + 1 Hz full viewport) (Runtime/Session/World/EntityEventChannel.cs:224-237 + Session/Handlers/FluidRegionHandler.cs; GameAdapter/World/FluidWorldSync.cs, FluidSimulationAuthority.cs, FluidRegionApplication.cs; Runtime/Session/World/FluidKernelViewSync.cs) | host -> guest (unicast per member, unreliable) | NetMsg.FluidRegion = 70 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs), host->guest only (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/FluidRegionHandler.cs). Not a per-operation event: the grid is convergent continuous state, so the trigger-driven path is the change-gated diff - FluidSimulationAuthority.Update computes DiffBox against the member's last-SENT baseline and calls SyncRegion only when the viewport differs (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs). Send: src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs -> IWorldControl.SendFluidRegion -> EntityEventChannel.SendFluidRegion with reliable: false (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs). Wire members: FluidRegion, FluidRegionDiffStream, FluidRegionFullStream. | 1 Hz full-viewport absolute RLE overwrite: base interval 1000 ms for AdaptiveStreamId.FluidRegionFullStream (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs), queried per member at src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs and sent at src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs. Apply semantics = absolute overwrite of every cell of the rectangle, with the omitted trailing zero runs cleared (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidRleCodec.cs -> src/CasualtiesUnknownOnline.GameAdapter/World/FluidRegionApplication.cs). The adaptive rate governor CAN stretch it: AdaptiveRatePolicy.GetEffectiveIntervalMs divides the base interval by the pressure factor (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveRatePolicy.cs) and the hard cap is MaxIntervalMs: 10_000 (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs); priority-2 Critical factor 0.15 gives ~6667 ms, capped at 10 s. The diff stream itself is 10 Hz base with MinHz 1 (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs). Both classes are actually measured via WirePayloadType.FluidRegionFull/Diff (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs). | A member's first stream is always a full viewport: a fresh MemberView has Center (0,0) and an all-zero baseline, so the first anchor mismatch takes the center-change branch (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs); a member with no remote player yet is skipped and healed by the 1 Hz full on entry (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs); moving to a new area re-sends the full viewport (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs); session reset clears every baseline (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs). No Seq-based missed-range recovery - msg.Seq is only logged (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/FluidRegionHandler.cs), there is no gap request, retransmit or ack. | Unreliable by design (reliable: false, src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs); the channel comment records that every region is an absolute overwrite so a lost one is healed by the next, and that a reliable stream would queue the newest snapshot behind retransmissions (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs). The diff baseline view.Grid is advanced while encoding (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs), so a lost diff is NOT re-derived by a later diff - only the 1 Hz full viewport heals it. Convergence to the host grid is guaranteed within the full interval (<=1 s healthy, <=10 s under Critical pressure). No ack, no retransmit, no gap detection. | OK | 20 | |
| F2 | Fluid interaction / drinking (FluidInteraction) (Runtime/Session/World/EntityEventChannel.cs:239-276 + Session/Handlers/FluidInteractionHandler.cs; GameAdapter/World/FluidInteractionSync.cs) | both (guest -> host report; host -> guest broadcast relay) | NetMsg.FluidInteraction = 71, bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs). Real trigger: FluidDrinkPatch Postfix on FluidManager.DrinkLiquid (src/CasualtiesUnknownOnline.GameAdapter/Patches/FluidDrinkPatch.cs) -> FluidInteractionSync.OnDrinkReported (src/CasualtiesUnknownOnline.GameAdapter/World/FluidInteractionSync.cs) -> IWorldControl.SendFluidInteraction -> EntityEventChannel.SendFluidInteraction: guest->host report via _sender.Send (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs), host->all via Broadcast (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs). The host applies to its own grid and relays to the other members, source excluded (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/FluidInteractionHandler.cs). Reliable both ways: PacketSender.Send defaults reliable = true (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs) and Broadcast -> SendToAll also defaults reliable = true (src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs). Wire members: FluidInteraction. | No dedicated FluidInteraction fallback - a drink is never re-reported. The resulting grid state is healed by row F1's 1 Hz full-viewport absolute stream (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs). The code records this as the fallback for the un-reported bath-soiled SetLiquid(5): low-frequency/low-perception, healed by the 1 Hz viewport snapshot, recorded design decision (src/CasualtiesUnknownOnline.GameAdapter/World/FluidInteractionSync.cs). | No per-drink replay log and no drink facts in the kernel checkpoint. The world-entry/reconnect backfill is the grid itself: F1's first full viewport on entry (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs) carries the host's post-drink grid, so a drink that happened before the member joined is already reflected. Accepted drinks are relayed to the other in-world members (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/FluidInteractionHandler.cs). | Reliable in both directions (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs). If a report is nevertheless swallowed (transport/session loss; there is no application-level retry), the host never clears the cell and the guest's locally cleared cell is refilled by the next full viewport (<=1 s, <=10 s under pressure) - the grid converges to the host's authority but the drink's grid effect is not re-applied. If the host's relay to a third guest is lost, that guest's stale water is cleared by the next full viewport. Duplicate/re-ordered events are idempotent: an already-empty cell is ignored (src/CasualtiesUnknownOnline.GameAdapter/World/FluidInteractionSync.cs). | OK | 15 | |
| F3 | Fluid presentation (water push / waterflow sound, FluidPresentation) (Runtime/Session/World/EntityEventChannel.cs:278-298 + Session/Handlers/FluidPresentationHandler.cs; GameAdapter/World/FluidPresentationApplication.cs) | host -> guest (unicast to guests whose viewport contains the cell) | NetMsg.FluidPresentation = 96, host->guest only (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs; src/CasualtiesUnknownOnline.Runtime/Session/Handlers/FluidPresentationHandler.cs). Real trigger = the host's SimulateBand: water push every 11 moves when liquidPushing (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs, send) and waterflow sound every 17 tile moves (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs, send). Send: SendPresentation -> IWorldControl.SendFluidPresentation (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs) -> EntityEventChannel.SendFluidPresentation with reliable: true (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs), targeted at each guest whose viewport contains the cell (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs). Guest replay: FluidPresentationApplication.Apply (src/CasualtiesUnknownOnline.GameAdapter/World/FluidPresentationApplication.cs). Wire members: FluidPresentation. | none. The only send call site is src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs -> src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs; there is no periodic re-send, no world-entry replay and no snapshot fallback. The code comment states a lost push would make the guest miss a local-body physics effect that the next streamed grid snapshot cannot heal (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs), and the recorded self-check agrees (docs/evidence/selfchecks/fluids/fluid-presentation-selfcheck.md). | none. A member entering or re-entering the world receives only future pushes/sounds; the 0.75 s WaterPusher and the sound are not replayed (src/CasualtiesUnknownOnline.GameAdapter/World/FluidPresentationApplication.cs). | Reliable by design (reliable: true, src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs) because the effect is unrecoverable, but the send is fire-and-forget (PacketSender.Send is void; a transport-level failure is only recorded in traffic stats, src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs), so a swallowed message is simply lost. The transient is accepted as non-replayable by design: it is a moment-in-time local-body push / sound with a 0.75 s lifetime (src/CasualtiesUnknownOnline.GameAdapter/World/FluidPresentationApplication.cs), and the recorded self-check (docs/evidence/selfchecks/fluids/fluid-presentation-selfcheck.md) states the grid snapshot cannot heal it. No convergence guarantee for the presentation itself. | Transient-by-design | 16 | |
| F4 | Fluid kernel facts (UpdateFluidRegion / ResetFluids / FluidRegionUpdatedEvent) (src/CasualtiesUnknownOnline.GameState/Domains/Fluids/*; Runtime/Session/World/FluidRegionKernelSync.cs (GameAdapter), FluidKernelProjection.cs, FluidKernelReadProjection.cs; GameAdapter/World/FluidRegionKernelSync.cs, FluidKernelViewSync.cs) | host -> guest (kernel CommittedBatch over KernelEnvelope) | none at the real trigger (a fluid-grid change). The kernel fluid table has no per-change event: the only production writer is the host's 5 s aggregation. The vocabulary exists - UpdateFluidRegionCommand -> FluidRegionUpdatedEvent (src/CasualtiesUnknownOnline.GameState/Domains/Fluids/FluidDomainModule.cs) and ResetFluidsCommand -> FluidsResetEvent (src/CasualtiesUnknownOnline.GameState/Domains/Fluids/FluidDomainModule.cs) - but the update command is issued only by the periodic projection (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidKernelProjection.cs) and ResetFluidsCommand is issued at the LAYER BOUNDARY, not on the wire: the host calls ItemKernelAuthority.TryResetFluids (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs) from the layer-boundary reset (Runtime/Session/World/LayerScopedTableReset.cs) when a new layer is generated, and the command has no WireCommandKind mapping (a remotely triggerable "wipe the fluid table" is a destructive trigger no peer may have - decision 174). Wire members: FluidRegionUpdated, FluidsReset, UpdateFluidRegion. | 5 s coarse aggregation: SummaryInterval = 5f (src/CasualtiesUnknownOnline.GameAdapter/World/FluidRegionKernelSync.cs), timer fires and ReportFluidRegions aggregates the whole grid into chunk totals/dominant type; FluidKernelProjection.Sync change-gates the upserts and writes zero totals for chunks that left the non-empty set (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidKernelProjection.cs); Commit -> ItemKernelAuthority.TryUpdateFluidRegion (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs) -> FluidRegionUpdatedEvent -> CommittedBatch broadcast reliably to guests (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs SendToGuests, reliable = true). Apply is an absolute upsert keyed by (ChunkX, ChunkY) (FluidStateTable.Upsert). This is a reliable kernel batch, not an adaptive stream, so the rate governor does not apply. | The FluidStateTable is a checkpoint field (src/CasualtiesUnknownOnline.GameState/GameCheckpoint.cs) and rides the wire checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/WireCheckpointAssembler.cs); it is sent on world entry (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs) and re-sent every 60 s (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs). The guest rebuilds the coarse facts from checkpoint restore and applied batches (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidKernelReadProjection.cs) and FluidKernelViewSync mirrors them into a log-only list (src/CasualtiesUnknownOnline.GameAdapter/World/FluidKernelViewSync.cs). The facts never write the Unity grid. | Reliable batches (SendToGuests default reliable = true, src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs) plus the 5 s re-aggregation and the 60 s checkpoint resend, so a lost batch converges on the next differing aggregate or checkpoint. Convergence is to the host grid, but the coarse facts lag the grid by up to 5 s and are a checkpoint/diagnostic surface, not the render path (recorded in docs/evidence/selfchecks/architecture/phase-d-fluids-shadow-selfcheck.md). ADJUDICATED: continuous field, no discrete trigger exists; the kernel fact is a deliberate 5 s coarse aggregation and the render path is the F1 stream. Finding (CLOSED by decision 174): ResetFluidsCommand had no production caller; it is now issued at the layer boundary by Runtime/Session/World/LayerScopedTableReset.cs and carries no wire form, and docs/architecture/domains.md records both. | OK | 16 | docs/architecture/domains.md correction |
| F5 | Fluid world-entry / reconnect backfill (Runtime/Session/World/WorldEntryFanout.cs, Runtime/Session/World/FluidKernelReadProjection.cs, GameAdapter/World/FluidKernelViewSync.cs, kernel checkpoint projection) | host -> guest (world-entry snapshot group + kernel checkpoint) | n/a for the backfill row - the event paths are F1 (grid diff), F2 (drink) and F4 (kernel facts). The backfill triggers are the world-entry edges: SceneStateHandler InWorld edge (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs) and the handshake reconnect path (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs) both call WorldEntryFanout.Send. | The recovery channels are the kernel checkpoint group (re-sent every 60 s, src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs) and row F1's 1 Hz full-viewport absolute stream (<=10 s under Critical pressure, src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs). | WorldEntryFanout.Send (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs) sends block state, block damage, trap layout, radiation line, kernel checkpoint (incl. Fluids, src/CasualtiesUnknownOnline.Runtime/Session/Items/WireCheckpointAssembler.cs), item snapshot, enemy snapshot and the completion marker - there is no fluid-grid snapshot in the group. The grid is backfilled by F1's first full viewport when the member's anchor appears (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs) and by a full viewport on every center change (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs). Reconnect re-fires the fan-out (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs) and the session reset clears all fluid baselines (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs). The guest-side kernel facts are rebuilt from the checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidKernelReadProjection.cs). | Checkpoint chunks are reliable (KernelProtocolService.Send -> PacketSender default reliable = true); the grid stream is unreliable and converges through the full viewport. There is no explicit fluid-snapshot completion marker - src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs marks the group complete, not the fluid grid; grid completeness rests on the 1 Hz full viewport, so a member can render its own generated grid for up to the full interval after entry (<=1 s healthy, <=10 s under pressure). | OK | 10 | |
| I1 | Item kernel commands/events (spawn, pickup, drop, destroy, transfer, update state, container sync, layer-boundary world-item reset) (Runtime/Session/Items/ItemService.cs, KernelProtocolCommandHandler.cs, GameState/Domains/Items/*, Runtime item projections) | both (guest->host CommandEnvelope; host->guest CommittedBatch broadcast) | Dedicated CommandEnvelope payloads on NetMsg.KernelEnvelope=122: WirePayloadType.ItemSpawnCommand=1 / ItemPickupCommand=2 / ItemDropCommand=3 / ItemDestroyCommand=4 / ItemUpdateStateCommand=5 / ItemTransferCommand=6 / ItemContainerSyncCommand=7 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs). Trigger = the adapter's verified commit funnel (ItemReportCommitter.CommitReport) -> ItemMessageFlowService.SendItemSpawned/PickedUp/Dropped/Destroyed -> _kernelProtocol.SendCommand (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemMessageFlowService.cs). Guest->host is reliable (PacketSender.Send defaults reliable=true, src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs); host->guest CommittedBatch broadcast is reliable (KernelProtocolService.SendToGuests defaults reliable=true). NOTE: the layer-boundary world-item reset (WorldItemsReset) is host-local by construction — its command has NO wire form, because a remotely triggerable "drop every world item" is a destructive trigger no peer may have. Wire members: ItemContainerSync, ItemDataUpdated, ItemDestroy, ItemDestroyed, ItemDrop, ItemPickup, ItemRelocated, ItemSpawn, ItemSpawned, ItemTransfer, ItemUpdateState, WorldItemsReset. | 5 s world-item keyframe: ItemSnapshotService.SendPeriodicItemSnapshot over the unreliable channel, pumped by ItemPositionAuthority.Update at AdaptiveStreamId.WorldItemSnapshotStream (base 5000 ms, MaxIntervalMs 30_000 -> governor may stretch it to 30 s). Apply is absolute: ItemReconcile kills standalone world items missing from the snapshot and materializes missing entries; carried items are healed by the 1 Hz CharacterData snapshot (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataCapture.cs). CAVEAT: the keyframe is skipped entirely when the host table is empty (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemSnapshotService.cs), so a swallowed destroy is not healed once the table is empty. | World entry / late join / reconnect: WorldEntryFanout.Send sends the kernel checkpoint + the full item snapshot reliably, then WorldSnapshotComplete. Guest missed-range recovery: KernelProtocolService.HandleCommittedBatch detects a revision gap and RequestRange; the host replays the journal or sends a fresh checkpoint. | Reliable in both directions; a lost host->guest committed batch is recoverable via range request/checkpoint, and world-item state converges through the 5 s keyframe except in the empty-table case. There is no guest-side re-report for a swallowed guest->host command: the host-authoritative snapshot overwrites the guest's local result instead of replaying it. | OK | 10 | |
| I2 | World-item movement stream (10 Hz, WorldItemMoveStream) (GameAdapter/Items/ItemWorldSync.cs, Runtime/Session/Items/ItemMessageFlowService.cs, KernelStateStreamService.cs, AdaptiveStreamId.WorldItemMoveStream) | host->guest | The real trigger is the drop/throw, not a 'moved' event: ItemWorldSync.OnItemDropped / OnItemThrown -> ItemReportCommitter.SendDropReport (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemReportCommitter.cs) -> WireCommandKind.ItemDrop on KernelEnvelope (reliable) carrying the drop-moment velocity/rotation/angular velocity; ItemWorldSync.OnItemInstantiated -> SendItemSpawned covers spawns. Motion itself has no dedicated event - the host's physics is published only by the stream. Wire members: WorldItemMoveStream. | 10 Hz (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs BaseHz: 10). Host pump ItemPositionAuthority.Update -> SendMovingItemMoves -> IItemControl.SendItemMove -> KernelProtocolService.SendStateStream -> KernelStateStreamService.SendStateStream with reliable:false (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelStateStreamService.cs). Apply is a soft correction, not a raw overwrite: ItemPositionFollow.OnRemoteItemMove stores targets, then writes rb.velocity/angularVelocity every tick and hard-snaps only past the distance threshold. Governor: MinHz 1 (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs), so 10 Hz can be stretched down to ~2 Hz under critical pressure. | World entry / late join / reconnect: WorldEntryFanout.Send -> SendItemSnapshot (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs) materializes every world item at the snapshot's X/Y before the stream takes over; the kernel checkpoint + range request (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs) rebuild the guest projection (ItemService.OnCheckpointRestored). | Unreliable with explicitly accepted loss (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemPositionAuthority.cs). The guest keeps simulating locally between ticks and each tick re-sends velocity/angular velocity, so convergence is continuous soft-correction rather than acknowledgement. | OK | 11 | |
| I3 | World-item periodic full-table snapshot (5 s, WorldItemSnapshotStream) (Runtime/Session/Items/ItemSnapshotService.cs + GameAdapter/Items/ItemPositionAuthority.cs (same as I2) + docs/decisions (decision #32)) | host->guest | This row IS the fallback half: it has no trigger of its own, only a timer (ItemPositionAuthority.Update). The event half is I1's dedicated kernel commands (spawn/pickup/drop/destroy) plus I7's update-state events; state changes reach the host table through those events. Wire members: WorldItemSnapshotStream. | 5000 ms base (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs) with MaxIntervalMs 30_000, so the adaptive governor may stretch the keyframe to 30 s (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveRatePolicy.cs); sent by ItemSnapshotService.SendPeriodicItemSnapshot over the unreliable channel to every handshaken guest. Apply is absolute overwrite of the TOP-LEVEL state: ItemReconcile kills standalone world items missing from the snapshot, aligns condition/favourited/liquids/components on divergence and materializes missing entries including container children; POSITION is deliberately not applied - it stays with I2. GAPS: skipped when the host table is empty (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemSnapshotService.cs); the wire payload has no Contents member (src/CasualtiesUnknownOnline.Protocol/Wire/WireItemData.cs), so contents of an EXISTING container are never re-aligned. CADENCE FINDING: the 5 s base can stretch to 30 s under pressure (todo/sync-cadence-review.md). | World entry / late join / reconnect: the same snapshot is sent reliably to one member by WorldEntryFanout.Send together with the kernel checkpoint; the guest rebuilds on CheckpointRestored (ItemService.OnCheckpointRestored -> KernelBatchItemProjection.Rebuild) and on projection failure (ProjectionDomain registration, src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemService.cs). | Unreliable, loss accepted by design (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemSnapshotService.cs). Staleness is gated on the receiver: a snapshot whose BaseGlobalRevision is older than the applied kernel revision is dropped (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemSnapshotStreamReceiver.cs), and the per-payload Seq rejects duplicates/out-of-order. | OK | 12 | |
| I4 | Container content sync (ItemContainerSync) (GameAdapter/Items/ContainerItemSync.cs, Runtime/Session/Items container services, KernelProtocolCommandHandler.cs) | both | Container.LoadItem / UnloadItem / UnloadAllItems postfixes (src/CasualtiesUnknownOnline.GameAdapter/Patches/ContainerItemPatches.cs) -> ContainerItemSync.OnLoadedIntoContainer / OnUnloadedFromContainer / OnUnloadedAll. World-container load = SendItemDropped with parentItemId (src/CasualtiesUnknownOnline.GameAdapter/Items/ContainerItemSync.cs) -> WireCommandKind.ItemDrop (reliable); unload/spill = SendItemDropped with parent 0; carried-container internal move = guest SendItemContainerContent -> WireCommandKind.ItemContainerSync (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemActionSync.cs, reliable) or host SendItemCarriedSync. All ride NetMsg.KernelEnvelope=122. | No dedicated container-content keyframe. World-container children DO ride the 5 s keyframe as independent entries (WireWorldItemState.ParentItemId), and ItemReconcile materializes any child missing locally and binds it (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemReconcile.cs -> src/CasualtiesUnknownOnline.GameAdapter/Items/RemoteItemSceneOps.cs); carried-container contents heal through the 1 Hz CharacterData snapshot, which captures items recursively (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataCapture.cs -> ItemStateCodec.CaptureItem) and whose receiver replaces the owner's fact tree wholesale (CloneFactTable.ApplySnapshot). NOT healed: contents of an EXISTING container are excluded from the top-level alignment (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemReconcile.cs; src/CasualtiesUnknownOnline.GameState/Domains/Items/ItemData.cs excludes contents from the kernel payload), so an extra or wrongly-bound child is never removed by the keyframe. | World entry / late join / reconnect: world-container children arrive as their own snapshot entries and are materialized + bound when missing (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemReconcile.cs); carried containers arrive through the character restore (CharacterDataStore.SendSavedCharacter) and the kernel checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs). | Reliable events (KernelEnvelope command + committed-batch broadcast); a lost host->guest batch is healed by the range-request/checkpoint recovery. A swallowed guest->host container-load is NOT healed by the live keyframe (the guest's child stays bound; the keyframe's standalone entry is not re-bound because ItemReconcile skips non-standalone items) - only a reconnect / world re-entry converges. | OK | 10 | |
| I5 | Pickup/drop in-flight operations (creation-before-operation ordering, drop pending, pickup origins) (GameAdapter/Items/PickupSync.cs, ItemReportCommitter.cs, NativeOperationCoordinator.cs, ItemApplication.cs) | both (guest->host reports; host->guest committed batches / rejections) | Trigger hooks: Body.PickUpItem prefix records the pickup origin (src/CasualtiesUnknownOnline.GameAdapter/Patches/BodyItemPatches.cs -> PickupSync.OnPickupStart, src/CasualtiesUnknownOnline.GameAdapter/Items/PickupSync.cs) and its postfix reports the landed pickup; Body.DropItem / ThrowItem prefixes report the drop (src/CasualtiesUnknownOnline.GameAdapter/Patches/BodyItemPatches.cs). Messages: WireCommandKind.ItemPickup / ItemDrop (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemMessageFlowService.cs) on reliable KernelEnvelope; the host answers a refused claim with CommandRejected -> ItemRejected -> ItemApplication.RollbackPickup. | No periodic re-send of a pending claim or a pending drop, and NO hold window: an operation for an item whose creation the host has not judged is refused AT ONCE, with the refused creation's own reason when there is one (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolCommandHandler.cs), and the reporting side settles every deferred creation report before it reports an operation on the same item (src/CasualtiesUnknownOnline.Runtime/Session/Items/PendingItemCreations.cs); the drop machine holds the report one frame for the final throw velocity (src/CasualtiesUnknownOnline.Runtime/Session/Items/DropPendingState.cs). The only absolute state fallback is I3's 5 s world-item keyframe, which converges the HOST's table, not the guest's local pickup/drop result. | Reconnect / world re-entry: the host's checkpoint + item snapshot (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs) re-materialize the authoritative world state and the character restore returns the carried item; the kernel journal range request (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs) replays a missed committed batch. The creation-first ordering on the sending side is what prevents the pickup-before-spawn reorder (a creation report is settled before any operation report on the same item, src/CasualtiesUnknownOnline.Runtime/Session/Items/PendingItemCreations.cs). | Reliable command channel. The in-flight race (pickup beating its spawn report) is prevented by the sender's creation-first ordering, and answered at once by the reject->rollback path when a sender skips it. A command swallowed at transport level has no guest-side re-report: the keyframe neither kills the guest's inventory copy nor re-materializes a duplicate (ItemApplication.FindWorldItem resolves the carried item), so the divergence persists until a reconnect. ADJUDICATED (adversarial review): a swallowed pickup/drop command leaves the guest's local result un-reconciled by the item keyframe; the divergence persists until reconnect. Ticket: todo/guest-command-loss-reconciliation.md (also covers I1's empty-table caveat). | Event-only gap | 9 | todo/guest-command-loss-reconciliation.md |
| I6 | Recipe unlock backfill (RecipeUnlock); the CraftReport item facts are healed by I3/P3 (Runtime/Session/Items/CraftSyncService.cs, Handlers/CraftReportHandler.cs, RecipeUnlockHandler.cs, GameAdapter/Items/CraftingSync.cs) | both (guest->host report; host->guest relay, source excluded) | NetMsg.CraftReport=76 and NetMsg.RecipeUnlock=77, both bidirectional report+relay. Triggers: CraftingSync.OnCraftEnd -> ReportCraft (Recipe.TryMake), OnCombineEnd -> ReportCraft, OnLiquidTransferFinished -> ReportCraft; blueprint use -> SendRecipeUnlock -> CraftSyncService.SendRecipeUnlock. Reliable (PacketSender default true). Wire members: CraftReport, RecipeUnlock. | CraftReport's durable effects have fallbacks: world-table entries ride I3's 5 s keyframe (src/CasualtiesUnknownOnline.Runtime/Session/Items/CraftSyncService.cs UpdateWorldItemState / RemoveWorldItemLocal feed the table the keyframe sends) and carried entries/products ride the 1 Hz CharacterData snapshot and the carried-fact path (src/CasualtiesUnknownOnline.Runtime/Session/Items/CraftSyncService.cs PublishCarriedSyncLocal). RecipeUnlock has NO periodic fallback: SendRecipeUnlock is called only from src/CasualtiesUnknownOnline.GameAdapter/Items/CraftingSync.cs, and FireRecipeUnlockReceived only applies locally + relays (src/CasualtiesUnknownOnline.Runtime/Session/Items/CraftSyncService.cs); the unlock is a per-process static write (src/CasualtiesUnknownOnline.GameAdapter/Items/RecipeUnlockApply.cs). | World entry / late join / reconnect: WorldEntryFanout.Send (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs) sends block/trap/radiation/checkpoint/item/enemy snapshots - no RecipeUnlock re-send. A guest that joins after an unlock keeps its own Recipes.recipes[idx].INT != 0 and never learns it; the CraftReport-driven item state itself is recovered by the checkpoint/snapshot group. | Reliable channel, but the recipe unlock is loss-accepting: craft classification is accept-with-adopt and untracked entries are skipped with a warning (src/CasualtiesUnknownOnline.Runtime/Session/Items/CraftSyncService.cs). There is no convergence guarantee for a swallowed RecipeUnlock - nothing re-derives it from host state. | Event-only gap | 11 | todo/recipe-unlock-fallback.md |
| I7 | Item state payload updates (ItemUpdateState: gun state, custom data, liquids, components) (GameAdapter/Items/GunStateSync.cs, CustomItemDataState.cs, ItemStateCodec.cs, Runtime item update path) | both (guest->host ItemUpdateState; host->guest committed batch / carried fact) | Triggers: Body.UseItemInHand / UseItem postfix (src/CasualtiesUnknownOnline.GameAdapter/Patches/UseItemPatches.cs) -> ItemUseSync.OnItemUsed; GunScript Update/Fire/TryRack/ToggleSafety/LoadMag/UnloadMag postfix (src/CasualtiesUnknownOnline.GameAdapter/Patches/GunStatePatches.cs and siblings) -> GunStateSync.TryReport (dedup) -> ItemUseSync.OnItemUsed (src/CasualtiesUnknownOnline.GameAdapter/Items/GunStateSync.cs). Payload = full digest incl. synthetic custom-data fields (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemStateCodec.cs). Message: WireCommandKind.ItemUpdateState (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemActionSync.cs) on reliable KernelEnvelope; the host's own change is a kernel UpdateItemState batch broadcast (ItemActionSync.SendWorldItemCorrection) or a carried-fact broadcast. | Carried items: the 1 Hz CharacterData snapshot is the fallback - CharacterDataCapture.Capture emits every carried/worn item with the full recursive component/liquid payload (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataCapture.cs) and CharacterDataSync re-reports at 1 Hz (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataSync.cs); the receiver replaces the owner's fact tree (CloneFactTable.ApplySnapshot). World items: I3's 5 s keyframe re-aligns condition/favourited/liquids/components on divergence (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemReconcile.cs), governor-stretchable to 30 s; the character snapshot cadence is fixed. | World entry / late join / reconnect: the kernel checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs) restores every item's Data (condition/favourited/slot/liquids/components) and the guest rebuilds the projection; the character restore (CharacterDataStore.SendSavedCharacter) carries the carried items' full component state. | Reliable event; convergence is guaranteed by the 1 Hz character snapshot (carried items) and the 5 s keyframe (world items), both absolute overwrites of the component/liquid payload. Accepted latency, not accepted loss. | OK | 9 | |
| I8 | Item id coordination (ItemIdWatermark, CarriedInventory) (Runtime/Session/Items/ItemIdCoordinator.cs, Handlers/ItemIdWatermarkHandler.cs, CarriedInventoryHandler.cs, GameAdapter/Items/ItemIdAllocator.cs, CarriedInventoryReporter.cs) | both (guest->host watermark report + CarriedInventory; host->guest watermark grant) | NetMsg.ItemIdWatermark=64 (bidirectional) and NetMsg.CarriedInventory=65 (guest->host). Triggers: every allocation ItemIdAllocator.Next -> SendItemIdWatermark -> ItemIdCoordinator.SendItemIdWatermark, reliable; the guest's generation-finished edge (CarriedInventoryReporter.Update) -> SendCarriedInventory -> ItemIdCoordinator.SendCarriedInventory, reliable. Wire members: CarriedInventory, ItemIdWatermark. | none. The watermark is reported only on allocation (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemIdAllocator.cs) with no periodic re-report, so a swallowed final report is only caught up by a later allocation (the counter only ever rises, src/CasualtiesUnknownOnline.GameAdapter/Items/ItemIdAllocator.cs). CarriedInventory has exactly one send site (src/CasualtiesUnknownOnline.GameAdapter/Items/CarriedInventoryReporter.cs) with no periodic re-send, and the 1 Hz CharacterData snapshot does NOT feed the host's arbitration transfer table (CharacterDataStore.SaveCharacterData only stores the snapshot), so a swallowed CarriedInventory leaves the host's transfer table empty for those ids. | The host re-grants the watermark on every MemberAdded (join/reconnect): src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemIdCoordinator.cs (session.MemberAdded += OnMemberAdded) -> -> GrantItemIdWatermark - but the granted value is the last RECEIVED report, so this backfill cannot repair a swallowed report. Partial kernel-side heal: the first use/slot report of an unregistered carried item is accepted-first and spawns the carried fact (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolCommandHandler.cs), which heals the kernel state but not the arbitration transfer table. | Reliable channel. The watermark is monotonic and self-heals on the next allocation; the CarriedInventory registration is event-only with no convergence guarantee (ItemArbitration.AdoptEvidence returns null). | Event-only gap | 8 | todo/carried-inventory-registration-re-report.md |
| I9 | Item presentation transients (fresh-drop motion, impact sounds, drop provenance) (GameAdapter/Items/ItemReconcile.cs, NonAuthoritativeItemImpactPolicy.cs, ItemDropState.cs, ItemDropProvenance*.cs) | host->guest for the fresh-drop flag; impact sounds are deliberately local-only (no direction) | fresh-drop: the FreshItemDrop component is captured at the trigger (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemWorldSync.cs) and rides the spawn/drop command (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemMessageFlowService.cs) and the world-item snapshot (WireWorldItemState.FreshItemDrop); the receiver re-adds the component (src/CasualtiesUnknownOnline.GameAdapter/Items/RemoteItemSceneOps.cs, src/CasualtiesUnknownOnline.GameAdapter/Items/ItemApplication.cs). Impact sounds: NO event - the two native item-prefab collision presentation paths are suppressed on guests by NonAuthoritativeItemImpactPolicy / NonAuthoritativeItemImpactGuard (src/CasualtiesUnknownOnline.GameAdapter/Patches/ItemCollisionEnter2DPatch.cs) because the guest copy is not the physics authority. Drop provenance: ItemDropProvenanceClassifier is a local routing decision (block drop / building-death drop fold into the break/trap report), not a wire surface of its own. | fresh-drop: the flag is part of every absolute item snapshot (5 s keyframe + world entry), so it is re-applied whenever the item is materialized. Impact sounds: none - suppression is state-driven (session active + guest + standalone world item), so there is nothing to re-send. | fresh-drop: the world-entry / late-join snapshot materializes the item with FreshItemDrop=true (src/CasualtiesUnknownOnline.GameAdapter/Items/RemoteItemSceneOps.cs). Impact sounds: nothing to recover - the host's own native impacts stay local to the host by design. | Explicitly accepted loss (docs/backlog/review/guest-background-ghost-item-ground-sounds.md). Fresh-drop presentation is loss-tolerant: a lost flag only changes the glow and the next absolute snapshot restores it. | Transient-by-design | 8 | |
| P1 | Player state stream (20 Hz host->guests + guest->host report) (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs, GameAdapter/Character/SessionStatePump.cs, GameAdapter/Character/RemotePlayerRenderer.cs) | Both. Host -> each guest: PlayerStreamExchange.BroadcastPlayerState sends one WireStateStream per recipient. Guest -> host: SendPlayerStateReport. Both ride WirePayloadType.PlayerStateStream = 304 inside StateStreamEnvelope over NetMsg.KernelEnvelope = 122. | No discrete per-frame event by design: continuous movement/pose has no discrete trigger, the row IS the stream. The discrete membership triggers do have dedicated events: PlayerJoin = 32 and PlayerLeave = 33 carrying the entity id + spawn anchor; the host sends them in StartMemberSync. Stream send trigger: EntitySyncService.Update host branch (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs) and guest branch (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs). Reliability: unreliable is explicit on both send sites. Wire members: PlayerStateBroadcast, PlayerStateReport. | This row is itself the periodic absolute fallback: default 20 Hz driven by AdaptiveStreamId.PlayerStateBroadcast / PlayerStateReport. Apply semantics: absolute overwrite gated by the monotonic stream sequence, then interpolated by SessionStatePump. The adaptive governor may stretch it: AdaptiveStreamCatalog gives both profiles MinHz = 1 / MaxHz = 60 with no BaseHz override (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs), AdaptiveRatePolicy applies pressure factors down to 0.25 for priority 1 (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveRatePolicy.cs) plus a 128 KB/s budget, so 20 Hz degrades to 15/10/5 Hz and is clamped no lower than 1 Hz. | World entry / late join: MaybeStartEntitySync -> StartMemberSync allocates the entity id, sends the self-activation PlayerJoin, roster-backfills every already-synced member's PlayerJoin, then pushes an immediate broadcast so the clone renders without waiting for the next tick (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs). Guest resets the gate on its own PlayerJoin (the host's snapshot sequence restarts with this join). Reconnect/re-entry is a fresh join + fresh entity id via UpsertEntity (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs). No missed-range replay: every frame is a complete absolute state, so the next tick supersedes a gap. | Reliable channel? No - unreliable by explicit choice. Accepted loss, documented in the PacketSender.Send doc comment (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs): only the 20 Hz state stream goes unreliable, because overwrite semantics + snapshot sequence make drops harmless. Convergence guarantee: the next absolute snapshot overwrites the buffer and the seq gate drops duplicates/out-of-order frames, so a swallowed frame leaves no permanent divergence. | OK | 14 | |
| P2 | Player kernel status/limb/skills facts (UpdatePlayerStatus) (src/CasualtiesUnknownOnline.GameState/Domains/Players/*, Runtime/Session/EntitySync/PlayerKernelStatusProjection.cs, Runtime/Session/CharacterData/PlayerKernelLimbProjection.cs) | Host -> guest. The host is the only writer (AuthorityKind.HostOnly); the committed batch rides NetMsg.KernelEnvelope (122) to every handshaken guest. The projection inputs are host-local: the 20 Hz player stream (alive/conscious) and the 1 Hz character snapshot + LimbStateEvent (limbs/body/skills). | Kernel command UpdatePlayerStatusCommand = 14 producing PlayerStatusUpdatedEvent = 111 in a CommittedBatchEnvelope. Trigger sites: EntitySyncService.ApplyPlayerStream -> PlayerKernelStatusProjection.Sync for alive/conscious, CharacterDataStore.SaveCharacterData -> SyncFromCharacterData for limbs/body/skills, CharacterDataStore.ApplyLimbStateEvent -> SyncFromLimbEvent. Emit: ItemKernelAuthority.TryUpdatePlayerStatus (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs) -> PlayerDomainModule decides Accept (src/CasualtiesUnknownOnline.GameState/Domains/Players/PlayerDomainModule.cs). Reliability: KernelProtocolService.BroadcastCommittedBatch -> SendToGuests default reliable=true. Wire members: PlayerStatusUpdated, UpdatePlayerStatus. The Players table has no reset command at all (decision 174): every fact it holds crosses a layer boundary, so the reset family has no member for it. | No periodic re-send of PlayerStatusUpdatedEvent itself (the projection only emits on a change, src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerKernelStatusProjection.cs returns early when equal). The periodic absolute fallback is the kernel checkpoint: the host re-sends it every 60 s to every in-world member and it carries the whole Players table. The projection inputs are themselves periodic: the 20 Hz stream (P1) re-drives alive/conscious and the 1 Hz character snapshot (P3) re-drives limb/body/skill facts. The checkpoint path is not adapted by the rate governor. | World entry: WorldEntryFanout.Send -> SendCheckpoint. Mid-session joiner: the handshake sends the checkpoint immediately. Missed-range: a revision gap buffers the batch and requests the range, the host replays the journal or sends a fresh checkpoint when the range is outside it (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs). Reconnect/re-entry: PlayerKernelRestoreProjection.Apply overlays the kernel terminal facts onto the saved snapshot, called from CharacterDataStore.SendSavedCharacter (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs). | Reliable: the committed batch is sent with reliable=true (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs/472) and the host journals the last 2048 batches. Loss is not accepted: a detected gap is repaired by a range request, an out-of-journal gap by a fresh checkpoint, and the 60 s checkpoint bounds any silent divergence to at most 60 s. | OK | 16 | |
| P3 | Character data snapshot (1 Hz) + host-side persistence (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs, Handlers/CharacterDataHandler.cs, Handlers/HostCharacterDataHandler.cs, Persistence/WorldCharacterBinder.cs, GameAdapter/Character/CharacterDataSync.cs) | Both. Guest -> host: NetMsg.CharacterData = 37. Host -> guest: the same id carries the reconnect restore, and NetMsg.HostCharacterData = 53 carries the host's own snapshot for clone-inventory rendering. | Event-level immediate re-report at the real trigger: an inventory-internal move (SwapSlots/SwitchHands) or any cross-player apply calls ReportInventoryChanged, which sends a non-throttled full snapshot, and leaving the world pushes a final snapshot (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataSync.cs). Send: CharacterDataStore.ReportCharacterData -> NetMsg.CharacterData and BroadcastHostCharacterData. Reliability: PacketSender.Send defaults reliable=true (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs). Wire members: CharacterData, HostCharacterData. | 1 Hz absolute full snapshot: CharacterReportInterval = 1f, pumped by CharacterDataSync.Update. Apply semantics: absolute overwrite of the host's per-SteamID saved snapshot and of the clone fact table (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataSync.cs). Not adapted by the rate governor (no AdaptiveStreamId for this path). | SendSavedCharacter hands the saved snapshot back on handshake, on the InWorld scene-state edge and on respawn, gated on the host being in a world. The host merges the item-arbitration transfer table over the snapshot and overlays kernel terminal facts (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs). The table itself is in-memory and session-scoped: it starts EMPTY and is filled by the live 1 Hz reports plus a restore's claim, which binds the world archive's stored characters into it. The CUT side obeys the same rule : a key two present players map to is carried by no file and the cut report names them, because a file under a shared key could later be claimed by the wrong player. The separate CasualtiesUnknownOnline.character-data.bin disk store is RETIRED (S4 scope 6) — the archive is the only persistent copy of a character (decisions 162/164), so a host restart refills this table from the archive, never from a local cache that could resurrect a player the package omits. A stored character whose key two present players claim is REFUSED, not handed to one of them, because an IP-direct session allows duplicate display names by design. | Reliable channel (PacketSender default reliable=true). A lost 1 Hz report self-heals on the next tick because the snapshot is absolute; a disconnect before the next tick is covered by the dedicated-event merges (limb/enemy/heal) and the final NotifyBodyLeft snapshot. A host restart does not resurrect a reconnect from local state: the world archive is the only persistent copy (decisions 162/164) and a restore refills the table through the claim, so a player the package omits cannot come back from stale data. A new run clears the in-memory table. | OK | 22 | |
| P4 | Limb latch events (LimbStateEvent) (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs, Handlers/LimbStateEventHandler.cs, GameAdapter/Patches/LimbStatePatches.cs, GameAdapter/Character/CharacterDataSync.cs) | Both. Guest -> host report; host -> every other guest broadcast relay with the source excluded (Handlers/LimbStateEventHandler.cs:31). | NetMsg.LimbStateEvent = 93 (the owner's own limb latch; host → guest broadcast relay (source excluded) - carries the body's full post-event limb + health terminal state'). Trigger: the Harmony postfixes on Limb.BreakBone/MendBone/Dislocate/UnDislocate/Dismember report only a verified latch transition and skip render clones into CharacterDataSync.ReportLimbStateEvent -> SendLimbStateEvent. Reliability: reliable:true on the host fan-out and PacketSender default reliable=true for the guest report. Wire members: LimbStateEvent. | 1 Hz absolute character snapshot (P3) carries the full limb set; the code states the fallback explicitly. The 1 Hz path is not adapted by the rate governor. | The host merges the event's full post-event limb + body health state into the owner's saved per-SteamID snapshot immediately, so a disconnect before the next 1 Hz report still restores every changed field. The event is also projected into the kernel players table (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs) and therefore into the 60 s checkpoint / PlayerKernelRestoreProjection. A late joiner receives the state through SendSavedCharacter or the next 1 Hz snapshot. | Reliable dedicated event plus a periodic absolute 1 Hz snapshot fallback; convergence is guaranteed by the snapshot's complete limb set. No accepted loss. | OK | 10 | |
| P5 | One-shot character presentation (CharacterSound, CharacterAttackAnim, CharacterLandingVisual, CharacterRagdoll) (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs, Handlers/CharacterSoundHandler.cs, CharacterAttackAnimHandler.cs, CharacterLandingVisualHandler.cs, CharacterRagdollHandler.cs, GameAdapter/Character/Character*Sync.cs) | Both. The owner reports its own one-shot to the host; the host broadcasts its own to every handshaken guest and relays a guest's report source-excluded. Ids: CharacterSound = 94 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs), CharacterAttackAnim = 113, CharacterLandingVisual = 114, CharacterRagdoll = 120. | Four dedicated one-shot messages, all reliable:true on the host fan-out. Triggers at the real game call sites: sound capture in GameAdapter/Patches/SoundPlayPatch.cs:61 and src/CasualtiesUnknownOnline.GameAdapter/Patches/SoundPlayAudioClipPatch.cs, attack animation in GameAdapter/Patches/BodyPatches.cs:109, landing in src/CasualtiesUnknownOnline.GameAdapter/Patches/BodyPatches.cs, ragdoll-key collapse in GameAdapter/Patches/PlayerCameraHandleInputPatch.cs:61. Wire members: CharacterAttackAnim, CharacterLandingVisual, CharacterRagdoll, CharacterSound. | CharacterSound / CharacterAttackAnim / CharacterLandingVisual: none - pure one-shot presentation with no durable state (nearest send sites src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs); nothing periodic can or should replay a past sound/animation/landing. CharacterRagdoll: the P1 20 Hz stream's Standing flag is the fallback, with RagdollPoseGate suppressing a stale standing=true snapshot for 500 ms. | None for the three pure visuals: a late joiner never sees a past one-shot and there is nothing to replay. CharacterRagdoll has a clone-creation queue that drains within the suppression window and then defers to the state stream (GameAdapter/Character/CharacterRagdollSync.cs:89), and the P1 stream is authoritative for the joiner's pose. | All four ride the reliable channel, so transport loss is not accepted; the recorded design intent is one-shot = one message and the trigger never rides the snapshot stream. Where a lost effect has no state to heal (sound/attack animation/landing) the degradation is explicitly recorded as acceptable in docs/evidence/selfchecks/presentation/footstep-sound-selfcheck.md (line 51:), and the ragdoll self-check records the same for its fallback case (docs/evidence/selfchecks/players/character-ragdoll-toggle-sync-selfcheck.md). CharacterRagdoll's only durable fact (lying/standing) is healed by P1. | Transient-by-design | 14 | |
| P6 | Carry set/clear kernel facts (src/CasualtiesUnknownOnline.GameState/Domains/Players/*, Runtime/Session/PlayerInteraction/PlayerCarryService.cs, PlayerKernelCarryProjection.cs, GameAdapter/Character/CarriedBody*.cs) | Both. Guest -> host NetMsg.PlayerCarryStartRequest = 99 / PlayerCarryStopRequest = 100 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs); host -> every guest the committed batch on NetMsg.KernelEnvelope (122). The host's own UI takes the same local path. | Kernel carry facts: SetPlayerCarryCommand / ClearPlayerCarryCommand -> PlayerCarrySetEvent = 118 / PlayerCarryClearedEvent = 119. Trigger: PlayerCarryService.PublishCarryState writes the single authoritative fact from HandleCarryStartRequest / HandleCarryStopRequest (guest request /). Emit: src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs. Broadcast: KernelProtocolService.BroadcastCommittedBatch (with default reliable=true). Wire members: ClearPlayerCarry, PlayerCarryCleared, PlayerCarrySet, SetPlayerCarry. | No dedicated periodic carry re-send; the periodic absolute fallback is the kernel checkpoint, which carries the whole Players table including CarrierOfSteamId/CarriedBySteamId, re-sent every 60 s to every in-world member (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs ->). The carried body's presentation additionally rides the P1 20 Hz stream. The checkpoint path is not rate-governor adapted. | World entry: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs; mid-session joiner: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs; both send the checkpoint. The guest rebuilds its mirror from the checkpoint players table. Missed-range: the batch is journaled and replayed on a revision gap, or a fresh checkpoint is sent (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs). Member leave / scene exit publishes a clear through the same kernel path (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerCarryService.cs). | Reliable committed batch (SendToGuests default reliable=true) plus the 2048-entry host journal; a lost batch is repaired by the range request or the next checkpoint. Convergence guarantee holds; no accepted loss. | OK | 12 | |
| P7 | Cross-player inventory transfer fact (RecordPlayerInventoryTransfer) (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInventoryTakeService.cs, PlayerInteractionResultAuthority.cs, GameState/Domains/Players/*) | Guest -> host NetMsg.PlayerInventoryTakeRequest = 97 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs); host -> every guest the journaled result event in a reliable CommittedBatch. | Kernel event PlayerInventoryTransferEvent = 120. Request trigger: PlayerInventoryTakeService.SendTakeRequest; host trigger: HandleTakeCore -> PublishTransfer -> PlayerInteractionResultAuthority.TryRecordPlayerInventoryTransfer. Apply: PlayerInteractionKernelProjection.ProjectTransfer fires only for the two participants -> GameAdapter/PlayerInteractionApply.OnPlayerInventoryTransfer. Wire members: PlayerInventoryTransfer. | None for the transfer fact itself, and the code says so explicitly: the adapter warns that the 1 Hz snapshot will not apply this authoritative fact. The host-side durable effect is written into both character snapshots immediately, and a successful local apply triggers an immediate full character re-report. | Reconnect: the host merges the item-arbitration transfer table over the saved character snapshot, and that is exactly where a guest-owned taken item is recorded. Kernel checkpoint at world entry / handshake / 60 s covers the host-recipient case (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs, src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs). Missed-range: the result batch is in the host journal and replayed on a revision gap (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs). | Request and result are reliable (PacketSender default; the result rides a reliable CommittedBatch) and the result is journaled, so a swallowed batch is recoverable while the guest kernel detects a revision gap. Caveat: the adapter apply is fire-and-forget - if the local body is not ready the fact is skipped (src/CasualtiesUnknownOnline.GameAdapter/PlayerInteractionApply.cs) and the only heal is the reconnect transfer-table merge; the journal-only event reduces to a no-op in the kernel, so the 60 s checkpoint does not replay it. | OK | 12 | |
| P8 | Cross-player heal / item-use results (RecordPlayerHealResult, RecordPlayerItemUseResult) (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerHealService.cs, PlayerItemUseService.cs, PlayerInteractionResultAuthority.cs, GameState/Domains/Players/*) | Guest -> host NetMsg.PlayerHealRequest = 102 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs) and PlayerItemUseRequest = 116; host -> every guest the journaled result event in a reliable CommittedBatch. | Kernel events PlayerHealResultEvent = 121 and PlayerItemUseResultEvent = 122. Request and result triggers ride the kernel envelope; apply: GameAdapter/PlayerInteractionApply.OnPlayerHealReceived / OnPlayerItemUseReceived. Wire members: PlayerHealResult, PlayerItemUseResult. | The host writes the post-heal / post-use body state into its authoritative character snapshots immediately, and the receiving participant re-reports the full character snapshot immediately on apply. The 1 Hz character snapshot (P3) is therefore the periodic absolute fallback for the target's health/limbs/worn item, and the consumed item's ownership is mirrored into the kernel item table / transfer table. | Reconnect/re-entry: the saved character snapshot (already updated at the result) plus PlayerKernelRestoreProjection for terminal facts; the kernel checkpoint at world entry / handshake / 60 s covers host-owned item changes (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs, src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs); the transfer table merge covers guest-owned consumed/worn items (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs). Missed-range: the result batch is journaled and replayed on a revision gap. | Reliable request plus reliable journaled result; the durable body/item effect is additionally snapshot-backed at 1 Hz and kernel-backed (checkpoint + journal), so a swallowed result converges on the next snapshot or the next checkpoint. No accepted loss. | OK | 10 | |
| P9 | Player interaction requests (take, carry start/stop, heal, item use, push) (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/*, Handlers/Player*RequestHandler.cs, PlayerPushResultHandler.cs, GameAdapter/PlayerInteractionApply.cs, PlayerPushApply.cs) | Guest -> host requests; host -> clients results. Ids: PlayerInventoryTakeRequest = 97 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs), PlayerCarryStartRequest = 99, PlayerCarryStopRequest = 100, PlayerHealRequest = 102, PlayerItemUseRequest = 116, PlayerPushRequest = 118, PlayerPushResult = 119. | Every request is a dedicated message sent reliable by default. Send triggers: src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInventoryTakeService.cs, src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerCarryService.cs, src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerHealService.cs, src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerItemUseService.cs. Results: take/carry/heal/use ride journaled kernel CommittedBatches (reliable) and are surfaced by PlayerInteractionKernelProjection / PlayerKernelCarryProjection; push is a direct reliable host->all message because it is transient presentation, recorded as decision #130 in docs/decisions/archive.md. Wire members: PlayerCarryStartRequest, PlayerCarryStopRequest, PlayerHealRequest, PlayerInventoryTakeRequest, PlayerItemUseRequest, PlayerPushRequest, PlayerPushResult. | Push: the resulting motion rides the P1 20 Hz player stream - src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerPushService.cs documents it, and PlayerPushApply applies the force locally. Take/carry/heal/use: no periodic re-send of the request/result, but the resulting durable facts are carried by the 60 s kernel checkpoint (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs) and the 1 Hz character snapshot (P3); the journal replays a missed batch on a revision gap. | Kernel checkpoint at world entry (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs), on handshake (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs) and every 60 s (the same fanout's in-session repair, pumped by src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs); range request recovery; reconnect restores the carry mirror from the checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerKernelCarryProjection.cs) and the character snapshot with the transfer-table merge (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs). | Requests and results are reliable. Push is transient-by-design: the force application can be lost with no retry, but the target's position/velocity converges through the P1 stream. A swallowed request is a no-op (one request per user gesture, no retry), which is acceptable because the user can repeat the gesture. | OK | 11 | |
| P10 | Remote inventory operations (RemoteInventoryOperationRequest, RemoteInventoryApply) (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerRemoteInventoryService.cs, Handlers/RemoteInventory*Handler.cs, GameAdapter/RemoteBackpack*.cs, RemoteInventoryOperationApply.cs) | Guest -> host NetMsg.RemoteInventoryOperationRequest = 125 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs); host -> owner NetMsg.RemoteInventoryApply = 126. | Two dedicated messages, both reliable (PacketSender default). Request trigger: native remote-backpack gestures in GameAdapter/RemoteBackpackOperationHandler.cs:23 -> PlayerRemoteInventoryService.SendRemoteInventoryOperation. Apply trigger: PlayerRemoteInventoryService.HandleRemoteInventoryOperation dispatch -> GameAdapter/RemoteInventoryOperationApply.Apply. Durable kinds also commit kernel commands (and the SyncContainerItemsCommand / UpdateItemStateCommand paths) and record the participant result event. Wire members: RemoteInventoryApply, RemoteInventoryOperationRequest. | No periodic re-send of the operation itself. The owner's apply triggers an immediate full character re-report and, for the state-only kinds, an explicit item-state report. The periodic absolute fallback for the resulting inventory is the 1 Hz character snapshot (P3); the committed item facts are covered by the 60 s kernel checkpoint. | Kernel checkpoint at world entry (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs), on handshake (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs) and every 60 s (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs) plus journal range recovery (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs/407). Reconnect restores the character snapshot with the transfer-table merge (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs). TransferToRequester delegates to the take path (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerRemoteInventoryService.cs), which is covered by P7. | Reliable request and reliable apply message; the durable effect is snapshot-backed (1 Hz) and kernel-backed (checkpoint + journal), so a swallowed message converges. Apply-side guard: if the owner's local body or item is missing the native operation is skipped with a warning (src/CasualtiesUnknownOnline.GameAdapter/RemoteInventoryOperationApply.cs/39) and the host's authoritative snapshot remains unchanged, so the next owner report restores the real state rather than silently diverging. | OK | 10 | |
| P11 | Medical operation sessions (StartRequest/StartAck/Update/State/EndRequest/EndCommitted/Cancel/TargetCheckRequest/TargetCheckAnswer) (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionService.cs, ShrapnelOperationSessionService.cs, OtherMedicalOperationSessionService.cs, MedicalOperationSessionPublisher.cs, ShrapnelStatePublisher.cs, MedicalInjectionReportBuffer.cs, ShrapnelPositionReportBuffer.cs, Handlers/MedicalOperation*Handler.cs, GameAdapter/RemoteMedical*.cs, MedicalOperationApply.cs) | Both. Operator -> host: StartRequest (127), Update (129), EndRequest (131), Cancel (133). Host -> clients: StartAck (128), State (130), EndCommitted (132) (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs). Host -> target: TargetCheckRequest (137). Target -> host: TargetCheckAnswer (138). | Nine dedicated ids. Start/send trigger; end/cancel at src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionService.cs and src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionService.cs. Host progress trigger; terminal trigger. Reliability: Start/End/Cancel ride PacketSender defaults (reliable); injection deltas are coalesced but reliable; ordinary shrapnel positions are explicitly unreliable and coalesced while ownership/break/release transitions bypass the buffer and are reliable. State/EndCommitted broadcasts are reliable (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionPublisher.cs). Wire members: MedicalInjectionReport, MedicalOperationCancel, MedicalOperationEndCommitted, MedicalOperationEndRequest, MedicalOperationStartAck, MedicalOperationStartRequest, MedicalOperationState, MedicalOperationUpdate, ShrapnelPositionReport, MedicalOperationTargetCheckRequest, MedicalOperationTargetCheckAnswer. | No periodic re-publish of the session state; the non-terminal State is an absolute snapshot published on every committed update (MedicalOperationInjectionApplier builds CommittedMl + Sequence + item + health + limbs at src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationInjectionApplier.cs), so the next update supersedes a lost one. The intermediate injection / shrapnel-position streams are adaptive (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs BaseHz 60, MinHz 1, priority 2 - the governor may stretch them to as low as 1 Hz under Critical pressure). The terminal EndCommitted is the reliable absolute reconciliation (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionService.cs), forced by the 15 s idle timeout. The durable body/item effect is snapshot-backed by P3 - the receiver re-reports immediately. | No session backfill by design: a medical session is transient, a late joiner sees nothing of an in-progress operation, and the operator/target receive the reliable terminal EndCommitted plus the resulting 1 Hz character snapshot. A participant disconnect terminates the session and publishes the terminal; shrapnel flushes its buffered position before a semantic terminal. | Start/End/Cancel/State/EndCommitted are reliable. Intermediate injection deltas are coalesced into reliable messages (a slow treatment cannot accumulate a lost delta into a wrong final dose; the EndRequest total is the final reconciliation, documented in src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalInjectionReportBuffer.cs). Ordinary shrapnel piece positions are unreliable by design with accepted loss: each update is an absolute position per piece and the terminal EndCommitted carries the final piece set. Convergence guaranteed by the terminal message plus the 1 Hz snapshot of the target body. | OK | 20 | |
| P12 | Player color (PlayerColorUpdate) (src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs, Handlers/PlayerColorUpdateHandler.cs) | Both. Guest -> host report; host -> other guests relay, and the host's own change is a full broadcast. Id: PlayerColorUpdate = 123 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs). | Dedicated NetMsg.PlayerColorUpdate = 123, reliable (PacketSender defaults reliable=true; SessionService.ReportLocalPlayerColor sends). Trigger: the Preferences color picker calls SessionService.ReportLocalPlayerColor. Host applies + relays, guest applies the host's own color. Wire members: PlayerColorUpdate. | none - there is no periodic color re-send. The closest evidence is the only send call sites, src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs (host broadcast) (guest report); a lost update is only refreshed by the join/handshake backfill. | Reconnect / late join: the color is carried on Handshake, HandshakeAck, PlayerJoin (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/PlayerJoinHandler.cs) and the host's own PlayerJoin construction (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs), so every member's color is re-established on any join/reconnect. | Reliable channel; each side's roster presence is the durable store. Two caveats: (1) a lost live update is only healed by a later join/handshake, so a color change can stay stale for the rest of the session; (2) the host's relay of a guest's color is dropped by every other guest - the relay stamps SteamId = the original guest while the guest accept branch requires memberId == sender, so third-party name tags converge only on the next join/handshake. | OK | 12 | |
| P13 | Ragdoll / limb pose presentation (src/CasualtiesUnknownOnline.GameAdapter/Character/LimbPoseCapture.cs, RagdollPoseApplication.cs, RemoteBodyDriver.cs, Runtime/Session/EntitySync/PlayerStreamExchange.cs) | Both. The exact limb poses ride the bidirectional P1 20 Hz player stream (host -> guest and guest -> host); the discrete ragdoll collapse rides the dedicated reliable CharacterRagdoll = 120 one-shot (owner -> host -> other guests). | Dedicated one-shot CharacterRagdoll = 120 at the real trigger: GameAdapter/Patches/PlayerCameraHandleInputPatch.cs:61 -> CharacterRagdollSync.Report, reliable (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs). The continuous limb pose has no discrete event by design - it is captured every frame (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs) and rides the P1 stream as WirePlayerStreamState.LimbPoses. | The P1 20 Hz absolute stream: each snapshot carries the full per-limb world position + z rotation, applied on the receiver and then written directly onto the clone. Absolute overwrite; the adaptive governor may stretch 20 Hz down to 1 Hz (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs MinHz = 1). | World entry / late join: the poses ride the same stream that starts with StartMemberSync's immediate broadcast (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs); a reconnect is a fresh join with a fresh entity id, so the first snapshot already carries the current pose. The ragdoll one-shot additionally has a clone-creation queue that drains within the 500 ms suppression window (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/RagdollPoseGate.cs) and then defers to the stream. | The stream is unreliable with accepted loss (overwrite semantics, P1). The one-shot is reliable and, when it beats the stream, RagdollPoseGate keeps the clone lying until a standing=false snapshot confirms the collapse or 500 ms elapse; CharacterRagdollSync arms that gate. Convergence: the next absolute snapshot carries the true pose. | OK | 8 | |
| N1 | Enemy snapshot + attack (EnemySnapshot, EnemyAttack) (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/EnemySnapshotHandler.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/EnemyAttackHandler.cs, src/CasualtiesUnknownOnline.GameAdapter/Character/EnemySyncCoordinator.cs, src/CasualtiesUnknownOnline.GameAdapter/Character/EnemyPresentationApplier.cs, src/CasualtiesUnknownOnline.GameAdapter/Character/EnemyCombatDirector.cs) | host -> guest (both messages) | EnemySnapshot = NetMsg 81, host -> guest, reliable: the host world-entry fan-out sends it on the member first InWorld edge and on re-entry/reconnect (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs; src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs); send at src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs, applied at src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs. EnemyAttack = NetMsg 83, host -> guest, reliable:true: the host enemy-combat director ANNOUNCES the enemy's action (which enemy, which kind, and the per-enemy monotonic AttackSeq) to EVERY in-world guest — the bite announcement fires when the bite action's cooldown/stun/range gates open and the crystal's fires at Lunge begin — and each guest judges on its OWN screen whether the attack reached its body (EnemyAttackLocalProbe + the pure Runtime rule src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemyAttackJudgment.cs) before applying it locally in src/CasualtiesUnknownOnline.GameAdapter/Character/EnemyCombatReplay.cs; the host names neither a victim nor a limb (decision 185) (src/CasualtiesUnknownOnline.GameAdapter/Character/EnemyCombatDirector.cs; src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs), the one-attack-once identity is the victim-side ledger src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemyAttackLedger.cs. Wire members: EnemyAttack, EnemySnapshot. | EnemySnapshot now HAS one: the host 60 s in-session repair group (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs SendInSessionRepair -> src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs) re-sends the absolute snapshot to every in-world member, and it is idempotent because each entry carries the host bind-time SPAWN anchor (src/CasualtiesUnknownOnline.Runtime/Protocol/Messages/EnemyStateMsg.cs SpawnPosition) that the guest pairs its frozen copies on; the live position stays presentation-only. An empty host table remains a no-op. EnemyAttack still has none, and by design: the announcement is a one-shot notification whose loss is accepted by design (it costs exactly one attack — the judging client's own screen is the only verdict, nothing host-side consumed a hit, nothing diverges, and a landed hit's terminal report chain is unchanged — review/enemy-hit-determination-local.md). The only periodic path is the N3 20 Hz EnemyStateStream absolute overwrite (reliable:false), which refreshes only continuous fields of already-bound ids and never re-issues an attack. | World entry / late join / reconnect: the WorldEntryFanout.Send snapshot group (EnemySnapshot carries the ids, the spawn anchors and RuntimeSpawns). A member that STAYS in the world: the same absolute snapshot rides the 60 s repair group, so a swallowed entry send heals without a reconnect - the guest generated baseline re-pairs on the anchor at any later time (before the anchor a repeat snapshot could not pair, and a failed pairing also cleared the mapping flag), RuntimeSpawns materialization stays identity-keyed and idempotent, and a removed id is never resurrected. EnemyAttack has no backfill path (see the gap ticket). | A swallowed EnemySnapshot used to be permanent for a member that stayed in the world, because nothing re-sent it and a later snapshot could not re-pair (the guest frozen copies were compared against the host live positions). It now heals on the 60 s cycle. A dropped EnemyAttack is still not re-issued, but it is no longer a lost host verdict: the host announced the enemy's action, the victim's own screen decided the hit, and nothing diverges — the next attack is announced normally (review/enemy-hit-determination-local.md, decision 185). | OK | 16 | review/enemy-hit-determination-local.md |
| N2 | Enemy kernel facts (Upsert/Remove/Reset + combat results) (src/CasualtiesUnknownOnline.GameState/Domains/Entities/*, Runtime enemy kernel projections) | both (host commits and broadcasts; guest reports ride the command envelope up) | KernelEnvelope (NetMsg 122) carrying CommittedBatch, reliable: every UpsertEnemyCommand / RemoveEnemyCommand / ResetEnemiesCommand / RecordEnemyBite&#124;Lunge&#124;EffectCommand commit is broadcast by KernelProtocolService.BroadcastCommittedBatch (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs; SendToGuests defaults reliable=true at src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs). Guest combat reports ride the same envelope Command path (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemyCombatKernelSubmitter.cs/63/86); the Entities domain maps each command to its event (src/CasualtiesUnknownOnline.GameState/Domains/Entities/EnemyDomainModule.cs) and the guest applies removals from the committed batch at src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs. Wire members: EnemiesReset, EnemyBiteResult, EnemyEffectResult, EnemyLungeResult, EnemyRemoved, EnemyUpserted, RecordEnemyBite, RecordEnemyEffect, RecordEnemyLunge, RemoveEnemy, UpsertEnemy. ResetEnemiesCommand is host-local and has NO WireCommandKind mapping (decision 174): the host issues it at the layer boundary, and the resulting EnemiesReset committed-batch row is how a guest learns the table restarted. | none periodic. Recovery is on demand: the world-entry checkpoint plus journal tail (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs; src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs) and the revision-gap range request (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs). | World entry/late join/reconnect: SendCheckpoint sends the full checkpoint then every journal batch above the checkpoint revision (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs; src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs). Missed-range recovery: a guest that sees a revision gap buffers the batch and requests the missing range (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs); when the range is outside the journal the host sends a fresh checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs). | Reliable (KernelEnvelope/CommittedBatch; SendToGuests default reliable=true). A lost batch is detected as a GlobalRevision gap and healed by journal replay or a fresh checkpoint; the guest applies enemy removals from the committed batch (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs). Convergence is guaranteed while the peer stays connected; a session drop ends the session (no host migration). | OK | 13 | |
| N3 | Enemy state stream (20 Hz, EnemyStateBroadcast) (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs, src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs, AdaptiveStreamId.EnemyStateBroadcast) | host -> guest | none at a discrete trigger. This row is the continuous presentation transport: EnemySyncService.Update publishes a WireStateStream batch (WirePayloadType.EnemyStateStream over NetMsg.KernelEnvelope) on the adaptive cadence (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs). Discrete enemy events (attack, removal) ride N1/N2 instead. Wire members: EnemyStateBroadcast. | 20 Hz base (StateStreamOptions.DefaultStateStreamHz = 20, src/CasualtiesUnknownOnline.Runtime/Configuration/StateStreamOptions.cs; AdaptiveStreamId.EnemyStateBroadcast profile src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs), unreliable, absolute overwrite with a per-host seq gate (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs reliable:false;). The adaptive governor may stretch it: priority 1, MinHz 1, Critical pressure factor 0.25 -> a 5 Hz floor (plus the byte budget), so the base 50 ms can become 200 ms. | The world-entry EnemySnapshot (N1) binds ids and materializes runtime spawns before the stream is useful (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs). The stream itself is update-only: it never creates or removes an aggregate (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs). | Unreliable by design: every frame is an absolute overwrite of the enemy continuous fields (Merge, src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs), so a lost frame is healed by the next one and stale/duplicate frames are dropped by the seq gate. Removal is not carried here; it rides the kernel EnemyRemovedEvent (N2) and a guest tombstone set blocks stream resurrection (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs). | OK | 12 | |
| N4 | Trader state (5 s fallback, TraderStateStream) (src/CasualtiesUnknownOnline.Runtime/Session/World/TradeChannel.cs, src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/TraderStateHandler.cs) | host -> guest | TraderState = NetMsg 72, host -> guest, reliable full overwrite, broadcast immediately when the host executes a trader interaction (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs for a host-local action; src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs after a guest TraderAction). Receive: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/TraderStateHandler.cs -> TradeStateSync.OnTraderStateReceived. Wire members: TraderState, TraderStateStream. | 5 s base absolute overwrite to every in-world member (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs; src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs: BaseIntervalMs 5000, MaxIntervalMs 30_000). Reliable: src/CasualtiesUnknownOnline.Runtime/Session/World/TradeChannel.cs uses PacketSender.Send default reliable=true, and the class comment keeps the fallback reliable so it cannot overtake an interaction result (src/CasualtiesUnknownOnline.Runtime/Session/World/TradeChannel.cs). The governor may stretch 5 s to 30 s under pressure (priority 2). CADENCE FINDING: the 5 s base can stretch to 30 s under pressure (todo/sync-cadence-review.md). | No dedicated world-entry send: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs does not include trader state. A member that enters after an interaction broadcast is covered by the same fallback loop over in-world members (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs), i.e. within one cadence (5 s, up to 30 s under pressure). Reconnect uses the same path. | Reliable plus absolute overwrite of every sync field and the stock (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs), so the trader state converges to the host after any missed broadcast. Asymmetry: a lost guest TraderAction is not re-sent, so the host never executes that trader-side change and the next overwrite reverts the guest provisional trader state (the acting side local item/exp stays - accept-first tradeoff). | OK | 10 | |
| N5 | Trader action / recruit / hostile swing (src/CasualtiesUnknownOnline.Runtime/Session/World/TradeChannel.cs, src/CasualtiesUnknownOnline.GameAdapter/World/TradeExecutor.cs, src/CasualtiesUnknownOnline.GameAdapter/World/TraderRecruitCoordinator.cs, src/CasualtiesUnknownOnline.GameAdapter/World/TraderSwingSync.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/Trader*Handler.cs) | TraderAction guest -> host; TraderRecruitRequest guest -> host; TraderRecruitResult host -> target; TraderSwing both (guest -> host report, host -> guest relay) | TraderAction = NetMsg 73, guest -> host, reliable: sent at the game method report point (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs); the host executes the trader-side change and broadcasts TraderState (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs). TraderRecruitRequest = NetMsg 107 (src/CasualtiesUnknownOnline.GameAdapter/World/TraderRecruitCoordinator.cs); TraderRecruitResult = NetMsg 108, reliable, directed to the revived player (src/CasualtiesUnknownOnline.GameAdapter/World/TraderRecruitCoordinator.cs). TraderSwing = NetMsg 115, reliable one-shot (src/CasualtiesUnknownOnline.GameAdapter/World/TraderSwingSync.cs; host fan-out src/CasualtiesUnknownOnline.Runtime/Session/World/TradeChannel.cs), replayed at src/CasualtiesUnknownOnline.GameAdapter/World/TraderSwingSync.cs. Wire members: TraderAction, TraderRecruitRequest, TraderRecruitResult, TraderSwing. | TraderAction: the trader-side state is healed by the N4 TraderState 5 s absolute fallback (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs). TraderRecruitResult: none - a reliable one-shot whose revived state is persisted host-side (src/CasualtiesUnknownOnline.GameAdapter/World/TraderRecruitCoordinator.cs) and owned/restored by the character-data row. TraderSwing: none, and the loss is explicitly accepted (src/CasualtiesUnknownOnline.Runtime/Protocol/Messages/TraderSwingMsg.cs). | Recruit: the host saves the revived character snapshot (src/CasualtiesUnknownOnline.GameAdapter/World/TraderRecruitCoordinator.cs), which the character-data row owns and hands back on world re-entry/reconnect. TraderAction: no replay - the N4 overwrite is the only convergence. TraderSwing: no replay by design (presentation-only). | All three use reliable sends by default. Swing loss is explicitly accepted as presentation degradation (src/CasualtiesUnknownOnline.Runtime/Protocol/Messages/TraderSwingMsg.cs). A lost TraderAction report is not re-issued; the N4 overwrite converges the trader to the host but silently reverts the guest provisional change. A lost recruit result is recoverable only through the character-data restore path, not by a periodic recruit re-send. | OK | 10 | |
| N6 | Chat (src/CasualtiesUnknownOnline.Runtime/Session/World/ChatChannel.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/ChatHandler.cs) | both (guest -> host report, host -> guest relay; never guest -> guest) | Chat = NetMsg 109, bidirectional, reliable: ChatService.TrySend adds the line locally and sends (src/CasualtiesUnknownOnline.Runtime/Session/Chat/ChatService.cs guest -> host); the host validates and relays except the author (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/ChatHandler.cs -> src/CasualtiesUnknownOnline.Runtime/Session/World/ChatChannel.cs); the guest report path is src/CasualtiesUnknownOnline.Runtime/Session/World/ChatChannel.cs. Wire members: Chat. | none - the only send call sites are src/CasualtiesUnknownOnline.Runtime/Session/World/ChatChannel.cs (guest report) and src/CasualtiesUnknownOnline.Runtime/Session/World/ChatChannel.cs (host relay); no periodic re-send exists. | none - the world-entry snapshot group (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs) contains no chat, and the ChatService 50-line buffer is local-only and cleared on session end (src/CasualtiesUnknownOnline.Runtime/Session/Chat/ChatService.cs); the chat selfcheck records no cross-session history. | Reliable transport (PacketSender.Send default reliable=true), but a line that is never delivered is not recovered by anything: no snapshot, no history backfill, no re-request. A disconnect/reconnect loses the line; the host relay also excludes the author (the author UI already showed it). ACCEPTED (audit adjudication): one-shot user message; reliable transport; no authoritative state; chat history backfill is a separate feature, not part of the sync contract. | Transient-by-design | 7 | |
| N7 | Speech bubbles (SpeechMsg) (src/CasualtiesUnknownOnline.Runtime/Session/World/SpeechChannel.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SpeechHandler.cs, src/CasualtiesUnknownOnline.GameAdapter/World/SpeechSync.cs) | both (player bubble: guest -> host report, host -> guest relay; trader bubble: host -> all) | SpeechMsg = NetMsg 74, bidirectional, reliable: SpeechSync.OnSpeechReported sends at the game talk path (src/CasualtiesUnknownOnline.GameAdapter/World/SpeechSync.cs guest report; src/CasualtiesUnknownOnline.GameAdapter/World/SpeechSync.cs host trader bubble); src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SpeechHandler.cs relays except the source; the report path is src/CasualtiesUnknownOnline.Runtime/Session/World/SpeechChannel.cs. The receiver writes the final string into the clone/trader talker (src/CasualtiesUnknownOnline.GameAdapter/World/SpeechSync.cs). Wire members: SpeechMsg. | none - the only send call sites are src/CasualtiesUnknownOnline.Runtime/Session/World/SpeechChannel.cs (report), src/CasualtiesUnknownOnline.Runtime/Session/World/SpeechChannel.cs (host all) and src/CasualtiesUnknownOnline.Runtime/Session/World/SpeechChannel.cs (host except source); no periodic re-send exists. | none - no world-entry snapshot and no history; the bubble is typed out and cleared locally and the text is never re-derived. | Reliable transport (PacketSender.Send default reliable=true), but a bubble that is never delivered is gone: no snapshot, no backfill, and no accepted-loss comment in SpeechMsg. Loss is only masked by transport reliability. ACCEPTED (audit adjudication): presentation bubble; no state divergence; no backfill by design. | Transient-by-design | 8 | |
| N8 | Mod API (ModMessage, ModCommandRequest, ModCommandResult) (src/CasualtiesUnknownOnline.Runtime/Session/Mods/*, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/ModMessageHandler.cs, ModCommandRequestHandler.cs, ModCommandResultHandler.cs, ModRateLimitPolicy.cs) | ModMessage both (guest -> host report; host -> peer/all); ModCommandRequest guest -> host; ModCommandResult host -> guest | ModMessage = NetMsg 75, reliable: ModChannel.SendToHost (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModChannel.cs), SendToPeer (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModChannel.cs), SendToAll (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModChannel.cs), applied at src/CasualtiesUnknownOnline.Runtime/Session/Handlers/ModMessageHandler.cs. ModCommandRequest = NetMsg 86 (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModCommandService.cs) and ModCommandResult = NetMsg 87 (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModCommandService.cs), applied at src/CasualtiesUnknownOnline.Runtime/Session/Handlers/ModCommandResultHandler.cs. Wire members: ModCommandRequest, ModCommandResult, ModMessage. | none - the send call sites above are the only ones; there is no periodic re-send and no snapshot/replay for any of the three messages. | none - no world-entry replay and no late-join state. A guest command request registers a pending callback (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModCommandService.cs) that only the result frame (or a session-end failure) resolves. | Frames are reliable at the transport (PacketSender.Send default), but the domain drops on purpose: the per-sender token bucket drops over-burst frames with a log and never queues them (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModLifecycle.cs; src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModRateLimitPolicy.cs), so a mod message or command request can be lost without recovery. The API doc records frame reliability only for the transport. ACCEPTED (audit adjudication): the rate-limit drop is the documented policy (ModRateLimitPolicy.cs:7); mod payloads are opaque and loss-tolerant. Finding: a dropped ModCommandRequest leaves the caller's pending callback unresolved until session end -> todo/mod-command-request-timeout.md. | Transient-by-design | 10 | todo/mod-command-request-timeout.md |
| N9 | Location ping (LocationPing) (src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingChannel.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/LocationPingHandler.cs) | both (guest -> host report, host -> guest relay) | LocationPing = NetMsg 124, bidirectional, reliable: LocationPingService.TryPlace sends on the middle-click trigger (src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingService.cs -> src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingChannel.cs guest, src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingChannel.cs host); the host relays except the source (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/LocationPingHandler.cs); the receiver adds the marker locally (src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingService.cs). Wire members: LocationPing. | none - and the absence is deliberate: src/CasualtiesUnknownOnline.Runtime/Protocol/Messages/LocationPingMsg.cs states there is no snapshot fallback. The only send call sites are src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingChannel.cs. | none - the marker is a 5 s local UI object (src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingService.cs) held in a per-player transient buffer and cleared on session end; no world-entry/backfill path. | Reliable transport (PacketSender.Send default), transient UI presentation with no persistent state (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/LocationPingHandler.cs); a lost ping is accepted by design and the marker expires after 5 s anyway. | Transient-by-design | 8 | |
| N10 | Tutorial claw stream (TutorialClawState) (src/CasualtiesUnknownOnline.Runtime/Session/Tutorial/TutorialClawService.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/TutorialClawStateHandler.cs, src/CasualtiesUnknownOnline.GameAdapter/Tutorial/TutorialClawSync.cs) | host -> guest | none at a discrete trigger - the only mechanism is the continuous stream: the adapter publishes the current claw state every frame (src/CasualtiesUnknownOnline.GameAdapter/Tutorial/TutorialClawSync.cs -> src/CasualtiesUnknownOnline.Runtime/Session/Tutorial/TutorialClawService.cs) and the service broadcasts it on the adaptive cadence (src/CasualtiesUnknownOnline.Runtime/Session/Tutorial/TutorialClawService.cs). There is no dedicated one-shot for a claw/grab/material edge. Wire members: TutorialClawBroadcast, TutorialClawState. | 20 Hz base (StateStreamOptions.DefaultStateStreamHz = 20; AdaptiveStreamId.TutorialClawBroadcast, priority 2, MinHz 1), unreliable, absolute overwrite with a seq gate (src/CasualtiesUnknownOnline.Runtime/Session/Tutorial/TutorialClawService.cs reliable:false;). The governor may stretch 20 Hz to 3 Hz under Critical pressure (factor 0.15). | none explicit - a joiner/reconnect waits for the next stream frame (at most 50 ms at base, about 333 ms at the 3 Hz floor); there is no world-entry claw snapshot and the host publishes only while TutorialHandler.main exists (src/CasualtiesUnknownOnline.GameAdapter/Tutorial/TutorialClawSync.cs). | Unreliable absolute overwrite: a lost frame is healed by the next one and stale/duplicate frames are dropped by the seq gate (src/CasualtiesUnknownOnline.Runtime/Session/Tutorial/TutorialClawService.cs). No history is needed; course/prop state remains per-side by design. | OK | 9 | |
| R1 | Run start / advance layer kernel facts (src/CasualtiesUnknownOnline.GameState/Domains/World/*, Runtime/Session/World/WorldRunStateMapper.cs, Runtime/Session/World/WorldRunProjection.cs, GameAdapter/Run/*, GameAdapter/WorldGen/*) | Host -> guests only. The host is the sole author of the run baseline (AuthorityKind.HostOnly); guests never send run commands and receive RunStartedEvent/RunAdvancedEvent inside a CommittedBatchEnvelope over NetMsg.KernelEnvelope. | Dedicated kernel events, not a snapshot. Real trigger 1 (run start) is the host click moment: src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs -> WorldParamsService.CaptureAtEntry (src/CasualtiesUnknownOnline.GameAdapter/World/WorldParamsService.cs) -> IWorldControl.PublishWorldParams -> CommitRunBaseline (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs) -> ItemKernelAuthority.TryStartRun (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs). Real trigger 2 (layer advance) is the GenerateWorld boundary: src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs -> WorldParamsService.CaptureAtBoundary -> the same PublishWorldParams -> TryAdvanceLayer (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs). Commit raises BatchCommitted and KernelProtocolService.BroadcastCommittedBatch (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs) fans out the envelope. Numeric ids: WirePayloadType.RunStartedEvent = 105, RunAdvancedEvent = 106 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs); WireEventKind.RunStarted/RunAdvanced (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelWireMapper.cs). Reliability: — reliable by default (PacketSender default true, src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs). Guest apply: HandleCommittedBatch -> ApplyBatchAndDrain -> _authority.Apply -> WorldService.RunBatchProjection -> ApplyRunProjection. Wire members: AdvanceLayer, RunAdvanced, RunStart, RunStarted. | Absolute kernel checkpoint, idempotent, no adaptive governor. The kernel checkpoint carries RunState and is an absolute overwrite of the guest kernel. It is not on its own timer but rides the host's 60 s world-resend cycle. It is reliable (PacketSender default reliable=true) and is NOT an AdaptiveStreamId, so the governor cannot stretch it. The 'run' projection is also rebuildable locally. | World entry / late join / reconnect: WorldEntryFanout.Send called from the InWorld edge (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs) and from a reconnect-while-InWorld handshake (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs). Mid-generation join: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs (kernel checkpoint carries the run baseline). Missed-range recovery: a revision gap buffers the future batch and requests the missing range; the host replays the journal range or falls back to a fresh checkpoint (HandleRangeRequest). Local projection failure: ProjectionHealthCoordinator rebuild. | Reliable channel in both directions; loss is not accepted but is tolerated by design at the envelope level: a duplicate batch is idempotent by OperationId, a gap is repaired by range request or a fresh checkpoint, and a checkpoint is an absolute overwrite, so convergence to host authority is guaranteed (no unhealed run/layer divergence). | OK | 18 | |
| R2 | World time (WorldTimeRequest, WorldTime, 5 s) (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldTimeChannel.cs, Handlers/WorldTimeHandler.cs, WorldTimeRequestHandler.cs, Session/World/WorldTimeLocalInitiation.cs, GameAdapter/World/WorldTimeSync.cs) | Both, star shaped: guest -> host WorldTimeRequest (NetMsg id 90) reports a speed the guest has ALREADY applied locally; host -> guest WorldTime (NetMsg id 91) carries the authoritative speed to every member — the change, the world-entry fan-out, the 5 s resend, and the answer that settles the initiator's pending intent. | Dedicated messages at the real triggers, never a state snapshot. Host -> guest: the arbitration verdict is broadcast on a real change, and EVERY request — accepted or refused — is answered with the authoritative speed (src/CasualtiesUnknownOnline.GameAdapter/World/WorldTimeSync.cs, Answer), so an accepted-but-unchanged request still settles the initiator; also from a local SetTimeScale change (OnLocalTimeScaleChanged), a direct Time.timeScale write adopted by the host, the world-entry fan-out and the 5 s resend. Guest -> host: a manual speed (hotkey, or the native left/right movement reset) writes the guest's own clock FIRST and is reported at the game's SetTimeScale hook. Numeric ids: src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs. Both are reliable (PacketSender default reliable=true; neither call site passes reliable:false). Receive: WorldTimeHandler and WorldTimeRequestHandler. Wire members: WorldTime, WorldTimeRequest. | Yes — an absolute, idempotent 5 s resend. Cadence: the 5 s resend is pumped by the frame loop and sent by the world-time broadcast; an authoritative value that arrives while a local initiation is pending also settles it. Semantics are absolute overwrite of the receiver's applied speed and the resend is explicitly idempotent. The governor cannot stretch it: WorldTime is not an AdaptiveStreamId (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamId.cs) and WorldTimeSync never queries AdaptiveStreamRateService. | World entry / re-entry: a member (re)entering the world gets the current speed immediately instead of waiting up to 5 s (inside OnRemoteSceneChanged). No dedicated snapshot exists; the 5 s resend plus the re-entry broadcast are the recovery. The guest enforces the last host speed locally if a direct write moved Time.timeScale (EnforceAppliedSpeed), suspended while a local initiation is in flight so the initiator's own lead window is not fought. | Reliable channel both ways (PacketSender default true). Loss is tolerated only in the sense that a dropped broadcast is harmless: the next 5 s resend is an absolute overwrite, so convergence is bounded by 5 s — and a pending local initiation settles on it too. No additive semantics anywhere (the sleep branch clears the standing request, WorldTimePolicy.Decide). | OK | 12 |  |
| R3 | World join/ready + late-join orchestration (WorldJoin, WorldReady, SceneState, PlayerJoin, PlayerLeave) (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs, SessionService.cs, Handlers/WorldJoinHandler.cs, WorldReadyHandler.cs, SceneStateHandler.cs, PlayerJoinHandler.cs, PlayerLeaveHandler.cs) | Both. Host -> guest: WorldJoin (20), WorldReady (21), PlayerJoin (32), PlayerLeave (33), WorldSnapshotComplete (110); guest -> host: SceneState (18) report, which the host relays to the other guests; host -> guest: SceneState broadcast. | Dedicated messages at the real triggers, all reliable (PacketSender default reliable=true). WorldJoin is sent at the host click moment (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs) and re-sent at host world entry to members not yet in world; WorldReady is sent on gate release or as a targeted late-joiner pass. SceneState is reported only on the local InWorld/InMenu edge and on session activation (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs); these are the only two call sites in the tree. PlayerJoin self-activation is sent once per member sync start plus a one-time roster backfill. Numeric ids: src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs. Wire members: PlayerJoin, PlayerLeave, SceneState, WorldJoin, WorldReady. | none | No periodic re-send of any orchestration message exists: SceneState is emitted only at the two edge call sites (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs for the host pump and src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs for the session-activation re-report) and PlayerJoin only at src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs and src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs; no timer, cadence constant or adaptive stream exists for WorldJoin/WorldReady/SceneState/PlayerJoin. The only time-driven things on this path are the host start-gate force-release (30 s, src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStartGate.cs) and the guest's 60 s give-up (src/CasualtiesUnknownOnline.GameAdapter/Run/StartGateCoordinator.cs), both of which abandon rather than heal. Backfill of the snapshot group exists but only on a NEW InWorld edge or handshake: SceneStateHandler fires WorldEntryFanout on the InWorld edge and HandshakeHandler fires it on a reconnect-while-InWorld, ending with the explicit group marker. Late join/start gate: WorldService.NotifyMemberInWorld sends a targeted WorldReady to a member entering after release. Missed-range recovery for the kernel part rides the kernel checkpoint inside the same fan-out (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs). A swallowed SceneState report or PlayerJoin while the peer stays connected is not re-driven: the host never marks the member InWorld (SceneStateHandler) and no code re-reports it, so the entity stream never starts and the guest is dropped as an unknown member (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs). | Reliable channel (PacketSender default true), but a send failure is ignored on these paths (Send returns void) and there is no retry/periodic re-report, so a swallowed orchestration event is only healed by a new handshake or a leave/re-enter edge. Guest-side safety valve: 60 s then back to the menu (src/CasualtiesUnknownOnline.GameAdapter/Run/StartGateCoordinator.cs). Convergence guarantee: not for a single lost message; no absolute re-report of member scene state exists. | Event-only gap | 17 | todo/session-control-convergence.md |
| R4 | Session lifecycle/control (Handshake, HandshakeAck, HandshakeAckAck, Ping, Pong, Kicked, Banned) (src/CasualtiesUnknownOnline.Runtime/Session/SessionPeerMaintenance.cs, SessionService.cs, Handlers/HandshakeHandler.cs, HandshakeAckHandler.cs, HandshakeAckAckHandler.cs, HostKickService.cs, HostBanService.cs) | Both. Guest -> host: Handshake (16), HandshakeAckAck (58), Ping (1), Pong (2); host -> guest: HandshakeAck (17), Pong, Kicked (111), Banned (112); Ping/Pong are bidirectional. | Dedicated control messages at real triggers, reliable (PacketSender default true). Guest handshake send/retry: (kicked at KickHandshake). Host acks every handshake, including repeats. Guest answers with the third leg. The host only marks the member confirmed on that leg. Ping/Pong: SessionService.RequestPing on the 5 s pump; PingHandler echoes, PongHandler records RTT. Kick/Ban: src/CasualtiesUnknownOnline.Runtime/Session/HostKickService.cs and src/CasualtiesUnknownOnline.Runtime/Session/HostBanService.cs send the dedicated message reliably before removing the member. Numeric ids: src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs. Wire members: Banned, Handshake, HandshakeAck, HandshakeAckAck, Kicked, Ping, Pong. | Partial. The Handshake leg has a 1 Hz idempotent retry (RetryHandshakeIfNeeded, send), and the host re-acks every repeat, so a lost Handshake or HandshakeAck heals. But the retry is gated on the session not being active. The guest flips SessionActive on receiving the ack, so once the ack arrives the guest stops retrying and a lost HandshakeAckAck has no periodic re-send. Ping/Pong runs at 5 s but carries no handshake state and does not re-drive it. Kicked/Banned are one-shot reliable with no fallback; the host's presence removal is authoritative and the guest notices only by leaving the lobby. | Reconnect: a guest that re-enters the lobby calls KickHandshake again, and the host reuses the presence and re-acks (HandshakeHandler). Host warm-up: un-handshaken lobby peers receive a Ping every 1 s so the lazy Steam P2P session establishes (src/CasualtiesUnknownOnline.Runtime/Session/SessionPeerMaintenance.cs, send). Presence reconciliation every 2 s removes vanished members / ends the session when the host is gone (CheckPeerPresence). No recovery exists for a lost HandshakeAckAck while both sides stay connected: the host keeps the member un-handshaken, so the start gate excludes it (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStartGate.cs) and the WorldReady fan-out skips it (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStartGate.cs). | Reliable channel (PacketSender default true; Kick/Ban pass reliable: true explicitly). Handshake and HandshakeAck loss is accepted and healed by the 1 Hz retry; HandshakeAckAck loss is NOT healed — the host's member stays Handshaken=false for the rest of the connection, which is a convergence gap (the guest believes it is connected). Kicked/Banned loss leaves the guest as a zombie until it leaves the lobby; the host side has already removed it. | Event-only gap | 16 | todo/session-control-convergence.md |
| R5 | Adaptive rate governor (all 11 AdaptiveStreamId profiles): cadence-only adaptation, never removes a fallback, terminal/full-state reconciliation per stream (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/*) | Host-side (and guest-side for the two report streams) cadence decision only; the governor itself sends nothing. It answers 'what interval should this stream use now?' for 11 registered stream ids. | Not applicable in the event sense: the governor has no trigger and no message of its own; it is a pure query surface (GetSendIntervalMs). Its only state is a per-peer log-change cache. The 11 profiles are declared once in (PlayerStateBroadcast, PlayerStateReport, EnemyStateBroadcast, TutorialClawBroadcast, MedicalInjectionReport, ShrapnelPositionReport, WorldItemMoveStream, WorldItemSnapshotStream, FluidRegionDiffStream, FluidRegionFullStream, TraderStateStream). | Cadence-only by construction; no fallback can be removed. Reliable control streams bypass adaptation entirely. Hz streams are floored at the profile MinHz=1 (MinHz declared in src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs), and interval-based fallbacks are hard-capped at the profile MaxIntervalMs (— e.g. WorldItemSnapshotStream BaseIntervalMs 5000 / MaxIntervalMs 30_000, src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs). So pressure can stretch a fallback (5 s -> up to 30 s for the item/trader keyframes) but never delete it. Every stream keeps a terminal/full-state reconciliation: item 5 s full-table keyframe, fluid 1 Hz full viewport, trader 5 s absolute snapshot, medical cumulative deltas ride the reliable channel with the terminal EndRequest total. | Per-stream, not in the governor: the fallback stream is the recovery (item/fluid/trader/enemy/tutorial full-state), plus the world-entry snapshot group. The governor's Reset clears per-peer rate caches on a session/traffic reset (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamRateService.cs). | The governor only adapts streams whose delivery mode tolerates loss (LatestWins absolute-overwrite or Cumulative with a terminal reconciliation); reliable control paths are never throttled. Accepted loss is explicit in src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamDeliveryMode.cs. Convergence guarantee comes from the per-stream fallback, not from the governor. | OK | 11 | |
| R6 | Cross-check: projection rebuild / health recovery (src/CasualtiesUnknownOnline.Runtime/Session/ProjectionHealth/*, Runtime/Session/World/WorldRunProjection.cs, docs/architecture/projection-framework.md) | Local (per client). Not a network surface: it rebuilds a local read model from the local kernel when a projection throws. It does not send or apply remote state. | Trigger is the kernel commit/apply event, not a network message: the domain projection is invoked under health tracking, e.g. the run domain under health tracking. A thrown projection is captured and the domain marked dirty (MarkFailure). | Main-thread pump, not a timer: driven by the service Update loop. Dirty domains are rebuilt from the kernel read model (Rebuild, success clears dirty). It is a local recovery mechanism, not a network fallback, so the adaptive governor does not apply. | Seven production domains are registered: run (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs), world-entities (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntityKernelProjection.cs), fluids (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidKernelReadProjection.cs), players-carry (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerKernelCarryProjection.cs), items (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemService.cs), remote-character-presentation (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/RemoteCharacterPresentationStore.cs), mod-status (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModStatusProjectionReadModel.cs). Rebuild callbacks read the kernel read model, e.g. the run domain under health tracking. This heals a broken local projection only; a lost network batch is healed by R1's checkpoint/range-request path. | No channel of its own; loss is not applicable. The guarantee is local convergence: a failed projection never rolls back the kernel commit, and the domain stays dirty until a rebuild succeeds (repeated failures escalate to Degraded, src/CasualtiesUnknownOnline.Runtime/Session/ProjectionHealth/ProjectionHealthCoordinator.cs). Not a substitute for a network fallback. | OK | 13 | |
| R7 | Cross-check: world fingerprint divergence detector (GameAdapter/Run/RunCoordinator.cs (grep WorldFingerprint), docs/backlog/review/world-determinism-world-fingerprint.md) | Local diagnostic only; nothing is sent or received. The fingerprint is written to the local log for manual peer-log comparison. | One-shot diagnostic at the world-entry edge, not a sync message. Guarded by a one-shot latch: -> -> LogWorldFingerprint. The only output is a log line (format string). | none | No periodic re-fingerprint, no comparison against the peer's value, no alert and no repair path exists; the only re-arm is the session-end reset (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs), i.e. the next world entry logs once again. Closest evidence of the absence: the single emit call site is LogWorldFingerprint's log statement (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs) and the only callers are the one-shot guard. Divergence is only observable by manually diffing the two peers' log lines, and the runtime never acts on it. The block-state 60 s absolute resend (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs) can mask block-cell differences but is not a fingerprint comparison. | Not applicable (no channel). The fingerprint is a best-effort diagnostic: it is emitted once per world entry and never re-sampled, so a divergence introduced after world entry is never detected by it. ADJUDICATED: diagnostic only, not a sync path; one-shot at world entry, manual peer-log comparison during final acceptance. Recorded in docs/backlog/review/world-determinism-world-fingerprint.md. | Transient-by-design | 8 | review/world-determinism-world-fingerprint.md (updated) |
| R8 | Cross-check: network traffic baseline (cost of fallbacks) (src/CasualtiesUnknownOnline.Runtime/Session/NetworkTraffic/*, docs/backlog/review/network-traffic-baseline.md) | Local observability only. PacketSender/PacketReceiver report raw send/receive facts; SessionService reports the ping/pong probe results. No payload is produced or applied for gameplay state. | Not a sync surface: the monitor observes the existing messages. Send facts are recorded per peer/message with the classified WirePayloadType, receive facts by payload type. Peer health is fed by the 5 s ping/pong probe (SessionService.RequestPing -> RecordPingSent/RecordPong). | The monitor's own cadence is the rolling window roll + periodic log, plus the per-peer health line. It is an observability cadence, not a state fallback; it neither sends nor heals gameplay state, so the adaptive governor does not apply. Its health snapshot is an input to the governor. | Not applicable — no state is synced. The probe loss accounting is the recovery-adjacent part: an unanswered probe is counted lost when the next probe goes out, which lets the adaptive layer lower cadence instead of dropping a fallback. | No channel of its own. The counters are observability-only (src/CasualtiesUnknownOnline.Runtime/Session/NetworkTraffic/NetworkTrafficMonitor.cs); the per-session reset clears them (Reset). The cost of fallbacks is measured per WirePayloadType, which is what this cross-check row exists for. | OK | 7 | |

## Wire vocabulary index (member -> owning matrix row)

Every wire/stream discriminator is listed here; SyncCoverageGateTests fails when a
member is missing, duplicated, or points at a row whose verdict is not in the vocabulary.

| Member | Kind | Row |
|---|---|---|
| Ping | NetMsg | R4 |
| Pong | NetMsg | R4 |
| Handshake | NetMsg | R4 |
| HandshakeAck | NetMsg | R4 |
| HandshakeAckAck | NetMsg | R4 |
| SceneState | NetMsg | R3 |
| WorldJoin | NetMsg | R3 |
| WorldReady | NetMsg | R3 |
| PlayerJoin | NetMsg | R3 |
| PlayerLeave | NetMsg | R3 |
| CharacterData | NetMsg | P3 |
| HostCharacterData | NetMsg | P3 |
| BlockDamaged | NetMsg | W1 |
| WorldBlockState | NetMsg | W1 |
| BlockPlaced | NetMsg | W1 |
| BuildingEntityDamaged | NetMsg | E1 |
| BuildingEntityOpened | NetMsg | E1 |
| EarthquakeStart | NetMsg | W3 |
| KeypadCode | NetMsg | W4 |
| ItemIdWatermark | NetMsg | I8 |
| CarriedInventory | NetMsg | I8 |
| EntityEvent | NetMsg | E2 |
| EntitySpawned | NetMsg | E3 |
| RuntimeEntitySnapshot | NetMsg | E3 |
| RuntimeEntityRejected | NetMsg | E3 |
| GeyserStateSnapshot | NetMsg | W5 |
| FluidRegion | NetMsg | F1 |
| FluidInteraction | NetMsg | F2 |
| TraderState | NetMsg | N4 |
| TraderAction | NetMsg | N5 |
| SpeechMsg | NetMsg | N7 |
| ModMessage | NetMsg | N8 |
| CraftReport | NetMsg | I6 |
| RecipeUnlock | NetMsg | I6 |
| TrapLayoutSnapshot | NetMsg | W6 |
| EnemySnapshot | NetMsg | N1 |
| EnemyAttack | NetMsg | N1 |
| ModCommandRequest | NetMsg | N8 |
| ModCommandResult | NetMsg | N8 |
| BlockDamageSnapshot | NetMsg | W2 |
| BlockDamageReport | NetMsg | W2 |
| WorldTimeRequest | NetMsg | R2 |
| WorldTime | NetMsg | R2 |
| LimbStateEvent | NetMsg | P4 |
| CharacterSound | NetMsg | P5 |
| FluidPresentation | NetMsg | F3 |
| PlayerInventoryTakeRequest | NetMsg | P9 |
| PlayerCarryStartRequest | NetMsg | P9 |
| PlayerCarryStopRequest | NetMsg | P9 |
| PlayerHealRequest | NetMsg | P9 |
| TutorialClawState | NetMsg | N10 |
| DynamiteExplosion | NetMsg | E4 |
| RadiationLineState | NetMsg | E5 |
| TraderRecruitRequest | NetMsg | N5 |
| TraderRecruitResult | NetMsg | N5 |
| Chat | NetMsg | N6 |
| WorldSnapshotComplete | NetMsg | W7 |
| Kicked | NetMsg | R4 |
| Banned | NetMsg | R4 |
| CharacterAttackAnim | NetMsg | P5 |
| CharacterLandingVisual | NetMsg | P5 |
| TraderSwing | NetMsg | N5 |
| PlayerItemUseRequest | NetMsg | P9 |
| PlayerPushRequest | NetMsg | P9 |
| PlayerPushResult | NetMsg | P9 |
| CharacterRagdoll | NetMsg | P5 |
| WorldBloodSpawn | NetMsg | E6 |
| KernelEnvelope | NetMsg | K1 |
| PlayerColorUpdate | NetMsg | P12 |
| LocationPing | NetMsg | N9 |
| RemoteInventoryOperationRequest | NetMsg | P10 |
| RemoteInventoryApply | NetMsg | P10 |
| MedicalOperationStartRequest | NetMsg | P11 |
| MedicalOperationStartAck | NetMsg | P11 |
| MedicalOperationTargetCheckRequest | NetMsg | P11 |
| MedicalOperationTargetCheckAnswer | NetMsg | P11 |
| MedicalOperationUpdate | NetMsg | P11 |
| MedicalOperationState | NetMsg | P11 |
| MedicalOperationEndRequest | NetMsg | P11 |
| MedicalOperationEndCommitted | NetMsg | P11 |
| MedicalOperationCancel | NetMsg | P11 |
| ItemSpawn | WireCommandKind | I1 |
| ItemPickup | WireCommandKind | I1 |
| ItemDrop | WireCommandKind | I1 |
| ItemDestroy | WireCommandKind | I1 |
| ItemUpdateState | WireCommandKind | I1 |
| ItemTransfer | WireCommandKind | I1 |
| ItemContainerSync | WireCommandKind | I1 |
| RunStart | WireCommandKind | R1 |
| AdvanceLayer | WireCommandKind | R1 |
| RecordTrapConsumed | WireCommandKind | E7 |
| RecordBuildingEntityHealth | WireCommandKind | E7 |
| RecordOpenedEntity | WireCommandKind | E7 |
| UpdatePlayerStatus | WireCommandKind | P2 |
| UpsertEnemy | WireCommandKind | N2 |
| RemoveEnemy | WireCommandKind | N2 |
| UpdateFluidRegion | WireCommandKind | F4 |
| SetPlayerCarry | WireCommandKind | P6 |
| ClearPlayerCarry | WireCommandKind | P6 |
| RecordEnemyBite | WireCommandKind | N2 |
| RecordEnemyLunge | WireCommandKind | N2 |
| RecordEnemyEffect | WireCommandKind | N2 |
| RecordTrapState | WireCommandKind | E7 |
| RangeRequest | WireCommandKind | K2 |
| CommandRejected | WireCommandKind | K1 |
| ItemSpawned | WireEventKind | I1 |
| ItemRelocated | WireEventKind | I1 |
| ItemDestroyed | WireEventKind | I1 |
| ItemDataUpdated | WireEventKind | I1 |
| RunStarted | WireEventKind | R1 |
| RunAdvanced | WireEventKind | R1 |
| TrapConsumed | WireEventKind | E7 |
| BuildingEntityHealthUpdated | WireEventKind | E7 |
| OpenedEntity | WireEventKind | E7 |
| WorldEntitiesReset | WireEventKind | E7 |
| PlayerStatusUpdated | WireEventKind | P2 |
| EnemyUpserted | WireEventKind | N2 |
| EnemyRemoved | WireEventKind | N2 |
| EnemiesReset | WireEventKind | N2 |
| FluidRegionUpdated | WireEventKind | F4 |
| FluidsReset | WireEventKind | F4 |
| PlayerCarrySet | WireEventKind | P6 |
| PlayerCarryCleared | WireEventKind | P6 |
| PlayerInventoryTransfer | WireEventKind | P7 |
| PlayerHealResult | WireEventKind | P8 |
| PlayerItemUseResult | WireEventKind | P8 |
| EnemyBiteResult | WireEventKind | N2 |
| EnemyLungeResult | WireEventKind | N2 |
| EnemyEffectResult | WireEventKind | N2 |
| TrapStateChanged | WireEventKind | E7 |
| WorldItemsReset | WireEventKind | I1 |
| PlayerStateBroadcast | AdaptiveStreamId | P1 |
| PlayerStateReport | AdaptiveStreamId | P1 |
| EnemyStateBroadcast | AdaptiveStreamId | N3 |
| TutorialClawBroadcast | AdaptiveStreamId | N10 |
| MedicalInjectionReport | AdaptiveStreamId | P11 |
| ShrapnelPositionReport | AdaptiveStreamId | P11 |
| WorldItemMoveStream | AdaptiveStreamId | I2 |
| WorldItemSnapshotStream | AdaptiveStreamId | I3 |
| FluidRegionDiffStream | AdaptiveStreamId | F1 |
| FluidRegionFullStream | AdaptiveStreamId | F1 |
| TraderStateStream | AdaptiveStreamId | N4 |

## Gap list (one ticket per gap; R3+R4 merged)

Closed gaps stay listed with their landing ticket so the list remains the audit's history.

| Gap | Row(s) | Severity | Ticket |
|---|---|---|---|
| Guest partial block damage has no re-report: the authoritative table (the GAME's own list, read at snapshot time) has no entry for a swallowed damage report and the host's absolute snapshot omits the cell, so neither side is corrected until the break (CLOSED 2026-09-18 by `review/guest-partial-block-damage-re-report.md`: the guest keeps each cell's absolute damage, the 60 s fallback re-reports the outstanding set, the host merges per cell — never below what it holds — and answers every reported cell with its authoritative value, so a refused row converges too) | W2 | Medium | `docs/backlog/review/guest-partial-block-damage-re-report.md` |
| The partial-damage absolute re-report and the live additive delta can overlap: the merge is a per-cell maximum, so two never-mutually-visible contributions under-count and a delta landing after the report that covers it over-counts | W2 (residual) | Low-Medium | `docs/backlog/todo/partial-damage-delta-report-overlap.md` |
| Runtime-created `BuildingEntity` spawns have no creation snapshot, no periodic re-report and no late-join backfill (CLOSED 2026-09-09 by `review/runtime-entity-spawn-backfill.md`: the host accepted-creation table + 60 s absolute re-broadcast + the guest pending re-report + the animal acknowledgement key list, with the round-3 defects fixed — token-keyed creation identity, geyser report/death key consistency, rejection of an unmaterializable report (decision 161: neither recorded nor relayed, answered to the reporter, whose pending entry and local copy both end there), and mod-animal late-join materialization) | E3 | Medium-High | `docs/backlog/review/runtime-entity-spawn-backfill.md` |
| `TrapLayoutSnapshot` was world-entry/InWorld-edge only; while a member stays continuously in the world there was no in-session repair (CLOSED 2026-09-18 by `review/trap-layout-snapshot-recovery.md`: the host's 60 s in-session repair re-derives the table from the live scene and re-sends it to every in-world member, so a swallowed snapshot converges without an InWorld edge; the entry fanout's residual staleness is recorded in row W6) | W6 | Medium | `docs/backlog/review/trap-layout-snapshot-recovery.md` |
| The world-entry trap-layout fanout sends the table as last derived, so a member entering between two repairs can materialize an entity the host has since removed (transient: the next repair makes the guest destroy the surplus) | W6 (residual) | Low-Medium | `docs/backlog/todo/trap-layout-entry-snapshot-staleness.md` |
| `EnemyAttack` has no re-issue path (CLOSED 2026-09-18 by `review/enemy-hit-determination-local.md`: the attack is announced and judged on the client it lands on, so a lost announcement costs exactly one attack and NO state diverges — there is no host verdict left to lose, and the next attack is announced normally) | N1 | Low-Medium | `docs/backlog/review/enemy-hit-determination-local.md` |
| A swallowed guest pickup/drop command leaves the guest's local in-flight result un-reconciled by the item keyframe; the empty-host-table case skips the keyframe entirely | I5, I1 (caveat) | Medium | `docs/backlog/todo/guest-command-loss-reconciliation.md` |
| `RecipeUnlock` has no periodic fallback and no world-entry/checkpoint member; late joiners never learn a prior unlock | I6 | Medium | `docs/backlog/todo/recipe-unlock-fallback.md` |
| `CarriedInventory` registration is one-shot; a swallowed report leaves the host's arbitration transfer table empty for those ids | I8 | Medium | `docs/backlog/todo/carried-inventory-registration-re-report.md` |
| Lost session control/readiness messages have no re-report: `HandshakeAckAck`, `SceneState`, `PlayerJoin`; `WorldSnapshotComplete` is inert | R3, R4 | Medium | `docs/backlog/todo/session-control-convergence.md` |
| A late checkpoint chunk set from a previous run is not validated against the current run before `Restore` | K2 (finding) | Low-Medium | `docs/backlog/todo/checkpoint-run-epoch-validation.md` |
| A `ModCommandRequest` dropped by the rate limiter leaves the caller's pending callback unresolved until session end | N8 (finding) | Low | `docs/backlog/todo/mod-command-request-timeout.md` |
| Fallback stretch caps and first-resend latency need a measured decision (item keyframe 5 s→30 s, trader 5 s→30 s, fluid full 1 s→10 s, block/damage/keypad/geyser 60 s first resend) | I3, N4, W1 | Medium | `docs/backlog/todo/sync-cadence-review.md` |

Already-owned findings cross-referenced, not duplicated:

- `KernelSaveFileStore` is unit-tested but has no production caller (the authoritative
  checkpoint is memory-only today) — owned by `docs/backlog/review/save-format-and-world-repository.md`
  (S1) and `docs/backlog/review/save-layer-end-save-and-restore.md` (S2).
- `ResetFluidsCommand` and `ResetEnemiesCommand` are host-local and carry no wire form — closed by
  the layer-boundary reset family (decision 174): `LayerScopedTableReset` issues
  both at the world-entry seam, so the commands have a real caller and no peer can reach them. The
  stale chunks INSIDE a layer are still cleared by the zero-total upserts from `FluidKernelProjection`,
  which is a separate mechanism from the boundary reset.
- `WorldSnapshotComplete` is inert — the marker is not recovery evidence; folded into
  `docs/backlog/todo/session-control-convergence.md`.
- Rows W3 (EarthquakeStart) and E4 (DynamiteExplosion) are one-shot events with no fallback of
  their own; they stay `OK` because the durable consequences are explicitly healed by the
  block/building/item fallbacks named in their rows. Their rows state that dependency so the
  verdict is not read as "the event itself is recoverable".

## Accepted losses (recorded reasons)

| Row | Surface | Reason |
|---|---|---|
| N6 | Chat lines | One-shot user message; reliable transport; no authoritative state; history backfill is a separate feature, not part of the sync contract |
| N7 | Speech bubbles | Presentation bubble; no state divergence; no backfill by design |
| N8 | Mod messages / commands / mod status | The rate-limit drop is the documented policy (`src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModRateLimitPolicy.cs`); mod payloads are opaque and loss-tolerant. The pending-callback side effect is tracked in `todo/mod-command-request-timeout.md` |
| N9 | Location ping | Transient UI marker; one-shot relay is the whole feature |
| F3 | Fluid presentation (water push / waterflow sound) | Reliable one-shot presentation; the region stream is the state path |
| I9 | Item presentation transients (fresh-drop motion, impact sounds) | Non-authoritative guest presentation is deliberately suppressed; the authoritative item state rides I1/I2/I3 |
| P5 | One-shot character presentation (sound / attack anim / landing / ragdoll) | Reliable one-shot presentation; the 1 Hz character snapshot is the state fallback |
| E6 | World blood decal | 120 s transient decal, explicitly "no periodic snapshot" (`src/CasualtiesUnknownOnline.Runtime/Protocol/Messages/WorldBloodSpawnMsg.cs`) |
| R7 | World fingerprint | Diagnostic only, not a sync path; one-shot at world entry, compared manually across peer logs during final acceptance |

## Cadence findings (sync durations that look unreasonable)

1. **Item keyframe 5 s → 30 s.** `src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs`; the keyframe is the only
   absolute heal for top-level item state and for phantom-item removal. 30 s is
   user-visible. Proposal: cap ≈10 s or exempt the keyframe.
2. **Trader fallback 5 s → 30 s.** `src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs`; interactions
   broadcast immediately, so this only heals a swallowed event. Proposal: cap ≈15 s. The
   `src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs` comment was corrected in this cycle (reliable, adaptive cadence), so the
   cadence ticket no longer needs to fix it.
3. **Fluid full-viewport reconciliation 1 s → 10 s.** `src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs`.
   Record the measured worst-case stale cell after an anchor move.
4. **Block / damage / keypad / geyser first resend is 60 s.**
   `src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs`; the documented lazy-P2P swallow window is up to ~30 s, so a
   mutation in that window can stay invisible for up to 60 s. Proposal: one early resend at
   the InWorld edge (5–10 s), then 60 s steady-state.
5. **`EnemySnapshot` has no resend** (row N1) and the **handshake retry stops on
   `SessionActive`** (row R4) — both tracked in their own tickets.

## Doc corrections landed in this cycle

| Doc | Correction |
|---|---|
| `docs/architecture/protocol.md` | The validator does reject multi-envelope frames (`src/CasualtiesUnknownOnline.Protocol/Wire/ProtocolFrameValidator.cs`); the `IsSupportedFrame` reference is stale (the check is `ProtocolFrameValidator.TryValidate` at `src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs`); the save section now says `KernelSaveFileStore` has no production caller today |
| `docs/architecture/glossary.md` | The "all old-epoch stream packets are rejected" claim is now true (stale-epoch state streams are dropped at `KernelProtocolService.HandleStateStream`) |
| `docs/architecture/current.md` | `ItemPendingPickupArbiter` does not exist; the mechanism is `PendingItemCreations` (the ordering rule) plus `KernelProtocolCommandHandler`'s immediate refusal — the old `PendingPickupQueue` hold window is deleted |
| `docs/architecture/domains.md` | Fluids: the kernel facts are a coarse periodic aggregation, not a grid rebuild source; `ResetFluidsCommand` is host-local (no wire form) and runs at the layer boundary |
| `docs/features/entities.md` | Trader state is not sent on world entry and the fallback is reliable + adaptive; geyser liquid type rides `GeyserStateSnapshot`, not the event's `Extra` |
| `docs/features/items.md` | The world-item keyframe is adaptive (5 s base, up to 30 s under pressure), not a fixed 5 s |
| `src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs` | Trader-state comment: reliable fallback, adaptive cadence |
| `docs/evidence/selfchecks/architecture/phase-e-legacy-inventory-selfcheck.md` | Direct `NetMsg` classification completed (crafting, color, ping, remote-inventory apply, medical-operation sessions) and pointed at this matrix as the source of truth |
| `docs/backlog/review/world-determinism-world-fingerprint.md` | Expanded with the audit evidence: the fingerprint is one-shot and local; peer comparison is a manual final-acceptance step |

## Stale historical evidence (not rewritten; do not cite as current)

These historical self-checks describe wire shapes that were removed by the typed-kernel
migration. They are kept as dated evidence snapshots, not as current claims: `NetMsg` 88
(`BuildingEntityHealthSnapshot`), `TrapStateSnapshot` / `OpenedEntitiesSnapshot`, `NetMsg`
95 (`ItemContainerContentMsg`), `NetMsg` 63 (`ItemCarriedSync`), `HandlerContext.SendWorldStateToMember`,
`ItemPendingPickupArbiter`, and `ToSnapshotEntryMsg`. The active docs above were corrected;
rewriting the historical records is out of scope for this audit.

## Cross-checks

- **Adaptive governor (row R5):** `AdaptiveStreamRateService` only answers "what cadence";
  every stream owner still sends the same payload, so the governor can stretch a fallback but
  cannot remove it. The stretch caps are the cadence findings above.
- **World fingerprint (row R7):** a diagnostic, not a divergence repair path; manual peer-log
  comparison remains the final-acceptance step.
- **Network traffic baseline:** the fallback cost is measured by
  `docs/backlog/review/network-traffic-baseline.md`; the cadence ticket must not regress it.
- **Projection health (row R6):** the per-domain dirty/rebuild loop is the recovery path for
  a failed apply; it does not correct authority.
- **Run-baseline rarity multipliers (decision 169, S3.4a):** the kernel run baseline now carries
  the game's two accumulated loot/trap multipliers, because they are world-generation INPUTS — a
  side that generates a layer with the game's fresh `1f` builds a different layer than the run's
  authority, which is what a guest joining mid-run used to do. They ride the existing kernel
  checkpoint / run-batch path (row R1) instead of a message of their own: no new discriminator, no
  new cadence, and the world-entry backfill story is R1's (the run baseline is part of the
  checkpoint). A sender that predates the field sends none, which means the game's own starting
  value. The cut/restore half of the same decision is the world archive, not a wire surface, and is
  declared in `docs/architecture/save-archive-format.md` section 3.4/4/6.1.

## Guard

`tests/CasualtiesUnknownOnline.NormativeGates.Tests/SyncCoverageGateTests.cs` fails when:

- a `NetMsg`, `WireCommandKind`, `WireEventKind` or `AdaptiveStreamId` member has no vocabulary
  index entry, or its index entry points at an unknown row;
- the owning row's text does not mention the member (an entry cannot be parked on an unrelated
  row);
- a row's verdict leaves the vocabulary or is empty;
- a row declares no evidence anchor, its anchor cell is not a count, a row id is duplicated, or
  the matrix drops below the 64-row floor;
- a row repeats evidence text (`path 'quote'`): the quote belongs in the evidence file only, and a
  row that copies it back is the double maintenance this contract removed;
- a row's declared anchor count disagrees with the number of entries
  `docs/evidence/sync-coverage-evidence.json` holds for that row id, or an entry names a row id
  the matrix does not have and is not marked `(none)` (document-level evidence that belongs to no
  row, such as the corrections table below; at most 5 such entries exist, so the marker cannot
  park coverage evidence out of every row's anchors);
- any evidence entry in `docs/evidence/sync-coverage-evidence.json` no longer has its quoted
  text in the file it names, the evidence file shrinks below the 700-entry floor, or its
  declared `count` disagrees with the entries it carries;
- the verdict summary table disagrees with the row verdicts;
- a gap row does not name an existing `docs/backlog/todo/*.md` ticket.

The gate carries negative-contract self-tests for each of those failure modes, and it was
falsified by hand: adding a throwaway `NetMsg` member made it fail with
`NetMsg.TestGateProbe is not declared in the wire vocabulary index`, removing the member made
it pass again, and silently flipping row W2's verdict to `OK` made the verdict-summary gate
fail. This is the rot guard the audit ticket asked for: a new sync feature cannot ship without
an event + fallback policy declaration.

**What the guard proves, and what it does not.** It proves the row, the evidence file and the
sources agree: a declared anchor count matches the entries that file holds for the row, every
entry's quote is still present in the file it names, and no row repeats a quote. It does NOT
prove that a quote is *relevant* to the row that owns it. The count is a declaration check, not a
completeness proof: an edit that deletes entries and lowers the count in the same change, or one
that copies another row's entries onto a row, is not caught mechanically. The 700-entry floor
bounds how much evidence can vanish silently; a review of the evidence diff covers the rest,
which is why a cycle that moves evidence anchors records that diff in its ticket.

Known limitation: 11 evidence quotes are shorter than 20 characters and repeat in their file
(for example `SendFullSet();`). A quote is matched as a whitespace-normalized substring of the
whole file it names, so such a quote cannot pin its own position on its own; those rows rely on
their longer sibling quotes in the same row. All other evidence is uniquely anchored.
















