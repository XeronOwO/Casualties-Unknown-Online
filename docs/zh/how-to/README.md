# 做一件事

一件事一页:你想做什么、步骤、能跑的例子、常见坑,以及怎么验证真的成了。每一页都假设你读过[你的第一个模组](../start/your-first-mod.md)。

- [给其他玩家发消息](send-a-network-message.md) —— 模组网络:谁能发什么,以及消息没送到时会怎样。
- 声明权限与主机命令 —— *正在编写*
- 读取游戏状态 —— *正在编写*
- 在界面里加一个面板 —— *正在编写*
- 注册内容 —— *正在编写*
- 让模组数据跨会话保留 —— *正在编写*
- 判断一个动作该由哪一侧裁决 —— *正在编写*

这些页面背后的精确契约在 `../reference/`,设计原因在 `../internals/`。
