namespace CasualtiesUnknownOnline.Runtime.Session.Commands;

/// <summary>The sort order for bracketed selector filters.</summary>
internal enum SelectorSort
{
	None,
	Nearest,
	Furthest,
	Random,
	Arbitrary,
}
