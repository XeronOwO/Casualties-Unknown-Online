# Start here

The first-run line: read it in order — every page ends with something you can watch working. It
assumes no knowledge of CUO and none of the game's internals.

1. [What CUO is](what-is-cuo.md) — the mod, a session, and what it deliberately leaves out.
2. Install and play — host a session, join one, and what to do when it fails. *(being written)*
3. Set up a development checkout — get the repository, build it, run the tests. *(being written)*
4. Your first mod — change one thing in the example mod and see it take effect. *(being written)*

Depth: this line stops at "it works". How each mechanism is built lives in `../internals/`, and the
exact contracts live in `../reference/`.
