# Sync Coverage Matrix — event sync + periodic fallback

- Status: Evidence (produced by `docs/backlog/review/sync-event-and-periodic-fallback-coverage-audit.md`)
- Audit date: 2026-09-09
- Scope: **A** kernel domains, **B** direct `NetMsg` families, **C** GameAdapter/Runtime sync
  surfaces, **D** adaptive streams
- Verdict vocabulary: `OK` | `Event-only gap` | `Fallback-only gap` | `Transient-by-design`

## Method

1. **S1 — freeze the inventory.** The row list was derived from the kernel domain table
   (`docs/architecture/domains.md`), the direct `NetMsg` classification
   (`docs/evidence/selfchecks/architecture/phase-e-legacy-inventory-selfcheck.md`), every
   `NetMsg` value in `src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs`, every
   `WireCommandKind` / `WireEventKind` member, and every `AdaptiveStreamId` member. No row
   was dropped without a reason.
2. **S2 — evidence collection.** Eight independent collectors produced per-row evidence:
   trigger message + numeric id, direction, reliability, periodic fallback cadence and apply
   semantics, backfill/reconnect path, loss semantics, and `path:line` references. The
   reference + quoted-line pairs are stored in `docs/evidence/sync-coverage-evidence.json`
   (505 collector entries + 231 inline-reference anchors = 736 entries) and **all 736 were
   mechanically verified against the working tree** (file exists, line in range, quoted text
   is a substring of that line). `SyncCoverageGateTests`
   re-runs that verification on every `dotnet test`, so a line shift fails the gate instead of
   silently rotting.
3. **S3 — adjudication.** Every row got exactly one verdict. The adjudication changed four
   collector verdicts (N6/N7/N8/R7 → `Transient-by-design` because the loss is accepted by
   design, and F4 → `OK` because a continuous field has no discrete trigger) and, after the
   independent adversarial review, three more (W2/W6/I5 → `Event-only gap` because their own
   loss text admits a non-convergent direction). The reasons are recorded in the row.
4. **S4 — gaps, guard, docs.** Every gap got an independent ticket (R3+R4 merged into one
   session-control ticket), the rot guard `SyncCoverageGateTests` landed, and the active docs
   that contradicted the matrix were corrected.

The audit itself changed no sync behavior. One protocol defect found during the audit
(stale-`RunEpoch` state-stream frames were forwarded, contradicting
`docs/architecture/glossary.md`) was fixed in the same cycle as a separate, tested change.
The independent adversarial review then found that the first version of that fix could drop a
legitimate stream after a foreign-epoch checkpoint restore; `ItemKernelAuthority.Restore` now
adopts the restored checkpoint's epoch, with a red → green regression test.

## Verdict summary

| Verdict | Count | Rows |
|---|---|---|
| `OK` | 45 | all rows not listed below |
| `Event-only gap` | 10 | W1, W2, W6, E3, I5, I6, I8, N1, R3, R4 |
| `Fallback-only gap` | 0 | — |
| `Transient-by-design` | 9 | N6, N7, N8, N9, F3, I9, P5, E6, R7 |
| `Unverified` | 0 | — |

**The user's seed finding is confirmed.** World blocks: host → guest has a 60 s absolute
re-send and the world-entry backfill, but the guest → host direction has no diff table and no
periodic re-report, so a swallowed guest mutation is invisible to the host table and the
host's absolute snapshot cannot heal it (row W1; ticket
`docs/backlog/todo/guest-block-mutation-re-report.md`).

## Matrix

| ID | Feature / domain (owner) | Direction + roles | Event sync | Periodic fallback | Backfill / recovery | Loss semantics | Verdict | Gap ticket |
|---|---|---|---|---|---|---|---|---|
| K1 | Kernel command + committed batch path (guest->host intent, host->guests confirmation) (Runtime/Session/Items/KernelProtocolService.cs, KernelProtocolCommandHandler.cs, Runtime/Session/Handlers/KernelEnvelopeHandler.cs, Protocol/Wire/CommandEnvelope.cs, CommittedBatchEnvelope.cs) | Both. Guest -> host CommandEnvelope (EnvelopeKind.Command = 1); host -> guests CommittedBatchEnvelope (EnvelopeKind.CommittedBatch = 2); both ride one transport frame NetMsg.KernelEnvelope (id 122), payload CommittedBatch = 100. | Dedicated envelopes, not a snapshot: CommandEnvelope (guest intent) and CommittedBatchEnvelope (host confirmation). Real trigger is the guest local item operation report: src/CasualtiesUnknownOnline.GameAdapter/Items/PickupSync.cs:141 ('_items.SendItemPickedUp(idComp.Id, ItemStateCodec.CaptureDigest(item, ItemStateCodec.SlotOf(item)));') and src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemMessageFlowService.cs:58 ('_kernelProtocol.SendCommand(new WireCommand') call IKernelProtocolControl.SendCommand (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:140 'public void SendCommand(WireCommand command, WirePayloadType payloadType)'). The host decodes/executes via KernelProtocolCommandHandler.Handle (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:332 '_commandHandler.Handle(sender, envelope);') and the commit raises BatchCommitted (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:596 'BatchCommitted?.Invoke(batch!);'), fanned out by KernelProtocolService.BroadcastCommittedBatch (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:68 'public void BroadcastCommittedBatch(CommittedBatch batch)'). Numeric ids: src/CasualtiesUnknownOnline.Protocol/Wire/EnvelopeKind.cs:10 'Command = 1,' 'CommittedBatch = 2,'. Reliability: src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29 'public void Send(ulong steamId, NetMsg msg, object? payload = null, bool reliable = true)' and src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:493 'private void SendToGuests(ProtocolFrame frame, bool reliable = true)' — reliable in both directions, no reliable:false on this path. Wire members: CommandRejected, KernelEnvelope. | Absolute fallback is the kernel checkpoint (row K3): the 60 s host cycle (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 'if (IsHostMode && _session.SessionActive && Time.unscaledTime - _lastSnapshotResend > 60f)' -> '_kernelProtocol.SendCheckpoint(member.SteamId); // the kernel checkpoint also covers lazy-session recovery for WorldEntities and the other kernel domains') plus world-entry/reconnect (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41 '_kernelProtocol.SendCheckpoint(steamId);', src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192 '_kernelProtocol.SendCheckpoint(sender);') and the gap-driven fresh checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:418 'SendCheckpoint(sender);'). A checkpoint is an absolute overwrite of the guest kernel (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:474 'var result = _authority.Restore(checkpoint);'), so an uncommitted guest prediction or a swallowed batch is reverted to host authority. No periodic re-send of individual commands exists, and none is needed: the channel is reliable and the send is gated on a live handshake (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:142 'if (_session.Role != SessionRole.Guest &#124;&#124; !_session.SessionActive &#124;&#124; _session.HostSteamId == 0)'). | World entry / late join / reconnect: WorldEntryFanout.Send -> SendCheckpoint (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41 '_kernelProtocol.SendCheckpoint(steamId);'). Mid-generation join: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192 '_kernelProtocol.SendCheckpoint(sender);'. Missed batch: revision-gap detection (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:345 'var expected = _authority.CurrentGlobalRevision + 1;') buffers the future batch and issues RequestRange (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:351 'RequestRange(expected, batch.GlobalRevision - 1);'), and the host replays the journal range or falls back to a fresh checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:418 'SendCheckpoint(sender);'). | Reliable ordered channel in both directions (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29 'public void Send(ulong steamId, NetMsg msg, object? payload = null, bool reliable = true)'; src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:114 '_sender.Send(targetSteamId, NetMsg.KernelEnvelope, frame);'). Loss is not accepted: a duplicate is idempotent by OperationId (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:88 'if (_appliedOperations.Contains(batch.OperationId))') and a gap is repaired by range request or checkpoint. Convergence guarantee: the host kernel is the single authority and the guest replay kernel converges to the host checkpoint/committed batch (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:365 'var result = _authority.Apply(batch);'), so a guest prediction that was never committed is corrected, not preserved. | OK |  |
| K2 | Checkpoint backfill + journal range recovery (late join / reconnect / gap) (Runtime/Session/Items/KernelProtocolService.cs (SendCheckpoint, HandleRangeRequest, RequestRange, journal), Runtime/Session/World/WorldEntryFanout.cs) | Host -> guest (checkpoint chunks and journal batches); guest -> host (WireCommandKind.RangeRequest, payload 202). | Trigger is a detected revision gap on the guest: src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:345 'var expected = _authority.CurrentGlobalRevision + 1;' computes the expected revision, the gap is buffered, and RequestRange is issued (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:351 'RequestRange(expected, batch.GlobalRevision - 1);'). RequestRange sends a RangeRequest through the same CommandEnvelope path (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:398 'SendCommand(new WireCommand'). The host answers in HandleRangeRequest (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:406 'private void HandleRangeRequest(ulong sender, WireCommand command)'): journal batches in range, or a fresh checkpoint when the journal is empty/out of window (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:418 'SendCheckpoint(sender);'). Wire members: RangeRequest. | The same SendCheckpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:94 'public void SendCheckpoint(ulong targetSteamId)') is the periodic fallback, driven every 60 s by row K3 (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 -> src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:131). It is absolute: the host takes authority.CreateCheckpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:101 'var checkpoint = _authority.CreateCheckpoint();') and the guest Restore replaces the whole kernel store (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:474 'var result = _authority.Restore(checkpoint);'; src/CasualtiesUnknownOnline.GameState/Kernel/GameStateStore.cs:66 'RunEpoch = checkpoint.RunEpoch;'). This path never calls AdaptiveStreamRateService, so the governor cannot stretch it. | World entry / late join / reconnect: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41 '_kernelProtocol.SendCheckpoint(steamId);' inside the ordered snapshot group that ends with src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:44 '_world.SendWorldSnapshotComplete(steamId);'; mid-generation handshake: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192 '_kernelProtocol.SendCheckpoint(sender);'; gap: RequestRange -> HandleRangeRequest; the journal is bounded (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:23 'private const int JournalCapacity = 2048;') and appended on every host commit (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:75 '_journal.Add(batch);'), and an out-of-window range falls back to a fresh checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:418 'SendCheckpoint(sender);'). | Reliable channel: checkpoint chunks and journal tail use PacketSender.Send defaults (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:114 '_sender.Send(targetSteamId, NetMsg.KernelEnvelope, frame);'; src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29). A lost chunk leaves the buffer incomplete and is healed by the next checkpoint (60 s cycle or a range-request fallback); the receiver restores only a complete set (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:466 'if (_checkpointChunks.Count != chunk.ChunkCount)'). Mixed chunks are rejected rather than silently applied (src/CasualtiesUnknownOnline.Runtime/Session/Items/WireCheckpointAssembler.cs:114 'if (chunk.RunEpoch != first.RunEpoch &#124;&#124; chunk.GlobalRevision != first.GlobalRevision)') and the buffer is cleared on failure (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:489 '_checkpointChunks.Clear();'). Worst-case convergence wait is the 60 s cycle. | OK |  |
| K3 | Periodic kernel checkpoint fallback (60 s host cycle) (GameAdapter/World/WorldEventSync.cs (Update), KernelProtocolService.SendCheckpoint) | Host -> each in-world guest (one SendCheckpoint per member). | The periodic cycle has no dedicated event envelope; its event-level counterparts share the same SendCheckpoint mechanism: world entry / late join (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41 '_kernelProtocol.SendCheckpoint(steamId);'), mid-generation handshake (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192 '_kernelProtocol.SendCheckpoint(sender);') and gap recovery (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:418 'SendCheckpoint(sender);'). The cycle is pumped from the frame loop (src/CasualtiesUnknownOnline.GameAdapter/GameAdapter.cs:232 '_domains.WorldEventSync.Update();'). | Cadence is a fixed 60 s wall-clock test in the host pump: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 'if (IsHostMode && _session.SessionActive && Time.unscaledTime - _lastSnapshotResend > 60f)', restricted to in-world members ( 'if (member.InWorld)') and sent per member ( '_kernelProtocol.SendCheckpoint(member.SteamId); // the kernel checkpoint also covers lazy-session recovery for WorldEntities and the other kernel domains'). Sender is KernelProtocolService.SendCheckpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:94 'public void SendCheckpoint(ulong targetSteamId)'), which builds an absolute checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:101 'var checkpoint = _authority.CreateCheckpoint();'). Apply semantics are absolute overwrite on the guest (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:474 'var result = _authority.Restore(checkpoint);'; src/CasualtiesUnknownOnline.GameState/Kernel/GameStateStore.cs:66 'RunEpoch = checkpoint.RunEpoch;'). The cadence is a hardcoded float and never calls AdaptiveStreamRateService, so the adaptive rate governor cannot stretch or remove it. | Same call path is reused for world entry (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41 '_kernelProtocol.SendCheckpoint(steamId);'), mid-generation handshake (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192 '_kernelProtocol.SendCheckpoint(sender);') and out-of-window range requests (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:418 'SendCheckpoint(sender);'). The 60 s block also re-sends the world block-state/damage snapshots and keypad codes in the same cycle (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:129 '_world.SendBlockStateSnapshot(member.SteamId);' '_world.SendBlockDamageSnapshot(member.SteamId); // the partial block damage rides the same world-entry resend (idempotent absolute set)' 'SendKeypadCodes(); // re-send the full set (idempotent — set codes are left alone) — covers the lazy-session swallow window and keypads created after the first send (the airdrop/command case, #128 follow-up)'). | Reliable send (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:114 '_sender.Send(targetSteamId, NetMsg.KernelEnvelope, frame);' uses PacketSender default true). A lost or swallowed chunk is re-sent by the next cycle (<= 60 s) and a partial set is never applied (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:466 'if (_checkpointChunks.Count != chunk.ChunkCount)'). Convergence is absolute: the guest kernel is overwritten from the host authoritative checkpoint, healing any swallowed batch or uncommitted prediction. | OK |  |
| K4 | Session reset / run-epoch isolation (KernelProtocolService.ResetForSessionEnd, Runtime/Session/SessionService.cs, RunEpoch filters) | Local reset (no wire message); isolation is enforced on the wire by the RunEpoch field carried in every EnvelopeHeader in both directions. | Trigger is the local session teardown: SessionService.TeardownSession fires SessionEnded (src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs:481 '_state.FireSessionEnded(); // the entity domain + the Game Adapter tear down on this'). KernelProtocolService subscribes at construction (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:59 '_session.SessionEnded += ResetForSessionEnd;') and ResetForSessionEnd (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:210 'public void ResetForSessionEnd()') bumps the authority epoch (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:37 '_kernel = new GameStateKernel(new RunEpoch(_kernel.RunEpoch.Value + 1));') and clears journal/chunk/pending state (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:213 '_journal.Clear();' '_checkpointChunks.Clear();' '_pendingBatches.Clear();') plus the stream seq counters (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:218 '_stateStreams.Reset();'). Inbound commands are epoch-filtered (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:319 'if (envelope.Header.RunEpoch != currentEpoch)') and batches too (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:338 'if (batch.RunEpoch.Value != _authority.CreateCheckpoint().RunEpoch.Value)'), with a second kernel-level check (src/CasualtiesUnknownOnline.GameState/GameStateKernel.cs:34 'if (command.RunEpoch != _store.RunEpoch' 'if (batch.RunEpoch != _store.RunEpoch)'). | The epoch rides every envelope header (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:511 'RunEpoch = batch?.RunEpoch.Value ?? _authority.CreateCheckpoint().RunEpoch.Value,') and every checkpoint, and the row K3 60 s absolute checkpoint re-establishes the host epoch on a guest because restore adopts the checkpoint epoch (src/CasualtiesUnknownOnline.GameState/Kernel/GameStateStore.cs:66 'RunEpoch = checkpoint.RunEpoch;'). A guest stuck on an old epoch is healed by the next checkpoint (60 s cycle, world entry or handshake). The adaptive governor is not involved. | World entry / reconnect checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41 '_kernelProtocol.SendCheckpoint(steamId);', src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192 '_kernelProtocol.SendCheckpoint(sender);') carries the current epoch; the checkpoint assembler refuses chunks that disagree on epoch or revision (src/CasualtiesUnknownOnline.Runtime/Session/Items/WireCheckpointAssembler.cs:114 'if (chunk.RunEpoch != first.RunEpoch &#124;&#124; chunk.GlobalRevision != first.GlobalRevision)'), so a mixed old/new checkpoint is discarded rather than applied. | The local teardown trigger cannot be lost over the network. Epoch filtering applies to commands (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:319), batches (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:338) and kernel state streams (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:283-298). The checkpoint receive site (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:462 'private void HandleCheckpoint(CheckpointEnvelope envelope)') still does not compare RunEpoch, and src/CasualtiesUnknownOnline.GameState/Kernel/GameStateStore.cs:66 overwrites the local epoch with the checkpoint epoch, so a stale checkpoint completing after a reset could rewind the guest epoch; tracked in todo/checkpoint-run-epoch-validation.md. Residual window: before the first checkpoint restore the guest still uses its local epoch, so a command or report sent in that window can be rejected by the host; the same ticket owns the handshake-carried expected epoch. The epoch filter warns once per sender and logs Debug afterwards, so a persistent mismatch cannot flood the log. Compatibility note: KernelWireMapper maps a wire batch epoch of 0 to the current epoch, so the batch filter only rejects a non-zero mismatched epoch (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelWireMapper.cs:309). | OK |  |
| K5 | Kernel state-stream transport (StateStreamEnvelope) (Runtime/Session/Items/KernelStateStreamService.cs, Protocol/Wire/WireStateStream.cs, Runtime/Session/EntitySync/PlayerStreamExchange.cs) | Both. Host -> guests (player/enemy/item streams) and guest -> host (player state report), all over NetMsg.KernelEnvelope with StateStreamEnvelope. | Dedicated envelope StateStreamEnvelope (src/CasualtiesUnknownOnline.Protocol/Wire/StateStreamEnvelope.cs:9 'public sealed class StateStreamEnvelope'), EnvelopeKind.StateStream = 4 (src/CasualtiesUnknownOnline.Protocol/Wire/EnvelopeKind.cs:13 'StateStream = 4,'); payload types StateStream = 301, ItemSnapshotStream = 302, WorldItemsSnapshotStream = 303, PlayerStateStream = 304 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs:77 'PlayerStateStream = 304,'), EnemyStateStream = 305. Host broadcast site: src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs:56 'kernelProtocol.BroadcastStateStreamTo(' invoked from src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:426 '_playerStream.BroadcastPlayerState();'; guest report site: src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs:71 'kernelProtocol.SendStateStreamTo(session.HostSteamId,' invoked from src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:434 '_playerStream.SendPlayerStateReport();'. Reliability is explicitly unreliable in both directions: src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs:60 'reliable: false);', and the shared kernel stream path defaults to unreliable (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelStateStreamService.cs:55 'SendToGuests(frame, reliable: false);'). | The stream is itself the periodic fallback: EntitySyncService.Update paces it with the adaptive interval (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:423 '_nextStateSendMs = nowMs + _adaptiveRates.GetSendIntervalMs(' for the host; the guest branch at src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:431-433). The governor can stretch the interval (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:12 'private const int MinHz = 1;' 'private const int MaxHz = 60;') but cannot remove the stream. Apply semantics are latest-wins absolute overwrite, seq-gated: src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs:90 'if (stream.Seq <= LastStateSeq)' 'LastStateSeq = stream.Seq;', applying at 'entities.ApplyPlayerState(state, target);'. | World entry / rejoin: EntitySyncService.StartMemberSync announces PlayerJoin and immediately pushes a full player state (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:533 '_playerStream.BroadcastPlayerState();') so a clone renders without waiting for a tick; the roster backfill (PlayerJoin per existing member) precedes it. Enemy world-entry backfill is the direct EnemySnapshot NetMsg (not this envelope). Session end resets the seq gate (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:486 '_playerStream.ResetSession();'). | Loss is explicitly accepted by design: src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:24-27 documents the 20 Hz player state stream as the unreliable-by-design path (overwrite semantics plus the snapshot sequence make drops harmless; other loss-tolerant streams such as the enemy, item-snapshot, fluid-region and tutorial-claw streams also pass reliable:false) (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:26 '/// semantics + snapshot sequence make drops harmless and avoid head-of-line'). Convergence: the next tick overwrites and the seq gate discards duplicates/out-of-order streams, so a swallowed stream self-heals within one adaptive interval. Epoch filter: the receive site (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:283-298) drops a stream whose header RunEpoch differs from the current run before the seq gate runs; within the same epoch the seq gate (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs:90 'if (stream.Seq <= LastStateSeq)') still discards duplicates and out-of-order frames. The direct NetMsg presentation streams (FluidRegion, TraderState, TutorialClawState) carry no RunEpoch and are outside this filter, so the glossary's old-epoch stream wording is scoped to kernel state streams. | OK |  |
| W1 | World block state: placed / broken / air writes (BlockPlaced, BlockDamaged, WorldBlockState) (GameAdapter/World/WorldEventSync.cs (OnBlockSet, Update 60s), Runtime/Session/World/WorldStateMessageService.cs, Handlers/BlockPlacedHandler.cs, BlockDamagedHandler.cs, BlockStateHandler.cs, GameAdapter/World/BlockBreakSync.cs) | Both. Host -> guest: BroadcastBlockPlaced for every host-side mutation plus the 60 s absolute WorldBlockState table. Guest -> host: SendBlockPlacedReport; the host arbitrates (placement must land on air, an air write must land on something), applies, records the cell in its difference table and relays with the source excluded. There is no guest -> host fallback in either direction. | Dedicated messages: BlockPlaced = 42 bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:33), BlockDamaged = 40 bidirectional , WorldBlockState = 41 host -> guest . Real trigger: the SetBlock postfix reports only a write that actually landed (src/CasualtiesUnknownOnline.GameAdapter/Patches/WorldGenerationSetBlockPatch.cs:58 'PatchBridge.Impl?.OnBlockSet(pos, block);'), which WorldEventSync.OnBlockSet routes host-side to a broadcast (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:210 '_world.BroadcastBlockPlaced(0, pos.x, pos.y, block);') and guest-side to a report ( '_world.SendBlockPlacedReport(pos.x, pos.y, block);' -> src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:202 '_sender.Send(_session.HostSteamId, NetMsg.BlockPlaced,'). The host's received report path applies SetBlock, records the difference and relays ( '_world.BroadcastBlockPlaced(sender, x, y, block); // the reporter already applied locally'). All sends use PacketSender.Send / Broadcast defaults, i.e. reliable = true (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29). Wire members: BlockDamaged, BlockPlaced, WorldBlockState. | Host -> guest only: 60 s absolute block-state table. Cadence src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 'if (IsHostMode && _session.SessionActive && Time.unscaledTime - _lastSnapshotResend > 60f)', send per in-world member '_world.SendBlockStateSnapshot(member.SteamId);' -> src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:310 '_sender.Send(targetSteamId, NetMsg.WorldBlockState, msg);'. Apply is an absolute overwrite of every listed cell (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:465 'WorldGeneration.world.SetBlock(pos, block.Block);'); the table is the host's deviation set, rebuilt on each generation. The cadence is a hardcoded Time.unscaledTime threshold; world blocks have no AdaptiveStreamId (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamId.cs:5 'Reliable control paths are intentionally not enumerated here because they') and WorldEventSync holds no AdaptiveStreamRateService, so the adaptive governor cannot stretch or remove it. Guest -> host: none. The only guest send is the live report at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:214; the guest keeps no local difference table (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:173 '// world and applies the table). Guests do not track — they only apply.'). CADENCE FINDING: the host->guest 60 s resend is the only absolute heal; a mutation inside the ~30 s lazy-P2P swallow window can stay invisible for up to 60 s (todo/sync-cadence-review.md). | World entry / late join / reconnect / per-layer regeneration: the ordered world-entry fanout sends the absolute table (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:37 '_world.SendBlockStateSnapshot(steamId);'), fired on the InWorld edge (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs:59 '_worldEntryFanout.Send(reporter);') and on reconnect-while-InWorld (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:145 '_worldEntryFanout.Send(sender);'). Because inWorld is false while generating (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:273-274), every layer regeneration re-fires the InWorld edge, so the table is re-sent per layer as well as on the 60 s cycle. Missed-range recovery is the absolute table itself; there is no guest report replay. | Reliable channel (PacketSender default reliable = true). Host -> guest converges: the next 60 s snapshot or the next world-entry fanout rewrites the absolute deviation table. Guest -> host does NOT converge for a swallowed report: the guest never re-reports and keeps no diff table, so the host table lacks the cell, the 60 s snapshot omits it and cannot heal it; the divergence persists until a reconnect regenerates the guest's world or a later host write lands on that cell. Bounded by the table cap MaxDamagedBlocks = 65536 (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:271-274 drops new cells when full), and SendBlockStateSnapshot returns early on an empty table . | Event-only gap | todo/guest-block-mutation-re-report.md |
| W2 | Partial block damage before the break (BlockDamageSnapshot) (Runtime/Session/World/BlockDamageRegistry.cs, Handlers/BlockDamageSnapshotHandler.cs, GameAdapter/World/BlockDamageCleaner.cs) | Both for the live relay (BlockDamaged = 40); the authoritative record and the periodic fallback are host -> guest only (BlockDamageSnapshot = 89, PacketHandler direction HostToGuest). | Dedicated message: BlockDamaged = 40 bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:31). Real trigger: the DamageBlock postfix (src/CasualtiesUnknownOnline.GameAdapter/Patches/WorldGenerationDamageBlockPatch.cs:56 'PatchBridge.Impl?.OnBlockDamaged(pos, dmg, bonusMetal);'). A surviving block is reported immediately with raw damage + MetalBonus (src/CasualtiesUnknownOnline.GameAdapter/World/BlockBreakSync.cs:109 '_world.SendBlockDamaged(new NetVector2(pos.x, pos.y), dmg, bonusMetal, null, null);'); a break holds one frame and goes out as ONE message with its drops ( '_world.SendBlockDamaged('). The host records the post-write absolute damage for the snapshot on its own and on every applied remote write (src/CasualtiesUnknownOnline.GameAdapter/World/BlockBreakSync.cs:389 '_world.ReportBlockDamage(cell.x, cell.y, blockDamage.damage);' -> src/CasualtiesUnknownOnline.Runtime/Session/World/BlockDamageRegistry.cs:49 '_damage[key] = damage;'). Reliable by PacketSender default. Wire members: BlockDamageSnapshot. | 60 s absolute BlockDamageSnapshot, host -> in-world members: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:130 '_world.SendBlockDamageSnapshot(member.SteamId); // the partial block damage rides the same world-entry resend (idempotent absolute set)' -> src/CasualtiesUnknownOnline.Runtime/Session/World/BlockDamageRegistry.cs:74 '_sender.Send(targetSteamId, NetMsg.BlockDamageSnapshot, new BlockDamageSnapshotMsg'. Apply is an absolute set, never additive (src/CasualtiesUnknownOnline.GameAdapter/World/BlockBreakSync.cs:368 'blockDamage.damage = entry.Damage;'), guarded against air cells, out-of-range damage and the game's 128-entry blockDamages cap (src/CasualtiesUnknownOnline.GameAdapter/World/BlockBreakSync.cs:340-360). Cadence is the hardcoded 60 s threshold at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122; BlockDamageRegistry has no AdaptiveStreamId and no AdaptiveStreamRateService, so the governor cannot stretch it. Guest -> host: none beyond the live relay; the registry is host-only (src/CasualtiesUnknownOnline.Runtime/Session/World/BlockDamageRegistry.cs:32 'if (_session.Role != SessionRole.Host)'). | World entry / late join / reconnect / per-layer regeneration: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:38 '_world.SendBlockDamageSnapshot(steamId);' in the ordered group, fired by the InWorld edge (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs:59) and by reconnect-while-InWorld (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:145); the 60 s cycle repeats it. The guest applies through OnBlockDamageSnapshot (src/CasualtiesUnknownOnline.GameAdapter/World/BlockBreakSync.cs:327) as an absolute set with a refreshed crack sprite ( 'blockDamage.UpdateSprite();'). | Reliable channel. Host -> guest converges: a swallowed damage relay is healed by the next absolute snapshot (world entry or <= 60 s). Guest -> host asymmetry: a swallowed damage report is not re-reported and the host registry has no entry for that cell, so the next host -> guest snapshot omits the cell and cannot correct either side; the mismatch lasts until the break, whose terminal block state is owned by W1 (the air-write relay plus the 60 s WorldBlockState table). Snapshot entries outside a surviving block's range are skipped with a warning (src/CasualtiesUnknownOnline.GameAdapter/World/BlockBreakSync.cs:346-351), and the game-side list cap is 128 , so entries beyond the cap stay unhealed. ADJUDICATED (adversarial review): the guest -> host partial-damage report has no re-report and its terminal heal is owned by W1, which is itself a gap; the row is therefore a gap, not OK. Same ticket as W1. | Event-only gap | todo/guest-block-mutation-re-report.md |
| W3 | EarthquakeStart (Runtime/Session/World/WorldStateMessageService.cs:217-231, Handlers/EarthquakeStartHandler.cs, GameAdapter world quake suppression) | Host -> guest only (PacketHandler direction HostToGuest; a guest never starts a quake because its timer is frozen). | Dedicated message: EarthquakeStart = 55 host -> guest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:40). Real trigger: the WorldGeneration.Update postfix detects earthquakeTime crossing from <= 0 to > 0 (src/CasualtiesUnknownOnline.GameAdapter/Patches/WorldGenerationUpdatePatch.cs:64 'PatchBridge.Impl?.OnEarthquakeStarted(__instance.earthquakeTime, __instance.earthquakeDelay);'). Host send: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:294 '_world.BroadcastEarthquakeStart(duration, nextDelay);' -> src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:224 '_session.Broadcast(NetMsg.EarthquakeStart, new EarthquakeStartMsg { Duration = duration, NextDelay = nextDelay });' (SessionService.Broadcast -> PacketSender.SendToAll, reliable = true by default). Receive/apply: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:314-315 writes earthquakeTime/earthquakeDelay on the guest. Wire members: EarthquakeStart. | none. The only send call site is src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:294; BroadcastEarthquakeStart has no other caller and no timer/cadence exists for it. There is no absolute quake state to resend: the guest's own timer is frozen every frame (src/CasualtiesUnknownOnline.GameAdapter/Patches/WorldGenerationUpdatePatch.cs:46 '__instance.earthquakeDelay = float.MaxValue;'), so a missed start cannot be reconstructed locally and is not re-sent. The quake's durable effect (its air writes) is healed by W1: each side's SetBlock(0) rides the air-write relay (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:210) and the host's 60 s WorldBlockState table . | No dedicated backfill for the event itself: a late joiner never receives an in-flight quake, and the next host quake is the only re-align (the guest applies earthquakeTime/earthquakeDelay at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:314-315). Durable terrain converges through W1's air-write relay and the 60 s WorldBlockState table, so a swallowed EarthquakeStart cannot permanently desync the world. | Reliable channel (PacketSender default). A swallowed EarthquakeStart is not re-sent: the guest misses that shake, but no independent divergence follows because the guest's timer is frozen (it never starts its own quake), and the blocks the quake broke still arrive through W1's relay + 60 s absolute table. Transient presentation loss with a durable-state heal owned by W1. ADJUDICATED (adversarial review): the event itself is one-shot with no fallback, but the guest's quake timer is frozen so no independent divergence follows; the durable air writes ride W1. Kept OK with that dependency stated explicitly. | OK |  |
| W4 | KeypadCode (Runtime/Session/World/WorldStateMessageService.cs:111-126, Handlers/KeypadCodeHandler.cs, GameAdapter keypad sync) | Host -> guest only (PacketHandler direction HostToGuest; the host is the code authority). | Dedicated message: KeypadCode = 57 host -> guest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:41). Real trigger 1, generation-time keypads: the first full send happens on the generation-completed baseline capture (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:352 'SendKeypadCodes(); // TryCaptureWorldBaseline runs once per generation — the first send happens here, the 60 s cycle re-sends'). Real trigger 2, runtime-created keypads: the creation message carries the code as creation-time data (src/CasualtiesUnknownOnline.GameAdapter/World/EntitySpawnSync.cs:141 '? WorldEventSync.EnsureKeypadCode(openable) // the host creates it — its code is host authority from the start', applied on the receiving copy at ). Send: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:123 '_sender.Send(member.SteamId, NetMsg.KeypadCode, msg);' (reliable by PacketSender default). Wire members: KeypadCode. | 60 s full-set absolute re-send, host -> handshaken members: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:135 'SendKeypadCodes(); // re-send the full set (idempotent — set codes are left alone) — covers the lazy-session swallow window and keypads created after the first send (the airdrop/command case, #128 follow-up)'. Apply is idempotent/absolute: the receiver writes only when the local code is empty (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:440 'if (string.IsNullOrEmpty(codeField.GetValue<string>()))' -> 'codeField.SetValue(match.Code);'). Cadence is the hardcoded 60 s threshold at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122; KeypadCode has no AdaptiveStreamId and WorldEventSync has no AdaptiveStreamRateService, so the governor cannot stretch it. | World entry: the generation-complete first send (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:352). Re-entry/reconnect while in world: the session scene-changed hook re-sends immediately (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:96 'SendKeypadCodes();', wired at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:59 and src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:63 to ISessionControl.RemoteSceneChanged, which HandshakeHandler fires for a reconnect-while-InWorld and SceneStateHandler for an InWorld edge). Per-layer generation re-sends because the first send is on the generation-complete edge. The 60 s cycle covers the lazy-session swallow window. | Reliable channel and idempotent apply: a lost code is healed by the next 60 s cycle, the next generation edge, or a re-entry/reconnect. Explicit exception in code: a guest that already generated its own code (local first use raced the broadcast) is left alone (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:440-444), so that local code wins on that side instead of converging to the host's — an accepted local race, not a transport loss. | OK |  |
| W5 | GeyserStateSnapshot (Runtime/Session/World/WorldStateMessageService.cs:94-109, Handlers/GeyserStateSnapshotHandler.cs, GameAdapter/World/GeyserStateSync.cs) | Host -> guest only (PacketHandler direction HostToGuest; the host's roll is the authority). | Dedicated message: GeyserStateSnapshot = 69 host -> guest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:54). Real trigger 1, generation-time geysers: the first full send on the generation-completed edge (src/CasualtiesUnknownOnline.GameAdapter/World/GeyserStateSync.cs:83 'SendFullSet();' guarded by _sentOnce at ). Real trigger 2, runtime-created geysers: the creation message carries LiquidType as creation-time data (src/CasualtiesUnknownOnline.GameAdapter/World/EntitySpawnSync.cs:252 'if (msg.LiquidType != 0 && created.GetComponentInChildren<GeyserScript>() != null) // Unity object — =='). Send: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:106 '_sender.Send(member.SteamId, NetMsg.GeyserStateSnapshot, msg);' (reliable by PacketSender default). Wire members: GeyserStateSnapshot. | 60 s full-set absolute re-send: src/CasualtiesUnknownOnline.GameAdapter/World/GeyserStateSync.cs:86 'if (Time.unscaledTime - _lastResend > 60f)' -> 'SendFullSet(); // re-enumerated every time — creations after the last send are included'. Apply is an idempotent same-value absolute set (src/CasualtiesUnknownOnline.GameAdapter/World/GeyserStateSync.cs:150 'Traverse.Create(geyser).Field("liquidType").SetValue(match.LiquidType); // byte — exact type (a SetValue(int) cast throws ArgumentException)'). Cadence is the hardcoded 60 s threshold; GeyserStateSnapshot has no AdaptiveStreamId and GeyserStateSync has no AdaptiveStreamRateService, so the governor cannot stretch it. | World entry: the generation-complete first send (src/CasualtiesUnknownOnline.GameAdapter/World/GeyserStateSync.cs:80-84). Re-entry/reconnect: the scene-changed hook re-sends immediately (src/CasualtiesUnknownOnline.GameAdapter/World/GeyserStateSync.cs:60 'SendFullSet();'). Per-layer generation re-sends because _sentOnce resets when generating starts and the first send re-fires. A snapshot that lands during local generation is deliberately dropped and healed by the next cycle (src/CasualtiesUnknownOnline.GameAdapter/World/GeyserStateSync.cs:135 '"[GeyserSnapshot] arrived during generation — deferred to the next cycle."'), i.e. a missed application is recoverable within 60 s. | Reliable channel; absolute idempotent apply. A lost value is healed by the next 60 s cycle, the next generation edge, or a re-entry/reconnect. Limitation: the snapshot only writes the liquid type onto an existing local GeyserScript (position-keyed, < 3 m), so it cannot materialize a geyser entity that is missing on the guest — entity existence is the trap-layout / entity-creation domain's responsibility. | OK |  |
| W6 | TrapLayoutSnapshot (Runtime/Session/World/TrapLayoutRegistry.cs, Handlers/TrapLayoutSnapshotHandler.cs, Runtime/Session/World/EntityEventChannel.cs:341, GameAdapter/World/TrapLayoutScanner.cs, TrapLayoutApplication.cs) | Host -> guest only (PacketHandler direction HostToGuest; the host's scene is the entity-layout authority). | Dedicated message: TrapLayoutSnapshot = 79 host -> guest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:84). The real trigger is the host's generation-finished falling-edge scan (src/CasualtiesUnknownOnline.GameAdapter/World/TrapLayoutScanner.cs:34 'if (!generating && _generating && _session.Role == SessionRole.Host && !_scanned)', one scan per layer), which records each entity (src/CasualtiesUnknownOnline.GameAdapter/World/TrapLayoutScanner.cs:48 '_world.ReportTrapLayout(entity.Entry.Kind, entity.Entry.X, entity.Entry.Y, entity.Entry.PrefabName);' -> src/CasualtiesUnknownOnline.Runtime/Session/World/TrapLayoutRegistry.cs:43 '_layout[key] = new TrapLayoutEntryMsg { Kind = kind, X = x, Y = y, PrefabName = prefabName };'). The wire send does NOT happen at that instant; the only send call site is the world-entry fanout (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:39 '_world.SendTrapLayoutSnapshot(steamId);' -> src/CasualtiesUnknownOnline.Runtime/Session/World/TrapLayoutRegistry.cs:55 '_sender.Send(targetSteamId, NetMsg.TrapLayoutSnapshot, msg);', reliable by PacketSender default). Apply: src/CasualtiesUnknownOnline.GameAdapter/World/TrapLayoutApplication.cs:65 'var alignment = TrapLayoutAlign.Align(hostLayout, localEntries);' (materialize missing, destroy surplus). Wire members: TrapLayoutSnapshot. | none. The only send call site is src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:39 (EntityEventChannel.SendTrapLayoutSnapshot -> TrapLayoutRegistry.SendSnapshot); no 60 s cycle includes it (the 60 s resend at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:129-135 sends only block state, block damage, the kernel checkpoint and keypad codes), and the kernel checkpoint does not carry the layout (no TrapLayout type exists anywhere in src/CasualtiesUnknownOnline.GameState). | World entry / late join: the ordered fanout sends the layout (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:39). Reconnect-while-InWorld: the same fanout (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:145 '_worldEntryFanout.Send(sender);'). Re-entry and per-layer regeneration: the InWorld edge fires the fanout (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs:59 '_worldEntryFanout.Send(reporter);'), and inWorld is false while generating (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:273-274), so each layer's generation re-fires the edge and re-sends the new layer's layout. A snapshot arriving during local generation is deferred and applied by the pump (src/CasualtiesUnknownOnline.GameAdapter/World/TrapLayoutApplication.cs:36-40). | Reliable channel. Recovery is edge-driven, not periodic: a swallowed snapshot is healed by the next InWorld edge (world entry, layer regeneration, reconnect or death -> re-entry); while a member stays continuously in the world there is no in-session repair. Application is intentionally partial: an entry whose prefab name does not load is skipped with a trace (structural members depend on the structure's deterministic placement), so those entities are never materialized on the guest. ADJUDICATED (adversarial review): the trap-layout snapshot is world-entry/InWorld-edge only; while a member stays continuously in the world there is no in-session repair. Ticket: todo/trap-layout-snapshot-recovery.md. | Event-only gap | todo/trap-layout-snapshot-recovery.md |
| W7 | World-entry snapshot group + WorldSnapshotComplete marker (Runtime/Session/World/WorldEntryFanout.cs, WorldStateMessageService.SendWorldSnapshotComplete, Handlers/WorldSnapshotCompleteHandler.cs, Runtime/Session/World/WorldService.cs) | Host -> guest only (PacketHandler direction HostToGuest). | Dedicated message: WorldSnapshotComplete = 110 host -> guest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:174). Real trigger: the end of the ordered world-entry fanout, sent last (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:44 '_world.SendWorldSnapshotComplete(steamId);' -> src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:79 '_sender.Send(targetSteamId, NetMsg.WorldSnapshotComplete, new WorldSnapshotCompleteMsg());', reliable by PacketSender default). Receive surface: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/WorldSnapshotCompleteHandler.cs:15 'ctx.World.FireWorldSnapshotCompleteReceived();' -> src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:70 'public void FireWorldSnapshotCompleteReceived() => WorldSnapshotCompleteReceived?.Invoke();'. The group members are BlockState, BlockDamage, TrapLayout, RadiationLineState, the kernel checkpoint, ItemSnapshot and EnemySnapshot (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:37-43). Wire members: WorldSnapshotComplete. | none for the marker and the group as a whole. The only send call site of SendWorldSnapshotComplete is src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:44. The 60 s cycle re-sends only a subset of the group: block state, block damage, kernel checkpoint and keypad codes (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:129-135); it does not re-send trap layout, radiation line, item snapshot, enemy snapshot or the marker. The subset cadence is the hardcoded threshold at src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 (no adaptive stream id applies). | The whole ordered group (and the marker) re-fires on every InWorld edge: first entry / late join (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs:59 '_worldEntryFanout.Send(reporter);'), reconnect-while-InWorld (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:145 '_worldEntryFanout.Send(sender);'), and per-layer regeneration because inWorld is false while generating (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:273-274). The kernel checkpoint inside the group provides revision-based recovery for the world-entity facts. | Reliable channel for every message in the group. A lost group message is healed by the next InWorld edge; while a member stays continuously in the world, the 60 s cycle heals only block state / block damage / kernel facts / keypad codes, so a lost trap layout, radiation line, item snapshot or enemy snapshot waits for the next edge. Separately, the marker itself is currently inert in production: WorldSnapshotCompleteReceived has no production subscriber (only tests subscribe), so the receiver cannot in practice distinguish a full backfill from a partial one. ADJUDICATED (adversarial review): the marker is inert (no production subscriber) and is NOT recovery evidence; the group's individual snapshots have their own rows/fallbacks. Marker tracked in todo/session-control-convergence.md. | OK |  |
| E1 | Building entity damage / open (BuildingEntityDamaged, BuildingEntityOpened) (Runtime/Session/World/WorldStateMessageService.cs:152-193, Handlers/BuildingEntityDamagedHandler.cs, Handlers/BuildingEntityOpenedHandler.cs, GameAdapter/World/WorldBuildingEntitySync.cs, Patches/BuildingEntityPatches.cs) | Both: guest -> host report (host applies to its own copy and relays), host -> guest broadcast relay (source excluded). | Dedicated messages NetMsg.BuildingEntityDamaged = 51 and NetMsg.BuildingEntityOpened = 52, both bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:36 'BuildingEntityDamaged = 51, // guest → host: report (host applies + relays); host → guest: broadcast relay (source excluded)' 'BuildingEntityOpened = 52, // guest → host: a crate/lock was opened (health = 0 write path); host → guest: broadcast relay (source excluded)'). Real triggers: the Body.Attack postfix detects the native health write by before/after diff (src/CasualtiesUnknownOnline.GameAdapter/Patches/BodyPatches.cs:117 'PatchBridge.Impl?.OnBuildingEntityDamaged(entity, before - entity.health, playHitSound: true, playHitFlash: true);'); the three open paths (Openable.OnUse, LockpingMinigame, KeypadMinigame) report the direct health = 0 write (src/CasualtiesUnknownOnline.GameAdapter/Patches/OpenablePatches.cs:32 'PatchBridge.Impl?.OnBuildingEntityOpened(build);'). Report send: src/CasualtiesUnknownOnline.GameAdapter/World/WorldBuildingEntitySync.cs:51 '_world.SendBuildingEntityDamaged(new NetVector2(pos.x, pos.y), damage, playHitSound, playHitFlash);'. Channel: host broadcast (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:187 '_session.Broadcast(NetMsg.BuildingEntityDamaged, msg);') or guest report ( '_sender.Send(_session.HostSteamId, NetMsg.BuildingEntityDamaged, msg);'). Reliable by PacketSender default (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29 'public void Send(ulong steamId, NetMsg msg, object? payload = null, bool reliable = true)'). Wire members: BuildingEntityDamaged, BuildingEntityOpened. | 60 s absolute resend: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 'if (IsHostMode && _session.SessionActive && Time.unscaledTime - _lastSnapshotResend > 60f)' -> '_kernelProtocol.SendCheckpoint(member.SteamId); // the kernel checkpoint also covers lazy-session recovery for WorldEntities and the other kernel domains', sent per in-world member . The checkpoint carries the host's building-health facts, recorded on every applied damage (src/CasualtiesUnknownOnline.GameAdapter/World/WorldBuildingEntitySync.cs:54 '_world.ReportBuildingEntityHealth(pos.x, pos.y, entity.health); // host-only — the late-joiner snapshot's fact source'; remote-apply path ), and the opened-entity facts ( 'an open is health = 0 — the snapshot covers it too'). Apply is absolute overwrite: src/CasualtiesUnknownOnline.GameAdapter/World/WorldBuildingEntitySync.cs:225 'entity.health = health;' and an opened entity becomes health 0 + RemoteEntityDeath. Cadence is a hardcoded Time.unscaledTime > 60f; there is no AdaptiveStreamId for world entities and WorldEventSync has no AdaptiveStreamRateService, so the adaptive rate governor cannot stretch or remove it. | World entry / late join / reconnect: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41 '_kernelProtocol.SendCheckpoint(steamId);' inside the ordered snapshot group that ends with WorldSnapshotComplete; a mid-generation handshake also sends it (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192 '_kernelProtocol.SendCheckpoint(sender);'). Guest restore raises WorldEntityKernelProjection.OnCheckpointRestored (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntityKernelProjection.cs:140 '_projectionHealth.Run("world-entities", checkpoint.GlobalRevision, () => Project(checkpoint));') -> BuildingHealthProjected / OpenedEntitiesProjected -> WorldBuildingEntitySync.OnBuildingHealthProjected / OnOpenedEntitiesProjected , applied with the same semantics as the live relay. Missed-range recovery: src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:351 'RequestRange(expected, batch.GlobalRevision - 1);' -> HandleRangeRequest replays journal batches or sends a fresh checkpoint. | Reliable channel for the live event and for the kernel committed batches / checkpoint (PacketSender default reliable = true). A lost host -> guest relay converges: the next 60 s checkpoint or world entry restores the host's absolute health / opened fact. A lost guest -> host report is NOT re-reported and there is no guest diff table: if the host already holds a fact for that position, the next checkpoint overwrites the guest's local damage with the host's stale value (silent revert); if the host never recorded that position, the damage stays host-unknown and the divergence persists until a later host write. A destroyed entity cannot be revived: a positive health report after a recorded 0 is rejected (src/CasualtiesUnknownOnline.GameState/Domains/WorldEntities/WorldEntityDomainModule.cs:57). | OK |  |
| E2 | Trap/mechanism events (EntityEvent) (Runtime/Session/World/EntityEventChannel.cs, Handlers/EntityEventHandler.cs, GameAdapter/World/TrapEntityScan.cs, TrapEffectApplier.cs, TrapVisualReplay.cs) | Both: guest -> host report, host -> guest broadcast relay (source excluded); the host applies the event to its own world before relaying. | Dedicated message NetMsg.EntityEvent = 66, bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:48 'EntityEvent = 66, // bidirectional: guest → host report of a triggered trap event; host → guest broadcast relay (source excluded — the host applies the event to its own world first)'); EntityEventMsg carries the numeric EntityEventKind, position, Extra, drops and (for checkpoint replay) ElapsedSeconds. Real triggers are the Harmony trap patches observing the native transition, e.g. the mine pressed -> exploded edge (src/CasualtiesUnknownOnline.GameAdapter/Patches/TrapMineExplosionPatch.cs:35 'PatchBridge.Impl?.OnTrapTriggered(EntityEventKind.MineExploded, __instance.transform.position, 0);') plus the OnDestroy chain path, and the equivalent edge patches for bear trap, turret, geyser, crystals, lifepod, unlocks, etc. Report send: EntityEventSync.OnTrapTriggered -> _world.SendEntityEvent (src/CasualtiesUnknownOnline.GameAdapter/World/EntityEventSync.cs:70 '_world.SendEntityEvent(new EntityEventMsg'); channel guest report (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:65 '_sender.Send(_session.HostSteamId, NetMsg.EntityEvent, msg);') or host broadcast ( '_session.Broadcast(NetMsg.EntityEvent, msg);'). Reliable by PacketSender default. Wire members: EntityEvent. | 60 s absolute kernel checkpoint: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 'if (IsHostMode && _session.SessionActive && Time.unscaledTime - _lastSnapshotResend > 60f)' -> '_kernelProtocol.SendCheckpoint(member.SteamId); // the kernel checkpoint also covers lazy-session recovery for WorldEntities and the other kernel domains'. The WorldEntities facts (one-shot consumptions + durable trap-state machine) are recorded by the host on every trigger (src/CasualtiesUnknownOnline.Runtime/Session/World/TrapStateRegistry.cs:159 '_kernelAuthority.TryExecuteCommand(composite, _session.LocalSteamId, out _, out _);') and projected back on the guest through TrapSnapshotProjected (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntityKernelProjection.cs:107 'TrapSnapshotProjected?.Invoke(projected);') -> EntityEventSync.OnTrapStateProjected replay. Transient repeatable kinds (GeyserActivated, TurretFired) and Warning edges are deliberately excluded from the snapshot (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntityKernelProjection.cs:86 'if (EntityEventProfiles.IsOneShotConsumption((EntityEventKind)trapState.Kind)' plus IsTransientTrapState), because the native cooldown re-arms the entity and a resend would replay a stale shot/eruption. Cadence is hardcoded; no adaptive governor applies. | World entry / late join / reconnect: the generated trap layout snapshot (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:39 '_world.SendTrapLayoutSnapshot(steamId);') aligns the guest's entity layout (TrapLayoutApplication materializes missing entries / destroys surplus) and the kernel checkpoint ( '_kernelProtocol.SendCheckpoint(steamId);') restores the one-shot consumptions and durable trap states, replayed by EntityEventSync.OnTrapStateProjected (src/CasualtiesUnknownOnline.GameAdapter/World/EntityEventSync.cs:155, apply at '_replay.Replay(msg.Kind, new Vector2(pos.X, pos.Y), msg.Extra, msg.ElapsedSeconds);'). Reconnect uses the same WorldEntryFanout group. Missed-range recovery: kernel range request (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:351 'RequestRange(expected, batch.GlobalRevision - 1);') replays journal batches or a fresh checkpoint. | Reliable channel (PacketSender default). A lost host -> guest relay converges for one-shot and durable kinds: the next checkpoint replays the consumption/state, and the per-entity guards drop duplicates. Transient repeatable kinds (GeyserActivated, TurretFired) are an explicitly accepted loss with a recorded reason (they re-arm natively; replaying them would be stale). A lost guest -> host report is not re-reported: the host's trap stays armed while the triggering guest's copy is already consumed, and the checkpoint only carries host-side facts, so no mechanism reconciles that direction (accepted star-topology proposal loss, same pattern as E1). | OK |  |
| E3 | Runtime entity spawn (EntitySpawned) (Runtime/Session/World/EntityEventChannel.cs:202-218, Handlers/EntitySpawnedHandler.cs, GameAdapter/World/EntitySpawnSync.cs) | Both: the creating side reports (keeps its local copy), the host creates its own copy and relays (source excluded). | Dedicated message NetMsg.EntitySpawned = 68, bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:51 'EntitySpawned = 68, // bidirectional: the creating side reports (keeps its local copy); the host creates its own and relays (source excluded) — items ride the item domain, entities ride this'). Real trigger: the BuildingEntity.Start postfix (src/CasualtiesUnknownOnline.GameAdapter/Patches/BuildingEntityStartPatch.cs:15 'private static void Postfix(BuildingEntity __instance) => PatchBridge.Impl?.OnEntityInstantiated(__instance);'), filtered by generation/RemoteApply/SpawnReplayMarker; creation-time data (keypad code, geyser liquid type, crystal tint) rides the same message. Report send: src/CasualtiesUnknownOnline.GameAdapter/World/EntitySpawnSync.cs:349 '_world.SendEntitySpawned(new EntitySpawnedMsg'; channel guest report (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:206 '_sender.Send(_session.HostSteamId, NetMsg.EntitySpawned, msg);') or host relay ( '_session.Broadcast(NetMsg.EntitySpawned, msg);'). Reliable by PacketSender default; FindExisting (1 m radius) makes the relay idempotent. Wire members: EntitySpawned. | none. The only EntitySpawned send sites are the report (src/CasualtiesUnknownOnline.GameAdapter/World/EntitySpawnSync.cs:349 '_world.SendEntitySpawned(new EntitySpawnedMsg') and the host relay (same file '_world.SendEntitySpawned(relay);'); there is no periodic re-report and no entity-list snapshot. The host's 60 s cycle (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 'if (IsHostMode && _session.SessionActive && Time.unscaledTime - _lastSnapshotResend > 60f)') re-sends only block state, block damage, the kernel checkpoint and keypad codes ( '_kernelProtocol.SendCheckpoint(member.SteamId); // the kernel checkpoint also covers lazy-session recovery for WorldEntities and the other kernel domains'; 'SendKeypadCodes(); // re-send the full set (idempotent — set codes are left alone) — covers the lazy-session swallow window and keypads created after the first send (the airdrop/command case, #128 follow-up)'), and the kernel WorldEntities state holds only consumption/health/opened/trap-state facts (src/CasualtiesUnknownOnline.GameState/Domains/WorldEntities/WorldEntityState.cs:11 'public sealed record WorldEntityState(' with members Consumptions/BuildingHealth/OpenedEntities/TrapStates), never a spawned-entity table. The 60 s keypad/geyser re-sends are position-keyed onto already-existing local entities (src/CasualtiesUnknownOnline.GameAdapter/World/GeyserStateSync.cs:140 'foreach (var geyser in Object.FindObjectsOfType<GeyserScript>())'), so they can heal a carried value but cannot materialize a missing entity. EntitySpawnSync's own comment assumes the geyser-state cycle covers a lost report ( 'if (geyser == null) // Unity object — == (destroyed — the 60 s geyser-state cycle covers a lost report)'), which only holds for the liquid type, not the entity. | none for BuildingEntity creations. The world-entry fan-out (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:37-44: block state, block damage, trap layout, radiation line, kernel checkpoint, item snapshot, enemy snapshot, completion marker) has no entity-creation snapshot. The only runtime-spawn backfill in the project is for enemies/animals: EnemySnapshot.RuntimeSpawns (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:285 'RuntimeSpawns =') -> EnemySyncCoordinator.MaterializeRuntimeSpawns, which binds/materializes animal copies only. A reconnect regenerates the deterministic world and re-applies the kernel checkpoint, which contains no runtime-created entity, so the copy stays missing. | Reliable event with idempotent FindExisting dedup, but a swallowed report (the documented lazy-P2P ~30 s window: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:106 '/// ~30 s after world entry — world-mutation broadcasts sent in that window') leaves a permanently one-sided entity: the creating side keeps its local copy, the host never creates/relays it, and no periodic or backfill mechanism re-sends the creation. No convergence guarantee; the divergence survives until a new layer regenerates the world. | Event-only gap | todo/runtime-entity-spawn-backfill.md |
| E4 | DynamiteExplosion (Runtime/Session/World/EntityEventChannel.cs:111-133, Handlers/DynamiteExplosionHandler.cs, GameAdapter/World/DynamiteExplosionSync.cs) | Both: guest -> host report, host -> guest broadcast relay (source excluded); the host applies the native explosion to its own world, guests replay the body/visual segment. | Dedicated message NetMsg.DynamiteExplosion = 105, bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:152 'DynamiteExplosion = 105, // bidirectional: guest → host report of a dynamite detonation; host → guest broadcast relay (source excluded)'). Real trigger: the postfix on the native detonation (src/CasualtiesUnknownOnline.GameAdapter/Patches/DynamiteExplodePatch.cs:30 'PatchBridge.Impl?.OnDynamiteExploded(itemId, __instance.transform.position);' on CustomItemBehaviour.DynamiteExplode, after the native explosion already ran). Report send: src/CasualtiesUnknownOnline.GameAdapter/World/DynamiteExplosionSync.cs:66 '_world.SendDynamiteExplosion(itemId, new NetVector2(position.x, position.y));'; channel guest report (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:115 '_sender.Send(_session.HostSteamId, NetMsg.DynamiteExplosion, msg);') or host broadcast ( '_session.Broadcast(NetMsg.DynamiteExplosion, msg);'). Reliable by PacketSender default; one-shot identity is the ItemInstanceId, with a seen-set duplicate guard (src/CasualtiesUnknownOnline.GameAdapter/World/DynamiteExplosionSync.cs:73 'if (itemId != 0 && !_seen.Add(itemId))'). Wire members: DynamiteExplosion. | none for the detonation event itself — a one-shot explosion has no absolute state to re-send, and there is no explosion snapshot anywhere. The durable consequences are healed by sibling rows: terrain by the block-state 60 s resend (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 'if (IsHostMode && _session.SessionActive && Time.unscaledTime - _lastSnapshotResend > 60f)'), buildings by the world-entities kernel checkpoint in the same cycle ( '_kernelProtocol.SendCheckpoint(member.SteamId); // the kernel checkpoint also covers lazy-session recovery for WorldEntities and the other kernel domains'), world items by the item domain's own snapshot fallback. Host apply is the native explosion inside RemoteApply (src/CasualtiesUnknownOnline.GameAdapter/World/DynamiteExplosionSync.cs:84 'WorldGeneration.CreateExplosion(ExplosionParams(pos));'); guest replay is visual + real-body only ( '_replay.ReplayExplosion(ExplosionParams(pos));'). | No explosion replay on world entry; a late joiner correctly never re-runs a past detonation. The aftermath is reconstructed from the same world-entry snapshot group (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:37-43): block state/damage carry the crater, the kernel checkpoint carries destroyed building health, the item snapshot carries the dropped items. Reconnect uses the same group. Missed-range recovery is kernel-only (committed batches / checkpoint), not an explosion replay. | Reliable event plus ItemInstanceId dedup, so a retransmit cannot double-apply. A swallowed event is not replayed for the blast's visual + instantaneous local-body damage segment; the world-state consequences still arrive through their own reliable channels (the trigger side already ran the native explosion and reported terrain/buildings/items) and converge via the block/building/item fallbacks named above. The 5 s lit-fuse visual on remote clones is a documented accepted local-only presentation residual (docs/evidence/selfchecks/items/dynamite-explosion-selfcheck.md:26). ADJUDICATED (adversarial review): the detonation event itself is one-shot with no fallback; the durable consequences ride the block / building / item fallbacks named in this row. Kept OK with that dependency stated explicitly. | OK |  |
| E5 | RadiationLineState (Runtime/Session/World/WorldStateMessageService.cs:240-262, Handlers/RadiationLineStateHandler.cs, GameAdapter/World/RadiationLineSync.cs) | Host -> guest only (host owns the line's active/timeGone world state; guests apply the absolute state and keep running their local per-frame presentation). | Dedicated message NetMsg.RadiationLineState = 106, HostToGuest (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:157 'RadiationLineState = 106, // host → guest: the authoritative radiation-line state (Active + TimeGone)'). There is no separate one-shot activation message: the real transition is detected in the host pump (src/CasualtiesUnknownOnline.GameAdapter/World/RadiationLineSync.cs:93 '&#124;&#124; active != _lastActive') and published immediately by Publish ( '_world.BroadcastRadiationLineState(new RadiationLineStateMsg') -> src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:248 '_session.Broadcast(NetMsg.RadiationLineState, state);'). The line's trigger itself is the vanilla layer timer or the host straggler policy (RadiationLineSync.TryActivateForStragglers). Reliable by PacketSender default. Wire members: RadiationLineState. | 5 Hz absolute resend while the line is active: src/CasualtiesUnknownOnline.GameAdapter/World/RadiationLineSync.cs:36 'private const float BroadcastIntervalSeconds = 0.2f;' applied by the due test ( '&#124;&#124; (active && Time.unscaledTime - _lastBroadcastTime >= BroadcastIntervalSeconds);'). Apply is absolute overwrite: 'line.active = true;' plus WriteTimeGone(msg.TimeGone) (or line.Deactivate() for the inactive state). The interval is a hardcoded constant; RadiationLineSync has no AdaptiveStreamRateService dependency and no AdaptiveStreamId covers it, so the adaptive rate governor cannot stretch or remove the resend. | World entry / late join / reconnect: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:40 '_world.SendRadiationLineState(steamId);' sends the stored absolute snapshot (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldStateMessageService.cs:259 '_sender.Send(targetSteamId, NetMsg.RadiationLineState, RadiationLineState);'), which is updated on every publish and by the solo/menu path (src/CasualtiesUnknownOnline.GameAdapter/World/RadiationLineSync.cs:87) so a solo -> lobby conversion hands the current state to the first guest. A new generation resets _hasPublished , so the first post-generation frame re-publishes the now-inactive line. No missed-range mechanism is needed (single small absolute message, reliable). | Reliable channel. The line is one-way per layer (once active it stays active until the next layer regenerates it), so the only loss that matters is a lost activation — healed within 0.2 s by the active-state resend. A lost deactivation does not exist: a new layer resets the state and the first frame re-publishes it (src/CasualtiesUnknownOnline.GameAdapter/World/RadiationLineSync.cs:92). Convergence is guaranteed by absolute overwrite of active + timeGone. | OK |  |
| E6 | WorldBloodSpawn (transient decal) (Runtime/Session/World/EntityEventChannel.cs:161-177, Handlers/WorldBloodSpawnHandler.cs, GameAdapter/World/WorldBloodSync.cs, WorldBloodReplay.cs, Runtime/Protocol/Messages/WorldBloodSpawnMsg.cs) | Both: guest -> host report, host -> guest broadcast relay (source excluded); each receiver replays the decal on its own world. | Dedicated message NetMsg.WorldBloodSpawn = 121, bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:218 'WorldBloodSpawn = 121, // bidirectional: guest → host report of a local player''s blood decal; host → guest broadcast relay (source excluded)'). Real trigger: the postfix observing the native BleedParticle dying-particle decal spawn (src/CasualtiesUnknownOnline.GameAdapter/Patches/BleedParticleWorldBloodPatch.cs:126 'PatchBridge.Impl?.OnWorldBloodSpawn(position, ground);'), only for the local player's own body (remote clones are suppressed). Report send: src/CasualtiesUnknownOnline.GameAdapter/World/WorldBloodSync.cs:43 '_world.SendWorldBloodSpawn(new WorldBloodSpawnMsg'; channel guest report / host relay (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:152-177; host relay at src/CasualtiesUnknownOnline.Runtime/Session/Handlers/WorldBloodSpawnHandler.cs:24 'ctx.Session.BroadcastExcept(sender, NetMsg.WorldBloodSpawn, msg);'). Reliable (default; the selfcheck records ''one reliable WorldBloodSpawnMsg''). Wire members: WorldBloodSpawn. | none, explicitly by design. The channel doc states it: src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:150 '/// transient and has no snapshot fallback.' and the message doc repeats it: src/CasualtiesUnknownOnline.Runtime/Protocol/Messages/WorldBloodSpawnMsg.cs:13 '/// no periodic snapshot is used.'. The decal self-destructs after 120 s (src/CasualtiesUnknownOnline.GameAdapter/World/WorldBloodReplay.cs:53 'Object.Destroy(decal, 120f);'), so there is no durable state to reconcile. | none. The world-entry fan-out (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:37-44) carries no blood-decal snapshot, and a late joiner or reconnect sees no previously spawned decals — accepted because the decal is presentation-only and expires in 120 s. No missed-range mechanism exists or is needed. | Reliable event; loss is explicitly accepted as presentation-only degradation (src/CasualtiesUnknownOnline.GameAdapter/World/WorldBloodSync.cs:9-15 and docs/evidence/selfchecks/world/world-blood-spawn-sync-selfcheck.md:6-7 ''transient (120 s lifetime, no periodic snapshot), so a lost message is presentation-only degradation''). No body or world state is touched, so no convergence mechanism is required. | Transient-by-design |  |
| E7 | WorldEntities kernel facts (trap phase/consumption, opened entities, building health) (src/CasualtiesUnknownOnline.GameState/Domains/WorldEntities/*, Runtime/Session/World projection files, docs/architecture/domains.md) | Host -> guest. The facts are committed by host-only kernel commands; guests report the underlying game event through the direct channels (E1/E2) and receive the facts as committed batches / checkpoints. | The facts ride NetMsg.KernelEnvelope = 122 as EnvelopeKind.CommittedBatch (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:223 'KernelEnvelope = 122, // bidirectional: the four-envelope kernel protocol rides one message id'). The trigger is the host command commit, e.g. the atomic trap batch (src/CasualtiesUnknownOnline.Runtime/Session/World/TrapStateRegistry.cs:159 '_kernelAuthority.TryExecuteCommand(composite, _session.LocalSteamId, out _, out _);') and the building-health / opened-entity commands (ItemKernelAuthority.TryRecordBuildingEntityHealth / TryRecordOpenedEntity). KernelProtocolService subscribes to the commit event (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:58 '_authority.BatchCommitted += BroadcastCommittedBatch;') and broadcasts the batch (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:91 'SendToGuests(frame);') through a reliable send (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:497 '_sender.Send(member.SteamId, NetMsg.KernelEnvelope, frame, reliable);' with SendToGuests default reliable = true). WorldEntities facts do NOT ride StateStreamEnvelope; HandleStateStream only surfaces item moves/item states/player-enemy streams. Wire members: BuildingEntityHealthUpdated, OpenedEntity, RecordBuildingEntityHealth, RecordOpenedEntity, RecordTrapConsumed, RecordTrapState, ResetWorldEntities, TrapConsumed, TrapStateChanged, WorldEntitiesReset. | 60 s absolute kernel checkpoint to every in-world member: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 'if (IsHostMode && _session.SessionActive && Time.unscaledTime - _lastSnapshotResend > 60f)' -> '_kernelProtocol.SendCheckpoint(member.SteamId); // the kernel checkpoint also covers lazy-session recovery for WorldEntities and the other kernel domains'. Apply is an absolute checkpoint restore: src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:474 'var result = _authority.Restore(checkpoint);' replaces the kernel state and raises CheckpointRestored -> WorldEntityKernelProjection.OnCheckpointRestored (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntityKernelProjection.cs:140 '_projectionHealth.Run("world-entities", checkpoint.GlobalRevision, () => Project(checkpoint));') -> the flat fact lists applied by EntityEventSync / WorldBuildingEntitySync. Additionally the projection is rebuilt from QueryWorldEntities when a projection apply throws (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntityKernelProjection.cs:136 'private void RebuildFromKernel() =>'). The cadence is hardcoded; there is no AdaptiveStreamId for world entities, so the adaptive governor cannot stretch it. | World entry / late join: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41 '_kernelProtocol.SendCheckpoint(steamId);' (same ordered group that ends with WorldSnapshotComplete). Mid-generation handshake: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192 '_kernelProtocol.SendCheckpoint(sender);'. Reconnect uses the same WorldEntryFanout group. Missed-range recovery: KernelProtocolService.HandleCommittedBatch detects a revision gap (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:345 'var expected = _authority.CurrentGlobalRevision + 1;') and requests the range ( 'RequestRange(expected, batch.GlobalRevision - 1);'); HandleRangeRequest replays the journal batches or sends a fresh checkpoint. Session end / new layer clears the facts (WorldStateMessageService.ResetSessionState / ResetDamagedBlocks). | Committed batches and checkpoint chunks are reliable (PacketSender default reliable = true), and apply is idempotent by operation id (ItemKernelAuthority.Apply) and by position-keyed fact replacement (WorldEntityState.WithConsumption/WithBuildingHealth/WithTrapState replace by position). A swallowed batch is recovered by the revision-gap range request or by the next absolute checkpoint (<= 60 s). Domain invariants reject illegal transitions: a destroyed building cannot be revived (src/CasualtiesUnknownOnline.GameState/Domains/WorldEntities/WorldEntityDomainModule.cs:57) and illegal trap-phase transitions are rejected . Convergence is the absolute WorldEntities state from the host. | OK |  |
| F1 | Fluid region stream (10 Hz diff + 1 Hz full viewport) (Runtime/Session/World/EntityEventChannel.cs:224-237 + Session/Handlers/FluidRegionHandler.cs; GameAdapter/World/FluidWorldSync.cs, FluidSimulationAuthority.cs, FluidRegionApplication.cs; Runtime/Session/World/FluidKernelViewSync.cs) | host -> guest (unicast per member, unreliable) | NetMsg.FluidRegion = 70 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:57), host->guest only (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/FluidRegionHandler.cs:14). Not a per-operation event: the grid is convergent continuous state, so the trigger-driven path is the change-gated diff - FluidSimulationAuthority.Update computes DiffBox against the member's last-SENT baseline and calls SyncRegion only when the viewport differs (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:306-309). Send: src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:477 -> IWorldControl.SendFluidRegion -> EntityEventChannel.SendFluidRegion with reliable: false (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:231). Wire members: FluidRegion, FluidRegionDiffStream, FluidRegionFullStream. | 1 Hz full-viewport absolute RLE overwrite: base interval 1000 ms for AdaptiveStreamId.FluidRegionFullStream (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:97-107), queried per member at src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:280-282 and sent at src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:311-314. Apply semantics = absolute overwrite of every cell of the rectangle, with the omitted trailing zero runs cleared (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidRleCodec.cs:48-52 -> src/CasualtiesUnknownOnline.GameAdapter/World/FluidRegionApplication.cs:29-32). The adaptive rate governor CAN stretch it: AdaptiveRatePolicy.GetEffectiveIntervalMs divides the base interval by the pressure factor (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveRatePolicy.cs:68) and the hard cap is MaxIntervalMs: 10_000 (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:107); priority-2 Critical factor 0.15 gives ~6667 ms, capped at 10 s. The diff stream itself is 10 Hz base with MinHz 1 (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:88-96). Both classes are actually measured via WirePayloadType.FluidRegionFull/Diff (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:103-106). | A member's first stream is always a full viewport: a fresh MemberView has Center (0,0) and an all-zero baseline, so the first anchor mismatch takes the center-change branch (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:299-303); a member with no remote player yet is skipped and healed by the 1 Hz full on entry (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:360); moving to a new area re-sends the full viewport (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:302); session reset clears every baseline (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:330). No Seq-based missed-range recovery - msg.Seq is only logged (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/FluidRegionHandler.cs:23), there is no gap request, retransmit or ack. | Unreliable by design (reliable: false, src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:231); the channel comment records that every region is an absolute overwrite so a lost one is healed by the next, and that a reliable stream would queue the newest snapshot behind retransmissions (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:223). The diff baseline view.Grid is advanced while encoding (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:449 ), so a lost diff is NOT re-derived by a later diff - only the 1 Hz full viewport heals it. Convergence to the host grid is guaranteed within the full interval (<=1 s healthy, <=10 s under Critical pressure). No ack, no retransmit, no gap detection. | OK |  |
| F2 | Fluid interaction / drinking (FluidInteraction) (Runtime/Session/World/EntityEventChannel.cs:239-276 + Session/Handlers/FluidInteractionHandler.cs; GameAdapter/World/FluidInteractionSync.cs) | both (guest -> host report; host -> guest broadcast relay) | NetMsg.FluidInteraction = 71, bidirectional (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:58). Real trigger: FluidDrinkPatch Postfix on FluidManager.DrinkLiquid (src/CasualtiesUnknownOnline.GameAdapter/Patches/FluidDrinkPatch.cs:16-28) -> FluidInteractionSync.OnDrinkReported (src/CasualtiesUnknownOnline.GameAdapter/World/FluidInteractionSync.cs:31-45) -> IWorldControl.SendFluidInteraction -> EntityEventChannel.SendFluidInteraction: guest->host report via _sender.Send (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:258), host->all via Broadcast (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:254). The host applies to its own grid and relays to the other members, source excluded (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/FluidInteractionHandler.cs:19-25). Reliable both ways: PacketSender.Send defaults reliable = true (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29) and Broadcast -> SendToAll also defaults reliable = true (src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs:132-136). Wire members: FluidInteraction. | No dedicated FluidInteraction fallback - a drink is never re-reported. The resulting grid state is healed by row F1's 1 Hz full-viewport absolute stream (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:311-314). The code records this as the fallback for the un-reported bath-soiled SetLiquid(5): low-frequency/low-perception, healed by the 1 Hz viewport snapshot, recorded design decision (src/CasualtiesUnknownOnline.GameAdapter/World/FluidInteractionSync.cs:15-17). | No per-drink replay log and no drink facts in the kernel checkpoint. The world-entry/reconnect backfill is the grid itself: F1's first full viewport on entry (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:299-303) carries the host's post-drink grid, so a drink that happened before the member joined is already reflected. Accepted drinks are relayed to the other in-world members (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/FluidInteractionHandler.cs:24). | Reliable in both directions (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29). If a report is nevertheless swallowed (transport/session loss; there is no application-level retry), the host never clears the cell and the guest's locally cleared cell is refilled by the next full viewport (<=1 s, <=10 s under pressure) - the grid converges to the host's authority but the drink's grid effect is not re-applied. If the host's relay to a third guest is lost, that guest's stale water is cleared by the next full viewport. Duplicate/re-ordered events are idempotent: an already-empty cell is ignored (src/CasualtiesUnknownOnline.GameAdapter/World/FluidInteractionSync.cs:66-69). | OK |  |
| F3 | Fluid presentation (water push / waterflow sound, FluidPresentation) (Runtime/Session/World/EntityEventChannel.cs:278-298 + Session/Handlers/FluidPresentationHandler.cs; GameAdapter/World/FluidPresentationApplication.cs) | host -> guest (unicast to guests whose viewport contains the cell) | NetMsg.FluidPresentation = 96, host->guest only (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:123; src/CasualtiesUnknownOnline.Runtime/Session/Handlers/FluidPresentationHandler.cs:12). Real trigger = the host's SimulateBand: water push every 11 moves when liquidPushing (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:201-224, send at ) and waterflow sound every 17 tile moves (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:148-162, send at ). Send: SendPresentation -> IWorldControl.SendFluidPresentation (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:245) -> EntityEventChannel.SendFluidPresentation with reliable: true (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:291), targeted at each guest whose viewport contains the cell (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:229-249). Guest replay: FluidPresentationApplication.Apply (src/CasualtiesUnknownOnline.GameAdapter/World/FluidPresentationApplication.cs:23-49). Wire members: FluidPresentation. | none. The only send call site is src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:245 -> src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:291; there is no periodic re-send, no world-entry replay and no snapshot fallback. The code comment states a lost push would make the guest miss a local-body physics effect that the next streamed grid snapshot cannot heal (src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:281-283), and the recorded self-check agrees (docs/evidence/selfchecks/fluids/fluid-presentation-selfcheck.md:72-73). | none. A member entering or re-entering the world receives only future pushes/sounds; the 0.75 s WaterPusher and the sound are not replayed (src/CasualtiesUnknownOnline.GameAdapter/World/FluidPresentationApplication.cs:54-63). | Reliable by design (reliable: true, src/CasualtiesUnknownOnline.Runtime/Session/World/EntityEventChannel.cs:291) because the effect is unrecoverable, but the send is fire-and-forget (PacketSender.Send is void; a transport-level failure is only recorded in traffic stats, src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:47-49), so a swallowed message is simply lost. The transient is accepted as non-replayable by design: it is a moment-in-time local-body push / sound with a 0.75 s lifetime (src/CasualtiesUnknownOnline.GameAdapter/World/FluidPresentationApplication.cs:62), and the recorded self-check (docs/evidence/selfchecks/fluids/fluid-presentation-selfcheck.md:72-73) states the grid snapshot cannot heal it. No convergence guarantee for the presentation itself. | Transient-by-design |  |
| F4 | Fluid kernel facts (UpdateFluidRegion / ResetFluids / FluidRegionUpdatedEvent) (src/CasualtiesUnknownOnline.GameState/Domains/Fluids/*; Runtime/Session/World/FluidRegionKernelSync.cs (GameAdapter), FluidKernelProjection.cs, FluidKernelReadProjection.cs; GameAdapter/World/FluidRegionKernelSync.cs, FluidKernelViewSync.cs) | host -> guest (kernel CommittedBatch over KernelEnvelope) | none at the real trigger (a fluid-grid change). The kernel fluid table has no per-change event: the only production writer is the host's 5 s aggregation. The vocabulary exists - UpdateFluidRegionCommand -> FluidRegionUpdatedEvent (src/CasualtiesUnknownOnline.GameState/Domains/Fluids/FluidDomainModule.cs:20) and ResetFluidsCommand -> FluidsResetEvent (src/CasualtiesUnknownOnline.GameState/Domains/Fluids/FluidDomainModule.cs:21) - but the update command is issued only by the periodic projection (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidKernelProjection.cs:63-75) and ResetFluidsCommand has no production caller (only src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:241 defines TryResetFluids; no call site exists). Wire members: FluidRegionUpdated, FluidsReset, ResetFluids, UpdateFluidRegion. | 5 s coarse aggregation: SummaryInterval = 5f (src/CasualtiesUnknownOnline.GameAdapter/World/FluidRegionKernelSync.cs:22), timer at , ReportFluidRegions at aggregates the whole grid into chunk totals/dominant type; FluidKernelProjection.Sync change-gates the upserts and writes zero totals for chunks that left the non-empty set (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidKernelProjection.cs:56-60); Commit -> ItemKernelAuthority.TryUpdateFluidRegion (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:230) -> FluidRegionUpdatedEvent -> CommittedBatch broadcast reliably to guests (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:91 SendToGuests, reliable = true). Apply is an absolute upsert keyed by (ChunkX, ChunkY) (FluidStateTable.Upsert). This is a reliable kernel batch, not an adaptive stream, so the rate governor does not apply. | The FluidStateTable is a checkpoint field (src/CasualtiesUnknownOnline.GameState/GameCheckpoint.cs:26) and rides the wire checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/WireCheckpointAssembler.cs:88-90); it is sent on world entry (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41) and re-sent every 60 s (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:131). The guest rebuilds the coarse facts from checkpoint restore and applied batches (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidKernelReadProjection.cs:85-97) and FluidKernelViewSync mirrors them into a log-only list (src/CasualtiesUnknownOnline.GameAdapter/World/FluidKernelViewSync.cs:35-42). The facts never write the Unity grid. | Reliable batches (SendToGuests default reliable = true, src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:493) plus the 5 s re-aggregation and the 60 s checkpoint resend, so a lost batch converges on the next differing aggregate or checkpoint. Convergence is to the host grid, but the coarse facts lag the grid by up to 5 s and are a checkpoint/diagnostic surface, not the render path (recorded in docs/evidence/selfchecks/architecture/phase-d-fluids-shadow-selfcheck.md:71-72). ADJUDICATED: continuous field, no discrete trigger exists; the kernel fact is a deliberate 5 s coarse aggregation and the render path is the F1 stream. Finding: ResetFluidsCommand has no production caller; docs/architecture/domains.md corrected in this cycle. | OK | docs/architecture/domains.md correction |
| F5 | Fluid world-entry / reconnect backfill (Runtime/Session/World/WorldEntryFanout.cs, Runtime/Session/World/FluidKernelReadProjection.cs, GameAdapter/World/FluidKernelViewSync.cs, kernel checkpoint projection) | host -> guest (world-entry snapshot group + kernel checkpoint) | n/a for the backfill row - the event paths are F1 (grid diff), F2 (drink) and F4 (kernel facts). The backfill triggers are the world-entry edges: SceneStateHandler InWorld edge (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs:42-59) and the handshake reconnect path (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:143-145) both call WorldEntryFanout.Send. | The recovery channels are the kernel checkpoint group (re-sent every 60 s, src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122-131) and row F1's 1 Hz full-viewport absolute stream (<=10 s under Critical pressure, src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:311-314). | WorldEntryFanout.Send (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:34-45) sends block state, block damage, trap layout, radiation line, kernel checkpoint (incl. Fluids, src/CasualtiesUnknownOnline.Runtime/Session/Items/WireCheckpointAssembler.cs:88-90), item snapshot, enemy snapshot and the completion marker - there is no fluid-grid snapshot in the group. The grid is backfilled by F1's first full viewport when the member's anchor appears (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:299-303 ) and by a full viewport on every center change (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:302). Reconnect re-fires the fan-out (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:145) and the session reset clears all fluid baselines (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:330). The guest-side kernel facts are rebuilt from the checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidKernelReadProjection.cs:94). | Checkpoint chunks are reliable (KernelProtocolService.Send -> PacketSender default reliable = true); the grid stream is unreliable and converges through the full viewport. There is no explicit fluid-snapshot completion marker - src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:44 marks the group complete, not the fluid grid; grid completeness rests on the 1 Hz full viewport, so a member can render its own generated grid for up to the full interval after entry (<=1 s healthy, <=10 s under pressure). | OK |  |
| I1 | Item kernel commands/events (spawn, pickup, drop, destroy, transfer, update state, container sync) (Runtime/Session/Items/ItemService.cs, KernelProtocolCommandHandler.cs, GameState/Domains/Items/*, Runtime item projections) | both (guest->host CommandEnvelope; host->guest CommittedBatch broadcast) | Dedicated CommandEnvelope payloads on NetMsg.KernelEnvelope=122: WirePayloadType.ItemSpawnCommand=1 / ItemPickupCommand=2 / ItemDropCommand=3 / ItemDestroyCommand=4 / ItemUpdateStateCommand=5 / ItemTransferCommand=6 / ItemContainerSyncCommand=7 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs:12-18). Trigger = the adapter's verified commit funnel (ItemReportCommitter.CommitReport) -> ItemMessageFlowService.SendItemSpawned/PickedUp/Dropped/Destroyed -> _kernelProtocol.SendCommand (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemMessageFlowService.cs:58). Guest->host is reliable (PacketSender.Send defaults reliable=true, src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29); host->guest CommittedBatch broadcast is reliable (KernelProtocolService.SendToGuests defaults reliable=true,). FINDING: WirePayloadType.ItemTransferCommand=6 / WireCommandKind.ItemTransfer have a validator + mapper (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelWireMapper.cs:439) but NO sender constructs them - item transfer is host-internal via PlayerInventoryTakeService/PlayerItemUseService TryTransfer, so 'transfer' has no wire event. Wire members: ItemContainerSync, ItemDataUpdated, ItemDestroy, ItemDestroyed, ItemDrop, ItemPickup, ItemRelocated, ItemSpawn, ItemSpawned, ItemTransfer, ItemUpdateState. | 5 s world-item keyframe: ItemSnapshotService.SendPeriodicItemSnapshot over the unreliable channel , pumped by ItemPositionAuthority.Update at AdaptiveStreamId.WorldItemSnapshotStream (base 5000 ms, MaxIntervalMs 30_000 -> governor may stretch it to 30 s). Apply is absolute: ItemReconcile kills standalone world items missing from the snapshot and materializes missing entries ; carried items are healed by the 1 Hz CharacterData snapshot (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataCapture.cs:65). CAVEAT: the keyframe is skipped entirely when the host table is empty (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemSnapshotService.cs:90), so a swallowed destroy is not healed once the table is empty. | World entry / late join / reconnect: WorldEntryFanout.Send sends the kernel checkpoint + the full item snapshot reliably, then WorldSnapshotComplete. Guest missed-range recovery: KernelProtocolService.HandleCommittedBatch detects a revision gap and RequestRange ; the host replays the journal or sends a fresh checkpoint . | Reliable in both directions; a lost host->guest committed batch is recoverable via range request/checkpoint, and world-item state converges through the 5 s keyframe except in the empty-table case. There is no guest-side re-report for a swallowed guest->host command: the host-authoritative snapshot overwrites the guest's local result instead of replaying it. | OK |  |
| I2 | World-item movement stream (10 Hz, WorldItemMoveStream) (GameAdapter/Items/ItemWorldSync.cs, Runtime/Session/Items/ItemMessageFlowService.cs, KernelStateStreamService.cs, AdaptiveStreamId.WorldItemMoveStream) | host->guest | The real trigger is the drop/throw, not a 'moved' event: ItemWorldSync.OnItemDropped / OnItemThrown -> ItemReportCommitter.SendDropReport (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemReportCommitter.cs:53) -> WireCommandKind.ItemDrop on KernelEnvelope (reliable) carrying the drop-moment velocity/rotation/angular velocity; ItemWorldSync.OnItemInstantiated -> SendItemSpawned covers spawns. Motion itself has no dedicated event - the host's physics is published only by the stream. Wire members: WorldItemMoveStream. | 10 Hz (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:76 BaseHz: 10). Host pump ItemPositionAuthority.Update -> SendMovingItemMoves -> IItemControl.SendItemMove -> KernelProtocolService.SendStateStream -> KernelStateStreamService.SendStateStream with reliable:false (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelStateStreamService.cs:55). Apply is a soft correction, not a raw overwrite: ItemPositionFollow.OnRemoteItemMove stores targets, then writes rb.velocity/angularVelocity every tick and hard-snaps only past the distance threshold. Governor: MinHz 1 (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:72), so 10 Hz can be stretched down to ~2 Hz under critical pressure. | World entry / late join / reconnect: WorldEntryFanout.Send -> SendItemSnapshot (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:42) materializes every world item at the snapshot's X/Y before the stream takes over; the kernel checkpoint + range request (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:326) rebuild the guest projection (ItemService.OnCheckpointRestored). | Unreliable with explicitly accepted loss: 'a lost tick is overwritten by the next' (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemPositionAuthority.cs:76). The guest keeps simulating locally between ticks and each tick re-sends velocity/angular velocity, so convergence is continuous soft-correction rather than acknowledgement. | OK |  |
| I3 | World-item periodic full-table snapshot (5 s, WorldItemSnapshotStream) (Runtime/Session/Items/ItemSnapshotService.cs + GameAdapter/Items/ItemPositionAuthority.cs (same as I2) + docs/decisions (decision #32)) | host->guest | This row IS the fallback half: it has no trigger of its own, only a timer (ItemPositionAuthority.Update ). The event half is I1's dedicated kernel commands (spawn/pickup/drop/destroy) plus I7's update-state events; state changes reach the host table through those events. Wire members: WorldItemSnapshotStream. | 5000 ms base (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:86) with MaxIntervalMs 30_000 , so the adaptive governor may stretch the keyframe to 30 s (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveRatePolicy.cs:78); sent by ItemSnapshotService.SendPeriodicItemSnapshot over the unreliable channel to every handshaken guest. Apply is absolute overwrite of the TOP-LEVEL state: ItemReconcile kills standalone world items missing from the snapshot , aligns condition/favourited/liquids/components on divergence and materializes missing entries including container children ; POSITION is deliberately not applied - it stays with I2. GAPS: skipped when the host table is empty (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemSnapshotService.cs:90); the wire payload has no Contents member (src/CasualtiesUnknownOnline.Protocol/Wire/WireItemData.cs:21-25), so contents of an EXISTING container are never re-aligned. CADENCE FINDING: the 5 s base can stretch to 30 s under pressure (todo/sync-cadence-review.md). | World entry / late join / reconnect: the same snapshot is sent reliably to one member by WorldEntryFanout.Send together with the kernel checkpoint ; the guest rebuilds on CheckpointRestored (ItemService.OnCheckpointRestored -> KernelBatchItemProjection.Rebuild) and on projection failure (ProjectionDomain registration, src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemService.cs:67). | Unreliable, loss accepted by design: 'drops are harmless (the next tick overwrites; the receiver reconciles)' (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemSnapshotService.cs:85-87). Staleness is gated on the receiver: a snapshot whose BaseGlobalRevision is older than the applied kernel revision is dropped (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemSnapshotStreamReceiver.cs:83), and the per-payload Seq rejects duplicates/out-of-order . | OK |  |
| I4 | Container content sync (ItemContainerSync) (GameAdapter/Items/ContainerItemSync.cs, Runtime/Session/Items container services, KernelProtocolCommandHandler.cs) | both | Container.LoadItem / UnloadItem / UnloadAllItems postfixes (src/CasualtiesUnknownOnline.GameAdapter/Patches/ContainerItemPatches.cs:38,) -> ContainerItemSync.OnLoadedIntoContainer / OnUnloadedFromContainer / OnUnloadedAll. World-container load = SendItemDropped with parentItemId (src/CasualtiesUnknownOnline.GameAdapter/Items/ContainerItemSync.cs:184,) -> WireCommandKind.ItemDrop (reliable); unload/spill = SendItemDropped with parent 0; carried-container internal move = guest SendItemContainerContent -> WireCommandKind.ItemContainerSync (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemActionSync.cs:76, reliable) or host SendItemCarriedSync. All ride NetMsg.KernelEnvelope=122. | No dedicated container-content keyframe. World-container children DO ride the 5 s keyframe as independent entries (WireWorldItemState.ParentItemId), and ItemReconcile materializes any child missing locally and binds it (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemReconcile.cs:152 -> src/CasualtiesUnknownOnline.GameAdapter/Items/RemoteItemSceneOps.cs:372); carried-container contents heal through the 1 Hz CharacterData snapshot, which captures items recursively (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataCapture.cs:65 -> ItemStateCodec.CaptureItem) and whose receiver replaces the owner's fact tree wholesale (CloneFactTable.ApplySnapshot). NOT healed: contents of an EXISTING container are excluded from the top-level alignment (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemReconcile.cs:103-105; src/CasualtiesUnknownOnline.GameState/Domains/Items/ItemData.cs:10 excludes contents from the kernel payload), so an extra or wrongly-bound child is never removed by the keyframe. | World entry / late join / reconnect: world-container children arrive as their own snapshot entries and are materialized + bound when missing (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemReconcile.cs:152-159); carried containers arrive through the character restore (CharacterDataStore.SendSavedCharacter) and the kernel checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41). | Reliable events (KernelEnvelope command + committed-batch broadcast); a lost host->guest batch is healed by the range-request/checkpoint recovery. A swallowed guest->host container-load is NOT healed by the live keyframe (the guest's child stays bound; the keyframe's standalone entry is not re-bound because ItemReconcile skips non-standalone items) - only a reconnect / world re-entry converges. | OK |  |
| I5 | Pickup/drop in-flight operations (pending pickup queue, drop pending, pickup origins) (GameAdapter/Items/PickupSync.cs, ItemReportCommitter.cs, NativeOperationCoordinator.cs, ItemApplication.cs) | both (guest->host reports; host->guest committed batches / rejections) | Trigger hooks: Body.PickUpItem prefix records the pickup origin (src/CasualtiesUnknownOnline.GameAdapter/Patches/BodyItemPatches.cs:76 -> PickupSync.OnPickupStart, src/CasualtiesUnknownOnline.GameAdapter/Items/PickupSync.cs:55) and its postfix reports the landed pickup; Body.DropItem / ThrowItem prefixes report the drop (src/CasualtiesUnknownOnline.GameAdapter/Patches/BodyItemPatches.cs:114,). Messages: WireCommandKind.ItemPickup / ItemDrop (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemMessageFlowService.cs:93) on reliable KernelEnvelope; the host answers a refused claim with CommandRejected -> ItemRejected -> ItemApplication.RollbackPickup . | No periodic re-send of a pending claim or a pending drop. The in-flight windows are time-bounded: the host's pending-pickup queue holds an unknown pickup 500 ms (src/CasualtiesUnknownOnline.Runtime/Session/Items/PendingPickupQueue.cs:17) and then rejects it (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolCommandHandler.cs:79); the drop machine holds the report one frame for the final throw velocity (src/CasualtiesUnknownOnline.Runtime/Session/Items/DropPendingState.cs:69). The only absolute state fallback is I3's 5 s world-item keyframe, which converges the HOST's table, not the guest's local pickup/drop result. | Reconnect / world re-entry: the host's checkpoint + item snapshot (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41-42) re-materialize the authoritative world state and the character restore returns the carried item; the kernel journal range request (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:326) replays a missed committed batch. The pending pickup queue itself is the in-flight recovery for the pickup-before-spawn reorder. | Reliable command channel. The in-flight race (pickup beating its spawn report) is healed by the 500 ms hold and by the reject->rollback path. A command swallowed at transport level has no guest-side re-report: the keyframe neither kills the guest's inventory copy nor re-materializes a duplicate (ItemApplication.FindWorldItem resolves the carried item), so the divergence persists until a reconnect. ADJUDICATED (adversarial review): a swallowed pickup/drop command leaves the guest's local result un-reconciled by the item keyframe; the divergence persists until reconnect. Ticket: todo/guest-command-loss-reconciliation.md (also covers I1's empty-table caveat). | Event-only gap | todo/guest-command-loss-reconciliation.md |
| I6 | Recipe unlock backfill (RecipeUnlock); the CraftReport item facts are healed by I3/P3 (Runtime/Session/Items/CraftSyncService.cs, Handlers/CraftReportHandler.cs, RecipeUnlockHandler.cs, GameAdapter/Items/CraftingSync.cs) | both (guest->host report; host->guest relay, source excluded) | NetMsg.CraftReport=76 and NetMsg.RecipeUnlock=77, both bidirectional report+relay . Triggers: CraftingSync.OnCraftEnd -> ReportCraft (Recipe.TryMake), OnCombineEnd -> ReportCraft, OnLiquidTransferFinished -> ReportCraft; blueprint use -> SendRecipeUnlock -> CraftSyncService.SendRecipeUnlock . Reliable (PacketSender default true). Wire members: CraftReport, RecipeUnlock. | CraftReport's durable effects have fallbacks: world-table entries ride I3's 5 s keyframe (src/CasualtiesUnknownOnline.Runtime/Session/Items/CraftSyncService.cs:133 UpdateWorldItemState / RemoveWorldItemLocal feed the table the keyframe sends) and carried entries/products ride the 1 Hz CharacterData snapshot and the carried-fact path (src/CasualtiesUnknownOnline.Runtime/Session/Items/CraftSyncService.cs:158 PublishCarriedSyncLocal). RecipeUnlock has NO periodic fallback: SendRecipeUnlock is called only from src/CasualtiesUnknownOnline.GameAdapter/Items/CraftingSync.cs:366, and FireRecipeUnlockReceived only applies locally + relays (src/CasualtiesUnknownOnline.Runtime/Session/Items/CraftSyncService.cs:214); the unlock is a per-process static write (src/CasualtiesUnknownOnline.GameAdapter/Items/RecipeUnlockApply.cs:41). | World entry / late join / reconnect: WorldEntryFanout.Send (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:34) sends block/trap/radiation/checkpoint/item/enemy snapshots - no RecipeUnlock re-send. A guest that joins after an unlock keeps its own Recipes.recipes[idx].INT != 0 and never learns it; the CraftReport-driven item state itself is recovered by the checkpoint/snapshot group. | Reliable channel, but the recipe unlock is loss-accepting: craft classification is accept-with-adopt and untracked entries are skipped with a warning (src/CasualtiesUnknownOnline.Runtime/Session/Items/CraftSyncService.cs:126-131). There is no convergence guarantee for a swallowed RecipeUnlock - nothing re-derives it from host state. | Event-only gap | todo/recipe-unlock-fallback.md |
| I7 | Item state payload updates (ItemUpdateState: gun state, custom data, liquids, components) (GameAdapter/Items/GunStateSync.cs, CustomItemDataState.cs, ItemStateCodec.cs, Runtime item update path) | both (guest->host ItemUpdateState; host->guest committed batch / carried fact) | Triggers: Body.UseItemInHand / UseItem postfix (src/CasualtiesUnknownOnline.GameAdapter/Patches/UseItemPatches.cs:35,) -> ItemUseSync.OnItemUsed; GunScript Update/Fire/TryRack/ToggleSafety/LoadMag/UnloadMag postfix (src/CasualtiesUnknownOnline.GameAdapter/Patches/GunStatePatches.cs:20 and siblings) -> GunStateSync.TryReport (dedup) -> ItemUseSync.OnItemUsed (src/CasualtiesUnknownOnline.GameAdapter/Items/GunStateSync.cs:62). Payload = full digest incl. synthetic custom-data fields (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemStateCodec.cs:254,). Message: WireCommandKind.ItemUpdateState (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemActionSync.cs:34) on reliable KernelEnvelope; the host's own change is a kernel UpdateItemState batch broadcast (ItemActionSync.SendWorldItemCorrection) or a carried-fact broadcast. | Carried items: the 1 Hz CharacterData snapshot is the fallback - CharacterDataCapture.Capture emits every carried/worn item with the full recursive component/liquid payload (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataCapture.cs:65) and CharacterDataSync re-reports at 1 Hz (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataSync.cs:220); the receiver replaces the owner's fact tree (CloneFactTable.ApplySnapshot). World items: I3's 5 s keyframe re-aligns condition/favourited/liquids/components on divergence (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemReconcile.cs:124-127), governor-stretchable to 30 s; the character snapshot cadence is fixed. | World entry / late join / reconnect: the kernel checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41) restores every item's Data (condition/favourited/slot/liquids/components) and the guest rebuilds the projection; the character restore (CharacterDataStore.SendSavedCharacter) carries the carried items' full component state. | Reliable event; convergence is guaranteed by the 1 Hz character snapshot (carried items) and the 5 s keyframe (world items), both absolute overwrites of the component/liquid payload. Accepted latency, not accepted loss. | OK |  |
| I8 | Item id coordination (ItemIdWatermark, CarriedInventory) (Runtime/Session/Items/ItemIdCoordinator.cs, Handlers/ItemIdWatermarkHandler.cs, CarriedInventoryHandler.cs, GameAdapter/Items/ItemIdAllocator.cs, CarriedInventoryReporter.cs) | both (guest->host watermark report + CarriedInventory; host->guest watermark grant) | NetMsg.ItemIdWatermark=64 (bidirectional) and NetMsg.CarriedInventory=65 (guest->host). Triggers: every allocation ItemIdAllocator.Next -> SendItemIdWatermark -> ItemIdCoordinator.SendItemIdWatermark , reliable; the guest's generation-finished edge (CarriedInventoryReporter.Update,) -> SendCarriedInventory -> ItemIdCoordinator.SendCarriedInventory, reliable. Wire members: CarriedInventory, ItemIdWatermark. | none. The watermark is reported only on allocation (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemIdAllocator.cs:33) with no periodic re-report, so a swallowed final report is only caught up by a later allocation (the counter only ever rises, src/CasualtiesUnknownOnline.GameAdapter/Items/ItemIdAllocator.cs:42). CarriedInventory has exactly one send site (src/CasualtiesUnknownOnline.GameAdapter/Items/CarriedInventoryReporter.cs:133) with no periodic re-send, and the 1 Hz CharacterData snapshot does NOT feed the host's arbitration transfer table (CharacterDataStore.SaveCharacterData only stores the snapshot), so a swallowed CarriedInventory leaves the host's transfer table empty for those ids. | The host re-grants the watermark on every MemberAdded (join/reconnect): src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemIdCoordinator.cs:42 (session.MemberAdded += OnMemberAdded) -> -> GrantItemIdWatermark - but the granted value is the last RECEIVED report, so this backfill cannot repair a swallowed report. Partial kernel-side heal: the first use/slot report of an unregistered carried item is accepted-first and spawns the carried fact (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolCommandHandler.cs:168), which heals the kernel state but not the arbitration transfer table. | Reliable channel. The watermark is monotonic and self-heals on the next allocation; the CarriedInventory registration is event-only with no convergence guarantee (ItemArbitration.AdoptEvidence logs 'no transfer-table entry, not arbitrated' and returns null). | Event-only gap | todo/carried-inventory-registration-re-report.md |
| I9 | Item presentation transients (fresh-drop motion, impact sounds, drop provenance) (GameAdapter/Items/ItemReconcile.cs, NonAuthoritativeItemImpactPolicy.cs, ItemDropState.cs, ItemDropProvenance*.cs) | host->guest for the fresh-drop flag; impact sounds are deliberately local-only (no direction) | fresh-drop: the FreshItemDrop component is captured at the trigger (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemWorldSync.cs:211) and rides the spawn/drop command (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemMessageFlowService.cs:67) and the world-item snapshot (WireWorldItemState.FreshItemDrop); the receiver re-adds the component (src/CasualtiesUnknownOnline.GameAdapter/Items/RemoteItemSceneOps.cs:403-405, src/CasualtiesUnknownOnline.GameAdapter/Items/ItemApplication.cs:272-275). Impact sounds: NO event - the two native item-prefab collision presentation paths are suppressed on guests by NonAuthoritativeItemImpactPolicy / NonAuthoritativeItemImpactGuard (src/CasualtiesUnknownOnline.GameAdapter/Patches/ItemCollisionEnter2DPatch.cs:18) because the guest copy is not the physics authority. Drop provenance: ItemDropProvenanceClassifier is a local routing decision (block drop / building-death drop fold into the break/trap report), not a wire surface of its own. | fresh-drop: the flag is part of every absolute item snapshot (5 s keyframe + world entry), so it is re-applied whenever the item is materialized. Impact sounds: none - suppression is state-driven (session active + guest + standalone world item), so there is nothing to re-send. | fresh-drop: the world-entry / late-join snapshot materializes the item with FreshItemDrop=true (src/CasualtiesUnknownOnline.GameAdapter/Items/RemoteItemSceneOps.cs:403). Impact sounds: nothing to recover - the host's own native impacts stay local to the host by design. | Explicitly accepted loss: 'Not adding a host-authored item-impact sound stream; the host already does not broadcast these one-shot physical sounds, and the fix removes only the non-authoritative local echo.' (docs/backlog/review/guest-background-ghost-item-ground-sounds.md:71-73). Fresh-drop presentation is loss-tolerant: a lost flag only changes the glow and the next absolute snapshot restores it. | Transient-by-design |  |
| P1 | Player state stream (20 Hz host->guests + guest->host report) (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs, GameAdapter/Character/SessionStatePump.cs, GameAdapter/Character/RemotePlayerRenderer.cs) | Both. Host -> each guest: PlayerStreamExchange.BroadcastPlayerState sends one WireStateStream per recipient (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs:49 'foreach (var target in synced)'). Guest -> host: SendPlayerStateReport (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs:71 'kernelProtocol.SendStateStreamTo(session.HostSteamId,'). Both ride WirePayloadType.PlayerStateStream = 304 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs:77 'PlayerStateStream = 304,') inside StateStreamEnvelope over NetMsg.KernelEnvelope = 122 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:223 'KernelEnvelope = 122, // bidirectional: the four-envelope kernel protocol rides one message id'). | No discrete per-frame event by design: continuous movement/pose has no discrete trigger, the row IS the stream. The discrete membership triggers do have dedicated events: PlayerJoin = 32 and PlayerLeave = 33 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:23 'PlayerJoin = 32, // host → guest: self-activation + roster announcement') carrying the entity id + spawn anchor; the host sends them in StartMemberSync (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:513 '_sender.Send(presence.SteamId, NetMsg.PlayerJoin, joinMsg); // self-activation'). Stream send trigger: EntitySyncService.Update host branch (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:426) and guest branch (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:434). Reliability: unreliable is explicit on both send sites (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs:60 'reliable: false);'). Wire members: PlayerStateBroadcast, PlayerStateReport. | This row is itself the periodic absolute fallback: default 20 Hz (src/CasualtiesUnknownOnline.Runtime/Configuration/StateStreamOptions.cs:17 'public const int DefaultStateStreamHz = 20;') driven by AdaptiveStreamId.PlayerStateBroadcast / PlayerStateReport. Apply semantics: absolute overwrite gated by the monotonic stream sequence (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs:90 'if (stream.Seq <= LastStateSeq)'), then interpolated by SessionStatePump (src/CasualtiesUnknownOnline.GameAdapter/Character/SessionStatePump.cs:66 'body.transform.position = position;'). The adaptive governor may stretch it: AdaptiveStreamCatalog gives both profiles MinHz = 1 / MaxHz = 60 with no BaseHz override (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:18,), AdaptiveRatePolicy applies pressure factors down to 0.25 for priority 1 (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveRatePolicy.cs:104) plus a 128 KB/s budget, so 20 Hz degrades to 15/10/5 Hz and is clamped no lower than 1 Hz. | World entry / late join: MaybeStartEntitySync -> StartMemberSync allocates the entity id, sends the self-activation PlayerJoin, roster-backfills every already-synced member's PlayerJoin, then pushes an immediate broadcast so the clone renders without waiting for the next tick (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:513, '_sender.Send(presence.SteamId, NetMsg.PlayerJoin,', '_playerStream.BroadcastPlayerState();'). Guest resets the gate on its own PlayerJoin (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:239 'LastStateSeq = 0; // the host's snapshot sequence restarts with this join'). Reconnect/re-entry is a fresh join + fresh entity id via UpsertEntity (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:560). No missed-range replay: every frame is a complete absolute state, so the next tick supersedes a gap. | Reliable channel? No - unreliable by explicit choice. Accepted loss, documented in the PacketSender.Send doc comment (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:24-28): only the 20 Hz state stream goes unreliable, because overwrite semantics + snapshot sequence make drops harmless. Convergence guarantee: the next absolute snapshot overwrites the buffer and the seq gate drops duplicates/out-of-order frames, so a swallowed frame leaves no permanent divergence. | OK |  |
| P2 | Player kernel status/limb/skills facts (UpdatePlayerStatus) (src/CasualtiesUnknownOnline.GameState/Domains/Players/*, Runtime/Session/EntitySync/PlayerKernelStatusProjection.cs, Runtime/Session/CharacterData/PlayerKernelLimbProjection.cs) | Host -> guest. The host is the only writer (AuthorityKind.HostOnly); the committed batch rides NetMsg.KernelEnvelope (122) to every handshaken guest. The projection inputs are host-local: the 20 Hz player stream (alive/conscious) and the 1 Hz character snapshot + LimbStateEvent (limbs/body/skills). | Kernel command UpdatePlayerStatusCommand = 14 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs:25 'UpdatePlayerStatusCommand = 14,') producing PlayerStatusUpdatedEvent = 111 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs:50 'PlayerStatusUpdatedEvent = 111,') in a CommittedBatchEnvelope. Trigger sites: EntitySyncService.ApplyPlayerStream -> PlayerKernelStatusProjection.Sync for alive/conscious (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:329 '_playerStatus.Sync(target.SteamId, target.Alive, target.Conscious);'), CharacterDataStore.SaveCharacterData -> SyncFromCharacterData for limbs/body/skills (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:93 '_playerLimbKernel.SyncFromCharacterData(steamId, msg);'), CharacterDataStore.ApplyLimbStateEvent -> SyncFromLimbEvent (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:337 '_playerLimbKernel.SyncFromLimbEvent(msg);'). Emit: ItemKernelAuthority.TryUpdatePlayerStatus (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:183) -> PlayerDomainModule decides Accept (src/CasualtiesUnknownOnline.GameState/Domains/Players/PlayerDomainModule.cs:30). Reliability: KernelProtocolService.BroadcastCommittedBatch -> SendToGuests default reliable=true (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:493 'private void SendToGuests(ProtocolFrame frame, bool reliable = true)'). Wire members: PlayersReset, PlayerStatusUpdated, ResetPlayers, UpdatePlayerStatus. | No periodic re-send of PlayerStatusUpdatedEvent itself (the projection only emits on a change, src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerKernelStatusProjection.cs:43 returns early when equal). The periodic absolute fallback is the kernel checkpoint: the host re-sends it every 60 s to every in-world member and it carries the whole Players table (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 'if (IsHostMode && _session.SessionActive && Time.unscaledTime - _lastSnapshotResend > 60f)' -> '_kernelProtocol.SendCheckpoint(member.SteamId);'). The projection inputs are themselves periodic: the 20 Hz stream (P1) re-drives alive/conscious and the 1 Hz character snapshot (P3) re-drives limb/body/skill facts. The checkpoint path is not adapted by the rate governor. | World entry: WorldEntryFanout.Send -> SendCheckpoint (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41 '_kernelProtocol.SendCheckpoint(steamId);'). Mid-session joiner: the handshake sends the checkpoint immediately (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192 '_kernelProtocol.SendCheckpoint(sender);'). Missed-range: a revision gap buffers the batch and requests the range (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:391 'private void RequestRange(ulong start, ulong end)'), the host replays the journal or sends a fresh checkpoint when the range is outside it (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:401). Reconnect/re-entry: PlayerKernelRestoreProjection.Apply overlays the kernel terminal facts onto the saved snapshot (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/PlayerKernelRestoreProjection.cs:26 'public void Apply(ulong steamId, CharacterDataMsg data)'), called from CharacterDataStore.SendSavedCharacter (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:196). | Reliable: the committed batch is sent with reliable=true (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:468/472) and the host journals the last 2048 batches (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:23 'private const int JournalCapacity = 2048;'). Loss is not accepted: a detected gap is repaired by a range request, an out-of-journal gap by a fresh checkpoint, and the 60 s checkpoint bounds any silent divergence to at most 60 s. | OK |  |
| P3 | Character data snapshot (1 Hz) + host-side persistence (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs, Handlers/CharacterDataHandler.cs, Handlers/HostCharacterDataHandler.cs, CharacterDataFileStore.cs, GameAdapter/Character/CharacterDataSync.cs) | Both. Guest -> host: NetMsg.CharacterData = 37 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:27 'CharacterData = 37,'). Host -> guest: the same id carries the reconnect restore, and NetMsg.HostCharacterData = 53 carries the host's own snapshot for clone-inventory rendering. | Event-level immediate re-report at the real trigger: an inventory-internal move (SwapSlots/SwitchHands) or any cross-player apply calls ReportInventoryChanged, which sends a non-throttled full snapshot (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataSync.cs:230 'ReportCharacterData(CharacterDataCapture.Capture(_mapper, localBody), throttled: false);'), and leaving the world pushes a final snapshot (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataSync.cs:278 '_characterData.ReportCharacterData(CharacterDataCapture.Capture(_mapper, prevBody));'). Send: CharacterDataStore.ReportCharacterData -> NetMsg.CharacterData (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:85 '_sender.Send(_session.HostSteamId, NetMsg.CharacterData, msg);') and BroadcastHostCharacterData (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:298 '_session.Broadcast(NetMsg.HostCharacterData, msg);'). Reliability: PacketSender.Send defaults reliable=true (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29). Wire members: CharacterData, HostCharacterData. | 1 Hz absolute full snapshot: CharacterReportInterval = 1f (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataSync.cs:48 'private const float CharacterReportInterval = 1f; // guest → host character snapshot (1 Hz)'), pumped by CharacterDataSync.Update (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataSync.cs:220 '_nextCharacterReportMs = nowMs + (long)(CharacterReportInterval * 1000f);' ->). Apply semantics: absolute overwrite of the host's per-SteamID saved snapshot (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:91 '_savedCharacters[steamId] = msg;') and of the clone fact table (src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterDataSync.cs:161). Not adapted by the rate governor (no AdaptiveStreamId for this path). | SendSavedCharacter hands the saved snapshot back on handshake (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:108 'ctx.CharacterData.SendSavedCharacter(sender);'), on the InWorld scene-state edge (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs:55 'ctx.CharacterData.SendSavedCharacter(reporter);') and on respawn (src/CasualtiesUnknownOnline.GameAdapter/World/RespawnCoordinator.cs:136 '_characterData.SendSavedCharacter(steamId);'), gated on the host being in a world (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:181 'internal void SendSavedCharacter(ulong steamId)' with ). The host merges the item-arbitration transfer table over the snapshot (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:191 'MergeTransferredItems(steamId, data);') and overlays kernel terminal facts (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:196). Host-restart persistence: the constructor loads the disk table (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:64 '_persistence.TryLoad(out _savedCharacters);') and every verified mutation persists it (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:210 'if (!_persistence.Save(_savedCharacters))'). | Reliable channel (PacketSender default reliable=true). A lost 1 Hz report self-heals on the next tick because the snapshot is absolute; a disconnect before the next tick is covered by the dedicated-event merges (limb/enemy/heal) and the final NotifyBodyLeft snapshot. Host-side disk persistence makes the restore survive a host restart; a new run voids it with an empty tombstone + delete (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:143). | OK |  |
| P4 | Limb latch events (LimbStateEvent) (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:347-369, Handlers/LimbStateEventHandler.cs, GameAdapter/Patches/LimbStatePatches.cs, GameAdapter/Character/CharacterDataSync.cs) | Both. Guest -> host report; host -> every other guest broadcast relay with the source excluded (Handlers/LimbStateEventHandler.cs:31 'ctx.Session.BroadcastExcept(sender, NetMsg.LimbStateEvent, msg);'). | NetMsg.LimbStateEvent = 93 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:113 'LimbStateEvent = 93, // bidirectional: guest → host report of the owner's own limb latch; host → guest broadcast relay (source excluded) - carries the body's full post-event limb + health terminal state'). Trigger: the Harmony postfixes on Limb.BreakBone/MendBone/Dislocate/UnDislocate/Dismember report only a verified latch transition and skip render clones (src/CasualtiesUnknownOnline.GameAdapter/Patches/LimbStatePatches.cs:71 'if (latchChanged && limb.GetComponentInParent<RemoteBodyDriver>() == null) // Unity object — ==' -> 'PatchBridge.Impl?.OnLimbStateEvent(limb);') into CharacterDataSync.ReportLimbStateEvent -> SendLimbStateEvent. Reliability: reliable:true on the host fan-out (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:363 'NetMsg.LimbStateEvent, msg, reliable: true);') and PacketSender default reliable=true for the guest report (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:367 '_sender.Send(_session.HostSteamId, NetMsg.LimbStateEvent, msg);'). Wire members: LimbStateEvent. | 1 Hz absolute character snapshot (P3) carries the full limb set; the code states the fallback explicitly (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:343 '/// already damaged locally). Reliable — a lost event self-heals on the next' '/// 1 Hz character snapshot, but the trigger rides the event, never the'). The 1 Hz path is not adapted by the rate governor. | The host merges the event's full post-event limb + body health state into the owner's saved per-SteamID snapshot immediately (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:332 'EnemyTerminalStateApplier.ApplyLimbState(data, msg);' followed by PersistTable), so a disconnect before the next 1 Hz report still restores every changed field. The event is also projected into the kernel players table (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:337) and therefore into the 60 s checkpoint / PlayerKernelRestoreProjection. A late joiner receives the state through SendSavedCharacter or the next 1 Hz snapshot. | Reliable dedicated event plus a periodic absolute 1 Hz snapshot fallback; convergence is guaranteed by the snapshot's complete limb set. No accepted loss. | OK |  |
| P5 | One-shot character presentation (CharacterSound, CharacterAttackAnim, CharacterLandingVisual, CharacterRagdoll) (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:377-469, Handlers/CharacterSoundHandler.cs, CharacterAttackAnimHandler.cs, CharacterLandingVisualHandler.cs, CharacterRagdollHandler.cs, GameAdapter/Character/Character*Sync.cs) | Both. The owner reports its own one-shot to the host; the host broadcasts its own to every handshaken guest and relays a guest's report source-excluded. Ids: CharacterSound = 94 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:118), CharacterAttackAnim = 113 , CharacterLandingVisual = 114 , CharacterRagdoll = 120 . | Four dedicated one-shot messages, all reliable:true on the host fan-out (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:388 'NetMsg.CharacterSound, msg, reliable: true);' 'NetMsg.CharacterAttackAnim, msg, reliable: true);' 'NetMsg.CharacterLandingVisual, msg, reliable: true);' 'NetMsg.CharacterRagdoll, msg, reliable: true);'). Triggers at the real game call sites: sound capture in GameAdapter/Patches/SoundPlayPatch.cs:61 and src/CasualtiesUnknownOnline.GameAdapter/Patches/SoundPlayAudioClipPatch.cs:59, attack animation in GameAdapter/Patches/BodyPatches.cs:109 'PatchBridge.Impl?.OnAttackAnim(prefab, direction, __instance.isRight, __instance.limbs[1].transform.position);', landing in src/CasualtiesUnknownOnline.GameAdapter/Patches/BodyPatches.cs:237, ragdoll-key collapse in GameAdapter/Patches/PlayerCameraHandleInputPatch.cs:61 'PatchBridge.Impl?.OnCharacterRagdoll((Vector2)body.transform.position);'. Wire members: CharacterAttackAnim, CharacterLandingVisual, CharacterRagdoll, CharacterSound. | CharacterSound / CharacterAttackAnim / CharacterLandingVisual: none - pure one-shot presentation with no durable state (nearest send sites src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:388,); nothing periodic can or should replay a past sound/animation/landing. CharacterRagdoll: the P1 20 Hz stream's Standing flag is the fallback (src/CasualtiesUnknownOnline.GameAdapter/Character/SessionStatePump.cs:95 'var effectiveStanding = entity.Standing;' -> 'body.standing = effectiveStanding;'), with RagdollPoseGate suppressing a stale standing=true snapshot for 500 ms (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/RagdollPoseGate.cs:20 'public const long SuppressWindowMs = 500;'). | None for the three pure visuals: a late joiner never sees a past one-shot and there is nothing to replay. CharacterRagdoll has a clone-creation queue that drains within the suppression window and then defers to the state stream (GameAdapter/Character/CharacterRagdollSync.cs:89), and the P1 stream is authoritative for the joiner's pose. | All four ride the reliable channel, so transport loss is not accepted; the recorded design intent is one-shot = one message and the trigger never rides the snapshot stream (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:374 '/// already heard the sound locally). Reliable — one sound = one message,'). Where a lost effect has no state to heal (sound/attack animation/landing) the degradation is explicitly recorded as acceptable in docs/evidence/selfchecks/presentation/footstep-sound-selfcheck.md:51-52 (line 51: 'character snapshot + 20 Hz state stream. A lost message is a missed one-shot'), and the ragdoll self-check records the same for its fallback case (docs/evidence/selfchecks/players/character-ragdoll-toggle-sync-selfcheck.md:30). CharacterRagdoll's only durable fact (lying/standing) is healed by P1. | Transient-by-design |  |
| P6 | Carry set/clear kernel facts (src/CasualtiesUnknownOnline.GameState/Domains/Players/*, Runtime/Session/PlayerInteraction/PlayerCarryService.cs, PlayerKernelCarryProjection.cs, GameAdapter/Character/CarriedBody*.cs) | Both. Guest -> host NetMsg.PlayerCarryStartRequest = 99 / PlayerCarryStopRequest = 100 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:135,); host -> every guest the committed batch on NetMsg.KernelEnvelope (122). The host's own UI takes the same local path. | Kernel carry facts: SetPlayerCarryCommand / ClearPlayerCarryCommand -> PlayerCarrySetEvent = 118 / PlayerCarryClearedEvent = 119 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs:57 'PlayerCarrySetEvent = 118,' 'PlayerCarryClearedEvent = 119,'). Trigger: PlayerCarryService.PublishCarryState writes the single authoritative fact (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerCarryService.cs:325 'if (!_kernelAuthority.TrySetPlayerCarry(' / 'if (!_kernelAuthority.TryClearPlayerCarry(') from HandleCarryStartRequest / HandleCarryStopRequest (guest request src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerCarryService.cs:91 '_sender.Send(_session.HostSteamId, NetMsg.PlayerCarryStartRequest, msg);' / ). Emit: src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:188. Broadcast: KernelProtocolService.BroadcastCommittedBatch (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:91 'SendToGuests(frame);' with default reliable=true). Wire members: ClearPlayerCarry, PlayerCarryCleared, PlayerCarrySet, SetPlayerCarry. | No dedicated periodic carry re-send; the periodic absolute fallback is the kernel checkpoint, which carries the whole Players table including CarrierOfSteamId/CarriedBySteamId, re-sent every 60 s to every in-world member (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 ->). The carried body's presentation additionally rides the P1 20 Hz stream. The checkpoint path is not rate-governor adapted. | World entry: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41; mid-session joiner: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192; both send the checkpoint. The guest rebuilds its mirror from the checkpoint players table (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerKernelCarryProjection.cs:71 '_carry.RebuildFromCheckpoint(checkpoint.Players);' -> src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerCarryService.cs:278 'internal void RebuildFromCheckpoint(PlayerStateTable? players)'). Missed-range: the batch is journaled and replayed on a revision gap, or a fresh checkpoint is sent (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:391,). Member leave / scene exit publishes a clear through the same kernel path (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerCarryService.cs:373-398). | Reliable committed batch (SendToGuests default reliable=true) plus the 2048-entry host journal; a lost batch is repaired by the range request or the next checkpoint. Convergence guarantee holds; no accepted loss. | OK |  |
| P7 | Cross-player inventory transfer fact (RecordPlayerInventoryTransfer) (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInventoryTakeService.cs, PlayerInteractionResultAuthority.cs, GameState/Domains/Players/*) | Guest -> host NetMsg.PlayerInventoryTakeRequest = 97 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:128); host -> every guest the journaled result event in a reliable CommittedBatch. | Kernel event PlayerInventoryTransferEvent = 120 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs:59 'PlayerInventoryTransferEvent = 120,'). Request trigger: PlayerInventoryTakeService.SendTakeRequest (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInventoryTakeService.cs:64 '_sender.Send(_session.HostSteamId, NetMsg.PlayerInventoryTakeRequest, msg);'); host trigger: HandleTakeCore -> PublishTransfer -> PlayerInteractionResultAuthority.TryRecordPlayerInventoryTransfer (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInventoryTakeService.cs:268 'if (!_resultAuthority.TryRecordPlayerInventoryTransfer(' -> src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInteractionResultAuthority.cs:26 'new RecordPlayerInventoryTransferCommand('). Apply: PlayerInteractionKernelProjection.ProjectTransfer fires only for the two participants (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInteractionKernelProjection.cs:86 '_interaction.FireTransferReceived(PlayerInteractionKernelCodec.ToTransferMessage(e));') -> GameAdapter/PlayerInteractionApply.OnPlayerInventoryTransfer. Wire members: PlayerInventoryTransfer. | None for the transfer fact itself, and the code says so explicitly: the adapter warns that the 1 Hz snapshot will not apply this authoritative fact (src/CasualtiesUnknownOnline.GameAdapter/PlayerInteractionApply.cs:30 'skipped (1 Hz snapshot will not apply this authoritative fact; a retry is required).'). The host-side durable effect is written into both character snapshots immediately (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInventoryTakeService.cs:168 '_characters.SaveCharacterData(from, newSource);' / ), and a successful local apply triggers an immediate full character re-report (src/CasualtiesUnknownOnline.GameAdapter/PlayerInteractionApply.cs:92 'domains.CharacterDataSync.ReportInventoryChanged(body);'). | Reconnect: the host merges the item-arbitration transfer table over the saved character snapshot (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:191 'MergeTransferredItems(steamId, data);' ->), and that is exactly where a guest-owned taken item is recorded (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInventoryTakeService.cs:180 '_items.AdoptTransferredItem(to, itemId, transferred);'). Kernel checkpoint at world entry / handshake / 60 s covers the host-recipient case (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192, src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:131). Missed-range: the result batch is in the host journal and replayed on a revision gap (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:391,). | Request and result are reliable (PacketSender default; the result rides a reliable CommittedBatch) and the result is journaled, so a swallowed batch is recoverable while the guest kernel detects a revision gap. Caveat: the adapter apply is fire-and-forget - if the local body is not ready the fact is skipped (src/CasualtiesUnknownOnline.GameAdapter/PlayerInteractionApply.cs:30) and the only heal is the reconnect transfer-table merge; the journal-only event reduces to a no-op in the kernel (src/CasualtiesUnknownOnline.GameState/Domains/Players/PlayerDomainModule.cs:67 'PlayerInventoryTransferEvent or PlayerHealResultEvent or PlayerItemUseResultEvent => current,'), so the 60 s checkpoint does not replay it. | OK |  |
| P8 | Cross-player heal / item-use results (RecordPlayerHealResult, RecordPlayerItemUseResult) (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerHealService.cs, PlayerItemUseService.cs, PlayerInteractionResultAuthority.cs, GameState/Domains/Players/*) | Guest -> host NetMsg.PlayerHealRequest = 102 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:142) and PlayerItemUseRequest = 116 ; host -> every guest the journaled result event in a reliable CommittedBatch. | Kernel events PlayerHealResultEvent = 121 and PlayerItemUseResultEvent = 122 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs:60 'PlayerHealResultEvent = 121,' 'PlayerItemUseResultEvent = 122,'). Request triggers: src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerHealService.cs:61 '_sender.Send(_session.HostSteamId, NetMsg.PlayerHealRequest, msg);' and src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerItemUseService.cs:65 '_sender.Send(_session.HostSteamId, NetMsg.PlayerItemUseRequest, msg);'. Host result triggers: src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerHealService.cs:239 'if (!_resultAuthority.TryRecordPlayerHealResult(' and src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerItemUseService.cs:445 'if (!_resultAuthority.TryRecordPlayerItemUseResult('. Apply: src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInteractionKernelProjection.cs:99 '_interaction.FireHealReceived(PlayerInteractionKernelCodec.ToHealMessage(e));' / -> GameAdapter/PlayerInteractionApply.OnPlayerHealReceived / OnPlayerItemUseReceived. Wire members: PlayerHealResult, PlayerItemUseResult. | The host writes the post-heal / post-use body state into its authoritative character snapshots immediately (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerHealService.cs:157 '_characters.SaveCharacterData(healer, newHealerData);' / ), and the receiving participant re-reports the full character snapshot immediately on apply (src/CasualtiesUnknownOnline.GameAdapter/PlayerInteractionApply.cs:209 'domains.CharacterDataSync.ReportInventoryChanged(body);' / ). The 1 Hz character snapshot (P3) is therefore the periodic absolute fallback for the target's health/limbs/worn item, and the consumed item's ownership is mirrored into the kernel item table / transfer table. | Reconnect/re-entry: the saved character snapshot (already updated at the result) plus PlayerKernelRestoreProjection for terminal facts; the kernel checkpoint at world entry / handshake / 60 s covers host-owned item changes (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192, src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:131); the transfer table merge covers guest-owned consumed/worn items (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:191). Missed-range: the result batch is journaled and replayed on a revision gap. | Reliable request plus reliable journaled result; the durable body/item effect is additionally snapshot-backed at 1 Hz and kernel-backed (checkpoint + journal), so a swallowed result converges on the next snapshot or the next checkpoint. No accepted loss. | OK |  |
| P9 | Player interaction requests (take, carry start/stop, heal, item use, push) (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/*, Handlers/Player*RequestHandler.cs, PlayerPushResultHandler.cs, GameAdapter/PlayerInteractionApply.cs, PlayerPushApply.cs) | Guest -> host requests; host -> clients results. Ids: PlayerInventoryTakeRequest = 97 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:128), PlayerCarryStartRequest = 99 , PlayerCarryStopRequest = 100 , PlayerHealRequest = 102 , PlayerItemUseRequest = 116 , PlayerPushRequest = 118 , PlayerPushResult = 119 . | Every request is a dedicated message sent reliable by default (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29 'public void Send(ulong steamId, NetMsg msg, object? payload = null, bool reliable = true)'). Send triggers: src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerInventoryTakeService.cs:64, src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerCarryService.cs:91 , src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerHealService.cs:61, src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerItemUseService.cs:65, src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerPushService.cs:88 '_sender.Send(_session.HostSteamId, NetMsg.PlayerPushRequest, msg);'. Results: take/carry/heal/use ride journaled kernel CommittedBatches (reliable) and are surfaced by PlayerInteractionKernelProjection / PlayerKernelCarryProjection; push is a direct reliable host->all message (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerPushService.cs:187 '_sender.SendToAll(_session.Members' -> 'NetMsg.PlayerPushResult, msg);') because it is transient presentation, recorded as decision #130 in docs/decisions/archive.md:120. Wire members: PlayerCarryStartRequest, PlayerCarryStopRequest, PlayerHealRequest, PlayerInventoryTakeRequest, PlayerItemUseRequest, PlayerPushRequest, PlayerPushResult. | Push: the resulting motion rides the P1 20 Hz player stream - src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerPushService.cs:20-21 documents it ('/// 20 Hz player state stream as the presentation fallback.'), and PlayerPushApply applies the force locally (src/CasualtiesUnknownOnline.GameAdapter/PlayerPushApply.cs:20 'public void Apply(PlayerPushResultMsg msg)'). Take/carry/heal/use: no periodic re-send of the request/result, but the resulting durable facts are carried by the 60 s kernel checkpoint (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:131) and the 1 Hz character snapshot (P3); the journal replays a missed batch on a revision gap. | Kernel checkpoint at world entry (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41), on handshake (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192) and every 60 s (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122/131); range request recovery (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:391 'private void RequestRange(ulong start, ulong end)' 'private void HandleRangeRequest(ulong sender, WireCommand command)'); reconnect restores the carry mirror from the checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerKernelCarryProjection.cs:71) and the character snapshot with the transfer-table merge (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:191). | Requests and results are reliable. Push is transient-by-design: the force application can be lost with no retry (src/CasualtiesUnknownOnline.GameAdapter/PlayerPushApply.cs:25 'domains.Log.LogWarning("[Push] result received but the local body is not ready — skipped.");'), but the target's position/velocity converges through the P1 stream. A swallowed request is a no-op (one request per user gesture, no retry), which is acceptable because the user can repeat the gesture. | OK |  |
| P10 | Remote inventory operations (RemoteInventoryOperationRequest, RemoteInventoryApply) (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerRemoteInventoryService.cs, Handlers/RemoteInventory*Handler.cs, GameAdapter/RemoteBackpack*.cs, RemoteInventoryOperationApply.cs) | Guest -> host NetMsg.RemoteInventoryOperationRequest = 125 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:240); host -> owner NetMsg.RemoteInventoryApply = 126 . | Two dedicated messages, both reliable (PacketSender default). Request trigger: native remote-backpack gestures in GameAdapter/RemoteBackpackOperationHandler.cs:23 'domains.PlayerInteraction.SendRemoteInventoryOperation(new RemoteInventoryOperationRequestMsg' -> PlayerRemoteInventoryService.SendRemoteInventoryOperation (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerRemoteInventoryService.cs:65 '_sender.Send(_session.HostSteamId, NetMsg.RemoteInventoryOperationRequest, msg);'). Apply trigger: PlayerRemoteInventoryService.HandleRemoteInventoryOperation dispatch (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerRemoteInventoryService.cs:208 '_sender.Send(owner, NetMsg.RemoteInventoryApply, apply);') -> GameAdapter/RemoteInventoryOperationApply.Apply. Durable kinds also commit kernel commands (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerRemoteInventoryService.cs:256 'if (!TryExecuteItemCommand(BuildDropOrSpawnCommand(owner, original, ItemLocation.World(position.X, position.Y)), out var rejection))' and the SyncContainerItemsCommand / UpdateItemStateCommand paths) and record the participant result event ( 'RecordTransfer(requester, owner, 0, removed, 0);'). Wire members: RemoteInventoryApply, RemoteInventoryOperationRequest. | No periodic re-send of the operation itself. The owner's apply triggers an immediate full character re-report (src/CasualtiesUnknownOnline.GameAdapter/RemoteInventoryOperationApply.cs:89 'domains.CharacterDataSync.ReportInventoryChanged(body);') and, for the state-only kinds, an explicit item-state report ( 'ReportAffectedState(source);'). The periodic absolute fallback for the resulting inventory is the 1 Hz character snapshot (P3); the committed item facts are covered by the 60 s kernel checkpoint. | Kernel checkpoint at world entry (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41), on handshake (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192) and every 60 s (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:131) plus journal range recovery (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:391/407). Reconnect restores the character snapshot with the transfer-table merge (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:191). TransferToRequester delegates to the take path (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerRemoteInventoryService.cs:228), which is covered by P7. | Reliable request and reliable apply message; the durable effect is snapshot-backed (1 Hz) and kernel-backed (checkpoint + journal), so a swallowed message converges. Apply-side guard: if the owner's local body or item is missing the native operation is skipped with a warning (src/CasualtiesUnknownOnline.GameAdapter/RemoteInventoryOperationApply.cs:32/39) and the host's authoritative snapshot remains unchanged, so the next owner report restores the real state rather than silently diverging. | OK |  |
| P11 | Medical operation sessions (StartRequest/StartAck/Update/State/EndRequest/EndCommitted/Cancel) (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionService.cs, ShrapnelOperationSessionService.cs, OtherMedicalOperationSessionService.cs, MedicalOperationSessionPublisher.cs, ShrapnelStatePublisher.cs, MedicalInjectionReportBuffer.cs, ShrapnelPositionReportBuffer.cs, Handlers/MedicalOperation*Handler.cs, GameAdapter/RemoteMedical*.cs, MedicalOperationApply.cs) | Both. Operator -> host: StartRequest (127), Update (129), EndRequest (131), Cancel (133). Host -> clients: StartAck (128), State (130), EndCommitted (132) (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:247-253). | Seven dedicated ids (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:247 'MedicalOperationStartRequest = 127,' ... 'MedicalOperationCancel = 133, // operator -> host: cancel an active session'). Start/send trigger: src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionService.cs:142 '_sender.Send(_session.HostSteamId, NetMsg.MedicalOperationStartRequest, msg);'; end/cancel at src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionService.cs:182 and src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionService.cs:197. Host progress trigger: '_publisher.PublishState(state!);'; terminal trigger: '_publisher.PublishEnd(end);'. Reliability: Start/End/Cancel ride PacketSender defaults (reliable); injection deltas are coalesced but reliable (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalInjectionReportBuffer.cs:104 '_sender.Send(_session.HostSteamId, NetMsg.MedicalOperationUpdate, msg, reliable: true);'); ordinary shrapnel positions are explicitly unreliable and coalesced (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/ShrapnelPositionReportBuffer.cs:133 '_sender.Send(_session.HostSteamId, NetMsg.MedicalOperationUpdate, msg, reliable);') while ownership/break/release transitions bypass the buffer and are reliable (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/ShrapnelOperationSessionService.cs:134 '}, reliable: true);'). State/EndCommitted broadcasts are reliable (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionPublisher.cs:60,). Wire members: MedicalInjectionReport, MedicalOperationCancel, MedicalOperationEndCommitted, MedicalOperationEndRequest, MedicalOperationStartAck, MedicalOperationStartRequest, MedicalOperationState, MedicalOperationUpdate, ShrapnelPositionReport. | No periodic re-publish of the session state; the non-terminal State is an absolute snapshot published on every committed update (MedicalOperationInjectionApplier builds CommittedMl + Sequence + item + health + limbs at src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationInjectionApplier.cs:79-91), so the next update supersedes a lost one. The intermediate injection / shrapnel-position streams are adaptive (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:50-58 BaseHz 60, MinHz 1, priority 2 - the governor may stretch them to as low as 1 Hz under Critical pressure). The terminal EndCommitted is the reliable absolute reconciliation (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionService.cs:568), forced by the 15 s idle timeout ( 'if (now - session.LastUpdateMs > OperationTimeoutMs)'). The durable body/item effect is snapshot-backed by P3 - the receiver re-reports immediately (src/CasualtiesUnknownOnline.GameAdapter/MedicalOperationApply.cs:142 'domains.CharacterDataSync.ReportInventoryChanged(body);'). | No session backfill by design: a medical session is transient, a late joiner sees nothing of an in-progress operation, and the operator/target receive the reliable terminal EndCommitted plus the resulting 1 Hz character snapshot. A participant disconnect terminates the session and publishes the terminal (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalOperationSessionService.cs:585 'Terminate(session, MedicalOperationTerminalReason.Disconnected);'); shrapnel flushes its buffered position before a semantic terminal (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/ShrapnelOperationSessionService.cs:406 'internal void FlushBeforeTerminal(ulong operationId) =>' / ). | Start/End/Cancel/State/EndCommitted are reliable. Intermediate injection deltas are coalesced into reliable messages (a slow treatment cannot accumulate a lost delta into a wrong final dose; the EndRequest total is the final reconciliation, documented in src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalInjectionReportBuffer.cs:15-18). Ordinary shrapnel piece positions are unreliable by design with accepted loss: each update is an absolute position per piece and the terminal EndCommitted carries the final piece set. Convergence guaranteed by the terminal message plus the 1 Hz snapshot of the target body. | OK |  |
| P12 | Player color (PlayerColorUpdate) (src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs:187-210, Handlers/PlayerColorUpdateHandler.cs) | Both. Guest -> host report; host -> other guests relay (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/PlayerColorUpdateHandler.cs:34 'session.BroadcastExcept(sender, NetMsg.PlayerColorUpdate, new PlayerColorUpdateMsg'), and the host's own change is a full broadcast (src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs:202 '((ISessionControl)this).Broadcast(NetMsg.PlayerColorUpdate, msg);'). Id: PlayerColorUpdate = 123 (src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:228). | Dedicated NetMsg.PlayerColorUpdate = 123, reliable (PacketSender defaults reliable=true; SessionService.ReportLocalPlayerColor sends at src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs:206 '_sender.Send(HostSteamId, NetMsg.PlayerColorUpdate, msg);'). Trigger: the Preferences color picker calls SessionService.ReportLocalPlayerColor (src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs:187 'public void ReportLocalPlayerColor(PlayerColorValue? color)'). Host applies + relays (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/PlayerColorUpdateHandler.cs:33 'session.GetOrCreateMember(sender).SelectedColor = color;'), guest applies the host's own color . Wire members: PlayerColorUpdate. | none - there is no periodic color re-send. The closest evidence is the only send call sites, src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs:202 (host broadcast) (guest report); a lost update is only refreshed by the join/handshake backfill. | Reconnect / late join: the color is carried on Handshake (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:103 'member.SelectedColor = selectedColor;'), HandshakeAck (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeAckHandler.cs:35 'member.SelectedColor = msg.HasColor ? msg.Color.ToNetColorRgba() : (NetColorRgba?)null;'), PlayerJoin (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/PlayerJoinHandler.cs:33) and the host's own PlayerJoin construction (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:541-554), so every member's color is re-established on any join/reconnect. | Reliable channel; each side's roster presence is the durable store. Two caveats: (1) a lost live update is only healed by a later join/handshake, so a color change can stay stale for the rest of the session; (2) the host's relay of a guest's color is dropped by every other guest - the relay stamps SteamId = the original guest (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/PlayerColorUpdateHandler.cs:36 'SteamId = sender,') while the guest accept branch requires memberId == sender (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/PlayerColorUpdateHandler.cs:44 'if (session.Role == SessionRole.Guest && sender == session.HostSteamId && memberId == sender)'), so third-party name tags converge only on the next join/handshake. | OK |  |
| P13 | Ragdoll / limb pose presentation (src/CasualtiesUnknownOnline.GameAdapter/Character/LimbPoseCapture.cs, RagdollPoseApplication.cs, RemoteBodyDriver.cs, Runtime/Session/EntitySync/PlayerStreamExchange.cs) | Both. The exact limb poses ride the bidirectional P1 20 Hz player stream (host -> guest and guest -> host); the discrete ragdoll collapse rides the dedicated reliable CharacterRagdoll = 120 one-shot (owner -> host -> other guests). | Dedicated one-shot CharacterRagdoll = 120 at the real trigger: GameAdapter/Patches/PlayerCameraHandleInputPatch.cs:61 'PatchBridge.Impl?.OnCharacterRagdoll((Vector2)body.transform.position);' -> CharacterRagdollSync.Report, reliable (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/CharacterDataStore.cs:463). The continuous limb pose has no discrete event by design - it is captured every frame (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:586 '? LimbPoseCapture.Capture(body)') and rides the P1 stream as WirePlayerStreamState.LimbPoses (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamWireMapper.cs:35 'LimbPoses = entity.LimbPoses?.ConvertAll(p => new WirePlayerLimbPose'). | The P1 20 Hz absolute stream: each snapshot carries the full per-limb world position + z rotation (src/CasualtiesUnknownOnline.GameAdapter/Character/LimbPoseCapture.cs:18 'internal static List<PlayerLimbPose>? Capture(Body body)'), applied on the receiver (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamWireMapper.cs:68 'target.LimbPoses = wire.LimbPoses?.ConvertAll(p => new PlayerLimbPose') and then written directly onto the clone (src/CasualtiesUnknownOnline.GameAdapter/Character/SessionStatePump.cs:129 'RagdollPoseApplication.Apply(body, entity.LimbPoses, driver);' -> src/CasualtiesUnknownOnline.GameAdapter/Character/RagdollPoseApplication.cs:50 'limb.transform.position = position;'). Absolute overwrite; the adaptive governor may stretch 20 Hz down to 1 Hz (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:18 MinHz = 1). | World entry / late join: the poses ride the same stream that starts with StartMemberSync's immediate broadcast (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:533); a reconnect is a fresh join with a fresh entity id, so the first snapshot already carries the current pose. The ragdoll one-shot additionally has a clone-creation queue that drains within the 500 ms suppression window (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/RagdollPoseGate.cs:20) and then defers to the stream. | The stream is unreliable with accepted loss (overwrite semantics, P1). The one-shot is reliable and, when it beats the stream, RagdollPoseGate keeps the clone lying until a standing=false snapshot confirms the collapse or 500 ms elapse (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/RagdollPoseGate.cs:41 'return nowMs - collapseMs <= SuppressWindowMs;'); CharacterRagdollSync arms that gate at src/CasualtiesUnknownOnline.GameAdapter/Character/CharacterRagdollSync.cs:181 'driver.RagdollCollapsePending = true;'. Convergence: the next absolute snapshot carries the true pose. | OK |  |
| N1 | Enemy snapshot + attack (EnemySnapshot, EnemyAttack) (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/EnemySnapshotHandler.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/EnemyAttackHandler.cs, src/CasualtiesUnknownOnline.GameAdapter/Character/EnemySyncCoordinator.cs, src/CasualtiesUnknownOnline.GameAdapter/Character/EnemyCombatDirector.cs) | host -> guest (both messages) | EnemySnapshot = NetMsg 81, host -> guest, reliable: the host world-entry fan-out sends it on the member first InWorld edge and on re-entry/reconnect (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:43; src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs:59); send at src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:292, applied at src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:340. EnemyAttack = NetMsg 83, host -> guest, reliable:true: the host enemy-combat director orders a spider bite or crystal lunge when its AI selects a remote victim (src/CasualtiesUnknownOnline.GameAdapter/Character/EnemyCombatDirector.cs:328; src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:149), applied by the victim in src/CasualtiesUnknownOnline.GameAdapter/Character/EnemyCombatReplay.cs:36-63. Wire members: EnemyAttack, EnemySnapshot. | none for either message. The only periodic path is the N3 20 Hz EnemyStateStream absolute overwrite (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:270, reliable:false), which refreshes only continuous fields of already-bound ids; it never re-sends EnemySnapshot.RuntimeSpawns and never re-issues an attack (the broadcast payload carries EnemyStates only, src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:263-268). The host bite path also writes the retreat/cooldown so it cannot re-order immediately (src/CasualtiesUnknownOnline.GameAdapter/Character/EnemyCombatDirector.cs:339-342). | EnemySnapshot is the world-entry/late-join/reconnect backfill itself (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:34-45; src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs:59; src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:145); a death -> menu -> re-enter cycle re-runs the whole group. EnemyAttack has no backfill/recovery: the receive site drops the command when the enemy id is not yet bound (src/CasualtiesUnknownOnline.GameAdapter/Character/EnemyCombatReplay.cs:46) and nothing re-issues it. | Both reliable (PacketSender.Send default reliable=true). The snapshot continuous payload is healed by the 20 Hz stream; a swallowed/refused EnemyAttack is unrecoverable: the host drops it with a warning when the victim is not in world (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:145), the victim drops it when the enemy id is unbound (src/CasualtiesUnknownOnline.GameAdapter/Character/EnemyCombatReplay.cs:46), and no convergence mechanism re-issues the bite. | Event-only gap | todo/enemy-snapshot-and-attack-recovery.md |
| N2 | Enemy kernel facts (Upsert/Remove/Reset + combat results) (src/CasualtiesUnknownOnline.GameState/Domains/Entities/*, Runtime enemy kernel projections) | both (host commits and broadcasts; guest reports ride the command envelope up) | KernelEnvelope (NetMsg 122) carrying CommittedBatch, reliable: every UpsertEnemyCommand / RemoveEnemyCommand / ResetEnemiesCommand / RecordEnemyBite&#124;Lunge&#124;EffectCommand commit is broadcast by KernelProtocolService.BroadcastCommittedBatch (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:91; SendToGuests defaults reliable=true at src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:468). Guest combat reports ride the same envelope Command path (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemyCombatKernelSubmitter.cs:40/63/86); the Entities domain maps each command to its event (src/CasualtiesUnknownOnline.GameState/Domains/Entities/EnemyDomainModule.cs:26-27,) and the guest applies removals from the committed batch at src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:427. Wire members: EnemiesReset, EnemyBiteResult, EnemyEffectResult, EnemyLungeResult, EnemyRemoved, EnemyUpserted, RecordEnemyBite, RecordEnemyEffect, RecordEnemyLunge, RemoveEnemy, ResetEnemies, UpsertEnemy. | none periodic. Recovery is on demand: the world-entry checkpoint plus journal tail (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:94-137; src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41) and the revision-gap range request (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:320-331). | World entry/late join/reconnect: SendCheckpoint sends the full checkpoint then every journal batch above the checkpoint revision (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:101-133; src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41). Missed-range recovery: a guest that sees a revision gap buffers the batch and requests the missing range (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:323,); when the range is outside the journal the host sends a fresh checkpoint (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:393). | Reliable (KernelEnvelope/CommittedBatch; SendToGuests default reliable=true). A lost batch is detected as a GlobalRevision gap and healed by journal replay or a fresh checkpoint; the guest applies enemy removals from the committed batch (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:427). Convergence is guaranteed while the peer stays connected; a session drop ends the session (no host migration). | OK |  |
| N3 | Enemy state stream (20 Hz, EnemyStateBroadcast) (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs, src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs, AdaptiveStreamId.EnemyStateBroadcast) | host -> guest | none at a discrete trigger. This row is the continuous presentation transport: EnemySyncService.Update publishes a WireStateStream batch (WirePayloadType.EnemyStateStream over NetMsg.KernelEnvelope) on the adaptive cadence (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:232-235,). Discrete enemy events (attack, removal) ride N1/N2 instead. Wire members: EnemyStateBroadcast. | 20 Hz base (StateStreamOptions.DefaultStateStreamHz = 20, src/CasualtiesUnknownOnline.Runtime/Configuration/StateStreamOptions.cs:17; AdaptiveStreamId.EnemyStateBroadcast profile src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:34-42), unreliable, absolute overwrite with a per-host seq gate (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:270 reliable:false;). The adaptive governor may stretch it: priority 1, MinHz 1, Critical pressure factor 0.25 -> a 5 Hz floor (plus the byte budget), so the base 50 ms can become 200 ms. | The world-entry EnemySnapshot (N1) binds ids and materializes runtime spawns before the stream is useful (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:273-293). The stream itself is update-only: it never creates or removes an aggregate (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:297-300). | Unreliable by design: every frame is an absolute overwrite of the enemy continuous fields (Merge, src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:348-373), so a lost frame is healed by the next one and stale/duplicate frames are dropped by the seq gate . Removal is not carried here; it rides the kernel EnemyRemovedEvent (N2) and a guest tombstone set blocks stream resurrection (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EnemySyncService.cs:353). | OK |  |
| N4 | Trader state (5 s fallback, TraderStateStream) (src/CasualtiesUnknownOnline.Runtime/Session/World/TradeChannel.cs, src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/TraderStateHandler.cs) | host -> guest | TraderState = NetMsg 72, host -> guest, reliable full overwrite, broadcast immediately when the host executes a trader interaction (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs:113 for a host-local action; src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs:169 after a guest TraderAction). Receive: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/TraderStateHandler.cs:21 -> TradeStateSync.OnTraderStateReceived. Wire members: TraderState, TraderStateStream. | 5 s base absolute overwrite to every in-world member (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs:71-95; src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:117-118: BaseIntervalMs 5000, MaxIntervalMs 30_000). Reliable: src/CasualtiesUnknownOnline.Runtime/Session/World/TradeChannel.cs:34 uses PacketSender.Send default reliable=true, and the class comment keeps the fallback reliable so it cannot overtake an interaction result (src/CasualtiesUnknownOnline.Runtime/Session/World/TradeChannel.cs:24-26). The governor may stretch 5 s to 30 s under pressure (priority 2). CADENCE FINDING: the 5 s base can stretch to 30 s under pressure (todo/sync-cadence-review.md). | No dedicated world-entry send: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:37-44 does not include trader state. A member that enters after an interaction broadcast is covered by the same fallback loop over in-world members (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs:84-95), i.e. within one cadence (5 s, up to 30 s under pressure). Reconnect uses the same path. | Reliable plus absolute overwrite of every sync field and the stock (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs:189-206), so the trader state converges to the host after any missed broadcast. Asymmetry: a lost guest TraderAction is not re-sent, so the host never executes that trader-side change and the next overwrite reverts the guest provisional trader state (the acting side local item/exp stays - accept-first tradeoff). | OK |  |
| N5 | Trader action / recruit / hostile swing (src/CasualtiesUnknownOnline.Runtime/Session/World/TradeChannel.cs, src/CasualtiesUnknownOnline.GameAdapter/World/TradeExecutor.cs, src/CasualtiesUnknownOnline.GameAdapter/World/TraderRecruitCoordinator.cs, src/CasualtiesUnknownOnline.GameAdapter/World/TraderSwingSync.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/Trader*Handler.cs) | TraderAction guest -> host; TraderRecruitRequest guest -> host; TraderRecruitResult host -> target; TraderSwing both (guest -> host report, host -> guest relay) | TraderAction = NetMsg 73, guest -> host, reliable: sent at the game method report point (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs:138); the host executes the trader-side change and broadcasts TraderState (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs:169). TraderRecruitRequest = NetMsg 107 (src/CasualtiesUnknownOnline.GameAdapter/World/TraderRecruitCoordinator.cs:109); TraderRecruitResult = NetMsg 108, reliable, directed to the revived player (src/CasualtiesUnknownOnline.GameAdapter/World/TraderRecruitCoordinator.cs:246). TraderSwing = NetMsg 115, reliable one-shot (src/CasualtiesUnknownOnline.GameAdapter/World/TraderSwingSync.cs:55; host fan-out src/CasualtiesUnknownOnline.Runtime/Session/World/TradeChannel.cs:126), replayed at src/CasualtiesUnknownOnline.GameAdapter/World/TraderSwingSync.cs:80. Wire members: TraderAction, TraderRecruitRequest, TraderRecruitResult, TraderSwing. | TraderAction: the trader-side state is healed by the N4 TraderState 5 s absolute fallback (src/CasualtiesUnknownOnline.GameAdapter/World/TradeStateSync.cs:93). TraderRecruitResult: none - a reliable one-shot whose revived state is persisted host-side (src/CasualtiesUnknownOnline.GameAdapter/World/TraderRecruitCoordinator.cs:229) and owned/restored by the character-data row. TraderSwing: none, and the loss is explicitly accepted (src/CasualtiesUnknownOnline.Runtime/Protocol/Messages/TraderSwingMsg.cs:13). | Recruit: the host saves the revived character snapshot (src/CasualtiesUnknownOnline.GameAdapter/World/TraderRecruitCoordinator.cs:229), which the character-data row owns and hands back on world re-entry/reconnect. TraderAction: no replay - the N4 overwrite is the only convergence. TraderSwing: no replay by design (presentation-only). | All three use reliable sends by default. Swing loss is explicitly accepted as presentation degradation (src/CasualtiesUnknownOnline.Runtime/Protocol/Messages/TraderSwingMsg.cs:13). A lost TraderAction report is not re-issued; the N4 overwrite converges the trader to the host but silently reverts the guest provisional change. A lost recruit result is recoverable only through the character-data restore path, not by a periodic recruit re-send. | OK |  |
| N6 | Chat (src/CasualtiesUnknownOnline.Runtime/Session/World/ChatChannel.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/ChatHandler.cs) | both (guest -> host report, host -> guest relay; never guest -> guest) | Chat = NetMsg 109, bidirectional, reliable: ChatService.TrySend adds the line locally and sends (src/CasualtiesUnknownOnline.Runtime/Session/Chat/ChatService.cs:66 guest -> host); the host validates and relays except the author (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/ChatHandler.cs:40 -> src/CasualtiesUnknownOnline.Runtime/Session/World/ChatChannel.cs:37); the guest report path is src/CasualtiesUnknownOnline.Runtime/Session/World/ChatChannel.cs:26. Wire members: Chat. | none - the only send call sites are src/CasualtiesUnknownOnline.Runtime/Session/World/ChatChannel.cs:26 (guest report) and src/CasualtiesUnknownOnline.Runtime/Session/World/ChatChannel.cs:37 (host relay); no periodic re-send exists. | none - the world-entry snapshot group (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:37-44) contains no chat, and the ChatService 50-line buffer is local-only and cleared on session end (src/CasualtiesUnknownOnline.Runtime/Session/Chat/ChatService.cs:108); the chat selfcheck records no cross-session history. | Reliable transport (PacketSender.Send default reliable=true), but a line that is never delivered is not recovered by anything: no snapshot, no history backfill, no re-request. A disconnect/reconnect loses the line; the host relay also excludes the author (the author UI already showed it). ACCEPTED (audit adjudication): one-shot user message; reliable transport; no authoritative state; chat history backfill is a separate feature, not part of the sync contract. | Transient-by-design |  |
| N7 | Speech bubbles (SpeechMsg) (src/CasualtiesUnknownOnline.Runtime/Session/World/SpeechChannel.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SpeechHandler.cs, src/CasualtiesUnknownOnline.GameAdapter/World/SpeechSync.cs) | both (player bubble: guest -> host report, host -> guest relay; trader bubble: host -> all) | SpeechMsg = NetMsg 74, bidirectional, reliable: SpeechSync.OnSpeechReported sends at the game talk path (src/CasualtiesUnknownOnline.GameAdapter/World/SpeechSync.cs:65 guest report; src/CasualtiesUnknownOnline.GameAdapter/World/SpeechSync.cs:52 host trader bubble); src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SpeechHandler.cs:25 relays except the source; the report path is src/CasualtiesUnknownOnline.Runtime/Session/World/SpeechChannel.cs:28. The receiver writes the final string into the clone/trader talker (src/CasualtiesUnknownOnline.GameAdapter/World/SpeechSync.cs:138). Wire members: SpeechMsg. | none - the only send call sites are src/CasualtiesUnknownOnline.Runtime/Session/World/SpeechChannel.cs:28 (report), src/CasualtiesUnknownOnline.Runtime/Session/World/SpeechChannel.cs:42 (host all) and src/CasualtiesUnknownOnline.Runtime/Session/World/SpeechChannel.cs:46 (host except source); no periodic re-send exists. | none - no world-entry snapshot and no history; the bubble is typed out and cleared locally and the text is never re-derived. | Reliable transport (PacketSender.Send default reliable=true), but a bubble that is never delivered is gone: no snapshot, no backfill, and no accepted-loss comment in SpeechMsg. Loss is only masked by transport reliability. ACCEPTED (audit adjudication): presentation bubble; no state divergence; no backfill by design. | Transient-by-design |  |
| N8 | Mod API (ModMessage, ModCommandRequest, ModCommandResult) (src/CasualtiesUnknownOnline.Runtime/Session/Mods/*, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/ModMessageHandler.cs, ModCommandRequestHandler.cs, ModCommandResultHandler.cs, ModRateLimitPolicy.cs) | ModMessage both (guest -> host report; host -> peer/all); ModCommandRequest guest -> host; ModCommandResult host -> guest | ModMessage = NetMsg 75, reliable: ModChannel.SendToHost (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModChannel.cs:41), SendToPeer (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModChannel.cs:52), SendToAll (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModChannel.cs:68), applied at src/CasualtiesUnknownOnline.Runtime/Session/Handlers/ModMessageHandler.cs:21. ModCommandRequest = NetMsg 86 (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModCommandService.cs:203) and ModCommandResult = NetMsg 87 (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModCommandService.cs:215), applied at src/CasualtiesUnknownOnline.Runtime/Session/Handlers/ModCommandResultHandler.cs:19. Wire members: ModCommandRequest, ModCommandResult, ModMessage. | none - the send call sites above are the only ones; there is no periodic re-send and no snapshot/replay for any of the three messages. | none - no world-entry replay and no late-join state. A guest command request registers a pending callback (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModCommandService.cs:327) that only the result frame (or a session-end failure) resolves. | Frames are reliable at the transport (PacketSender.Send default), but the domain drops on purpose: the per-sender token bucket drops over-burst frames with a log and never queues them (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModLifecycle.cs:283; src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModRateLimitPolicy.cs:7), so a mod message or command request can be lost without recovery. The API doc records frame reliability only for the transport. ACCEPTED (audit adjudication): the rate-limit drop is the documented policy (ModRateLimitPolicy.cs:7); mod payloads are opaque and loss-tolerant. Finding: a dropped ModCommandRequest leaves the caller's pending callback unresolved until session end -> todo/mod-command-request-timeout.md. | Transient-by-design | todo/mod-command-request-timeout.md |
| N9 | Location ping (LocationPing) (src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingChannel.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/LocationPingHandler.cs) | both (guest -> host report, host -> guest relay) | LocationPing = NetMsg 124, bidirectional, reliable: LocationPingService.TryPlace sends on the middle-click trigger (src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingService.cs:73 -> src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingChannel.cs:41 guest, src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingChannel.cs:37 host); the host relays except the source (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/LocationPingHandler.cs:24); the receiver adds the marker locally (src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingService.cs:123). Wire members: LocationPing. | none - and the absence is deliberate: src/CasualtiesUnknownOnline.Runtime/Protocol/Messages/LocationPingMsg.cs:10 states there is no snapshot fallback. The only send call sites are src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingChannel.cs:37, . | none - the marker is a 5 s local UI object (src/CasualtiesUnknownOnline.Runtime/Session/World/LocationPingService.cs:20) held in a per-player transient buffer and cleared on session end; no world-entry/backfill path. | Reliable transport (PacketSender.Send default), transient UI presentation with no persistent state (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/LocationPingHandler.cs:12); a lost ping is accepted by design and the marker expires after 5 s anyway. | Transient-by-design |  |
| N10 | Tutorial claw stream (TutorialClawState) (src/CasualtiesUnknownOnline.Runtime/Session/Tutorial/TutorialClawService.cs, src/CasualtiesUnknownOnline.Runtime/Session/Handlers/TutorialClawStateHandler.cs, src/CasualtiesUnknownOnline.GameAdapter/Tutorial/TutorialClawSync.cs) | host -> guest | none at a discrete trigger - the only mechanism is the continuous stream: the adapter publishes the current claw state every frame (src/CasualtiesUnknownOnline.GameAdapter/Tutorial/TutorialClawSync.cs:49 -> src/CasualtiesUnknownOnline.Runtime/Session/Tutorial/TutorialClawService.cs:55) and the service broadcasts it on the adaptive cadence (src/CasualtiesUnknownOnline.Runtime/Session/Tutorial/TutorialClawService.cs:93-98,). There is no dedicated one-shot for a claw/grab/material edge. Wire members: TutorialClawBroadcast, TutorialClawState. | 20 Hz base (StateStreamOptions.DefaultStateStreamHz = 20; AdaptiveStreamId.TutorialClawBroadcast, priority 2, MinHz 1), unreliable, absolute overwrite with a seq gate (src/CasualtiesUnknownOnline.Runtime/Session/Tutorial/TutorialClawService.cs:116 reliable:false;). The governor may stretch 20 Hz to 3 Hz under Critical pressure (factor 0.15). | none explicit - a joiner/reconnect waits for the next stream frame (at most 50 ms at base, about 333 ms at the 3 Hz floor); there is no world-entry claw snapshot and the host publishes only while TutorialHandler.main exists (src/CasualtiesUnknownOnline.GameAdapter/Tutorial/TutorialClawSync.cs:42-49). | Unreliable absolute overwrite: a lost frame is healed by the next one and stale/duplicate frames are dropped by the seq gate (src/CasualtiesUnknownOnline.Runtime/Session/Tutorial/TutorialClawService.cs:63). No history is needed; course/prop state remains per-side by design. | OK |  |
| R1 | Run start / advance layer kernel facts (src/CasualtiesUnknownOnline.GameState/Domains/World/*, Runtime/Session/World/WorldRunStateMapper.cs, GameAdapter/Run/*, GameAdapter/WorldGen/*) | Host -> guests only. The host is the sole author of the run baseline (AuthorityKind.HostOnly); guests never send run commands and receive RunStartedEvent/RunAdvancedEvent inside a CommittedBatchEnvelope over NetMsg.KernelEnvelope. | Dedicated kernel events, not a snapshot. Real trigger 1 (run start) is the host click moment: src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:170 '_params.CaptureAtEntry(isTutorial); // the generation baseline — captured at the click moment, before any run randomness is consumed' -> WorldParamsService.CaptureAtEntry (src/CasualtiesUnknownOnline.GameAdapter/World/WorldParamsService.cs:49) -> IWorldControl.PublishWorldParams (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:466 'public void PublishWorldParams(WorldStartParams parameters)') -> CommitRunBaseline (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:472) -> ItemKernelAuthority.TryStartRun (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:109). Real trigger 2 (layer advance) is the GenerateWorld boundary: src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:195 '_params.OnGenerateBoundary();' -> WorldParamsService.CaptureAtBoundary -> the same PublishWorldParams -> TryAdvanceLayer (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:120). Commit raises BatchCommitted (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:596 'BatchCommitted?.Invoke(batch!);') and KernelProtocolService.BroadcastCommittedBatch (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:68) fans out the envelope ( 'SendToGuests(frame);'). Numeric ids: WirePayloadType.RunStartedEvent = 105, RunAdvancedEvent = 106 (src/CasualtiesUnknownOnline.Protocol/Wire/WirePayloadType.cs:44-45); WireEventKind.RunStarted/RunAdvanced (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelWireMapper.cs:341-343). Reliability: src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:493 'private void SendToGuests(ProtocolFrame frame, bool reliable = true)' — reliable by default (PacketSender default true, src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:29). Guest apply: HandleCommittedBatch -> ApplyBatchAndDrain -> _authority.Apply -> WorldService.RunBatchProjection -> ApplyRunProjection . Wire members: AdvanceLayer, RunAdvanced, RunStart, RunStarted. | Absolute kernel checkpoint, idempotent, no adaptive governor. The kernel checkpoint carries RunState and is an absolute overwrite of the guest kernel (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:474 'var result = _authority.Restore(checkpoint);'). It is not on its own timer but rides the host's 60 s world-resend cycle: src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122 'if (IsHostMode && _session.SessionActive && Time.unscaledTime - _lastSnapshotResend > 60f)' -> '_kernelProtocol.SendCheckpoint(member.SteamId); // the kernel checkpoint also covers lazy-session recovery for WorldEntities and the other kernel domains'. It is reliable (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:114 '_sender.Send(targetSteamId, NetMsg.KernelEnvelope, frame);', PacketSender default reliable=true) and is NOT an AdaptiveStreamId, so the governor cannot stretch it. The 'run' projection is also rebuildable locally: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:91 '_projectionHealth.Register(new ProjectionDomain("run", RebuildRunFromKernel, () => _kernelAuthority.CurrentGlobalRevision));'. | World entry / late join / reconnect: WorldEntryFanout.Send (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41 '_kernelProtocol.SendCheckpoint(steamId);') called from the InWorld edge (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs:59) and from a reconnect-while-InWorld handshake (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:145). Mid-generation join: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:192 (kernel checkpoint carries the run baseline). Missed-range recovery: a revision gap buffers the future batch and requests the missing range (src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:351 'RequestRange(expected, batch.GlobalRevision - 1);'); the host replays the journal range or falls back to a fresh checkpoint (HandleRangeRequest ). Local projection failure: ProjectionHealthCoordinator rebuild (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:537 'private void RebuildRunFromKernel()'). | Reliable channel in both directions; loss is not accepted but is tolerated by design at the envelope level: a duplicate batch is idempotent by OperationId (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemKernelAuthority.cs:88 'if (_appliedOperations.Contains(batch.OperationId))'), a gap is repaired by range request or a fresh checkpoint, and a checkpoint is an absolute overwrite, so convergence to host authority is guaranteed (no unhealed run/layer divergence). | OK |  |
| R2 | World time (WorldTimeRequest, WorldTime, 5 s) (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldTimeChannel.cs, Handlers/WorldTimeHandler.cs, WorldTimeRequestHandler.cs, GameAdapter/World/WorldTimeSync.cs) | Both, star shaped: guest -> host WorldTimeRequest (NetMsg id 90) carries the local speed intent; host -> guest WorldTime (NetMsg id 91) carries the authoritative speed to every member. | Dedicated messages at the real triggers, never a state snapshot. Host -> guest: the policy verdict is broadcast on a real change (src/CasualtiesUnknownOnline.GameAdapter/World/WorldTimeSync.cs:245-255, send at '_worldTime.Broadcast(_appliedSpeed);'), from the guest request path (OnRequestReceived ), from a local SetTimeScale change (OnLocalTimeScaleChanged '_requestedSpeed = ToWorldTimeSpeed(speed);') and from a direct Time.timeScale write adopted by the host ( '_worldTime.Broadcast(_appliedSpeed);'). Guest -> host: a manual speed intent is reported at the game's SetTimeScale hook (src/CasualtiesUnknownOnline.GameAdapter/World/WorldTimeSync.cs:121 '_worldTime.SendRequest(ToWorldTimeSpeed(speed));'). Wire send: src/CasualtiesUnknownOnline.Runtime/Session/World/WorldTimeChannel.cs:43 '_session.Broadcast(NetMsg.WorldTime, new WorldTimeMsg { Speed = speed });' '_sender.Send(_session.HostSteamId, NetMsg.WorldTimeRequest, new WorldTimeRequestMsg { Speed = speed });'. Numeric ids: src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:108-109. Both are reliable (PacketSender default reliable=true; neither call site passes reliable:false). Receive: WorldTimeHandler ( 'ctx.WorldTime.FireTimeReceived(msg.Speed);') and WorldTimeRequestHandler ( 'ctx.WorldTime.FireRequestReceived(sender, msg.Speed);'). Wire members: WorldTime, WorldTimeRequest. | Yes — an absolute, idempotent 5 s resend. Cadence: src/CasualtiesUnknownOnline.GameAdapter/World/WorldTimeSync.cs:47 'private const float ResendIntervalSeconds = 5f;', pumped at with the send at '_worldTime.Broadcast(_appliedSpeed);'. Semantics are absolute overwrite of the receiver's applied speed (src/CasualtiesUnknownOnline.GameAdapter/World/WorldTimeSync.cs:208 '_appliedSpeed = incoming;') and the resend is explicitly idempotent ( '// the 5 s periodic resend is idempotent — do not replay the speed-change UI sound every tick'). The governor cannot stretch it: WorldTime is not an AdaptiveStreamId (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamId.cs) and WorldTimeSync never queries AdaptiveStreamRateService. | World entry / re-entry: a member (re)entering the world gets the current speed immediately instead of waiting up to 5 s (src/CasualtiesUnknownOnline.GameAdapter/World/WorldTimeSync.cs:221 '_worldTime.Broadcast(_appliedSpeed);' inside OnRemoteSceneChanged). Late join / reconnect also receives it through the world-entry backfill path? No dedicated snapshot exists; the 5 s resend plus the re-entry broadcast are the recovery. The guest also enforces the last host speed locally if a direct write moved Time.timeScale (EnforceAppliedSpeed,), so a swallowed broadcast self-heals within 5 s. | Reliable channel both ways (PacketSender default true). Loss is tolerated only in the sense that a dropped broadcast is harmless: the next 5 s resend is an absolute overwrite, so convergence is bounded by 5 s. No additive semantics anywhere (the policy clears the request on override, WorldTimePolicy.Decide ). | OK |  |
| R3 | World join/ready + late-join orchestration (WorldJoin, WorldReady, SceneState, PlayerJoin, PlayerLeave) (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs, SessionService.cs, Handlers/WorldJoinHandler.cs, WorldReadyHandler.cs, SceneStateHandler.cs, PlayerJoinHandler.cs, PlayerLeaveHandler.cs) | Both. Host -> guest: WorldJoin (20), WorldReady (21), PlayerJoin (32), PlayerLeave (33), WorldSnapshotComplete (110); guest -> host: SceneState (18) report, which the host relays to the other guests; host -> guest: SceneState broadcast. | Dedicated messages at the real triggers, all reliable (PacketSender default reliable=true). WorldJoin is sent at the host click moment (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:174 '_world.SendWorldJoin(isTutorial);') and re-sent at host world entry to members not yet in world ( '_world.SendWorldJoin(_world.WorldParams?.IsTutorial ?? false);'); WorldReady is sent on gate release (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:198 '_sender.Send(member.SteamId, NetMsg.WorldReady, msg);') or as a targeted late-joiner pass . SceneState is reported only on the local InWorld/InMenu edge and on session activation (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:358 '_session.ReportSceneState(inWorld ? SceneStateType.InWorld : SceneStateType.InMenu, sceneName, pos);'); these are the only two call sites in the tree. PlayerJoin self-activation is sent once per member sync start (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:513 '_sender.Send(presence.SteamId, NetMsg.PlayerJoin, joinMsg); // self-activation') plus a one-time roster backfill . Numeric ids: src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:18-24. Wire members: PlayerJoin, PlayerLeave, SceneState, WorldJoin, WorldReady. | none | No periodic re-send of any orchestration message exists: SceneState is emitted only at the two edge call sites (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:358 ) and PlayerJoin only at src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:513 and src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/EntitySyncService.cs:528; no timer, cadence constant or adaptive stream exists for WorldJoin/WorldReady/SceneState/PlayerJoin. The only time-driven things on this path are the host start-gate force-release (30 s, src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:52) and the guest's 60 s give-up (src/CasualtiesUnknownOnline.GameAdapter/Run/StartGateCoordinator.cs:187), both of which abandon rather than heal. Backfill of the snapshot group exists but only on a NEW InWorld edge or handshake: SceneStateHandler fires WorldEntryFanout on the InWorld edge (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/SceneStateHandler.cs:59 '_worldEntryFanout.Send(reporter);') and HandshakeHandler fires it on a reconnect-while-InWorld (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:145 '_worldEntryFanout.Send(sender);'), ending with the explicit group marker (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:44 '_world.SendWorldSnapshotComplete(steamId);'). Late join/start gate: WorldService.NotifyMemberInWorld sends a targeted WorldReady to a member entering after release . Missed-range recovery for the kernel part rides the kernel checkpoint inside the same fan-out (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntryFanout.cs:41). A swallowed SceneState report or PlayerJoin while the peer stays connected is not re-driven: the host never marks the member InWorld (SceneStateHandler 'member.InWorld = msg.State == (byte)SceneStateType.InWorld;') and no code re-reports it, so the entity stream never starts and the guest is dropped as 'no member with that entity id' (src/CasualtiesUnknownOnline.Runtime/Session/EntitySync/PlayerStreamExchange.cs:103). | Reliable channel (PacketSender default true), but a send failure is ignored on these paths (Send returns void) and there is no retry/periodic re-report, so a swallowed orchestration event is only healed by a new handshake or a leave/re-enter edge. Guest-side safety valve: 60 s then back to the menu (src/CasualtiesUnknownOnline.GameAdapter/Run/StartGateCoordinator.cs:187). Convergence guarantee: not for a single lost message; no absolute re-report of member scene state exists. | Event-only gap | todo/session-control-convergence.md |
| R4 | Session lifecycle/control (Handshake, HandshakeAck, HandshakeAckAck, Ping, Pong, Kicked, Banned) (src/CasualtiesUnknownOnline.Runtime/Session/SessionPeerMaintenance.cs, SessionService.cs, Handlers/HandshakeHandler.cs, HandshakeAckHandler.cs, HandshakeAckAckHandler.cs, HostKickService.cs, HostBanService.cs) | Both. Guest -> host: Handshake (16), HandshakeAckAck (58), Ping (1), Pong (2); host -> guest: HandshakeAck (17), Pong, Kicked (111), Banned (112); Ping/Pong are bidirectional. | Dedicated control messages at real triggers, reliable (PacketSender default true). Guest handshake send/retry: src/CasualtiesUnknownOnline.Runtime/Session/SessionPeerMaintenance.cs:73 '_sender.Send(_identity.HostSteamId, NetMsg.Handshake, CreateHandshakeMsg());' (kicked at KickHandshake ). Host acks every handshake, including repeats: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeHandler.cs:173 '_sender.Send(sender, NetMsg.HandshakeAck, new HandshakeAckMsg'. Guest answers with the third leg: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeAckHandler.cs:56 '_sender.Send(sender, NetMsg.HandshakeAckAck, new HandshakeAckAckMsg());'. The host only marks the member confirmed on that leg: src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeAckAckHandler.cs:43 'member.Handshaken = true;'. Ping/Pong: SessionService.RequestPing on the 5 s pump; PingHandler echoes , PongHandler records RTT . Kick/Ban: src/CasualtiesUnknownOnline.Runtime/Session/HostKickService.cs:31 and src/CasualtiesUnknownOnline.Runtime/Session/HostBanService.cs:57 send the dedicated message reliably before removing the member. Numeric ids: src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:11-17,. Wire members: Banned, Handshake, HandshakeAck, HandshakeAckAck, Kicked, Ping, Pong. | Partial. The Handshake leg has a 1 Hz idempotent retry (src/CasualtiesUnknownOnline.Runtime/Session/SessionPeerMaintenance.cs:34 'private const float HandshakeRetryInterval = 1f;', RetryHandshakeIfNeeded , send ), and the host re-acks every repeat, so a lost Handshake or HandshakeAck heals. But the retry is gated on the session not being active: src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs:281 'if (!SessionActive)' -> '_peerMaintenance.RetryHandshakeIfNeeded();'. The guest flips SessionActive on receiving the ack (src/CasualtiesUnknownOnline.Runtime/Session/Handlers/HandshakeAckHandler.cs:39 'session.SessionActive = true;'), so once the ack arrives the guest stops retrying and a lost HandshakeAckAck has no periodic re-send. Ping/Pong runs at 5 s (src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs:31 'private const float PingInterval = 5f;') but carries no handshake state and does not re-drive it. Kicked/Banned are one-shot reliable with no fallback; the host's presence removal is authoritative and the guest notices only by leaving the lobby. | Reconnect: a guest that re-enters the lobby calls KickHandshake again (src/CasualtiesUnknownOnline.Runtime/Session/SessionService.cs:400 '_peerMaintenance.KickHandshake();'), and the host reuses the presence and re-acks (HandshakeHandler ). Host warm-up: un-handshaken lobby peers receive a Ping every 1 s so the lazy Steam P2P session establishes (src/CasualtiesUnknownOnline.Runtime/Session/SessionPeerMaintenance.cs:78-121, send ). Presence reconciliation every 2 s removes vanished members / ends the session when the host is gone (CheckPeerPresence ). No recovery exists for a lost HandshakeAckAck while both sides stay connected: the host keeps the member un-handshaken, so the start gate excludes it (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:114) and the WorldReady fan-out skips it (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:196). | Reliable channel (PacketSender default true; Kick/Ban pass reliable: true explicitly). Handshake and HandshakeAck loss is accepted and healed by the 1 Hz retry; HandshakeAckAck loss is NOT healed — the host's member stays Handshaken=false for the rest of the connection, which is a convergence gap (the guest believes it is connected). Kicked/Banned loss leaves the guest as a zombie until it leaves the lobby; the host side has already removed it. | Event-only gap | todo/session-control-convergence.md |
| R5 | Adaptive rate governor (all 11 AdaptiveStreamId profiles): cadence-only adaptation, never removes a fallback, terminal/full-state reconciliation per stream (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/*) | Host-side (and guest-side for the two report streams) cadence decision only; the governor itself sends nothing. It answers 'what interval should this stream use now?' for 11 registered stream ids. | Not applicable in the event sense: the governor has no trigger and no message of its own; it is a pure query surface (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamRateService.cs:41 'public int GetEffectiveHz(AdaptiveStreamId streamId, ulong peerId)' GetSendIntervalMs). Its only state is a per-peer log-change cache . The 11 profiles are declared once in src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:15 'private static readonly IReadOnlyDictionary<AdaptiveStreamId, AdaptiveStreamProfile> Profiles =' (PlayerStateBroadcast, PlayerStateReport, EnemyStateBroadcast, TutorialClawBroadcast, MedicalInjectionReport, ShrapnelPositionReport, WorldItemMoveStream, WorldItemSnapshotStream, FluidRegionDiffStream, FluidRegionFullStream, TraderStateStream). | Cadence-only by construction; no fallback can be removed. Reliable control streams bypass adaptation entirely (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveRatePolicy.cs:30 'if (profile.DeliveryMode == AdaptiveStreamDeliveryMode.ReliableControl)'). Hz streams are floored at the profile MinHz=1 (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveRatePolicy.cs:44 'return Clamp(effective, profile.MinHz, profile.MaxHz);', MinHz declared in src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:12), and interval-based fallbacks are hard-capped at the profile MaxIntervalMs (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveRatePolicy.cs:78 'effective = Math.Min(effective, profile.MaxIntervalMs);' — e.g. WorldItemSnapshotStream BaseIntervalMs 5000 / MaxIntervalMs 30_000, src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:86-87). So pressure can stretch a fallback (5 s -> up to 30 s for the item/trader keyframes) but never delete it. Every stream keeps a terminal/full-state reconciliation: item 5 s full-table keyframe (src/CasualtiesUnknownOnline.GameAdapter/Items/ItemPositionAuthority.cs:52 'if (now >= _nextItemSnapshotMs)'), fluid 1 Hz full viewport (src/CasualtiesUnknownOnline.GameAdapter/World/FluidSimulationAuthority.cs:311 'if (now >= _nextFull)'), trader 5 s absolute snapshot, medical cumulative deltas ride the reliable channel with the terminal EndRequest total (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/MedicalInjectionReportBuffer.cs:104 '_sender.Send(_session.HostSteamId, NetMsg.MedicalOperationUpdate, msg, reliable: true);'). | Per-stream, not in the governor: the fallback stream is the recovery (item/fluid/trader/enemy/tutorial full-state), plus the world-entry snapshot group. The governor's Reset clears per-peer rate caches on a session/traffic reset (src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamRateService.cs:158-162). | The governor only adapts streams whose delivery mode tolerates loss (LatestWins absolute-overwrite or Cumulative with a terminal reconciliation); reliable control paths are never throttled. Accepted loss is explicit in src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamDeliveryMode.cs:17-29. Convergence guarantee comes from the per-stream fallback, not from the governor. | OK |  |
| R6 | Cross-check: projection rebuild / health recovery (src/CasualtiesUnknownOnline.Runtime/Session/ProjectionHealth/*, docs/architecture/projection-framework.md) | Local (per client). Not a network surface: it rebuilds a local read model from the local kernel when a projection throws. It does not send or apply remote state. | Trigger is the kernel commit/apply event, not a network message: the domain projection is invoked under health tracking (src/CasualtiesUnknownOnline.Runtime/Session/ProjectionHealth/ProjectionHealthCoordinator.cs:73 'public void Run(string domain, ulong revision, Action projection)'), e.g. the run domain at src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:509 '_projectionHealth.Run("run", batch.GlobalRevision, () =>'. A thrown projection is captured and the domain marked dirty (, MarkFailure ). | Main-thread pump, not a timer: src/CasualtiesUnknownOnline.Runtime/Session/ProjectionHealth/ProjectionHealthCoordinator.cs:105 'public void Pump()' driven by the service Update loop ( 'void ICuoService.Update() => Pump();'). Dirty domains are rebuilt from the kernel read model (Rebuild , success clears dirty at 'state.Dirty = false;'). It is a local recovery mechanism, not a network fallback, so the adaptive governor does not apply. | Seven production domains are registered: run (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:91), world-entities (src/CasualtiesUnknownOnline.Runtime/Session/World/WorldEntityKernelProjection.cs:43), fluids (src/CasualtiesUnknownOnline.Runtime/Session/World/FluidKernelReadProjection.cs:38), players-carry (src/CasualtiesUnknownOnline.Runtime/Session/PlayerInteraction/PlayerKernelCarryProjection.cs:38), items (src/CasualtiesUnknownOnline.Runtime/Session/Items/ItemService.cs:67), remote-character-presentation (src/CasualtiesUnknownOnline.Runtime/Session/CharacterData/RemoteCharacterPresentationStore.cs:47), mod-status (src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModStatusProjectionReadModel.cs:44). Rebuild callbacks read the kernel read model, e.g. the run domain at src/CasualtiesUnknownOnline.Runtime/Session/World/WorldService.cs:537 'private void RebuildRunFromKernel()'. This heals a broken local projection only; a lost network batch is healed by R1's checkpoint/range-request path. | No channel of its own; loss is not applicable. The guarantee is local convergence: a failed projection never rolls back the kernel commit, and the domain stays dirty until a rebuild succeeds (repeated failures escalate to Degraded, src/CasualtiesUnknownOnline.Runtime/Session/ProjectionHealth/ProjectionHealthCoordinator.cs:194-197). Not a substitute for a network fallback. | OK |  |
| R7 | Cross-check: world fingerprint divergence detector (GameAdapter/Run/RunCoordinator.cs (grep WorldFingerprint), docs/backlog/review/world-determinism-world-fingerprint.md) | Local diagnostic only; nothing is sent or received. The fingerprint is written to the local log for manual peer-log comparison. | One-shot diagnostic at the world-entry edge, not a sync message. Guarded by a one-shot latch: src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:70 'private bool _worldFingerprintLogged;' 'if (inWorld && !_worldFingerprintLogged)' -> '_worldFingerprintLogged = true;' -> LogWorldFingerprint . The only output is a log line (, format string at '"[WorldFingerprint] {W}x{H}: {B0:X16} {B1:X16} {B2:X16} {B3:X16} {B4:X16} {B5:X16} {B6:X16} {B7:X16} total {Total:X16}",'). | none | No periodic re-fingerprint, no comparison against the peer's value, no alert and no repair path exists; the only re-arm is the session-end reset (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:501 '_worldFingerprintLogged = false;'), i.e. the next world entry logs once again. Closest evidence of the absence: the single emit call site is LogWorldFingerprint's log statement (src/CasualtiesUnknownOnline.GameAdapter/Run/RunCoordinator.cs:399-402) and the only callers are the one-shot guard at . Divergence is only observable by manually diffing the two peers' log lines (docs/history/audits/worldgen-determinism-audit.md:35 'compare `[GenStream]` segment fingerprints and `[WorldFingerprint]` between'), and the runtime never acts on it. The block-state 60 s absolute resend (src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122-133) can mask block-cell differences but is not a fingerprint comparison. | Not applicable (no channel). The fingerprint is a best-effort diagnostic: it is emitted once per world entry and never re-sampled, so a divergence introduced after world entry is never detected by it. ADJUDICATED: diagnostic only, not a sync path; one-shot at world entry, manual peer-log comparison during final acceptance. Recorded in docs/backlog/review/world-determinism-world-fingerprint.md. | Transient-by-design | review/world-determinism-world-fingerprint.md (updated) |
| R8 | Cross-check: network traffic baseline (cost of fallbacks) (src/CasualtiesUnknownOnline.Runtime/Session/NetworkTraffic/*, docs/backlog/review/network-traffic-baseline.md) | Local observability only. PacketSender/PacketReceiver report raw send/receive facts; SessionService reports the ping/pong probe results. No payload is produced or applied for gameplay state. | Not a sync surface: the monitor observes the existing messages. Send facts are recorded per peer/message with the classified WirePayloadType (src/CasualtiesUnknownOnline.Runtime/Session/PacketSender.cs:48 '_traffic.RecordSend(steamId, msg, frame.Length, success, ClassifyPayloadType(payload));' -> src/CasualtiesUnknownOnline.Runtime/Session/NetworkTraffic/NetworkTrafficMonitor.cs:34 'internal void RecordSend(ulong steamId, NetMsg msg, int byteCount, bool success, WirePayloadType? payloadType = null) =>'), receive facts by payload type . Peer health is fed by the 5 s ping/pong probe (SessionService.RequestPing -> RecordPingSent/RecordPong). | The monitor's own cadence is the rolling window roll + periodic log (src/CasualtiesUnknownOnline.Runtime/Session/NetworkTraffic/NetworkTrafficMonitor.cs:86 '_log.LogInformation("[NetworkTraffic] {Window}", NetworkTrafficWindowLog.Format(window));'), plus the per-peer health line . It is an observability cadence, not a state fallback; it neither sends nor heals gameplay state, so the adaptive governor does not apply. Its health snapshot is an input to the governor (src/CasualtiesUnknownOnline.Runtime/Session/NetworkTraffic/NetworkTrafficMonitor.cs:57 'internal bool TryGetHealthSnapshot(ulong steamId, out PeerHealthTracker.PeerHealthSnapshot snapshot) =>'). | Not applicable — no state is synced. The probe loss accounting is the recovery-adjacent part: an unanswered probe is counted lost when the next probe goes out (src/CasualtiesUnknownOnline.Runtime/Session/NetworkTraffic/PeerHealthTracker.cs:31 'state.PingsLost++;'), which lets the adaptive layer lower cadence instead of dropping a fallback. | No channel of its own. The counters are observability-only (src/CasualtiesUnknownOnline.Runtime/Session/NetworkTraffic/NetworkTrafficMonitor.cs:18-21); the per-session reset clears them (Reset ). The cost of fallbacks is measured per WirePayloadType, which is what this cross-check row exists for. | OK |  |

## Wire vocabulary index (member -> owning matrix row)

Every wire/stream discriminator is listed here; SyncCoverageGateTests fails when a
member is missing, duplicated, or points at a row whose verdict is not in the vocabulary.

| Member | Kind | Row |
|---|---|---|
| Ping | NetMsg | R4 |
| Pong | NetMsg | R4 |
| Handshake | NetMsg | R4 |
| HandshakeAck | NetMsg | R4 |
| HandshakeAckAck | NetMsg | R4 |
| SceneState | NetMsg | R3 |
| WorldJoin | NetMsg | R3 |
| WorldReady | NetMsg | R3 |
| PlayerJoin | NetMsg | R3 |
| PlayerLeave | NetMsg | R3 |
| CharacterData | NetMsg | P3 |
| HostCharacterData | NetMsg | P3 |
| BlockDamaged | NetMsg | W1 |
| WorldBlockState | NetMsg | W1 |
| BlockPlaced | NetMsg | W1 |
| BuildingEntityDamaged | NetMsg | E1 |
| BuildingEntityOpened | NetMsg | E1 |
| EarthquakeStart | NetMsg | W3 |
| KeypadCode | NetMsg | W4 |
| ItemIdWatermark | NetMsg | I8 |
| CarriedInventory | NetMsg | I8 |
| EntityEvent | NetMsg | E2 |
| EntitySpawned | NetMsg | E3 |
| GeyserStateSnapshot | NetMsg | W5 |
| FluidRegion | NetMsg | F1 |
| FluidInteraction | NetMsg | F2 |
| TraderState | NetMsg | N4 |
| TraderAction | NetMsg | N5 |
| SpeechMsg | NetMsg | N7 |
| ModMessage | NetMsg | N8 |
| CraftReport | NetMsg | I6 |
| RecipeUnlock | NetMsg | I6 |
| TrapLayoutSnapshot | NetMsg | W6 |
| EnemySnapshot | NetMsg | N1 |
| EnemyAttack | NetMsg | N1 |
| ModCommandRequest | NetMsg | N8 |
| ModCommandResult | NetMsg | N8 |
| BlockDamageSnapshot | NetMsg | W2 |
| WorldTimeRequest | NetMsg | R2 |
| WorldTime | NetMsg | R2 |
| LimbStateEvent | NetMsg | P4 |
| CharacterSound | NetMsg | P5 |
| FluidPresentation | NetMsg | F3 |
| PlayerInventoryTakeRequest | NetMsg | P9 |
| PlayerCarryStartRequest | NetMsg | P9 |
| PlayerCarryStopRequest | NetMsg | P9 |
| PlayerHealRequest | NetMsg | P9 |
| TutorialClawState | NetMsg | N10 |
| DynamiteExplosion | NetMsg | E4 |
| RadiationLineState | NetMsg | E5 |
| TraderRecruitRequest | NetMsg | N5 |
| TraderRecruitResult | NetMsg | N5 |
| Chat | NetMsg | N6 |
| WorldSnapshotComplete | NetMsg | W7 |
| Kicked | NetMsg | R4 |
| Banned | NetMsg | R4 |
| CharacterAttackAnim | NetMsg | P5 |
| CharacterLandingVisual | NetMsg | P5 |
| TraderSwing | NetMsg | N5 |
| PlayerItemUseRequest | NetMsg | P9 |
| PlayerPushRequest | NetMsg | P9 |
| PlayerPushResult | NetMsg | P9 |
| CharacterRagdoll | NetMsg | P5 |
| WorldBloodSpawn | NetMsg | E6 |
| KernelEnvelope | NetMsg | K1 |
| PlayerColorUpdate | NetMsg | P12 |
| LocationPing | NetMsg | N9 |
| RemoteInventoryOperationRequest | NetMsg | P10 |
| RemoteInventoryApply | NetMsg | P10 |
| MedicalOperationStartRequest | NetMsg | P11 |
| MedicalOperationStartAck | NetMsg | P11 |
| MedicalOperationUpdate | NetMsg | P11 |
| MedicalOperationState | NetMsg | P11 |
| MedicalOperationEndRequest | NetMsg | P11 |
| MedicalOperationEndCommitted | NetMsg | P11 |
| MedicalOperationCancel | NetMsg | P11 |
| ItemSpawn | WireCommandKind | I1 |
| ItemPickup | WireCommandKind | I1 |
| ItemDrop | WireCommandKind | I1 |
| ItemDestroy | WireCommandKind | I1 |
| ItemUpdateState | WireCommandKind | I1 |
| ItemTransfer | WireCommandKind | I1 |
| ItemContainerSync | WireCommandKind | I1 |
| RunStart | WireCommandKind | R1 |
| AdvanceLayer | WireCommandKind | R1 |
| RecordTrapConsumed | WireCommandKind | E7 |
| RecordBuildingEntityHealth | WireCommandKind | E7 |
| RecordOpenedEntity | WireCommandKind | E7 |
| ResetWorldEntities | WireCommandKind | E7 |
| UpdatePlayerStatus | WireCommandKind | P2 |
| ResetPlayers | WireCommandKind | P2 |
| UpsertEnemy | WireCommandKind | N2 |
| RemoveEnemy | WireCommandKind | N2 |
| ResetEnemies | WireCommandKind | N2 |
| UpdateFluidRegion | WireCommandKind | F4 |
| ResetFluids | WireCommandKind | F4 |
| SetPlayerCarry | WireCommandKind | P6 |
| ClearPlayerCarry | WireCommandKind | P6 |
| RecordEnemyBite | WireCommandKind | N2 |
| RecordEnemyLunge | WireCommandKind | N2 |
| RecordEnemyEffect | WireCommandKind | N2 |
| RecordTrapState | WireCommandKind | E7 |
| RangeRequest | WireCommandKind | K2 |
| CommandRejected | WireCommandKind | K1 |
| ItemSpawned | WireEventKind | I1 |
| ItemRelocated | WireEventKind | I1 |
| ItemDestroyed | WireEventKind | I1 |
| ItemDataUpdated | WireEventKind | I1 |
| RunStarted | WireEventKind | R1 |
| RunAdvanced | WireEventKind | R1 |
| TrapConsumed | WireEventKind | E7 |
| BuildingEntityHealthUpdated | WireEventKind | E7 |
| OpenedEntity | WireEventKind | E7 |
| WorldEntitiesReset | WireEventKind | E7 |
| PlayerStatusUpdated | WireEventKind | P2 |
| PlayersReset | WireEventKind | P2 |
| EnemyUpserted | WireEventKind | N2 |
| EnemyRemoved | WireEventKind | N2 |
| EnemiesReset | WireEventKind | N2 |
| FluidRegionUpdated | WireEventKind | F4 |
| FluidsReset | WireEventKind | F4 |
| PlayerCarrySet | WireEventKind | P6 |
| PlayerCarryCleared | WireEventKind | P6 |
| PlayerInventoryTransfer | WireEventKind | P7 |
| PlayerHealResult | WireEventKind | P8 |
| PlayerItemUseResult | WireEventKind | P8 |
| EnemyBiteResult | WireEventKind | N2 |
| EnemyLungeResult | WireEventKind | N2 |
| EnemyEffectResult | WireEventKind | N2 |
| TrapStateChanged | WireEventKind | E7 |
| PlayerStateBroadcast | AdaptiveStreamId | P1 |
| PlayerStateReport | AdaptiveStreamId | P1 |
| EnemyStateBroadcast | AdaptiveStreamId | N3 |
| TutorialClawBroadcast | AdaptiveStreamId | N10 |
| MedicalInjectionReport | AdaptiveStreamId | P11 |
| ShrapnelPositionReport | AdaptiveStreamId | P11 |
| WorldItemMoveStream | AdaptiveStreamId | I2 |
| WorldItemSnapshotStream | AdaptiveStreamId | I3 |
| FluidRegionDiffStream | AdaptiveStreamId | F1 |
| FluidRegionFullStream | AdaptiveStreamId | F1 |
| TraderStateStream | AdaptiveStreamId | N4 |

## Gap list (one ticket per gap; R3+R4 merged)

| Gap | Row(s) | Severity | Ticket |
|---|---|---|---|
| Guest block mutations have no periodic re-report; the host's 60 s absolute snapshot cannot heal a swallowed guest report (partial block damage shares the asymmetry) | W1, W2 | High | `docs/backlog/todo/guest-block-mutation-re-report.md` |
| Runtime-created `BuildingEntity` spawns have no creation snapshot, no periodic re-report and no late-join backfill | E3 | Medium-High | `docs/backlog/todo/runtime-entity-spawn-backfill.md` |
| `TrapLayoutSnapshot` is world-entry/InWorld-edge only; while a member stays continuously in the world there is no in-session repair | W6 | Medium | `docs/backlog/todo/trap-layout-snapshot-recovery.md` |
| `EnemySnapshot` is world-entry/reconnect only (the 20 Hz stream cannot rebind); `EnemyAttack` has no recovery | N1 | Medium | `docs/backlog/todo/enemy-snapshot-and-attack-recovery.md` |
| A swallowed guest pickup/drop command leaves the guest's local in-flight result un-reconciled by the item keyframe; the empty-host-table case skips the keyframe entirely | I5, I1 (caveat) | Medium | `docs/backlog/todo/guest-command-loss-reconciliation.md` |
| `RecipeUnlock` has no periodic fallback and no world-entry/checkpoint member; late joiners never learn a prior unlock | I6 | Medium | `docs/backlog/todo/recipe-unlock-fallback.md` |
| `CarriedInventory` registration is one-shot; a swallowed report leaves the host's arbitration transfer table empty for those ids | I8 | Medium | `docs/backlog/todo/carried-inventory-registration-re-report.md` |
| Lost session control/readiness messages have no re-report: `HandshakeAckAck`, `SceneState`, `PlayerJoin`; `WorldSnapshotComplete` is inert | R3, R4 | Medium | `docs/backlog/todo/session-control-convergence.md` |
| A late checkpoint chunk set from a previous run is not validated against the current run before `Restore` | K2 (finding) | Low-Medium | `docs/backlog/todo/checkpoint-run-epoch-validation.md` |
| A `ModCommandRequest` dropped by the rate limiter leaves the caller's pending callback unresolved until session end | N8 (finding) | Low | `docs/backlog/todo/mod-command-request-timeout.md` |
| Fallback stretch caps and first-resend latency need a measured decision (item keyframe 5 s→30 s, trader 5 s→30 s, fluid full 1 s→10 s, block/damage/keypad/geyser 60 s first resend) | I3, N4, W1 | Medium | `docs/backlog/todo/sync-cadence-review.md` |

Already-owned findings cross-referenced, not duplicated:

- `KernelSaveFileStore` is unit-tested but has no production caller (the authoritative
  checkpoint is memory-only today) — owned by `docs/backlog/todo/save-system-mid-run-and-layer-end.md`.
- `ResetFluidsCommand` has no production caller — recorded in this matrix and corrected in
  `docs/architecture/domains.md`; not a sync gap (the stale chunks are cleared by zero-total
  upserts from `FluidKernelProjection`).
- `WorldSnapshotComplete` is inert — the marker is not recovery evidence; folded into
  `docs/backlog/todo/session-control-convergence.md`.
- Rows W3 (EarthquakeStart) and E4 (DynamiteExplosion) are one-shot events with no fallback of
  their own; they stay `OK` because the durable consequences are explicitly healed by the
  block/building/item fallbacks named in their rows. Their rows state that dependency so the
  verdict is not read as "the event itself is recoverable".

## Accepted losses (recorded reasons)

| Row | Surface | Reason |
|---|---|---|
| N6 | Chat lines | One-shot user message; reliable transport; no authoritative state; history backfill is a separate feature, not part of the sync contract |
| N7 | Speech bubbles | Presentation bubble; no state divergence; no backfill by design |
| N8 | Mod messages / commands / mod status | The rate-limit drop is the documented policy (`src/CasualtiesUnknownOnline.Runtime/Session/Mods/ModRateLimitPolicy.cs:7`); mod payloads are opaque and loss-tolerant. The pending-callback side effect is tracked in `todo/mod-command-request-timeout.md` |
| N9 | Location ping | Transient UI marker; one-shot relay is the whole feature |
| F3 | Fluid presentation (water push / waterflow sound) | Reliable one-shot presentation; the region stream is the state path |
| I9 | Item presentation transients (fresh-drop motion, impact sounds) | Non-authoritative guest presentation is deliberately suppressed; the authoritative item state rides I1/I2/I3 |
| P5 | One-shot character presentation (sound / attack anim / landing / ragdoll) | Reliable one-shot presentation; the 1 Hz character snapshot is the state fallback |
| E6 | World blood decal | 120 s transient decal, explicitly "no periodic snapshot" (`src/CasualtiesUnknownOnline.Runtime/Protocol/Messages/WorldBloodSpawnMsg.cs:13`) |
| R7 | World fingerprint | Diagnostic only, not a sync path; one-shot at world entry, compared manually across peer logs during final acceptance |

## Cadence findings (sync durations that look unreasonable)

1. **Item keyframe 5 s → 30 s.** `src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:77-87`; the keyframe is the only
   absolute heal for top-level item state and for phantom-item removal. 30 s is
   user-visible. Proposal: cap ≈10 s or exempt the keyframe.
2. **Trader fallback 5 s → 30 s.** `src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:108-118`; interactions
   broadcast immediately, so this only heals a swallowed event. Proposal: cap ≈15 s. The
   `src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs:61` comment was corrected in this cycle (reliable, adaptive cadence), so the
   cadence ticket no longer needs to fix it.
3. **Fluid full-viewport reconciliation 1 s → 10 s.** `src/CasualtiesUnknownOnline.Runtime/Session/AdaptiveSync/AdaptiveStreamCatalog.cs:97-107`.
   Record the measured worst-case stale cell after an anchor move.
4. **Block / damage / keypad / geyser first resend is 60 s.**
   `src/CasualtiesUnknownOnline.GameAdapter/World/WorldEventSync.cs:122-135`; the documented lazy-P2P swallow window is up to ~30 s, so a
   mutation in that window can stay invisible for up to 60 s. Proposal: one early resend at
   the InWorld edge (5–10 s), then 60 s steady-state.
5. **`EnemySnapshot` has no resend** (row N1) and the **handshake retry stops on
   `SessionActive`** (row R4) — both tracked in their own tickets.

## Doc corrections landed in this cycle

| Doc | Correction |
|---|---|
| `docs/architecture/protocol.md` | The validator does reject multi-envelope frames (`src/CasualtiesUnknownOnline.Protocol/Wire/ProtocolFrameValidator.cs:40-48`); the `IsSupportedFrame` reference is stale (the check is `ProtocolFrameValidator.TryValidate` at `src/CasualtiesUnknownOnline.Runtime/Session/Items/KernelProtocolService.cs:186`); the save section now says `KernelSaveFileStore` has no production caller today |
| `docs/architecture/glossary.md` | The "all old-epoch stream packets are rejected" claim is now true (stale-epoch state streams are dropped at `KernelProtocolService.HandleStateStream`) |
| `docs/architecture/current.md` | `ItemPendingPickupArbiter` does not exist; the mechanism is `PendingPickupQueue` |
| `docs/architecture/domains.md` | Fluids: the kernel facts are a coarse periodic aggregation, not a grid rebuild source; `ResetFluidsCommand` has no production caller |
| `docs/features/entities.md` | Trader state is not sent on world entry and the fallback is reliable + adaptive; geyser liquid type rides `GeyserStateSnapshot`, not the event's `Extra` |
| `docs/features/items.md` | The world-item keyframe is adaptive (5 s base, up to 30 s under pressure), not a fixed 5 s |
| `src/CasualtiesUnknownOnline.Runtime/Protocol/NetMsg.cs` | Trader-state comment: reliable fallback, adaptive cadence |
| `docs/evidence/selfchecks/architecture/phase-e-legacy-inventory-selfcheck.md` | Direct `NetMsg` classification completed (crafting, color, ping, remote-inventory apply, medical-operation sessions) and pointed at this matrix as the source of truth |
| `docs/backlog/review/world-determinism-world-fingerprint.md` | Expanded with the audit evidence: the fingerprint is one-shot and local; peer comparison is a manual final-acceptance step |

## Stale historical evidence (not rewritten; do not cite as current)

These historical self-checks describe wire shapes that were removed by the typed-kernel
migration. They are kept as dated evidence snapshots, not as current claims: `NetMsg` 88
(`BuildingEntityHealthSnapshot`), `TrapStateSnapshot` / `OpenedEntitiesSnapshot`, `NetMsg`
95 (`ItemContainerContentMsg`), `NetMsg` 63 (`ItemCarriedSync`), `HandlerContext.SendWorldStateToMember`,
`ItemPendingPickupArbiter`, and `ToSnapshotEntryMsg`. The active docs above were corrected;
rewriting the historical records is out of scope for this audit.

## Cross-checks

- **Adaptive governor (row R5):** `AdaptiveStreamRateService` only answers "what cadence";
  every stream owner still sends the same payload, so the governor can stretch a fallback but
  cannot remove it. The stretch caps are the cadence findings above.
- **World fingerprint (row R7):** a diagnostic, not a divergence repair path; manual peer-log
  comparison remains the final-acceptance step.
- **Network traffic baseline:** the fallback cost is measured by
  `docs/backlog/review/network-traffic-baseline.md`; the cadence ticket must not regress it.
- **Projection health (row R6):** the per-domain dirty/rebuild loop is the recovery path for
  a failed apply; it does not correct authority.

## Guard

`tests/CasualtiesUnknownOnline.NormativeGates.Tests/SyncCoverageGateTests.cs` fails when:

- a `NetMsg`, `WireCommandKind`, `WireEventKind` or `AdaptiveStreamId` member has no vocabulary
  index entry, or its index entry points at an unknown row;
- the owning row's text does not mention the member (an entry cannot be parked on an unrelated
  row);
- a row's verdict leaves the vocabulary or is empty;
- a row carries no `path.cs:line` evidence reference, a row id is duplicated, or the matrix
  drops below the 64-row floor;
- any of the 736 evidence entries in `docs/evidence/sync-coverage-evidence.json` no longer
  matches its referenced source line (the quote is not a substring of that line), or the
  evidence file shrinks below the 736-entry floor;
- an inline `path:line` reference in the matrix has no anchor in that evidence file, its
  inline quote does not match the source line, or it uses a bare continuation `:N`;
- the verdict summary table disagrees with the row verdicts;
- a gap row does not name an existing `docs/backlog/todo/*.md` ticket.

The gate carries negative-contract self-tests for each of those failure modes, and it was
falsified by hand: adding a throwaway `NetMsg` member made it fail with
`NetMsg.TestGateProbe is not declared in the wire vocabulary index`, removing the member made
it pass again, and silently flipping row W2's verdict to `OK` made the verdict-summary gate
fail. This is the rot guard the audit ticket asked for: a new sync feature cannot ship without
an event + fallback policy declaration, and evidence lines cannot silently drift.

Known limitation: 11 evidence quotes are shorter than 20 characters and repeat in their file
(for example `SendFullSet();`), so their own line content cannot disambiguate a shift; those
rows rely on their longer sibling quotes and on the inline-ref anchors. All other evidence is
uniquely anchored.
















